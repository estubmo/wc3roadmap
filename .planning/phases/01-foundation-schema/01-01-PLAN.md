---
phase: 01-foundation-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - content-collections.ts
  - src/router.tsx
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/smoke.test.ts
autonomous: true
requirements: [DATA-06, OSS-02]
user_setup: []

must_haves:
  truths:
    - "`npm run build` completes with exit code 0 on a clean checkout"
    - "`npx vitest run` discovers and passes at least one test"
    - "Every core dependency in package.json is pinned to an exact version (no ^ or ~ ranges)"
  artifacts:
    - "package.json with exact-pinned deps + scripts (dev, build, build:content, typecheck, test)"
    - "package-lock.json committed"
    - "vite.config.ts with contentCollections() as the FIRST plugin, then tanstackStart(), nitro(), viteReact()"
    - "tsconfig.json with the content-collections path alias and strict mode"
    - "vitest.config.ts"
    - "content-collections.ts (minimal empty-collections config, filled by plan 01-06)"
  key_links:
    - "vite.config.ts plugin order: contentCollections() must run before tanstackStart() so generated types exist"
    - "tsconfig paths alias 'content-collections' -> ./.content-collections/generated"
    - "nitro() plugin present so Vercel zero-config detection works (plan 01-08)"
---

<objective>
Scaffold a minimal, buildable TanStack Start application with the content-collections + Zod + Vitest toolchain wired and every core dependency pinned to an exact, known-working version. This is the foundation every other Phase 1 plan builds on (D-11, D-14).

Purpose: Surface deploy-target and integration friction on day one (per D-12), and give every downstream plan a working build + test harness.
Output: A scaffolded repo that builds (`npm run build`), type-checks (`npm run typecheck`), and runs tests (`npx vitest run`).
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold TanStack Start app with a single placeholder route</name>
  <files>package.json, src/router.tsx, src/routes/__root.tsx, src/routes/index.tsx, .gitignore</files>
  <read_first>
    - .claude/CLAUDE.md (pinned version table — the dependency source of truth; "What NOT to Use" table)
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Recommended Project Structure", "Installation", "Common Pitfalls")
  </read_first>
  <action>
    Scaffold a minimal TanStack Start app using `npx @tanstack/cli create` (React 19, Vite). Reduce it to a single placeholder home route at src/routes/index.tsx (a static page naming the project, no data fetching) plus src/routes/__root.tsx and the router setup. Use `@tanstack/react-start` (NOT the old `@tanstack/start`) and `@tanstack/react-router` bundled with it. Add `.content-collections/` and `.env` to .gitignore (the generated content dir is always regenerated; never commit secrets per CLAUDE.md). Do NOT add Tailwind, shadcn, motion, xyflow, drizzle, better-auth, or react-query — those are reserved for Phases 2-4 per the RESEARCH "Phase 1 Does NOT Install" table.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/index.tsx exists and renders a static placeholder page
    - package.json names "@tanstack/react-start" (not "@tanstack/start")
    - .gitignore contains a line for `.content-collections/` and a line for `.env`
    - No Phase 2-4 packages (@xyflow/react, drizzle-orm, better-auth, @tanstack/react-query, motion, tailwindcss) appear in package.json dependencies
  </acceptance_criteria>
  <done>The scaffold type-checks and contains exactly one placeholder route.</done>
</task>

<task type="auto">
  <name>Task 2: Wire content-collections + nitro into Vite, pin all dependencies exactly</name>
  <files>vite.config.ts, tsconfig.json, content-collections.ts, package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 5: Vite Configuration (order matters)", "Standard Stack" version table, Pitfall 1/2/3)
    - .claude/CLAUDE.md (Version Compatibility matrix)
    - vite.config.ts (the scaffold-generated file being modified)
  </read_first>
  <action>
    Install the content pipeline + deploy adapter: @content-collections/core, @content-collections/vite, @content-collections/mdx, zod (v4), nitro, and dev deps typescript, tsx, vitest, @tanstack/eslint-plugin-router, prettier. Pin EVERY core dependency to an exact version (no ^ or ~) using the versions in RESEARCH "Standard Stack" (@tanstack/react-start 1.168.26, @content-collections/core 0.15.2, @content-collections/vite 0.3.0, @content-collections/mdx 0.2.2, zod 4.4.3, nitro 3.x resolved pin) — applications use exact pins so fresh installs are reproducible. In vite.config.ts set the plugin array order to contentCollections() FIRST, then tanstackStart(), then nitro(), then viteReact() (per Pitfall 1/A1 — contentCollections must generate types before other plugins run; per Pitfall 3 nitro is required for Vercel detection). Add the tsconfig path alias mapping "content-collections" to ./.content-collections/generated and keep strict mode on. Create content-collections.ts at repo root exporting an empty-collections defineConfig (the nodes collection is added in plan 01-06) so the build resolves the plugin import. Add npm scripts: dev, build, build:content (runs the content-collections generation step — confirm the exact CLI invocation against content-collections docs via Context7), typecheck (tsc --noEmit), test (vitest run). Set package.json "license" to "GPL-3.0-or-later" (the documented default; confirmed/overridden by the checkpoint in plan 01-04). Commit package-lock.json.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - vite.config.ts lists contentCollections() as the first element of the plugins array, before tanstackStart()
    - No dependency in package.json uses a `^` or `~` range (verify: `node -e "const d={...require('./package.json').dependencies,...require('./package.json').devDependencies};for(const[k,v]of Object.entries(d))if(/[\^~]/.test(v))process.exit(1)"`)
    - nitro appears in package.json dependencies
    - tsconfig.json compilerOptions.paths contains a "content-collections" entry
    - package.json scripts include build, build:content, typecheck, and test
  </acceptance_criteria>
  <done>Project builds with the full plugin chain; all deps exact-pinned; lockfile committed.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Vitest and add a smoke test (Wave 0 framework install)</name>
  <files>vitest.config.ts, src/smoke.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-VALIDATION.md ("Test Framework", "Wave 0 Gaps")
    - vite.config.ts (to mirror resolve/alias config into the test config)
  </read_first>
  <action>
    Create vitest.config.ts (node environment is sufficient for Phase 1 — all tests are schema/logic unit tests, no DOM). Add a trivial src/smoke.test.ts asserting `1 + 1 === 2` so the harness is provably wired and `npx vitest run` exits 0 with at least one passing test. This satisfies the Nyquist Wave 0 framework-install gap; downstream plans (01-02, 01-05, 01-07) add real tests.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 and reports at least 1 passing test
    - vitest.config.ts exists
  </acceptance_criteria>
  <done>Vitest runs and the smoke test passes.</done>
</task>

</tasks>

<artifacts_produced>
New files: package.json, package-lock.json, vite.config.ts, tsconfig.json, vitest.config.ts, .gitignore, content-collections.ts (empty stub), src/router.tsx, src/routes/__root.tsx, src/routes/index.tsx, src/smoke.test.ts.
New npm scripts: dev, build, build:content, typecheck, test.
Pinned dependencies: @tanstack/react-start, @tanstack/react-router, @content-collections/core, @content-collections/vite, @content-collections/mdx, zod, nitro; dev: typescript, tsx, vitest, @tanstack/eslint-plugin-router, prettier.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Third-party package code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (content-collections, nitro, tanstack, zod) | high | mitigate | Exact version pinning + committed package-lock.json + `npm ci` in CI (plan 01-07). All packages cleared by the RESEARCH "Package Legitimacy Audit" (all Approved; SUS verdicts are benign "too-new" publishes from established, public-repo, high-download packages) — no blocking human checkpoint required per that audit. |
| T-01-CFG | Tampering | vite.config.ts plugin order | medium | mitigate | contentCollections() pinned as first plugin; build verifies the chain resolves. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run typecheck` exits 0
- `npx vitest run` passes
- No semver ranges in package.json
</verification>

<success_criteria>
A clean checkout builds, type-checks, and runs its test suite; every core dependency is pinned exactly; the content-collections + nitro + Vitest toolchain is wired (supports ROADMAP success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-01-SUMMARY.md` when done.
</output>
