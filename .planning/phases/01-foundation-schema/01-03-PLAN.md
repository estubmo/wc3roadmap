---
phase: 01-foundation-schema
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - CONTEXT.md
  - docs/adr/001-stack-choice.md
  - docs/adr/002-content-graph-decoupling.md
  - docs/adr/003-patch-registry-primitive.md
  - docs/upgrade-policy.md
autonomous: true
requirements: [OSS-02]

must_haves:
  truths:
    - "CONTEXT.md captures the project's ubiquitous domain language with one definition per core term"
    - "docs/adr/ holds the foundational ADRs for stack choice, content/engine decoupling, and the patch-registry primitive"
    - "The upgrade policy documents how pinned dependencies are reviewed and bumped (D-14)"
  artifacts:
    - "CONTEXT.md (repo root)"
    - "docs/adr/001-stack-choice.md, 002-content-graph-decoupling.md, 003-patch-registry-primitive.md"
    - "docs/upgrade-policy.md"
  key_links:
    - "Every later phase reads CONTEXT.md and appends ADRs — these files are the architecture contract"
---

<objective>
Scaffold the architecture-discipline foundations: the root CONTEXT.md domain glossary and the foundational ADRs, plus the dependency upgrade policy (D-15, D-14, OSS-02). These exist before any feature phase so the deep-module + ADR discipline is in place from the start (ROADMAP success criterion 6).

Purpose: Make the codebase AI-navigable and decisions auditable; document why dependencies are pinned and how they are upgraded.
Output: CONTEXT.md, three foundational ADRs, upgrade-policy.md. (The GPL-3.0 licensing ADR 004 lives with the LICENSE in plan 01-04.)
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.claude/CLAUDE.md
@.agents/skills/codebase-design/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CONTEXT.md domain glossary</name>
  <files>CONTEXT.md</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-15 term list; the <specifics> "one file = one node" principle)
    - .agents/skills/codebase-design/SKILL.md (Glossary — model the precise-vocabulary discipline)
    - .planning/PROJECT.md (product framing and constraints)
  </read_first>
  <action>
    Write repo-root CONTEXT.md defining the project's ubiquitous domain language, one crisp definition per term. Cover at minimum: node, nodeType (MECHANIC vs CONCEPTUAL), skillType (macro/micro/mental), difficulty, race, mastery state (untouched/learning/mastered), pathway, signal, patch, patchId, threshold (masteryThreshold), citation, applicationNote, prerequisite (soft, never hard-locking per D-09/DATA-05), meta_volatile, last_reviewed, patch_context. State the "one .mdx file fully describes one node" principle (D-01) and that prerequisites are declared on nodes, not in a separate edges file. Note that this file is appended to by every later phase.
  </action>
  <verify>
    <automated>test -f CONTEXT.md && grep -q "MECHANIC" CONTEXT.md && grep -q "CONCEPTUAL" CONTEXT.md && grep -qi "patchId" CONTEXT.md && grep -qi "prerequisite" CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTEXT.md exists at repo root
    - CONTEXT.md defines nodeType with both MECHANIC and CONCEPTUAL
    - CONTEXT.md defines patch/patchId, mastery state, pathway, signal, threshold, citation, and prerequisite (as soft/non-locking)
  </acceptance_criteria>
  <done>CONTEXT.md defines every core domain term.</done>
</task>

<task type="auto">
  <name>Task 2: Author foundational ADRs 001-003</name>
  <files>docs/adr/001-stack-choice.md, docs/adr/002-content-graph-decoupling.md, docs/adr/003-patch-registry-primitive.md</files>
  <read_first>
    - .claude/CLAUDE.md ("Recommended Stack", "Alternatives Considered", "What NOT to Use")
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Architectural Responsibility Map", "Architecture Patterns")
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (Claude's Discretion: ADR seed set; D-04/D-05)
  </read_first>
  <action>
    Write three ADRs in a standard ADR format (Context / Decision / Consequences / Status: Accepted). 001-stack-choice: TanStack Start + content-collections + Zod v4 + Neon/Drizzle (forward) + Vercel, citing the CLAUDE.md "Alternatives Considered" rationale (why not contentlayer, Prisma, Auth.js, Cytoscape). 002-content-graph-decoupling: the graph engine receives only display-essential data (NodeSummary) while full node content loads lazily (DATA-02); the content schema is a deep module the graph never reaches behind. 003-patch-registry-primitive: patch version modeled as a curated ordered TS registry with a CURRENT_PATCH pointer and referential integrity enforced at build time (D-04/D-05), referenced by all three schemas. Cross-reference src/lib/patches.ts.
  </action>
  <verify>
    <automated>test -f docs/adr/001-stack-choice.md && test -f docs/adr/002-content-graph-decoupling.md && test -f docs/adr/003-patch-registry-primitive.md</automated>
  </verify>
  <acceptance_criteria>
    - All three ADR files exist under docs/adr/
    - Each ADR has Context, Decision, and Consequences sections and a Status line
    - 002 explicitly states the graph receives only display-essential data (DATA-02 decoupling)
  </acceptance_criteria>
  <done>Three foundational ADRs committed.</done>
</task>

<task type="auto">
  <name>Task 3: Document the dependency upgrade policy</name>
  <files>docs/upgrade-policy.md</files>
  <read_first>
    - .claude/CLAUDE.md ("Version Compatibility" matrix; "What NOT to Use")
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Version Drift Warning", Pitfall 6, Open Questions 3-4)
  </read_first>
  <action>
    Write docs/upgrade-policy.md (satisfies D-14's "documented upgrade policy"). Cover: dependencies are pinned to exact versions for the application; upgrades require reading the changelog and re-running CI (typecheck + content validation + build) before the pin is bumped; the CLAUDE.md version-compatibility matrix is the compatibility source of truth; and the known forward-looking drift items to watch (drizzle-orm 0.44.x vs 0.45.x and drizzle-kit 0.25.x vs 0.31.x for Phase 4; nitro is currently a 3.x beta — verify the resolved pin builds on Vercel; content-collections 0.15.2 is a recent publish — read its changelog before any bump).
  </action>
  <verify>
    <automated>test -f docs/upgrade-policy.md && grep -qi "exact" docs/upgrade-policy.md && grep -qi "changelog" docs/upgrade-policy.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/upgrade-policy.md exists
    - It states dependencies are exact-pinned and upgrades require changelog review + CI green
    - It names the drizzle and nitro drift items as forward watch-list entries
  </acceptance_criteria>
  <done>Upgrade policy documented.</done>
</task>

</tasks>

<artifacts_produced>
New files: CONTEXT.md, docs/adr/001-stack-choice.md, docs/adr/002-content-graph-decoupling.md, docs/adr/003-patch-registry-primitive.md, docs/upgrade-policy.md.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | No runtime component, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-DOC | Repudiation | undocumented architecture decisions | low | accept | ADR discipline records decisions with rationale; no runtime attack surface in this plan. |
</threat_model>

<verification>
- CONTEXT.md and all four docs exist and contain the required sections/terms (greps above)
</verification>

<success_criteria>
CONTEXT.md and the foundational ADRs exist with the deep-module + ADR discipline documented before feature phases begin (ROADMAP success criterion 6); the upgrade policy satisfies the D-14 documentation requirement.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-03-SUMMARY.md` when done.
</output>
