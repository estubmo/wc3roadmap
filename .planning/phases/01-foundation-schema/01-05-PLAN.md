---
phase: 01-foundation-schema
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - src/schemas/node.ts
  - src/schemas/node.test.ts
  - src/schemas/mastery.ts
  - src/schemas/mastery.test.ts
  - src/schemas/progress.ts
  - src/schemas/progress.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, OSS-02]

must_haves:
  truths:
    - "nodeType is a first-class enum with exactly MECHANIC and CONCEPTUAL; other strings are rejected"
    - "patch_context, last_reviewed, and meta_volatile are all required (non-optional) on the node schema"
    - "All three schemas (node, masteryThreshold, progressRecord) carry a patchId validated against the registry"
    - "NodeSummary is the graph-display subset; the full NodeFrontmatter has the additional content fields (DATA-02 decoupling)"
    - "prerequisites is an array of node-id strings, accepting an empty array (soft, never hard-locking)"
  artifacts:
    - "src/schemas/node.ts (NodeSummarySchema, NodeSummary, NodeFrontmatterSchema, NodeFrontmatter)"
    - "src/schemas/mastery.ts (MasteryThresholdSchema, MasteryThreshold)"
    - "src/schemas/progress.ts (MasteryStateSchema, MasteryState, ProgressRecordSchema, ProgressRecord)"
    - "co-located *.test.ts for each schema"
  key_links:
    - "All three schemas import PATCH_IDS from src/lib/patches and build patchId via z.enum(PATCH_IDS)"
    - "NodeFrontmatterSchema extends NodeSummarySchema so the graph subset stays a strict subset"
---

<objective>
Define all three Zod v4 content schemas now (D-10): the node schema in full, plus real (minimal) masteryThreshold and progressRecord schemas. Each carries patchId wired to the registry, proving the patch primitive spans every schema from the first commit (DATA-01..05, ROADMAP success criteria 2/3/4).

Purpose: Establish the typed single source of truth downstream phases consume; bake MECHANIC/CONCEPTUAL and the patch primitive in from day one.
Output: src/schemas/{node,mastery,progress}.ts + tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.agents/skills/codebase-design/SKILL.md
@src/lib/patches.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Node schema (NodeSummary + full NodeFrontmatter), test-first</name>
  <files>src/schemas/node.ts, src/schemas/node.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 3: Three Zod Schemas", "Zod v4 facts", "Code Examples")
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-07 field list, D-08, D-09)
    - src/lib/patches.ts (PATCH_IDS import)
    - docs/spdx-header-convention.md (the confirmed SPDX variant for line 1)
  </read_first>
  <behavior>
    - NodeFrontmatterSchema accepts nodeType "MECHANIC" and "CONCEPTUAL"; rejects "mechanical" or any other string
    - Parsing fails when patch_context, last_reviewed, or meta_volatile is absent
    - last_reviewed must match YYYY-MM-DD; a bad date format is rejected
    - patchId must be a registry id; an unknown patchId is rejected
    - prerequisites accepts [] and accepts an array of id strings
    - NodeSummarySchema contains only id, title, nodeType, race, prerequisites; NodeFrontmatterSchema parses a full valid object including skillType, difficulty, tags, patchId, patch_context, last_reviewed, meta_volatile, citations
    - each citation requires a non-empty applicationNote; a citation missing applicationNote is rejected
  </behavior>
  <action>
    Write src/schemas/node.test.ts FIRST (RED), then src/schemas/node.ts (GREEN). Use Zod v4 idioms only: z.enum([...]) (never z.nativeEnum), `error` param (never message), .check() if a multi-issue refinement is needed (never .superRefine). Define NodeSummarySchema (id, title, nodeType, race, prerequisites) and NodeFrontmatterSchema = NodeSummarySchema.extend({ skillType macro|micro|mental, difficulty beginner|intermediate|advanced, tags string[], patchId z.enum(PATCH_IDS imported from ../lib/patches), patch_context non-empty string, last_reviewed YYYY-MM-DD string, meta_volatile boolean, citations array of { source, url optional, applicationNote non-empty } }). Export the inferred types NodeSummary and NodeFrontmatter via z.infer. Line 1 is the SPDX header per docs/spdx-header-convention.md. nodeType values are the locked literals MECHANIC and CONCEPTUAL (D-07). PARALLEL-SCHEMA SYNC NOTE: the node frontmatter shape is intentionally defined twice — here in src/schemas/node.ts (project zod, runtime/test surface) and inline in content-collections.ts (injected z, build-time surface) per RESEARCH Pattern 1 (the schema function must use the injected z, not a separate zod import, to avoid version mismatch). These two definitions MUST stay field-for-field identical (same fields, enums, regexes, optionality). Any field change here must be mirrored in content-collections.ts (plan 01-06) and vice versa; treat the two as a single logical schema with two physical sites.
  </action>
  <verify>
    <automated>npx vitest run src/schemas/node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/schemas/node.ts exports NodeSummarySchema, NodeSummary, NodeFrontmatterSchema, NodeFrontmatter
    - `npx vitest run src/schemas/node.test.ts` passes including the rejection cases for missing patch_context/last_reviewed/meta_volatile and bad nodeType
    - No use of z.nativeEnum or .superRefine in node.ts
    - Line 1 of node.ts is the SPDX header matching docs/spdx-header-convention.md
  </acceptance_criteria>
  <done>Node schema accepts valid nodes and rejects every malformed case under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: masteryThreshold and progressRecord schemas, test-first</name>
  <files>src/schemas/mastery.ts, src/schemas/mastery.test.ts, src/schemas/progress.ts, src/schemas/progress.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 3" — mastery.ts and progress.ts examples)
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-10 — schemas minimal but real, each carries patchId)
    - src/lib/patches.ts (PATCH_IDS import)
  </read_first>
  <behavior>
    - MasteryThresholdSchema requires nodeId, nodeType (MECHANIC|CONCEPTUAL), patchId (registry-validated), and a thresholdDefinition record; rejects an unknown patchId
    - MasteryStateSchema accepts exactly untouched | learning | mastered
    - ProgressRecordSchema requires userId, nodeId, patchId (registry-validated), masteryState (from MasteryStateSchema), lastUpdated (ISO datetime); rejects an unknown patchId and an invalid masteryState
  </behavior>
  <action>
    Write tests FIRST then implement. mastery.ts: MasteryThresholdSchema { nodeId string, nodeType z.enum(["MECHANIC","CONCEPTUAL"]), patchId z.enum(PATCH_IDS), thresholdDefinition z.record(z.string(), z.unknown()) } — keep it deliberately open; Phase 7/8 extend it, do not add signal specifics now (per D-10 and the deferred list). progress.ts: MasteryStateSchema = z.enum(["untouched","learning","mastered"]); ProgressRecordSchema { userId, nodeId, patchId z.enum(PATCH_IDS), masteryState MasteryStateSchema, lastUpdated ISO datetime } — progress is patch-tagged per DATA-04. Export inferred types. Line 1 of each is the SPDX header. Both import PATCH_IDS from ../lib/patches so patchId referential integrity holds across all three schemas.
  </action>
  <verify>
    <automated>npx vitest run src/schemas/mastery.test.ts src/schemas/progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - mastery.ts exports MasteryThresholdSchema + MasteryThreshold; progress.ts exports MasteryStateSchema, MasteryState, ProgressRecordSchema, ProgressRecord
    - Both schemas reject an unknown patchId (covered by passing tests)
    - All three schema files import PATCH_IDS from ../lib/patches (patchId present on node, threshold, and progress — DATA-04)
    - `npx vitest run src/schemas/mastery.test.ts src/schemas/progress.test.ts` passes
  </acceptance_criteria>
  <done>Both schemas are real (not stubs), patch-tagged, and tested.</done>
</task>

</tasks>

<artifacts_produced>
New files: src/schemas/node.ts (NodeSummarySchema, NodeSummary, NodeFrontmatterSchema, NodeFrontmatter), src/schemas/mastery.ts (MasteryThresholdSchema, MasteryThreshold), src/schemas/progress.ts (MasteryStateSchema, MasteryState, ProgressRecordSchema, ProgressRecord), plus co-located *.test.ts for each.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw frontmatter / external data -> Zod parse | Untrusted shape validated here (ASVS V5 Input Validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-VAL | Tampering | malformed content reaching consumers | medium | mitigate | Zod v4 schemas reject malformed shapes; build-time validation (01-06) means runtime never parses raw frontmatter. |
| T-01-PID | Tampering | invalid patchId | low | mitigate | patchId is z.enum(PATCH_IDS) on all three schemas — unknown ids rejected at parse/build. |
</threat_model>

<verification>
- `npx vitest run src/schemas` passes (all three schemas)
- `npm run typecheck` exits 0
</verification>

<success_criteria>
MECHANIC/CONCEPTUAL are distinct first-class enums (criterion 2); patch_context/last_reviewed/meta_volatile are required (criterion 3); patchId appears on node, threshold, and progress schemas from the first commit (criterion 4); the graph-display subset is decoupled from full content (DATA-02).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-05-SUMMARY.md` when done.
</output>
