---
phase: 01-foundation-schema
plan: 06
type: execute
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - content-collections.ts
  - content/nodes/map-control.mdx
autonomous: true
requirements: [DATA-06, DATA-03, DATA-01]

must_haves:
  truths:
    - "A node is authored as one .mdx file with Zod-validated frontmatter plus a prose body (D-01)"
    - "Building content fails when a node's frontmatter is malformed (missing required field, unknown patchId, citation without applicationNote)"
    - "Building content fails when the MDX body is missing its required How-to-Apply section"
    - "build:content generates the typed .content-collections/generated module"
  artifacts:
    - "content-collections.ts defining the nodes collection (per-document Zod schema + transform)"
    - "content/nodes/map-control.mdx (a valid seed node)"
  key_links:
    - "content-collections.ts imports PATCH_IDS from src/lib/patches to wire patchId referential integrity into the collection schema"
    - "the schema function uses the INJECTED z (not a separate zod import) per RESEARCH Pitfall 1"
---

<objective>
Wire the content-collections pipeline: define the nodes collection so each MDX node file is Zod-validated at build time and its body is checked for the required How-to-Apply section, and author one valid seed node (D-01, D-03, D-06, DATA-01, DATA-03). This is the build-time gate behind ROADMAP success criterion 1.

Purpose: One .mdx file fully describes a node (frontmatter + prose), validated by the build — optimized for AI-authored, PR-reviewed content (D-02).
Output: content-collections.ts nodes collection + a seed node.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@src/lib/patches.ts
@content-collections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the nodes collection (per-document schema + transform)</name>
  <files>content-collections.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 1: content-collections Configuration with Zod Schema", Pitfall 1, "Anti-Patterns to Avoid")
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-01, D-03; field list from D-07)
    - content-collections.ts (the empty stub from 01-01 being replaced)
    - src/lib/patches.ts (PATCH_IDS import for patchId)
    - docs/spdx-header-convention.md (line-1 header variant)
  </read_first>
  <action>
    Replace the empty content-collections.ts stub with a defineCollection for "nodes" (directory content/nodes, include **/*.mdx). Inside the schema function use ONLY the injected z (per Pitfall 1 — do not import zod here). Validate the full node frontmatter shape mirroring NodeFrontmatterSchema: id (kebab-case regex), title, nodeType z.enum(["MECHANIC","CONCEPTUAL"]), skillType, difficulty, race, tags (array, default []), prerequisites (array, default []), patchId z.enum(PATCH_IDS imported from ./src/lib/patches cast to the required tuple), patch_context (non-empty), last_reviewed (YYYY-MM-DD regex), meta_volatile (boolean), citations (array of { source, url optional, applicationNote non-empty }). In the transform function, throw a clear per-document error if the MDX body does not contain the required How-to-Apply heading (the literal section title authors must include — see RESEARCH Pattern 1), then call compileMDX(context, document) and return the document with the compiled output. Keep the schema function (per-document shape) free of cross-document checks — prerequisite resolution and acyclicity belong to the CI script in 01-07. Line 1 is the SPDX header.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - content-collections.ts defines a "nodes" collection with directory content/nodes
    - The schema function uses the injected z and imports PATCH_IDS from ./src/lib/patches for patchId
    - The transform throws when the required How-to-Apply section is absent from the body
    - citations entries require a non-empty applicationNote
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The nodes collection validates frontmatter shape per-document and enforces the body section in transform.</done>
</task>

<task type="auto">
  <name>Task 2: Author a valid seed node and confirm content generation</name>
  <files>content/nodes/map-control.mdx</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Recommended Project Structure", "System Architecture Diagram")
    - content-collections.ts (the schema authored in Task 1 — match its required fields exactly)
    - src/lib/patches.ts (use a real patchId from the registry)
  </read_first>
  <action>
    Author content/nodes/map-control.mdx: frontmatter with every required field set to valid values (nodeType MECHANIC, a real registry patchId, valid YYYY-MM-DD last_reviewed, meta_volatile boolean, at least one citation with an applicationNote, empty prerequisites), and a prose body that includes the required How-to-Apply section. This is a minimal-but-real seed (full content authoring is Phase 3/9) that proves the pipeline accepts a well-formed node. Confirm `npm run build:content` generates .content-collections/generated containing this node.
  </action>
  <verify>
    <automated>npm run build:content</automated>
  </verify>
  <acceptance_criteria>
    - content/nodes/map-control.mdx exists with complete valid frontmatter and a How-to-Apply section in the body
    - `npm run build:content` exits 0 and generates the .content-collections/generated module including map-control
    - `npm run build` still exits 0 with the seed node present
  </acceptance_criteria>
  <done>A valid seed node passes the full build-time content pipeline.</done>
</task>

</tasks>

<artifacts_produced>
Modified: content-collections.ts (nodes collection: per-document Zod schema + transform with How-to-Apply enforcement + compileMDX).
New file: content/nodes/map-control.mdx (valid seed node).
Generated (gitignored): .content-collections/generated/ (typed content module consumed by the validate script in 01-07).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX node files -> build-time validation | Authored (incl. AI-authored) content validated before it can reach runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-MDX | Tampering | malformed frontmatter at runtime | medium | mitigate | content-collections validates at BUILD time; runtime never parses raw frontmatter (ASVS V5). |
| T-01-SEC | Information Disclosure | validation error verbosity | low | accept | Build-time only; errors are developer-facing in CI logs, no end-user exposure in Phase 1. |
</threat_model>

<verification>
- `npm run build:content` exits 0 with the valid seed node
- `npm run build` exits 0
- `npm run typecheck` exits 0
</verification>

<success_criteria>
Adding a node is editing one MDX file that the build validates against the Zod schema and rejects on malformed/missing fields or a missing How-to-Apply section (ROADMAP success criteria 1 and 3; DATA-01/03/06).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-06-SUMMARY.md` when done.
</output>
