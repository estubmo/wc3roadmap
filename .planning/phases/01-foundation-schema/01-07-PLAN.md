---
phase: 01-foundation-schema
plan: 07
type: execute
wave: 4
depends_on: ["01-06", "01-02"]
files_modified:
  - scripts/lib/detectCycles.ts
  - scripts/lib/detectCycles.test.ts
  - scripts/lib/validators.ts
  - scripts/lib/validators.test.ts
  - scripts/validate-content.ts
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements: [DATA-07, DATA-05]

must_haves:
  truths:
    - "CI fails the build when a node references a prerequisite id that does not exist"
    - "CI fails the build when the prerequisite graph contains a cycle"
    - "CI fails the build when a node carries a patchId not in the registry"
    - "CI generates content (build:content) BEFORE the validate script imports the generated module"
    - "CI uses npm ci so installs are reproducible from the committed lockfile"
  artifacts:
    - "scripts/lib/detectCycles.ts (DFS 3-color cycle detection)"
    - "scripts/lib/validators.ts (prerequisite-resolution + patchId-membership checks)"
    - "scripts/validate-content.ts (orchestrator that exits non-zero on any error)"
    - ".github/workflows/ci.yml (validate job)"
    - "package.json validate script"
  key_links:
    - "build:content must run before validate-content.ts (generated module dependency — RESEARCH Pitfall 4)"
    - "validate-content.ts imports allNodes from the generated content-collections module and PATCH_IDS from src/lib/patches"
---

<objective>
Add the cross-document CI validation layer that the per-document content-collections schema cannot do: prerequisite-id resolution, prerequisite-graph acyclicity, and patchId referential integrity, wired into a GitHub Actions job with correct build ordering (DATA-07, DATA-05). Schema validation runs as a required CI check that fails the build on malformed content (D-13). This closes ROADMAP success criterion 1 (CI fails the build on malformed content).

Purpose: Encode the WC3 domain invariants (soft DAG prerequisites, registry-valid patches) as a blocking CI check.
Output: validate script + pure validators + GitHub Actions workflow.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@src/lib/patches.ts
@content-collections.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure validators — cycle detection + prerequisite/patch checks, test-first</name>
  <files>scripts/lib/detectCycles.ts, scripts/lib/detectCycles.test.ts, scripts/lib/validators.ts, scripts/lib/validators.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 4: DAG Cycle Detection", "scripts/validate-content.ts structure")
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-09 — prerequisites soft + acyclic)
    - .planning/phases/01-foundation-schema/01-VALIDATION.md (detectCycles + prereq-resolve rows)
  </read_first>
  <behavior>
    - detectCycles returns [] for an acyclic graph (a -> b -> c)
    - detectCycles returns a non-empty error array for a cycle (a -> b -> a), and the message names the cycle path
    - detectCycles handles self-reference (a -> a) as a cycle
    - validatePrerequisiteIds returns an error for a node whose prerequisite id is not in the node set; [] when all resolve
    - validatePatchIds returns an error for a node whose patchId is not in PATCH_IDS; [] when all valid
  </behavior>
  <action>
    Write tests FIRST with synthetic node arrays, then implement. scripts/lib/detectCycles.ts exports detectCycles(nodes: {id, prerequisites: string[]}[]): string[] using the DFS 3-color (WHITE/GRAY/BLACK) algorithm from RESEARCH Pattern 4 — it collects and returns error strings rather than throwing (the orchestrator aggregates). scripts/lib/validators.ts exports validatePrerequisiteIds(nodes) and validatePatchIds(nodes, validIds) as pure functions returning string[]. Keep these decoupled from content-collections so they are unit-testable with plain arrays. Line 1 of each file is the SPDX header.
  </action>
  <verify>
    <automated>npx vitest run scripts/lib/detectCycles.test.ts scripts/lib/validators.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - detectCycles returns [] for acyclic input and a non-empty array naming the cycle for cyclic input (passing tests)
    - validatePrerequisiteIds and validatePatchIds return errors for the bad cases and [] for the good cases (passing tests)
    - `npx vitest run scripts/lib/detectCycles.test.ts scripts/lib/validators.test.ts` passes
  </acceptance_criteria>
  <done>All three pure validators are tested green.</done>
</task>

<task type="auto">
  <name>Task 2: validate-content.ts orchestrator + npm script</name>
  <files>scripts/validate-content.ts, package.json</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("scripts/validate-content.ts structure", Pitfall 4)
    - scripts/lib/detectCycles.ts, scripts/lib/validators.ts (the functions to orchestrate)
    - content-collections.ts (the generated "content-collections" import path)
  </read_first>
  <action>
    Write scripts/validate-content.ts: import the generated node collection from the "content-collections" alias and PATCH_IDS from ../src/lib/patches, run validatePrerequisiteIds, validatePatchIds, and detectCycles over the corpus, aggregate all error strings, print them readably, and process.exit(1) if any exist (else print a pass line and exit 0). Add an npm "validate" script that runs it via tsx. Because the generated module only exists after content generation, the validate script depends on build:content having run first (Pitfall 4) — document this ordering in a comment and rely on the CI job (Task 3) to sequence it. Line 1 is the SPDX header.
  </action>
  <verify>
    <automated>npm run build:content && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:content && npm run validate` exits 0 on the current corpus (the valid seed node)
    - package.json has a "validate" script
    - validate-content.ts imports allNodes from the generated content-collections module and PATCH_IDS from the registry
  </acceptance_criteria>
  <done>The orchestrator passes on the valid corpus and is wired to exit non-zero on any cross-document error.</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions CI workflow with correct build ordering</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 6: GitHub Actions CI Workflow", Pitfall 4, "Anti-Patterns" — npm ci not npm install)
    - package.json (the scripts the job invokes)
  </read_first>
  <action>
    Write .github/workflows/ci.yml with a "validate" job triggered on push to main and on pull_request: checkout, setup-node (node 20, npm cache), `npm ci` (NOT npm install — reproducible from lockfile), then steps in this exact order: build:content (generate the content module) -> typecheck (tsc --noEmit) -> validate (cross-document checks) -> build (full app build, which re-runs content-collections frontmatter validation). The build:content-before-validate ordering is mandatory because the validate script imports the generated module (Pitfall 4). Do not put Vercel secrets in this workflow (deploy is handled via the Vercel GitHub App in 01-08); no secrets are referenced here.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "npm ci" .github/workflows/ci.yml && grep -q "build:content" .github/workflows/ci.yml && grep -q "validate" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists with a job running on pull_request
    - The workflow uses `npm ci` (not `npm install`)
    - build:content appears before the validate step in the job
    - No secret/token is referenced in the workflow file
  </acceptance_criteria>
  <done>CI runs content generation, typecheck, cross-document validation, and build in the correct order on every PR.</done>
</task>

</tasks>

<artifacts_produced>
New files: scripts/lib/detectCycles.ts (detectCycles), scripts/lib/validators.ts (validatePrerequisiteIds, validatePatchIds), scripts/validate-content.ts, .github/workflows/ci.yml, plus co-located *.test.ts.
New npm script: validate.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node content corpus -> CI gate | Malformed/cyclic content blocked before merge |
| GitHub Actions runner -> logs | CI output visible to repo collaborators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-CI | Tampering | dependency install in CI | high | mitigate | `npm ci` from committed lockfile (no lockfile drift); ASVS V14. |
| T-01-LEAK | Information Disclosure | secrets in CI logs | low | mitigate | No secrets referenced in ci.yml; Vercel handled by GitHub App with tokens stored as repo secrets, never echoed. |
| T-01-DAG | Tampering | cyclic/invalid prerequisite graph | medium | mitigate | validate-content.ts fails the build on cycles, unresolved prereqs, and invalid patchIds (DATA-05/07). |
</threat_model>

<verification>
- `npx vitest run scripts/lib` passes
- `npm run build:content && npm run validate` exits 0
- ci.yml uses npm ci and orders build:content before validate
</verification>

<success_criteria>
Malformed content (missing/unknown prerequisite, cycle, invalid patchId) fails the build in CI with a clear error (ROADMAP success criterion 1; DATA-05/DATA-07).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-07-SUMMARY.md` when done.
</output>
