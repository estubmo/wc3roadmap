---
phase: 01-foundation-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/patches.ts
  - src/lib/patches.test.ts
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "The patch registry is the single source of valid WC3 patch IDs for the whole codebase"
    - "CURRENT_PATCH resolves to the last (highest-order) entry in the registry"
    - "getPatch() throws on an unknown id (fail-fast), returns the entry on a known id"
    - "PATCH_IDS is a non-empty tuple usable directly by z.enum()"
  artifacts:
    - "src/lib/patches.ts exporting PatchEntry, PATCHES, CURRENT_PATCH, PATCH_IDS, getPatch"
    - "src/lib/patches.test.ts"
  key_links:
    - "PATCH_IDS is imported by content-collections.ts (01-06) and all three schemas (01-05) to wire patchId referential integrity"
---

<objective>
Build the patch-version primitive as a deep module: a small interface (4 exports) over a curated, ordered registry of WC3 patches (D-04, D-05, DATA-04). This module is the single place a new patch is ever added, and the authoritative source of valid patch IDs consumed by every schema.

Purpose: Patch version is a system-wide primitive present from the first schema commit (ROADMAP success criterion 4), not bolted on later.
Output: src/lib/patches.ts + its test.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.agents/skills/codebase-design/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch registry module (deep module, test-first)</name>
  <files>src/lib/patches.ts, src/lib/patches.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md ("Pattern 2: Patch Registry Module (Deep Module)", Pitfall 2)
    - .agents/skills/codebase-design/SKILL.md (deep vs shallow — keep the interface to 4 exports)
    - .planning/phases/01-foundation-schema/01-VALIDATION.md (patch registry test row)
  </read_first>
  <behavior>
    - PATCH_IDS contains every registry id and is typed as [string, ...string[]] (accepted by z.enum without a cast error)
    - CURRENT_PATCH === PATCHES[PATCHES.length - 1]
    - getPatch("<a known id>") returns the matching PatchEntry
    - getPatch("does-not-exist") throws an Error whose message includes the unknown id
    - PATCHES order values are strictly ascending (0,1,2,...)
  </behavior>
  <action>
    Write src/lib/patches.test.ts FIRST (RED) asserting the behaviors above, then implement src/lib/patches.ts (GREEN). Export: interface PatchEntry { readonly id; readonly order: number; readonly released: string (ISO date); readonly objectIdMapVersion: number } — objectIdMapVersion is the reserved hook for Phase 8 replay parsing per D-04/the deferred list, do not use it now. Seed PATCHES with at least two real WC3 patch entries as `const ... satisfies readonly PatchEntry[]`. Export CURRENT_PATCH (last entry), PATCH_IDS (mapped ids cast to [string, ...string[]] per Pitfall 2), and getPatch(id) which throws on unknown id. Line 1 of patches.ts is the SPDX header `// SPDX-License-Identifier: GPL-3.0-or-later` (the documented Phase 1 default; see 01-04). Keep the array private to the module — callers use getPatch()/PATCH_IDS, never index PATCHES directly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/patches.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/patches.ts exports PatchEntry, PATCHES, CURRENT_PATCH, PATCH_IDS, getPatch
    - `npx vitest run src/lib/patches.test.ts` passes
    - Line 1 of src/lib/patches.ts is `// SPDX-License-Identifier: GPL-3.0-or-later`
    - getPatch throws on an unknown id (covered by a passing test)
  </acceptance_criteria>
  <done>Patch registry exports the 4-symbol interface; all tests green.</done>
</task>

</tasks>

<artifacts_produced>
New file: src/lib/patches.ts exporting PatchEntry (type), PATCHES, CURRENT_PATCH, PATCH_IDS, getPatch.
New file: src/lib/patches.test.ts.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure in-repo curated data) | Registry is developer-authored, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-PATCH | Tampering | unknown patchId entering a schema | low | mitigate | getPatch() fails fast on unknown ids; schemas derive z.enum from PATCH_IDS so invalid ids are rejected at build time (01-05/01-06). |
</threat_model>

<verification>
- `npx vitest run src/lib/patches.test.ts` passes
- `npm run typecheck` exits 0
</verification>

<success_criteria>
The patch primitive exists as a deep module with a 4-symbol interface, is referentially testable, and is ready to be imported by all three schemas (ROADMAP success criterion 4).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-02-SUMMARY.md` when done.
</output>
