---
phase: 01-foundation-schema
plan: 08
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - vercel.json
  - README.md
autonomous: false
requirements: [OSS-01]
user_setup:
  - service: vercel
    why: "Live deploy from day one (D-12) — connect the GitHub repo so every push builds and deploys to a real URL"
    env_vars: []
    dashboard_config:
      - task: "Import the GitHub repo into a new Vercel project (Vercel GitHub App) and confirm framework auto-detection (TanStack Start via nitro)"
        location: "Vercel Dashboard -> Add New -> Project -> Import Git Repository"
      - task: "Confirm the production build command is `npm run build` and the install command uses the committed lockfile"
        location: "Vercel Dashboard -> Project -> Settings -> Build & Development"

must_haves:
  truths:
    - "A production build succeeds locally with the nitro adapter (Vercel-detectable output)"
    - "The connected Vercel project deploys the placeholder app to a live, publicly reachable URL"
    - "The live URL is recorded in the README"
  artifacts:
    - "vercel.json (only if non-default config is required; otherwise rely on zero-config detection)"
    - "README.md with the live deploy URL and a project description"
  key_links:
    - "nitro() plugin (from 01-01) is what makes Vercel zero-config detection work (RESEARCH Pitfall 3)"
---

<objective>
Take the project live on Vercel from day one (D-12): confirm the nitro-adapted production build is Vercel-deployable, connect the GitHub repo via the Vercel GitHub App, and verify the placeholder app renders at a real public URL. This proves ROADMAP success criterion 5's "builds, deploys" clause and surfaces deploy-target friction now rather than in Phase 2.

Purpose: An early public URL for community feedback and an end-to-end pipeline (push -> CI -> deploy) validated before feature work begins.
Output: a live deployment + README pointing to it.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Vercel-deployable production build + write README</name>
  <files>vercel.json, README.md</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-RESEARCH.md (Pitfall 3 — nitro requirement; "Deployment Target")
    - .claude/CLAUDE.md ("Deployment Target" table)
    - vite.config.ts (confirm nitro() is wired from 01-01)
  </read_first>
  <action>
    Verify `npm run build` produces nitro output that Vercel can detect zero-config (the nitro() plugin from 01-01 handles this — Pitfall 3). Only add a vercel.json if a non-default setting is genuinely required (e.g. an explicit build/install command); prefer zero-config and omit vercel.json otherwise. Write README.md: project name and one-paragraph description (free, open-source, GPL-3.0 WC3 learning roadmap), the GPL-3.0 license note, a "Live: <URL>" line (filled after the deploy in Task 2), and basic dev commands (npm ci, npm run dev, npm run build, npm run validate). Do NOT commit any Vercel token or secret (CLAUDE.md: never commit secrets/.env).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and emits the nitro/Vercel output directory
    - README.md exists with the project description, GPL-3.0 note, a Live URL placeholder/line, and dev commands
    - No secret or token is committed (no .env, no token in vercel.json)
  </acceptance_criteria>
  <done>Production build is Vercel-ready and the README is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Connect Vercel and verify the live deployment</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-08-PLAN.md (the user_setup block above — the Vercel connection steps)
  </read_first>
  <action>
    After the user completes the Vercel project connection (see user_setup), trigger a deploy by pushing to the connected branch and let the Vercel GitHub App build and deploy. Once the deployment is green, record the resolved production URL in README.md's Live line. The placeholder home route must render at that URL.
  </action>
  <verify>
    <human-check>
      1. Open the Vercel production URL in a browser.
      2. Confirm the placeholder home page renders (no 404, no SSR error).
      3. Confirm the URL is recorded in README.md.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The Vercel production URL serves the placeholder app (HTTP 200, page renders)
    - README.md Live line contains the actual production URL
  </acceptance_criteria>
  <done>The app is live at a public Vercel URL recorded in the README.</done>
</task>

</tasks>

<artifacts_produced>
New file: README.md (project description, GPL-3.0 note, live URL, dev commands).
Optional: vercel.json (only if non-default config is required).
External: a live Vercel project + production URL (connected via the Vercel GitHub App).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo -> Vercel build/runtime | Source crosses into the hosting provider |
| public internet -> deployed app | Anonymous users reach the live URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-DEPLOY | Information Disclosure | Vercel token / secrets | medium | mitigate | Tokens live in Vercel/GitHub settings, never in the repo or vercel.json; .env gitignored. |
| T-01-SURFACE | Tampering | publicly deployed app | low | accept | Phase 1 app is a static placeholder with no user input, auth, or data — minimal attack surface (auth/DB arrive in Phase 4). |
</threat_model>

<verification>
- `npm run build` exits 0 with nitro output
- Live Vercel URL renders the placeholder app (human-check)
- README records the live URL
</verification>

<success_criteria>
The project builds and deploys to a live public URL with GPL-3.0 licensing in place (ROADMAP success criterion 5; D-12; OSS-01 public code/deployment).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema/01-08-SUMMARY.md` when done.
</output>
