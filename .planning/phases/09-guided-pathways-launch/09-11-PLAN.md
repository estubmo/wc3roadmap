---
phase: 09-guided-pathways-launch
plan: 11
type: execute
wave: 3
depends_on: ["09-09"]
files_modified:
  - src/components/graph/GraphNode.tsx
autonomous: true
requirements: [PATH-01, CONT-05]
must_haves:
  truths:
    - "every pathway node shows a 1-based step-number badge; mastered steps get the rune-gold badge fill"
    - "exactly one node (the first non-mastered step) shows the 'Next' cue with an entrance-only animation"
    - "stale nodes show a neutral clock marker on the canvas; non-stale nodes show nothing"
  artifacts:
    - "GraphNode step-number badge, 'Next' cue pill, and stale marker overlays"
  key_links:
    - "reads data.stepIndex/data.isNextStep/data.stale set by RoadmapGraph (09-09)"
---

<objective>
Render the three new node-face overlays (PATH-01/D-04, CONT-05/D-08): the pathway step-number badge, the single "Next" cue, and the neutral staleness marker — all as absolutely-positioned children following GraphNode's existing overlay + aria conventions.

Purpose: Make the pathway explicitly ordered ("do this next") and let users spot stale content without opening each panel.
Output: extended GraphNode.tsx.
</objective>

<artifacts>
This plan produces (all in src/components/graph/GraphNode.tsx):
- Step-number badge (top-left, 20px circle; default obsidian styling / mastered rune-500 fill; renders only for pathway-step nodes; `aria-label="Step {n} of {total}"`).
- "Next" cue pill (top: -28px, centered; rune-600 border / rune-400 text + ChevronDown; entrance-only motion; `aria-label="Next recommended step"`).
- Stale marker (bottom-right; 10px Clock; neutral `#e9e8ee` at 55% opacity; `role="img"` + aria-label; renders only when data.stale).
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/components/graph/GraphNode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GraphNodeData type + add step-number badge and stale marker</name>
  <files>src/components/graph/GraphNode.tsx</files>
  <read_first>
    - src/components/graph/GraphNode.tsx (full — GraphNodeData at 48-51; faction-tint overlay at 227-239 and difficulty-dots aria table at 131-137 as the overlay+aria patterns to clone; top row at 253-307)
    - 09-UI-SPEC.md §Pathway Step Numbers (badge: 20px circle, top -8px/left -8px, 10px/600 numeral, default obsidian-800 bg + obsidian-600 border, mastered rune-500 bg / obsidian-950 text; pathway nodes only) + §Staleness Graph Node Marker (10px Clock bottom 4px/right 4px, #e9e8ee 55%, role img) + §Color (staleness never rune-gold)
    - 09-PATTERNS.md §GraphNode.tsx (transient data fields; role img + aria-label lookup pattern)
  </read_first>
  <action>
    Edit GraphNode.tsx. Extend the GraphNodeData type with the transient fields set by RoadmapGraph (09-09): stepIndex?: number, isNextStep?: boolean, stale?: boolean (these are node.data, not schema fields). Read them off d. Add a step-number badge: an absolutely-positioned span at top -8px / left -8px, 20px diameter circle (border-radius 9999px), numeral d.stepIndex at 10px/600 var(--font-sans); render ONLY when stepIndex is defined (pathway nodes only). Default styling: background var(--color-obsidian-800), border 1px solid var(--color-obsidian-600), text #e9e8ee at 80%; when masteryState === "mastered" use background var(--color-rune-500), text var(--color-obsidian-950), no border (accent item 10). aria-label "Step {stepIndex} of {pathway total}" — pass the total via a new optional data field or compute from stepIndex context (use data.pathwayTotal set in 09-09 if present, else omit the count from the label gracefully). Add the stale marker: import Clock from lucide-react; render an absolutely-positioned span at bottom 4px / right 4px with a 10px Clock, color #e9e8ee at 55% opacity, role="img", aria-label "Content may be outdated for the current patch"; render ONLY when d.stale === true. Keep all existing overlays (faction tint, mastery badge, difficulty dots) untouched. Inline CSS variables / neutral hex per UI-SPEC (staleness neutral is the one allowed non-token color, exactly as the UI-SPEC specifies).
  </action>
  <verify>
    <automated>grep -q "stepIndex" src/components/graph/GraphNode.tsx && grep -q "Clock" src/components/graph/GraphNode.tsx && grep -q "Step " src/components/graph/GraphNode.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Step badge renders for pathway nodes (gold when mastered), stale marker renders only when data.stale; both use role/aria per convention; tsc clean.
  </acceptance_criteria>
  <done>Step-number badge + stale marker present and gated correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Next" cue pill with entrance-only animation</name>
  <files>src/components/graph/GraphNode.tsx</files>
  <read_first>
    - 09-UI-SPEC.md §Pathway Step Numbers → "Next" cue (pill top -28px centered, "Next" 10px/600 uppercase letter-spacing 0.05em rune-400 on obsidian-800, rune-600 border, ChevronDown 10px rune-400; entrance only initial opacity 0 y -4 → animate opacity 1 y 0, duration 0.25 easeOut, no loop) + §Animation Contract
    - src/components/graph/GraphNode.tsx (motion import already present at line 33)
  </read_first>
  <action>
    Add the "Next" cue to GraphNode, rendered ONLY when d.isNextStep === true. Use a motion.div (motion/react, already imported) pill positioned absolute, centered above the node at top -28px (clears the step badge), containing the uppercase text "Next" (10px/600, letter-spacing 0.05em, color var(--color-rune-400)) on background var(--color-obsidian-800) with border 1px solid var(--color-rune-600), border-radius 9999px, padding 2px 8px; place a ChevronDown (lucide, 10px, color var(--color-rune-400)) directly below the pill pointing at the node. Animate entrance only: initial { opacity: 0, y: -4 }, animate { opacity: 1, y: 0 }, transition { duration: 0.25, ease: "easeOut" } — no loop, no repeat (Phase 2 rule + PROG-05). aria-label="Next recommended step" on the pill so the signal is not color-only. Because the next node is by definition never mastered, its step badge stays in the default (non-gold) styling — no visual conflict.
  </action>
  <verify>
    <automated>grep -q "isNextStep" src/components/graph/GraphNode.tsx && grep -q "ChevronDown" src/components/graph/GraphNode.tsx && grep -q "Next recommended step" src/components/graph/GraphNode.tsx && npx tsc --noEmit && npm run test</automated>
  </verify>
  <acceptance_criteria>
    Exactly one node (isNextStep) shows the entrance-only "Next" pill + ChevronDown; no looping animation; aria-label present; tsc + suite green.
  </acceptance_criteria>
  <done>The "Next" cue renders on the single next step with an entrance-only animation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure presentational overlays over trusted transient node data |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Tampering | node-face overlays | low | accept | Overlays render from trusted client-derived node data; no external input, no new attacker surface. No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; npm run test green.
- grep confirms stepIndex/Clock/ChevronDown/"Next recommended step" present.
- Manual (09-13): pathway nodes numbered 1..8, one "Next" cue, mastered step badges gold, stale marker on meta-volatile out-of-patch nodes.
</verification>

<success_criteria>
The canvas shows an explicitly ordered pathway with a single "do this next" cue and neutral staleness markers — no new rune-gold hierarchy muddying (F-01).
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-11-SUMMARY.md` when done.
</output>