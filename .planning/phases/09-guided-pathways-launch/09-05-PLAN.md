---
phase: 09-guided-pathways-launch
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/__root.tsx
  - src/components/NotFoundPage.tsx
autonomous: true
requirements: [PATH-03]
must_haves:
  truths:
    - "a single TooltipProvider is mounted high in the tree so the staleness tooltip (09-12) has its required Radix context"
    - "an unknown URL renders a branded 404 page with a link back to the roadmap"
    - "OG/Twitter share meta tags are present on first server-rendered load"
  artifacts:
    - "TooltipProvider wrapping the app tree in __root.tsx"
    - "notFoundComponent wired on the root route"
    - "src/components/NotFoundPage.tsx"
    - "OG + twitter:card meta entries in the root head()"
  key_links:
    - "TooltipProvider is the required ancestor for NodePanelContent's staleness Tooltip (09-12, Pitfall 1)"
---

<objective>
Launch-polish + Tooltip foundation (D-16, PATH-03). Mount the app-wide `<TooltipProvider>` (first-ever Tooltip use in the codebase — Pitfall 1), add OG/Twitter share meta to the root `head()`, and wire a branded 404 via TanStack Router's `notFoundComponent`.

Purpose: Make the app shareable and error-graceful for launch, and unblock the staleness tooltip by providing its required Radix context once, high in the tree.
Output: extended __root.tsx + NotFoundPage component.
</objective>

<artifacts>
This plan produces:
- `<TooltipProvider>` mounted in RootDocument (src/routes/__root.tsx), wrapping the app tree alongside the existing QueryClientProvider.
- `notFoundComponent: NotFoundPage` on the root route.
- OG (`og:title`/`og:description`/`og:type`/`og:image`) + `twitter:card` + `description` meta in the root head().
- `src/components/NotFoundPage.tsx` (new).
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@src/routes/__root.tsx
@src/components/ui/tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the branded NotFoundPage component</name>
  <files>src/components/NotFoundPage.tsx</files>
  <read_first>
    - src/routes/index.tsx EmptyState() (lines 82-123 — centered-column layout + inline CSS-variable typography convention to clone)
    - 09-UI-SPEC.md §Launch Polish → 404 page (copy + layout: 22px pathway-heading h1, 14px body, gold CTA, obsidian-950 bg, gap 16px, padding-top 64px)
    - 09-UI-SPEC.md §Copywriting Contract (404 heading/body/CTA exact copy)
  </read_first>
  <action>
    Create src/components/NotFoundPage.tsx with the SPDX header. Render a centered column (min-height 100dvh, background var(--color-obsidian-950), gap 16px, padding-top 64px) matching index.tsx EmptyState conventions (inline styles, CSS variables only, no hardcoded hex). Heading in the 22px/600 Space Grotesk pathway-heading role with the exact 404 heading copy from the Copywriting Contract; body at 14px/400/1.65 Outfit at opacity 0.7 with the 404 body copy; a gold-CTA Button (shadcn Button, gold treatment matching the SignInButton family — rune-500 fill / obsidian-950 text) using TanStack Router Link to "/" with the 404 CTA label. Use h1 for the heading (standard heading hierarchy per Accessibility Baseline).
  </action>
  <verify>
    <automated>test -f src/components/NotFoundPage.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    NotFoundPage renders a centered obsidian column with the exact 404 copy and a gold CTA linking to "/"; tsc clean.
  </acceptance_criteria>
  <done>NotFoundPage component exists, typed, styled per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TooltipProvider, wire notFoundComponent, add OG meta in __root.tsx</name>
  <files>src/routes/__root.tsx</files>
  <read_first>
    - src/routes/__root.tsx (full — head() at 29-48, RootDocument provider tree at 52-80)
    - src/components/ui/tooltip.tsx (TooltipProvider export)
    - 09-RESEARCH.md Pattern 4 (notFoundComponent + head() meta with property: keys) + Pitfall 1 (provider must be an ancestor, added once)
    - 09-UI-SPEC.md §Page Metadata / OG Tags (exact tag values)
  </read_first>
  <action>
    In src/routes/__root.tsx: (1) Import TooltipProvider from #/components/ui/tooltip and wrap the app subtree inside RootDocument with a single <TooltipProvider> — place it inside QueryClientProvider around SiteHeader + {children} + Toaster (one provider only, high in the tree per Pitfall 1; do NOT add a second provider in NodePanelContent). (2) Import NotFoundPage from #/components/NotFoundPage and add notFoundComponent: NotFoundPage as a sibling config key alongside head/shellComponent on createRootRoute. (3) Extend the head() meta array with a description entry (name:"description") plus OG entries using property: keys (og:title, og:description, og:type "website", og:image) and a twitter:card "summary_large_image" entry — exact values per the UI-SPEC OG table. Leave the existing charSet/viewport/title and the stylesheet link unchanged. For og:image, reference a static /og-image.png path in public (the asset itself is out of scope — a placeholder path is acceptable and noted for the content/design workstream).
  </action>
  <verify>
    <automated>grep -q "TooltipProvider" src/routes/__root.tsx && grep -q "notFoundComponent" src/routes/__root.tsx && grep -q "og:title" src/routes/__root.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Exactly one TooltipProvider wraps the tree; notFoundComponent points to NotFoundPage; head() includes description + og:* + twitter:card; tsc clean; app still renders (dev server boots).
  </acceptance_criteria>
  <done>__root.tsx mounts TooltipProvider once, serves OG meta, and renders NotFoundPage for unknown routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| request → SSR head/meta | Meta values are static strings rendered server-side; no user input reflected |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | OG meta / 404 route | low | accept | Static meta strings, no reflected input; 404 uses the framework's first-class notFoundComponent (no hand-rolled catch-all). No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; dev server boots.
- Navigating to an unknown path renders NotFoundPage with a working "/" link.
- View-source on the landing page shows og:title / twitter:card meta.
</verification>

<success_criteria>
TooltipProvider is available app-wide (unblocking 09-12), unknown URLs render a branded 404, and share links carry OG/Twitter meta.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-05-SUMMARY.md` when done.
</output>
