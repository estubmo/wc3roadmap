---
phase: 09-guided-pathways-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/graph.ts
  - docs/adr/013-graph-projection-staleness-widening.md
autonomous: true
requirements: [CONT-05]
must_haves:
  truths:
    - "GraphDisplayNode carries a stale boolean so the on-canvas staleness marker can render without a content query"
    - "the deliberate ADR-002 boundary widening is recorded in a dedicated ADR"
  artifacts:
    - "stale: z.boolean() on GraphDisplayNodeSchema"
    - "docs/adr/013-graph-projection-staleness-widening.md"
  key_links:
    - "stale is populated by the index.tsx loader (09-10) and rendered by GraphNode (09-11)"
---

<objective>
Widen the graph-display projection boundary with a single new `stale: boolean` field (D-09) and document the deliberate ADR-002 widening in a new ADR 013. Per the ADR-005/006 rule, any GraphDisplayNode field addition requires its own ADR.

Purpose: Let the on-canvas staleness marker (D-08) read a typed boolean off the node projection instead of pulling content fields onto the graph layer — a knowingly-accepted, ADR-gated boundary widening.
Output: extended graph.ts + ADR 013.
</objective>

<artifacts>
This plan produces:
- `GraphDisplayNodeSchema.stale: z.boolean()` — src/schemas/graph.ts (header projection-field list updated to include "stale — ADR 013").
- `docs/adr/013-graph-projection-staleness-widening.md` — new ADR (Status: Accepted).
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/schemas/graph.ts
@docs/adr/005-graph-display-node.md
@docs/adr/002-content-graph-decoupling.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stale field to GraphDisplayNodeSchema</name>
  <files>src/schemas/graph.ts</files>
  <read_first>
    - src/schemas/graph.ts (full — extend block at 42-60, header projection-field list at 11-14 and 36-40)
    - 09-PATTERNS.md §graph.ts (exact addition guidance)
    - 09-RESEARCH.md Pattern 2 + Anti-Patterns (only stale gets schema treatment; stepIndex/isNextStep must NOT be added here)
  </read_first>
  <action>
    Add stale: z.boolean() to the GraphDisplayNodeSchema.extend({...}) block, with a doc-comment citing D-09 and ADR 013 explaining it is the computed staleness flag for the on-canvas marker and a deliberate ADR-002 boundary widening. Update BOTH header comment lists (the file-level projection-fields list around lines 11-14 and the schema-level list around 36-40) to add "stale — ADR 013". Do NOT add stepIndex or isNextStep here — those are pathway-derived transient node.data fields computed client-side in RoadmapGraph (09-09), never schema fields (RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>grep -n "stale" src/schemas/graph.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    stale: z.boolean() present in the extend block; header lists updated; no stepIndex/isNextStep added; tsc clean.
  </acceptance_criteria>
  <done>GraphDisplayNodeSchema has stale boolean; header docs reference ADR 013; tsc --noEmit clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR 013 documenting the staleness projection widening</name>
  <files>docs/adr/013-graph-projection-staleness-widening.md</files>
  <read_first>
    - docs/adr/005-graph-display-node.md (Status/Date/Phase header block + Context/Decision/Consequences shape to clone verbatim)
    - docs/adr/002-content-graph-decoupling.md (the boundary rule being widened)
    - 09-CONTEXT.md D-09 (the widening rationale: on-canvas marker justifies the extra boundary field)
  </read_first>
  <action>
    Create docs/adr/013-graph-projection-staleness-widening.md cloning the ADR 005 structure (Status: Accepted; Date; Phase 9; Context; Decision; Consequences). Context: staleness (meta_volatile + patchId vs CURRENT_PATCH) is content-derived, but D-08 requires an on-canvas marker so users spot stale nodes without opening each panel; pulling meta_volatile/patchId onto the graph layer would violate ADR 002. Decision: add exactly one derived boolean (stale) to GraphDisplayNodeSchema, computed in the loader via the single isStale predicate (src/lib/staleness.ts) — the source content fields never cross the boundary, only the derived boolean. Consequences: the projection boundary is not frozen, only ADR-gated (this is the first widening since ADR 006); future additions still require their own ADR.
  </action>
  <verify>
    <automated>test -f docs/adr/013-graph-projection-staleness-widening.md && grep -qi "Accepted" docs/adr/013-graph-projection-staleness-widening.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    ADR 013 exists with Status Accepted and Context/Decision/Consequences sections that reference D-09, ADR 002, and the single isStale predicate.
  </acceptance_criteria>
  <done>ADR 013 authored following the ADR 005 shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content → graph projection | Widening the boundary risks leaking content fields onto the graph layer if done by spreading frontmatter |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | graph projection boundary | low | mitigate | Only a single derived boolean crosses the boundary; source content fields stay on the content layer (ADR 013), enforced by explicit field-by-field projection in 09-10. No new packages. |
</threat_model>

<verification>
- grep confirms stale in graph.ts; tsc --noEmit clean.
- ADR 013 file exists with the accepted decision.
</verification>

<success_criteria>
GraphDisplayNode has a typed stale boolean with a dedicated ADR justifying the boundary widening; no content fields leaked onto the graph layer.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-04-SUMMARY.md` when done.
</output>
