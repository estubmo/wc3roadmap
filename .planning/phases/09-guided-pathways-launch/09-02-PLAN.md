---
phase: 09-guided-pathways-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/staleness.ts
  - src/lib/staleness.test.ts
autonomous: true
requirements: [CONT-05]
must_haves:
  truths:
    - "isStale returns true iff meta_volatile is true AND patchId differs from the current patch id"
    - "a single staleness predicate exists in the codebase (no duplicated boolean expression)"
  artifacts:
    - "src/lib/staleness.ts (pure isStale predicate)"
    - "src/lib/staleness.test.ts (all four truth-table combinations)"
  key_links:
    - "consumed by the index.tsx loader projection (09-10) and NodePanelContent staleness strip (09-12)"
---

<objective>
Create the single-source-of-truth staleness predicate (D-06 locked trigger) used by BOTH the graph loader projection and the detail-panel strip. Success criterion 5 requires meta-volatile nodes whose patch has moved to surface a staleness indicator; this pure function is the one rule that decides staleness everywhere.

Purpose: Guarantee exactly one staleness rule in the codebase (per D-15 discretion note "keep a single source-of-truth staleness predicate").
Output: src/lib/staleness.ts + colocated test.
</objective>

<artifacts>
This plan produces:
- `src/lib/staleness.ts` — exports `isStale(metaVolatile: boolean, patchId: string, currentPatchId: string): boolean`.
- `src/lib/staleness.test.ts` — Vitest truth-table coverage.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/lib/patches.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing truth-table test for isStale</name>
  <files>src/lib/staleness.test.ts</files>
  <read_first>
    - 09-RESEARCH.md Code Examples §Staleness predicate + Validation Architecture (staleness.test.ts row)
    - 09-PATTERNS.md §staleness.ts (D-06 locked trigger, no label field on PatchEntry)
    - src/lib/patches.ts (PatchEntry has id only — no label; CURRENT_PATCH.id is the comparison value)
  </read_first>
  <behavior>
    - metaVolatile true + patchId !== currentPatchId → true (stale).
    - metaVolatile true + patchId === currentPatchId → false.
    - metaVolatile false + patchId !== currentPatchId → false.
    - metaVolatile false + patchId === currentPatchId → false.
  </behavior>
  <action>
    Create src/lib/staleness.test.ts covering all four boolean combinations of the D-06 trigger. Import isStale via a RELATIVE import (./staleness) — vitest has no "#/" alias resolver. Use concrete patch id strings (for example a current id and an older id) so the patchId inequality path is exercised. Do NOT create the implementation yet — RED first.
  </action>
  <verify>
    <automated>npx vitest run src/lib/staleness.test.ts 2>&1 | grep -qiE "fail|error|cannot find|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    Test asserts all four truth-table rows and fails (RED) because isStale does not exist yet.
  </acceptance_criteria>
  <done>Test file present with 4 cases; vitest run is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement isStale predicate (GREEN)</name>
  <files>src/lib/staleness.ts</files>
  <read_first>
    - 09-RESEARCH.md Code Examples §Staleness predicate (verbatim body)
    - src/lib/patches.ts (SPDX header + module doc-comment convention)
  </read_first>
  <action>
    Create src/lib/staleness.ts with the SPDX two-line header and a module doc-comment framing it as the single staleness predicate: cite D-06 (locked trigger), D-09 (loader projection consumer) and D-07 (panel consumer), and state explicitly that both the GraphDisplayNode.stale projection and the panel check call this one function so there is exactly one staleness rule. Export isStale(metaVolatile, patchId, currentPatchId) returning metaVolatile && patchId !== currentPatchId. No imports — pure boolean logic only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/staleness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    All four truth-table cases pass; module has zero imports.
  </acceptance_criteria>
  <done>vitest run src/lib/staleness.test.ts passes; tsc --noEmit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure boolean over build-time content fields + patch registry; no runtime input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information Disclosure | staleness signal | low | accept | Staleness is a non-sensitive content-trust hint derived from public patch ids; no user data. No new packages (no SC gate). |
</threat_model>

<verification>
- npx vitest run src/lib/staleness.test.ts is green.
- tsc --noEmit clean.
</verification>

<success_criteria>
isStale is the one staleness rule; four truth-table combinations verified; ready for the loader and panel consumers.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-02-SUMMARY.md` when done.
</output>
