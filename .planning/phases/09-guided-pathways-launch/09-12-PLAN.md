---
phase: 09-guided-pathways-launch
plan: 12
type: execute
wave: 3
depends_on: ["09-02", "09-05"]
files_modified:
  - src/components/graph/NodePanelContent.tsx
autonomous: true
requirements: [CONT-05]
must_haves:
  truths:
    - "a meta-volatile node whose patch has moved shows an 'Unreviewed for {patch}' strip below the panel header"
    - "the strip carries a tooltip that opens on hover, focus, AND tap (touch), explaining the staleness"
    - "reviewed / current-patch nodes show no strip and no reserved space"
  artifacts:
    - "staleness strip in NodePanelContent replacing the deferred D-15 hook"
  key_links:
    - "uses isStale (09-02) on the already-fetched NodeFrontmatter; relies on the app-wide TooltipProvider (09-05)"
---

<objective>
Surface the panel staleness indicator (success criterion 5, D-06/D-07): a subtle "Unreviewed for {patch}" strip below the panel header, with a touch-capable tooltip carrying the full explanation. Replaces the deferred D-15 hook already flagged in NodePanelContent.

Planner decision (RESEARCH Pitfall 4 / A1): use CURRENT_PATCH.id directly in the copy (matching the authoritative Copywriting Contract, which uses {CURRENT_PATCH.id}). Do NOT add a label field to PatchEntry — the id string is sufficient and lowest-risk.

Purpose: Calibrate user trust in meta-volatile content honestly, consistent with the "trustworthy / no decorative science" core value.
Output: extended NodePanelContent.tsx.
</objective>

<artifacts>
This plan produces (in src/components/graph/NodePanelContent.tsx):
- A staleness strip (below the header, above the scrollable body) rendered only when isStale(node.meta_volatile, node.patchId, CURRENT_PATCH.id).
- A controlled shadcn Tooltip (open on hover/focus AND tap) with the full explanation copy.
- Removal/update of the two deferred D-15 comments (lines ~35-36 and ~226-227).
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/components/graph/NodePanelContent.tsx
@src/lib/staleness.ts
@src/lib/patches.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the staleness strip with a touch-capable tooltip</name>
  <files>src/components/graph/NodePanelContent.tsx</files>
  <read_first>
    - src/components/graph/NodePanelContent.tsx (full — query at 233-238 returns full NodeFrontmatter incl. meta_volatile/patchId; header ends ~331; deferred D-15 comments at 35-36 and 226-227)
    - 09-RESEARCH.md Code Examples §Panel staleness strip + Pitfall 1 (TooltipProvider already mounted in 09-05) + Pitfall 2 (Radix Tooltip has no default tap support — use controlled open + onClick) + Pitfall 4 (use CURRENT_PATCH.id)
    - 09-UI-SPEC.md §Staleness Indicator — Node Detail Panel (strip padding 6px 20px, obsidian-800 bg, obsidian-700 border-bottom, 12px Clock neutral 65%, 11px/600 neutral text, tabIndex 0) + §Copywriting Contract (pill + tooltip exact copy) + §Accessibility Baseline (contrast ≥4.5:1; keyboard focusable; not hover-only)
  </read_first>
  <action>
    Edit NodePanelContent. Import isStale from #/lib/staleness, CURRENT_PATCH from #/lib/patches, Clock from lucide-react, and Tooltip/TooltipTrigger/TooltipContent from #/components/ui/tooltip. After the header block and before the scrollable body div, render the strip ONLY when node is loaded and isStale(node.meta_volatile, node.patchId, CURRENT_PATCH.id) is true. The strip: padding 6px 20px, background var(--color-obsidian-800), border-bottom 1px solid var(--color-obsidian-700); a 12px Clock (neutral #e9e8ee ~65% opacity, aria-hidden) + 6px gap + the pill text at 11px/600 neutral tone (NOT rune-gold) using the exact Copywriting Contract pill copy with CURRENT_PATCH.id interpolated. Make the whole strip the tooltip trigger: use a controlled Tooltip (local open state via useState) wired so onOpenChange updates it AND an onClick on the trigger toggles open (Pitfall 2 — gives tap support on touch); the trigger element is keyboard-focusable (tabIndex 0). TooltipContent shows the exact tooltip copy from the Copywriting Contract (interpolating node.patchId and CURRENT_PATCH.id). Do NOT add a new data query — node already carries meta_volatile/patchId. Verify the neutral text opacity clears 4.5:1 contrast on obsidian-800; bump opacity if needed (Accessibility Baseline). Finally, update the two deferred D-15 comments (module doc at ~35-36 and the in-body note at ~226-227) to state that the staleness UI is now implemented (D-06/D-07) rather than deferred.
  </action>
  <verify>
    <automated>grep -q "isStale" src/components/graph/NodePanelContent.tsx && grep -q "Unreviewed for" src/components/graph/NodePanelContent.tsx && grep -q "Tooltip" src/components/graph/NodePanelContent.tsx && npx tsc --noEmit && npm run test</automated>
  </verify>
  <acceptance_criteria>
    Strip renders only for stale nodes; tooltip opens on hover, focus, and tap; copy matches the Copywriting Contract with CURRENT_PATCH.id; the deferred D-15 comments are updated to implemented; tsc + suite green.
  </acceptance_criteria>
  <done>The panel staleness strip replaces the deferred hook and is touch-accessible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content field → panel render | Staleness derived from already-fetched, build-time content fields |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Information Disclosure | staleness copy | low | accept | Copy exposes only public patch ids and a content-review date concept; no user data. Tooltip uses the app-wide TooltipProvider (09-05). No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; npm run test green.
- grep confirms isStale + "Unreviewed for" + Tooltip present; no "deferred" staleness comment remains.
- Manual (09-13): a meta-volatile out-of-patch node shows the strip; tapping it on a touch viewport opens the tooltip; a current-patch node shows nothing.
</verification>

<success_criteria>
Meta-volatile out-of-patch nodes honestly signal staleness in the panel via a touch-accessible tooltip; current nodes show nothing.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-12-SUMMARY.md` when done.
</output>