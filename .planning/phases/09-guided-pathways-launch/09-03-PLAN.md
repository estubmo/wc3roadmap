---
phase: 09-guided-pathways-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/node.ts
  - content-collections.ts
  - content/nodes/army-positioning.mdx
autonomous: true
requirements: [CONT-04, CONT-05]
must_haves:
  truths:
    - "every node MDX file must explicitly declare launch_ready as a boolean or the content build fails"
    - "all 17 existing nodes carry launch_ready: false until the citation-audit workstream flips them"
    - "auditNote is an optional per-node frontmatter field carrying the citation-audit verdict/rationale"
  artifacts:
    - "launch_ready field on NodeFrontmatterSchema + content-collections mirror"
    - "auditNote field on NodeFrontmatterSchema + content-collections mirror"
    - "17 migrated MDX frontmatter blocks (launch_ready: false)"
  key_links:
    - "launch_ready read by the launched-graph loader filter (09-10) and the launch-gate CI validators (09-08)"
    - "auditNote enforced-on-launch by validateAuditTrail (09-08)"
---

<objective>
Add the `launch_ready` publishability flag and the `auditNote` citation-audit-trail field to the node content schema (CONT-04, CONT-05, D-12/D-13/D-15) following the repo's parallel-schema-sync convention, and migrate all 17 existing nodes to explicitly declare `launch_ready: false`.

Planner decisions locked here (RESEARCH Open Questions 1 + 3, D-15 discretion):
- launch_ready is a REQUIRED boolean with NO schema default — every MDX file must declare it explicitly so an omission fails CI loudly and the D-10 upgrade-review pass is visible in each file's diff (RESEARCH Q3 recommendation).
- All 17 existing nodes are migrated to launch_ready: false (they are re-audited by the parallel content workstream, D-11 — this plan does NOT flip any to true; flipping is the human citation audit, CONT-05).
- The citation-audit trail (D-13, RESEARCH Q1) is modelled as an OPTIONAL frontmatter field `auditNote: z.string()` — keeping "one .mdx fully describes one node". The launch-time requirement (auditNote must be present when launch_ready is true) is enforced by the CI validator in plan 09-08, not by the schema, to avoid coupling the always-on content build to launch state.

Purpose: The schema mechanism + gate substrate for the ~25-node launch bar and the auditable no-decorative-science trail.
Output: extended node.ts + content-collections.ts (field-for-field mirror), 17 migrated MDX files.
</objective>

<artifacts>
This plan produces:
- `NodeFrontmatterSchema.launch_ready: z.boolean()` (required, no default) — src/schemas/node.ts.
- `NodeFrontmatterSchema.auditNote: z.string().optional()` — src/schemas/node.ts.
- Field-for-field mirror of both fields in content-collections.ts.
- `launch_ready: false` added to the frontmatter of all 17 files under content/nodes/.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/schemas/node.ts
@content-collections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launch_ready + auditNote to NodeFrontmatterSchema and content-collections mirror</name>
  <files>src/schemas/node.ts, content-collections.ts</files>
  <read_first>
    - src/schemas/node.ts lines 339-431 (NodeFrontmatterSchema.extend block; meta_volatile at ~389-395 and patch_context at ~379-381 as the "required, no default" precedent)
    - content-collections.ts lines 24-53 (schema object; meta_volatile mirror at line 53) and the parallel-schema-sync doc comments
    - 09-PATTERNS.md §node.ts (parallel-schema-sync is a binding repo-wide rule) + §Shared Patterns
    - 09-RESEARCH.md Open Questions 1 + 3 (auditNote + no-default decisions)
  </read_first>
  <action>
    In src/schemas/node.ts, add two fields inside the NodeFrontmatterSchema.extend({...}) block. First, launch_ready: z.boolean() with NO .default() — a required boolean, matching the "required, no default" density of meta_volatile/patch_context. Give it a doc-comment citing D-12/D-15/CONT-04 explaining: excluded from the launched graph when false; the citation audit (CONT-05) flips it to true; no default forces every MDX to declare it so CI fails loudly on omission. Second, auditNote: z.string().optional() with a doc-comment citing D-13/CONT-05 explaining it records the per-node citation-audit verdict + rationale (the auditable no-decorative-science trail); optional at the schema layer, required-when-launch_ready enforced by CI (plan 09-08). In content-collections.ts add BOTH fields field-for-field identical (same names, same z types, mirrored doc-comment cross-references) in the same commit — this is the binding parallel-schema-sync rule (used for quiz, autoDetect, replayCriteria). Do not add a default in either file.
  </action>
  <verify>
    <automated>grep -c "launch_ready" src/schemas/node.ts content-collections.ts && grep -c "auditNote" src/schemas/node.ts content-collections.ts</automated>
  </verify>
  <acceptance_criteria>
    Both fields present in both files with identical z types; launch_ready has no .default(); auditNote is .optional(); tsc --noEmit clean.
  </acceptance_criteria>
  <done>launch_ready + auditNote appear field-for-field in node.ts and content-collections.ts; no schema-level default on launch_ready.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 17 existing node MDX files to launch_ready: false</name>
  <files>content/nodes/army-positioning.mdx</files>
  <read_first>
    - content/nodes/army-positioning.mdx (frontmatter shape; meta_volatile line is the anchor to add launch_ready beneath)
    - 09-CONTEXT.md D-11 (writing/re-review is the parallel workstream; this task only adds the field mechanically, does NOT audit content)
  </read_first>
  <action>
    Add the frontmatter line launch_ready: false to EVERY .mdx file under content/nodes/ (all 17 files, not only army-positioning.mdx which is listed as the representative file_modified). Place the line adjacent to meta_volatile in each frontmatter block for consistency. Do NOT set any node to true and do NOT edit body content or citations — flipping launch_ready to true is the human citation audit performed by the parallel content workstream (D-11, CONT-05), out of scope for this code task. Leave auditNote absent on the existing 17 (it is optional; the workstream adds it when it audits). After editing, run the content build so the generated corpus picks up the new required field.
  </action>
  <verify>
    <automated>test "$(grep -rl 'launch_ready:' content/nodes/*.mdx | wc -l)" = "$(ls content/nodes/*.mdx | wc -l)" && npm run build:content</automated>
  </verify>
  <acceptance_criteria>
    Every file in content/nodes/*.mdx contains a launch_ready frontmatter line; npm run build:content succeeds (proving the now-required field is satisfied on all nodes); no node was flipped to true.
  </acceptance_criteria>
  <done>All 17 MDX files declare launch_ready: false; content build passes with the new required field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content pipeline → launched graph | A node marked launch_ready without a completed citation audit would ship unverified content (core-value violation, not a classic security bug) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Repudiation | launch_ready flip without audit trail | medium | mitigate | Default false + required auditNote-on-launch (enforced in 09-08) makes an unaudited launch_ready:true node fail the launch gate; the audit verdict is recorded in-file (D-13). |
| T-09-03b | Tampering | npm/content deps | low | accept | No new packages installed this phase (no SC checkpoint required). |
</threat_model>

<verification>
- grep confirms launch_ready + auditNote in both schema files; launch_ready has no default.
- All 17 MDX files declare launch_ready.
- npm run build:content and npm run validate both pass (existing validators still green; launch-gate wiring lands in 09-08).
</verification>

<success_criteria>
The publishability flag + audit-trail field exist and are enforced-required by the content build; existing corpus explicitly declares launch_ready: false, making the D-10 re-audit visible per file.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-03-SUMMARY.md` when done.
</output>
