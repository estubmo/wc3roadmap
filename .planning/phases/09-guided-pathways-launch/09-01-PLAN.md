---
phase: 09-guided-pathways-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pathway-progress.ts
  - src/lib/pathway-progress.test.ts
autonomous: true
requirements: [PATH-04]
must_haves:
  truths:
    - "computePathwayProgress counts only mastered steps, ignores in-progress and untouched"
    - "nextStepId is the first non-mastered step in step order, or null when all mastered"
  artifacts:
    - "src/lib/pathway-progress.ts (pure module, exported computePathwayProgress + PathwayProgress type)"
    - "src/lib/pathway-progress.test.ts (colocated Vitest, all counting + nextStep cases)"
  key_links:
    - "consumed by RoadmapGraph displayNodes memo + PathwayBanner in plan 09-09"
---

<objective>
Create the pure pathway-progress derivation module that powers the completion progress bar (PATH-04, D-01/D-02) and the "next node" cue (D-04). This is a single deep-module pure function over the existing Phase-5 masteryMap — no new store, no framework imports.

Purpose: A single, unit-tested source of truth for "how many pathway steps are mastered" and "which step is next", so both PathwayBanner (masteredCount/total) and RoadmapGraph (per-node stepIndex/isNextStep) derive from one rule instead of duplicating the count.
Output: src/lib/pathway-progress.ts + colocated test.
</objective>

<artifacts>
This plan produces:
- `src/lib/pathway-progress.ts` — exports `computePathwayProgress(steps, masteryMap)` and the `PathwayProgress` interface (`{ masteredCount: number; total: number; nextStepId: string | null }`).
- `src/lib/pathway-progress.test.ts` — Vitest coverage.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@scripts/validate-pathway.ts
@src/lib/graph-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for computePathwayProgress</name>
  <files>src/lib/pathway-progress.test.ts</files>
  <read_first>
    - scripts/validate-pathway.ts (pure-fn + doc-comment convention to mirror)
    - 09-RESEARCH.md Pattern 1 (worked implementation, lines 232-260) and Validation Architecture → Test Map (PATH-04 row)
    - 09-PATTERNS.md §pathway-progress.ts (MasteryState import + relative-import caveat)
  </read_first>
  <behavior>
    - Empty mastery map: masteredCount 0, total = steps.length, nextStepId = steps[0].
    - All steps mastered: masteredCount = total, nextStepId = null.
    - Mixed: only "mastered" entries increment the count; "in-progress" and "untouched" do NOT (D-02).
    - nextStepId is the FIRST non-mastered step in the given step order, even when a later step is already mastered (order matters, not position of first gap only after mastered prefix).
    - total always equals steps.length regardless of mastery.
  </behavior>
  <action>
    Create the colocated Vitest test for computePathwayProgress. Import the function and the MasteryState type using a RELATIVE import (../schemas/progress and ./pathway-progress) — vitest.config.ts has no "#/" alias resolver (09-PATTERNS.md §#-path-alias caveat, verified). Cover every case in the behavior block: use a representative 8-step id array matching the beginner-fundamentals shape. Include a case where a mid-order step is mastered but an earlier step is not, asserting nextStepId is the earlier unmastered id (proves order-correct next-step selection per D-04). Do NOT create the implementation yet — the test must fail to compile/run first (RED).
  </action>
  <verify>
    <automated>npx vitest run src/lib/pathway-progress.test.ts 2>&1 | grep -qiE "fail|error|cannot find|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    Test file exists, references computePathwayProgress and all behavior cases, and fails (RED) because the implementation does not yet exist.
  </acceptance_criteria>
  <done>Test file present with all behavior cases; vitest run is RED (module not found or assertions cannot execute).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement computePathwayProgress (GREEN)</name>
  <files>src/lib/pathway-progress.ts</files>
  <read_first>
    - 09-RESEARCH.md Pattern 1 (verbatim implementation is provided, lines 242-259)
    - scripts/validate-pathway.ts (SPDX header + "deep module" doc-comment shape to clone)
  </read_first>
  <action>
    Create src/lib/pathway-progress.ts. Open with the two-line SPDX header (GPL-3.0-or-later + copyright) exactly as scripts/validate-pathway.ts does, then a module doc-comment describing it as a deep module: a single pure function, no side effects, no content-collections or framework imports, plain-data-in / plain-data-out, citing D-01/D-02/D-04 and PATH-04. Import the MasteryState type from #/schemas/progress (path alias is correct for the app build; only the TEST uses relative imports). Export the PathwayProgress interface and computePathwayProgress(steps: readonly string[], masteryMap: Record<string, MasteryState>) per the RESEARCH Pattern 1 worked example: iterate steps in order, increment masteredCount only when masteryMap[id] === "mastered" (default missing entries to "untouched"), and record nextStepId as the first step whose state is not "mastered" (null when none). Return { masteredCount, total: steps.length, nextStepId }.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pathway-progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    All test cases pass (GREEN). Function is pure (no imports beyond the MasteryState type). masteredCount ignores in-progress/untouched; nextStepId respects step order.
  </acceptance_criteria>
  <done>vitest run src/lib/pathway-progress.test.ts passes; tsc --noEmit clean for this module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure function over already-trusted in-memory masteryMap (Phase 5); no I/O, no external input |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | computePathwayProgress inputs | low | accept | Inputs are build-time pathway steps + Phase-5 masteryMap; no attacker-reachable surface. No new packages installed (no SC gate). |
</threat_model>

<verification>
- npx vitest run src/lib/pathway-progress.test.ts is green.
- tsc --noEmit reports no errors for the new module.
</verification>

<success_criteria>
computePathwayProgress is a pure, tested function returning correct masteredCount/total/nextStepId; ready for consumption by plan 09-09.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-01-SUMMARY.md` when done.
</output>
