---
phase: 09-guided-pathways-launch
plan: 09
type: execute
wave: 2
depends_on: ["09-01", "09-04"]
files_modified:
  - src/components/graph/PathwayBanner.tsx
  - src/components/graph/RoadmapGraph.tsx
autonomous: true
requirements: [PATH-04, PATH-01]
must_haves:
  truths:
    - "the banner shows a slim rune-gold fill bar + 'N of total mastered' driven by mastered-only counting"
    - "at full completion the bar shows a quiet 'Fundamentals complete' with no fanfare"
    - "each pathway node carries a stepIndex and an isNextStep flag in its node data, computed client-side"
    - "the graph node also receives the stale flag from its GraphDisplayNode projection"
  artifacts:
    - "PathwayBanner progress bar (replaces the static count line; totalNodes prop removed)"
    - "RoadmapGraph displayNodes memo enriched with stepIndex/isNextStep/stale + computePathwayProgress call"
  key_links:
    - "consumes computePathwayProgress (09-01) and GraphDisplayNode.stale (09-04); feeds GraphNode overlays (09-11)"
---

<objective>
Wire the mastery-tied completion progress bar (PATH-04, D-01/D-02/D-03) into PathwayBanner and compute the per-node pathway data (stepIndex, isNextStep) + stale passthrough in RoadmapGraph's displayNodes memo (PATH-01, D-04). Remove the now-dead totalNodes prop (Pitfall 6).

Purpose: The banner becomes a live "skills mastered" status; the canvas nodes gain the transient data the GraphNode overlays (09-11) render.
Output: extended PathwayBanner + RoadmapGraph.
</objective>

<artifacts>
This plan produces:
- PathwayBanner progress bar (motion/react fill + label; "Fundamentals complete" at 100%); `totalNodes` prop removed, `masteredCount`/`total` props added; `role="progressbar"` aria.
- RoadmapGraph displayNodes memo: calls computePathwayProgress once, adds `stepIndex` (1-based, pathway-step nodes only), `isNextStep`, and `stale` (from the GraphDisplayNode) into each node's `data`; passes masteredCount/total to PathwayBanner; drops totalNodes.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/components/graph/PathwayBanner.tsx
@src/components/graph/RoadmapGraph.tsx
@src/lib/pathway-progress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the static count line in PathwayBanner with the progress bar</name>
  <files>src/components/graph/PathwayBanner.tsx</files>
  <read_first>
    - src/components/graph/PathwayBanner.tsx (full — the third p block at 110-120 to replace; props at 29-45; inline CSS-variable convention)
    - 09-UI-SPEC.md §Pathway Completion Progress Bar (6px track, rune-500 fill, motion width 0.4s easeOut, label 13px/600 opacity 0.85, 240px max-width, "Fundamentals complete" at 100% rune-400) + §Accessibility Baseline (progressbar aria) + §Animation Contract
    - 09-PATTERNS.md §PathwayBanner (remove totalNodes; no 4th line)
  </read_first>
  <action>
    Edit PathwayBanner. Remove the totalNodes prop from PathwayBannerProps and add masteredCount: number and total: number. Replace the third p element (the static "{steps.length} of {totalNodes} nodes" line) with the progress bar per UI-SPEC: a track div (height 6px, border-radius 9999px, max-width 240px, background var(--color-obsidian-700)) containing a motion.div fill (from motion/react) whose width animates to masteredCount/total*100% with transition duration 0.4 easeOut and background var(--color-rune-500); apply role="progressbar" with aria-valuenow={masteredCount} aria-valuemin={0} aria-valuemax={total} aria-label="Pathway completion" on the track. Below the bar (4px gap) render the label at 13px/600 opacity 0.85: when masteredCount < total show the "N of total mastered" copy from the Copywriting Contract; when masteredCount === total show "Fundamentals complete" at color var(--color-rune-400) (the only 100% delta — no confetti/toast/shimmer, D-03/PROG-05). Optionally crossfade the label swap via AnimatePresence per the Animation Contract. Keep all other banner content and the explore CTA unchanged; inline CSS variables only.
  </action>
  <verify>
    <automated>grep -q "progressbar" src/components/graph/PathwayBanner.tsx && grep -q "Fundamentals complete" src/components/graph/PathwayBanner.tsx && ! grep -q "totalNodes" src/components/graph/PathwayBanner.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    totalNodes fully removed; masteredCount/total drive a rune-500 fill bar with progressbar aria; 100% shows "Fundamentals complete" with no fanfare; tsc clean.
  </acceptance_criteria>
  <done>PathwayBanner renders the mastery progress bar; no dead totalNodes prop.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich RoadmapGraph displayNodes with stepIndex/isNextStep/stale + feed the banner</name>
  <files>src/components/graph/RoadmapGraph.tsx</files>
  <read_first>
    - src/components/graph/RoadmapGraph.tsx (full — displayNodes memo at 158-175, masteryMap subscription at 145, PathwayBanner render at 319-324)
    - src/lib/pathway-progress.ts (computePathwayProgress signature) and 09-RESEARCH.md Pattern 1 (called once in the displayNodes memo)
    - 09-RESEARCH.md Anti-Patterns (stepIndex/isNextStep are transient node.data, NOT schema fields)
  </read_first>
  <action>
    Edit RoadmapGraph's GraphCanvas. Import computePathwayProgress from #/lib/pathway-progress. Inside the displayNodes useMemo (which already has masteryMap + pathwaySet in scope), compute the pathway step index map from pathway.steps (id -> 1-based index) and call computePathwayProgress(pathway.steps, masteryMap) to get nextStepId. For each layout node, merge into its data: stepIndex (the 1-based pathway index when the node is a pathway step, else undefined), pathwayTotal (pathway.steps.length — used by the GraphNode step-badge aria "Step {n} of {total}"), isNextStep (true when node.id === nextStepId), and stale (read from the corresponding rawNodes entry's stale field — GraphDisplayNode.stale from 09-04; find by id, same as filteredDisplayNodes does). Keep the existing masteryState merge and the pathway-dim style logic intact; add pathway.steps and masteryMap to the memo deps if not already present. Then compute masteredCount/total via the same computePathwayProgress result and pass masteredCount={...} total={...} to PathwayBanner in place of the removed totalNodes prop. Do not add stepIndex/isNextStep to any Zod schema (transient data only).
  </action>
  <verify>
    <automated>grep -q "computePathwayProgress" src/components/graph/RoadmapGraph.tsx && grep -q "isNextStep" src/components/graph/RoadmapGraph.tsx && ! grep -q "totalNodes" src/components/graph/RoadmapGraph.tsx && npx tsc --noEmit && npm run test</automated>
  </verify>
  <acceptance_criteria>
    displayNodes data carries stepIndex/isNextStep/stale; computePathwayProgress is called once; PathwayBanner receives masteredCount/total (no totalNodes); tsc + full test suite green.
  </acceptance_criteria>
  <done>RoadmapGraph feeds the banner and node overlays; memoization conventions preserved; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Pure client-side derivation over trusted masteryMap + build-time pathway/nodes |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09 | Tampering | progress derivation | low | accept | Derived from Phase-5 masteryMap + build-time pathway; no external input, no new attacker surface. No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; npm run test green.
- grep confirms progressbar + "Fundamentals complete" in PathwayBanner and computePathwayProgress/isNextStep in RoadmapGraph; totalNodes gone from both.
</verification>

<success_criteria>
The banner shows a mastery-tied progress bar (mastered-only, no fanfare) and the canvas nodes carry the transient stepIndex/isNextStep/stale data the overlays need.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-09-SUMMARY.md` when done.
</output>