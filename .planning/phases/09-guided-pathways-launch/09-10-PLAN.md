---
phase: 09-guided-pathways-launch
plan: 10
type: execute
wave: 2
depends_on: ["09-02", "09-03", "09-04", "09-07"]
files_modified:
  - src/routes/index.tsx
autonomous: true
requirements: [CONT-04, PATH-03]
must_haves:
  truths:
    - "the launched (production) graph excludes non-launch_ready nodes; local dev shows all nodes so the pathway is verifiable pre-content"
    - "each GraphDisplayNode carries a computed stale boolean from the single isStale predicate"
    - "the first-visit intro overlay is mounted in the home route"
  artifacts:
    - "index.tsx loader: env-gated launch_ready filter + stale projection field"
    - "PathwayIntroOverlay mounted inside the home ProgressProvider"
  key_links:
    - "uses isStale (09-02), launch_ready (09-03), GraphDisplayNode.stale (09-04), PathwayIntroOverlay (09-07)"
---

<objective>
Complete the loader boundary work in the home route: project the new `stale` boolean via the single isStale predicate (D-06/D-09), exclude non-launch_ready nodes from the launched graph (CONT-04/D-12), and mount the first-visit intro overlay (PATH-03/D-05). This plan is the sole owner of index.tsx changes this phase.

Planner decision (RESEARCH Open Question 2 / A3): the launch_ready filter is environment-gated — active only in production (import.meta.env.PROD). In local dev it is bypassed so all 17 nodes stay visible and the pathway/overlay can be verified before the content workstream flips nodes to launch_ready. This matches D-12's "launched graph excludes non-launch_ready" without emptying the dev graph.

Purpose: Wire the launched-graph gate + on-canvas staleness data + intro overlay into the landing route.
Output: extended index.tsx loader + component.
</objective>

<artifacts>
This plan produces:
- index.tsx loader: `.filter` on launch_ready gated by `import.meta.env.PROD`, applied before the projection; `stale: isStale(n.meta_volatile, n.patchId, CURRENT_PATCH.id)` added as an explicit named field in the GraphDisplayNodeSchema.safeParse projection.
- `<PathwayIntroOverlay />` mounted inside the Home component's ProgressProvider.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-PATTERNS.md
@src/routes/index.tsx
@src/lib/staleness.ts
@src/lib/patches.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add env-gated launch_ready filter + stale projection to the loader</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (full — loader projection block at 50-64; note the explicit field-by-field safeParse, NEVER a spread)
    - 09-RESEARCH.md Pattern 2 (filter before map; stale as one explicit field) + Open Question 2/A3 (env-gating) + Anti-Patterns (no frontmatter spread)
    - 09-PATTERNS.md §index.tsx (add filter before map; add stale named field)
    - src/lib/staleness.ts (isStale) + src/lib/patches.ts (CURRENT_PATCH.id)
  </read_first>
  <action>
    Edit the index.tsx loader. Import isStale from #/lib/staleness and CURRENT_PATCH from #/lib/patches. Before the existing .map projection, insert a filter: when import.meta.env.PROD is true, keep only nodes whose launch_ready === true (CONT-04/D-12 launched-graph exclusion); when not PROD, keep all nodes (dev carve-out, A3). Add a code comment citing D-12/CONT-04 + Open Question 2 for the env gate. Inside the GraphDisplayNodeSchema.safeParse({...}) object, add one more explicit named field stale: isStale(n.meta_volatile, n.patchId, CURRENT_PATCH.id) — do NOT spread n and do NOT add meta_volatile/patchId to the projection (only the derived boolean crosses the boundary, ADR 013). Keep every existing projected field unchanged.
  </action>
  <verify>
    <automated>grep -q "isStale" src/routes/index.tsx && grep -q "import.meta.env.PROD" src/routes/index.tsx && grep -q "launch_ready" src/routes/index.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Loader filters launch_ready only under PROD; stale is projected via isStale as a single named field; no frontmatter spread; tsc clean; dev graph still shows all nodes.
  </acceptance_criteria>
  <done>Loader gates launch_ready in production and projects the stale boolean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount PathwayIntroOverlay in the home route</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (Home component at 129-190; ProgressProvider wrap; ClientOnly precedent at 184-186)
    - src/components/graph/PathwayIntroOverlay.tsx (self-gating, no props)
    - 09-RESEARCH.md Pitfall 3 (overlay is client-only and default-closed — it self-guards; a ClientOnly wrapper is optional since the component already guards window)
  </read_first>
  <action>
    Import PathwayIntroOverlay from #/components/graph/PathwayIntroOverlay and render it inside the Home component's ProgressProvider subtree (alongside the existing main/panel structure, so it overlays the spotlighted pathway per D-05). The component self-gates on the localStorage flag and defaults closed, so no extra ClientOnly wrapper is required; if the executor prefers matching the existing NodeDetailPanel convention, wrapping it in <ClientOnly fallback={null}> is acceptable. Do not gate it on auth — it shows for first-time visitors regardless of sign-in.
  </action>
  <verify>
    <automated>grep -q "PathwayIntroOverlay" src/routes/index.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    PathwayIntroOverlay is mounted in Home within ProgressProvider; tsc clean; dev server boots and shows the overlay on a fresh (cleared-localStorage) load.
  </acceptance_criteria>
  <done>Intro overlay is wired into the landing route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content pipeline → launched graph | Non-launch_ready content must not reach the production graph |
| build env flag → filter behavior | The PROD gate decides whether draft nodes are visible |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Information Disclosure | draft node exposure in prod | medium | mitigate | import.meta.env.PROD filter excludes non-launch_ready nodes from the production graph; the launch gate (09-08) blocks deploy until ≥25 are ready. Dev-only visibility carries no user data. No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; dev server boots.
- In dev, all nodes render (filter bypassed); a production build filters to launch_ready.
- Fresh load shows the intro overlay; stale nodes carry data.stale=true into the canvas.
</verification>

<success_criteria>
The launched graph excludes drafts (prod-gated), every node carries a derived stale flag, and first-time visitors get the intro overlay.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-10-SUMMARY.md` when done.
</output>
