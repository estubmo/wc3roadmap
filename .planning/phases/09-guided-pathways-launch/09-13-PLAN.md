---
phase: 09-guided-pathways-launch
plan: 13
type: execute
wave: 4
depends_on: ["09-08", "09-09", "09-10", "09-11", "09-12", "09-06"]
files_modified:
  - CONTEXT.md
autonomous: true
requirements: [PATH-01, PATH-02, PATH-03, PATH-04, CONT-04, CONT-05]
must_haves:
  truths:
    - "CONTEXT.md domain language covers pathway progress, next step, staleness, launch_ready, and audit note"
    - "the phase's five success criteria are verified end-to-end before launch sign-off"
  artifacts:
    - "CONTEXT.md domain-language extension for Phase 9 terms"
    - "recorded end-of-phase human verification of the five success criteria"
  key_links:
    - "closes out the guided-pathways + launch mechanism; content flip to launch_ready remains the parallel workstream (D-11)"
---

<objective>
Close the phase: extend the root CONTEXT.md ubiquitous-domain-language with the Phase 9 terms (architecture discipline), and run the end-of-phase human verification of the five ROADMAP success criteria (config human_verify_mode = end-of-phase, so this is a verify human-check, not a blocking checkpoint task).

Purpose: Keep the domain language and ADR discipline current, and confirm the guided-pathways + launch mechanism works before public announcement — while acknowledging the ≥25 launch_ready content bar is filled by the parallel content workstream (D-11).
Output: extended CONTEXT.md + recorded verification.
</objective>

<artifacts>
This plan produces:
- CONTEXT.md domain-language entries: pathway progress / next step / staleness / launch_ready / audit note (and any others surfaced).
- A recorded human-check verdict for the five Phase 9 success criteria in the SUMMARY.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-CONTEXT.md
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CONTEXT.md with Phase 9 domain terms</name>
  <files>CONTEXT.md</files>
  <read_first>
    - CONTEXT.md (existing domain-language structure/section to append to)
    - 09-CONTEXT.md (D-01..D-16) + 09-RESEARCH.md (staleness predicate, launch_ready, audit trail)
  </read_first>
  <action>
    Append Phase 9 terms to the root CONTEXT.md ubiquitous-language section, each with a one-line definition tied to the code: pathway progress (mastered-only completion of a pathway's steps, D-02), next step (first non-mastered pathway step, the "do this next" cue, D-04), staleness (meta_volatile node whose patchId no longer matches CURRENT_PATCH — the single isStale predicate, D-06), launch_ready (per-node publishability flag gating inclusion in the launched graph, CONT-04/D-12), audit note (recorded per-node citation-audit verdict/rationale, CONT-05/D-13). Match the existing entry style; do not restructure prior terms. Note the ADR 013 projection-boundary widening in the relevant term if the file cross-references ADRs.
  </action>
  <verify>
    <automated>grep -qi "launch_ready" CONTEXT.md && grep -qi "staleness" CONTEXT.md && grep -qi "next step" CONTEXT.md && echo TERMS-OK</automated>
  </verify>
  <acceptance_criteria>
    CONTEXT.md contains the five Phase 9 terms with code-tied definitions in the existing style.
  </acceptance_criteria>
  <done>Domain language extended; no prior terms disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: End-of-phase verification of the five success criteria</name>
  <files>CONTEXT.md</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 9 Success Criteria (the five acceptance statements)
    - 09-VALIDATION.md (Per-Task Verification Map; manual-only rows for the visual criteria and the citation audit)
  </read_first>
  <action>
    Run the automated suite (npm run test) and content validation (npm run validate) as the objective backstop, then perform the end-of-phase human check of the five ROADMAP criteria in dev (npm run dev): (1) a first-time visitor lands on the spotlighted 8-step pathway with numbered steps + a single "Next" cue and a 0/8 progress bar, not the raw graph; (2) mastering pathway nodes advances the bar (mastered-only) and reaching 8/8 shows the quiet "Fundamentals complete" with no fanfare; (3) the ≥25 launch_ready gate mechanism exists and blocks launch (LAUNCH_GATE=1 npm run validate:launch reports the shortfall against the current corpus — the actual ≥25 authored nodes are supplied by the parallel content workstream, D-11, and are NOT a code deliverable of this phase); (4) the citation-audit trail mechanism exists (auditNote field + validateAuditTrail) and non-launch_ready nodes are excluded from the production graph; (5) a meta-volatile out-of-patch node shows the staleness strip + tooltip in its panel and a marker on the canvas. Record each criterion's verdict in the SUMMARY. Do NOT block the phase on the ≥25 content count (that is the content workstream's gate); DO flag if any mechanism is missing.
  </action>
  <verify>
    <human-check>
      Confirm in dev: (1) default landing = numbered spotlighted pathway + Next cue + progress bar (not raw graph); (2) mastering nodes advances the bar, 8/8 shows "Fundamentals complete" with no confetti/toast; (3) LAUNCH_GATE=1 npm run validate:launch reports the launch-blocking shortfall; (4) production build excludes non-launch_ready nodes; (5) a stale node shows the panel strip+tooltip and the canvas marker. Reply "approved" or list issues.
    </human-check>
    <automated>npm run test && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    Automated suite + content validation green; each of the five success criteria has a recorded human verdict; any missing mechanism is flagged (content-count shortfall is expected and NOT a blocker per D-11).
  </acceptance_criteria>
  <done>Five criteria verified (mechanisms), suite green, verdicts recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content workstream → launch | Launch depends on ≥25 audited launch_ready nodes flipped outside this phase |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13 | Repudiation | premature launch | medium | mitigate | The deploy gate (validate:launch, 09-08) blocks launch until the audited-content invariants hold; the human verify records mechanism completeness. No new packages this phase. |
</threat_model>

<verification>
- npm run test + npm run validate green.
- CONTEXT.md carries the five Phase 9 terms.
- Recorded human verdict for all five success criteria.
</verification>

<success_criteria>
Domain language current; the guided-pathways + launch mechanism verified end-to-end; launch correctly gated on the parallel content workstream's ≥25 audited nodes.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-13-SUMMARY.md` when done.
</output>
