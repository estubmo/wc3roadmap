---
phase: 09-guided-pathways-launch
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/about.tsx
autonomous: true
requirements: [PATH-03]
must_haves:
  truths:
    - "a static About/Privacy page is reachable at /about with the project mission, GPL-3.0 mention, and a one-line privacy statement"
    - "the about page carries its own OG title override"
  artifacts:
    - "src/routes/about.tsx (new route with head() OG override)"
  key_links:
    - "linked from the About page back to / and from the app footer/header as the content workstream wires navigation"
---

<objective>
Add the minimal static About/Privacy page (D-16 launch polish). Kept intentionally small — mission, open-source/GPL-3.0 mention, one-line privacy statement, link home. Not a marketing site.

Purpose: A trustworthy, honest About surface for launch that states what data is stored (BattleTag, progress records) and the GPL-3.0 openness commitment.
Output: src/routes/about.tsx.
</objective>

<artifacts>
This plan produces:
- `src/routes/about.tsx` — new file-route at /about with a per-route head() OG override and static prose content.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@src/routes/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the About/Privacy route</name>
  <files>src/routes/about.tsx</files>
  <read_first>
    - src/routes/index.tsx (createFileRoute + head() shape + EmptyState typography conventions to clone)
    - 09-UI-SPEC.md §Launch Polish → About / Privacy Page (layout: single column max-width 640px, padding 48px 24px; 22px page heading; 15px section headings; 14px/1.65 body; rune-400 links) + §Page Metadata (about head() override)
    - .planning/PROJECT.md (core-value mission copy) and docs/adr/004-gpl3-licensing.md (GPL-3.0-or-later rationale)
  </read_first>
  <action>
    Create src/routes/about.tsx via createFileRoute("/about") with SPDX header. Add a head() returning meta with an about-specific title and og:title/og:description override (property: keys, per UI-SPEC). Render a single centered column (max-width 640px, padding 48px 24px, obsidian-950 background) with inline CSS-variable styles only (no hardcoded hex). Content: an h1 in the 22px/600 Space Grotesk pathway-heading role ("About WC3 Learning Roadmap"), then h2 section headings in the 15px/600 node-title role for "Open source" and "Privacy". Body prose at 14px/400/1.65 Outfit: project mission distilled from PROJECT.md core value; a GPL-3.0(-or-later) mention with a link to the repository LICENSE; a one-line privacy statement naming exactly what is stored (BattleTag, per-node progress records) and that there are no third-party trackers; and a link back to "/" using TanStack Router Link. Style links with the existing MDX anchor convention (color var(--color-rune-400), underline var(--color-rune-600)).
  </action>
  <verify>
    <automated>test -f src/routes/about.tsx && grep -q "og:title" src/routes/about.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    /about renders the mission, an "Open source"/GPL section, a "Privacy" section naming BattleTag + progress records, a link home, and its own og:title; tsc clean.
  </acceptance_criteria>
  <done>About route exists at /about with OG override and the required content; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| request → static SSR page | Fully static prose; no user input, no data fetch |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06 | Information Disclosure | privacy statement accuracy | low | mitigate | Privacy copy states only what is actually stored (BattleTag + progress records), matching the Phase 4/5 data model; no over-claim. No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean; /about renders.
- Page names GPL-3.0 and the stored-data list; links home resolve.
</verification>

<success_criteria>
A minimal, honest About/Privacy page ships with its own OG override.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-06-SUMMARY.md` when done.
</output>
