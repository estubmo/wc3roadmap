---
phase: 09-guided-pathways-launch
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/PathwayIntroOverlay.tsx
autonomous: true
requirements: [PATH-03]
must_haves:
  truths:
    - "a first-time visitor sees a dismissible intro overlay once; returning visitors never see it again"
    - "the localStorage read never runs during SSR and causes no hydration mismatch"
    - "any dismissal path (CTA, X, Escape, backdrop) sets the one-time seen flag"
  artifacts:
    - "src/components/graph/PathwayIntroOverlay.tsx (client-only shadcn Dialog)"
  key_links:
    - "mounted inside the home route ProgressProvider by plan 09-10"
---

<objective>
Build the first-visit intro overlay (PATH-01/03, D-05): a dismissible shadcn Dialog that appears once, gated by a localStorage flag, reinforcing that the landing view is an ordered pathway. This plan builds the self-contained component; plan 09-10 mounts it in the home route.

Purpose: Orient a novice ("Start here — 8 fundamentals") before the raw graph competes for attention, without ever re-nagging returning visitors.
Output: src/components/graph/PathwayIntroOverlay.tsx.
</objective>

<artifacts>
This plan produces:
- `src/components/graph/PathwayIntroOverlay.tsx` — client-only Dialog component; localStorage key `wc3rm:pathway-intro-seen`.
</artifacts>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guided-pathways-launch/09-RESEARCH.md
@.planning/phases/09-guided-pathways-launch/09-UI-SPEC.md
@src/components/auth/RegionSelector.tsx
@src/lib/local-progress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PathwayIntroOverlay (client-only, SSR-safe, one-time flag)</name>
  <files>src/components/graph/PathwayIntroOverlay.tsx</files>
  <read_first>
    - src/components/auth/RegionSelector.tsx (existing shadcn Dialog consumer — Dialog/DialogTitle/DialogDescription import + usage shape)
    - src/lib/local-progress.ts (SSR-guard convention: typeof window undefined guard; key prefix wc3rm:)
    - 09-RESEARCH.md Pitfall 3 (default-closed dialog + useEffect localStorage check — never read localStorage in a useState initializer) and Anti-Patterns (hydration)
    - 09-UI-SPEC.md §First-Visit Intro Overlay (title/body/CTA copy, gold CTA accent item 12, backdrop dismiss allowed) + §Copywriting Contract
    - 09-PATTERNS.md §PathwayIntroOverlay (localStorage key correction: use wc3rm:pathway-intro-seen, NOT wc3roadmap:)
  </read_first>
  <action>
    Create src/components/graph/PathwayIntroOverlay.tsx with the SPDX header. Use the shadcn Dialog primitives (from #/components/ui/dialog) following the RegionSelector usage shape. Define the localStorage key constant as wc3rm:pathway-intro-seen (correcting the UI-SPEC assumption of a wc3roadmap: prefix — the verified repo prefix is wc3rm:, per local-progress.ts). Initialize the Dialog open state to false. Inside a useEffect that runs once post-mount, read the flag with a typeof-window guard wrapped in try/catch (mirroring getLocalProgress) and call setOpen(true) only when the stored value is not the string "true" — do NOT read localStorage in the useState initializer (Pitfall 3 hydration hazard). Provide a single dismiss handler that writes the flag (guarded, try/catch) and closes the dialog; wire it via onOpenChange so the CTA button, the built-in X, Escape, and backdrop click all dismiss AND set the flag (do NOT override onInteractOutside — this is not RegionSelector's captive flow). Content per UI-SPEC: DialogTitle "Start here" styled to the 22px Space Grotesk pathway-heading role; DialogDescription/body with the exact intro body copy at 14px/400/1.65; a full gold-CTA Button (rune-500 fill / obsidian-950 text, SignInButton family) with the exact CTA label. Inline CSS-variable styles only, no hardcoded hex. Export the component with no required props (it self-gates).
  </action>
  <verify>
    <automated>test -f src/components/graph/PathwayIntroOverlay.tsx && grep -q "wc3rm:pathway-intro-seen" src/components/graph/PathwayIntroOverlay.tsx && grep -q "useEffect" src/components/graph/PathwayIntroOverlay.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Component compiles; open state defaults to false and flips via a post-mount useEffect localStorage check (no useState-initializer localStorage read); every dismiss path writes wc3rm:pathway-intro-seen; copy matches the Copywriting Contract.
  </acceptance_criteria>
  <done>PathwayIntroOverlay exists, is SSR-safe, one-time-gated, and typed; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → client | A tampered/malformed flag value could throw or re-show/never-show the overlay |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Denial of Service | intro-overlay localStorage read | low | mitigate | Wrap the read in try/catch with a safe default (same convention as local-progress.ts); a tampered value only affects whether a dismissible dialog shows — zero security impact. No new packages. |
</threat_model>

<verification>
- tsc --noEmit clean.
- Manual (deferred to 09-13 end-of-phase verify): first load shows the overlay; after dismiss + reload it does not reappear.
</verification>

<success_criteria>
A self-contained, SSR-safe, one-time intro overlay component ready to mount in the home route.
</success_criteria>

<output>
Create `.planning/phases/09-guided-pathways-launch/09-07-SUMMARY.md` when done.
</output>
