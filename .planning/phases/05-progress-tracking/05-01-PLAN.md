---
phase: 05-progress-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/toggle-group.tsx
  - src/components/ui/sonner.tsx
  - src/routes/__root.tsx
autonomous: true
requirements: [PROG-04]
must_haves:
  truths:
    - "sonner is installed and a single <Toaster /> renders at app root"
    - "shadcn toggle-group + sonner component source exist under src/components/ui/"
    - "Toast notifications can fire from any client component (error rollback + merge success)"
  artifacts:
    - src/components/ui/toggle-group.tsx
    - src/components/ui/sonner.tsx
    - "package.json with sonner dependency"
    - "src/routes/__root.tsx mounting <Toaster />"
  key_links:
    - "Toaster mounted inside QueryClientProvider in __root.tsx → toasts from useProgressMutation (05-06) reach the DOM"
---

<objective>
Bootstrap the Phase 5 UI toolchain: install the single new npm package (`sonner`), add the shadcn `toggle-group` and `sonner` components, and mount a single `<Toaster />` at the app root so downstream plans (05-06 mutation hook, 05-07 ProgressProvider) can surface error-rollback and merge-success toasts.

Purpose: The mastery marking UI (05-06 `MasteryControls`) needs `toggle-group`; the optimistic-update rollback path (D-09) and the localStorage merge (D-07) need `sonner` toasts. Installing these first unblocks the Wave 3 UI plans.
Output: `sonner` npm dependency, two shadcn component files, a mounted Toaster.

## Artifacts this phase produces (whole-phase manifest)

This section lists every NEW symbol/file Phase 5 creates so the plan-review-convergence source-grounding pass can exclude them from drift verification. They do NOT exist before this phase.

- DB table: `nodeProgress` (`node_progress`) in `src/db/schema.ts` + `nodeProgressRelations` (05-03)
- Schema changes: `MasteryStateSchema` enum value `in-progress` (replaces `learning`); `source` field on `ProgressRecordSchema`; `MasteryState` type becomes canonical in `src/schemas/progress.ts` (05-02)
- Server fns: `getUserProgress` / `getUserProgressHandler`, `setNodeMastery` / `setNodeMasteryHandler`, `mergeProgressOnSignIn` / `mergeProgressOnSignInHandler` in `src/server/progress.ts` (05-04)
- Client store module: `src/lib/local-progress.ts` (`getLocalProgress`, `setLocalMastery`, `clearLocalProgress`, `isAlreadyMerged`, `markMerged`) (05-05)
- Query keys: `progressKeys` in `src/lib/progress-keys.ts` (05-05)
- Zustand additions: `masteryMap`, `setNodeMastery`, `initMasteryMap` on `useGraphStore` (05-05)
- Hook: `useProgressMutation` in `src/hooks/useProgressMutation.ts` (05-06)
- Components: `MasteryControls` (`src/components/graph/MasteryControls.tsx`), `ProgressProvider` (`src/components/graph/ProgressProvider.tsx`) (05-06, 05-07)
- shadcn UI: `src/components/ui/toggle-group.tsx`, `src/components/ui/sonner.tsx` (05-01)
- Docs: `docs/adr/009-progress-persistence.md`; Phase 5 terms in root `CONTEXT.md` (05-09)
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-UI-SPEC.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@src/routes/__root.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner + add shadcn toggle-group and sonner components</name>
  <read_first>
    - package.json (confirm sonner is not already present; confirm pinned-version discipline)
    - src/components/ui/button.tsx (existing shadcn new-york component style to match)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (Standard Stack — sonner@2.0.7, Package Legitimacy Audit: sonner Approved)
  </read_first>
  <action>
    Run `npm install sonner` (research-pinned 2.0.7; sonner is Approved in the Package Legitimacy Audit — no human-verify checkpoint needed). Then run `npx shadcn add toggle-group sonner` to copy the new-york-style component source into `src/components/ui/toggle-group.tsx` and `src/components/ui/sonner.tsx`. If the shadcn CLI prompts non-interactively-blocked, fall back to copying the component source from ui.shadcn.com for the new-york style (research Assumption A5 fallback). Add the SPDX header `// SPDX-License-Identifier: GPL-3.0-or-later` + copyright line to any shadcn-generated file that lacks it (project SPDX convention; routeTree.gen.ts is the only exemption). Do NOT modify any other component.
  </action>
  <verify>
    <automated>test -f src/components/ui/toggle-group.tsx && test -f src/components/ui/sonner.tsx && node -e "require('./package.json').dependencies.sonner || process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains a `sonner` entry
    - `src/components/ui/toggle-group.tsx` exists and exports `ToggleGroup` and `ToggleGroupItem`
    - `src/components/ui/sonner.tsx` exists and exports a `Toaster`
    - Both new ui files carry the SPDX `GPL-3.0-or-later` header on line 1
  </acceptance_criteria>
  <done>sonner installed; toggle-group + sonner shadcn components present under src/components/ui/.</done>
</task>

<task type="auto">
  <name>Task 2: Mount <Toaster /> at app root</name>
  <read_first>
    - src/routes/__root.tsx (existing QueryClientProvider + SiteHeader composition — Toaster mounts here, inside the provider, after the content wrapper)
    - src/components/ui/sonner.tsx (the Toaster export to import)
    - .planning/phases/05-progress-tracking/05-UI-SPEC.md (§Toast Specifications — position: bottom-right desktop / bottom-center mobile; obsidian-800 bg, obsidian-600 border)
  </read_first>
  <action>
    Import `Toaster` from `#/components/ui/sonner` into `src/routes/__root.tsx` and render a single `<Toaster />` inside the existing `QueryClientProvider`, immediately after the `<div style={{ paddingTop: "48px" }}>{children}</div>` wrapper (still inside the provider, before the closing provider tag). Configure `position="bottom-right"` and `theme="dark"` (UI-SPEC §Toast Specifications). Do not introduce a second Toaster anywhere. Keep `<Scripts />` OUTSIDE the provider exactly as it is now.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "Toaster" src/routes/__root.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/__root.tsx` imports `Toaster` from `#/components/ui/sonner`
    - Exactly one `<Toaster` JSX usage exists in `src/routes/__root.tsx`
    - The Toaster is rendered inside `QueryClientProvider` (not outside, not wrapping `<Scripts />`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A single Toaster renders at the app root; client components can call `toast(...)` from sonner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New `sonner` package enters the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | sonner npm install | low | mitigate | sonner is Approved in RESEARCH Package Legitimacy Audit (44M weekly downloads, 3-yr-old, verified repo) — no [ASSUMED]/[SUS] flag, no checkpoint required; pin to research version |
| T-05-01a | Information Disclosure | Toaster rendering | low | accept | Toast bodies carry only generic copy (UI-SPEC) — no user data, no error internals surfaced |
</threat_model>

<verification>
- `npm test` (full suite) stays green — no behavioral change to existing tests
- `npx tsc --noEmit` exits 0
- A single `<Toaster />` is mounted at root
</verification>

<success_criteria>
sonner installed (Approved package), toggle-group + sonner shadcn components present, single Toaster mounted at app root inside QueryClientProvider.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-01-SUMMARY.md` when done.
</output>
