---
phase: 05-progress-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/progress.ts
  - src/schemas/progress.test.ts
  - src/lib/mock-mastery.ts
  - src/components/graph/MasteryBadge.tsx
autonomous: true
requirements: [PROG-01, PROG-04]
must_haves:
  truths:
    - "MasteryStateSchema accepts 'in-progress' and rejects 'learning' (D-03 canonical vocabulary)"
    - "ProgressRecordSchema carries a source field defaulting to 'manual' (D-04 forward-design)"
    - "MasteryState is sourced from a single place (src/schemas/progress.ts); mock-mastery re-exports it"
    - "MasteryBadge displays 'In Progress' (not 'Learning') for the in-progress state"
  artifacts:
    - "src/schemas/progress.ts with in-progress enum + source field"
    - "src/schemas/progress.test.ts updated for new vocabulary + source"
    - "src/lib/mock-mastery.ts re-exporting MasteryState from schemas/progress"
    - "src/components/graph/MasteryBadge.tsx with In Progress label"
  key_links:
    - "MasteryStateSchema enum value 'in-progress' → DB text column value → graph node color contract (single vocabulary, no translation layer, D-03)"
---

<objective>
Make `in-progress` the canonical mastery-state value end-to-end (D-03): migrate `MasteryStateSchema` from `["untouched","learning","mastered"]` to `["untouched","in-progress","mastered"]`, add the forward-designed `source` field to `ProgressRecordSchema` (D-04), establish `src/schemas/progress.ts` as the single source of the `MasteryState` type, and fix the lingering "Learning" label in `MasteryBadge` (D-03 cleanup).

Purpose: Resolves the pre-existing schema-vs-UI vocabulary drift. The DB table (05-03), server fns (05-04), and client store (05-05) all consume this enum, so it must be correct before they are built. No DB data migration is needed — no progress records exist yet (research Pattern 9).
Output: Corrected Zod schema, updated tests, single-source `MasteryState` type, corrected badge label.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@.planning/phases/05-progress-tracking/05-PATTERNS.md
@src/schemas/progress.ts
@src/lib/mock-mastery.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate MasteryStateSchema to in-progress + add source field; single-source the MasteryState type</name>
  <read_first>
    - src/schemas/progress.ts (the file being edited — current enum on line 37, ProgressRecordSchema, exported MasteryState type on line 40)
    - src/lib/patches.ts (PATCH_IDS import already present — leave untouched)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/schemas/progress.ts — exact edits; §src/lib/mock-mastery.ts — re-export approach)
    - src/lib/mock-mastery.ts (currently defines its own MasteryState with the same three values; becomes a re-export)
  </read_first>
  <behavior>
    - MasteryStateSchema.safeParse("in-progress").success === true
    - MasteryStateSchema.safeParse("learning").success === false
    - ProgressRecordSchema parses a record with source: "manual" and source: "auto"
    - ProgressRecordSchema defaults source to "manual" when the field is absent
    - The MasteryState type imported from "#/lib/mock-mastery" is structurally identical to the one from "#/schemas/progress"
  </behavior>
  <action>
    In `src/schemas/progress.ts`: change `MasteryStateSchema` to `z.enum(["untouched", "in-progress", "mastered"])` (per D-03 — remove `learning` entirely, no backward-compat alias). Update the MasteryStateSchema JSDoc so the middle state reads `in-progress` with the gloss "player has started but not yet met the mastery threshold". Add a `source` field to `ProgressRecordSchema` immediately after `masteryState`: `source: z.enum(["manual", "auto"]).default("manual")` with a JSDoc noting D-04 — only `manual` is written in Phase 5; `auto` is reserved for Phase 7/8 auto-detection and a manual mark may override an auto state. Leave `userId: z.string()` as-is (D-05: the UUID guarantee lives in the auth/DB layer, not this Zod schema — note this in a one-line comment referencing D-05). Keep `patchId: z.enum(PATCH_IDS)` and `lastUpdated` unchanged. In `src/lib/mock-mastery.ts`: replace the local `export type MasteryState = "untouched" | "in-progress" | "mastered";` with a re-export `export type { MasteryState } from "#/schemas/progress";` so schemas/progress.ts is the single source of truth, and add a `@deprecated` JSDoc tag to `getMockMastery` noting it is Phase-2 mock data replaced by real persistence in Phase 5 (the function stays for the /preview/* dev routes and MobileNodeList).
  </action>
  <verify>
    <automated>npm test -- progress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/schemas/progress.ts` MasteryStateSchema enum is exactly `["untouched","in-progress","mastered"]` (no `learning`)
    - `src/schemas/progress.ts` ProgressRecordSchema contains `source: z.enum(["manual", "auto"]).default("manual")`
    - `src/lib/mock-mastery.ts` re-exports MasteryState from `#/schemas/progress` (no local union literal type definition remains)
    - `getMockMastery` in mock-mastery.ts carries an `@deprecated` JSDoc tag
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>in-progress is the canonical enum value; source field present; MasteryState single-sourced from schemas/progress.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update progress schema tests for new vocabulary + source field</name>
  <read_first>
    - src/schemas/progress.test.ts (the file being edited — line 36 accepts 'learning'; line 72 loops over ["untouched","learning","mastered"])
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/schemas/progress.test.ts — exact replacements)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (Pitfall 6 — test file still references learning)
  </read_first>
  <behavior>
    - Test: MasteryStateSchema accepts "in-progress"
    - Test: MasteryStateSchema rejects "learning" (renamed)
    - Test: ProgressRecordSchema accepts source "manual" and "auto"
    - Test: ProgressRecordSchema defaults source to "manual" when omitted
  </behavior>
  <action>
    In `src/schemas/progress.test.ts`: replace the `it("accepts 'learning'", ...)` block with `it("accepts 'in-progress'", ...)` asserting `MasteryStateSchema.safeParse("in-progress").success` is true. Add a new test `it("rejects 'learning' (renamed to in-progress)", ...)` asserting `safeParse("learning").success` is false. In the acceptance loop (currently `for (const masteryState of ["untouched", "learning", "mastered"] as const)`) replace `"learning"` with `"in-progress"`. Add a `describe("ProgressRecordSchema — source field (D-04)", ...)` block with: accepts `source: "manual"`, accepts `source: "auto"`, and a test that a record omitting `source` parses with the result's `source` equal to `"manual"` (use `.parse(...)` and read the returned object since `.default` only materializes on parse). Do not weaken any existing patchId/lastUpdated assertion.
  </action>
  <verify>
    <automated>npm test -- progress</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- progress` passes with zero failures
    - `src/schemas/progress.test.ts` contains a test asserting `safeParse("learning").success` is false
    - `src/schemas/progress.test.ts` contains a test that an omitted `source` defaults to `"manual"` on parse
    - No remaining `"learning"` literal is asserted as a valid value anywhere in the file
  </acceptance_criteria>
  <done>Schema tests prove the new vocabulary and the source field; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix MasteryBadge in-progress label to "In Progress"</name>
  <read_first>
    - src/components/graph/MasteryBadge.tsx (line 15 import from mock-mastery; line 48 renders "Learning")
    - .planning/phases/05-progress-tracking/05-UI-SPEC.md (§Copywriting Contract — MasteryBadge in-progress label = "In Progress")
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§MasteryBadge.tsx — label + import)
  </read_first>
  <action>
    In `src/components/graph/MasteryBadge.tsx`: change the in-progress pill text from `Learning` to `In Progress` (D-03 canonical copy, UI-SPEC §Copywriting Contract). Update the import on line 15 from `import type { MasteryState } from "#/lib/mock-mastery";` to `import type { MasteryState } from "#/schemas/progress";` (single source). Update the module JSDoc line that says `Renders "Learning" pill for "in-progress"` to say `Renders "In Progress" pill`. Leave all colors (rune-600 bg, rune-300 text) and the mastered branch unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "In Progress" src/components/graph/MasteryBadge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/MasteryBadge.tsx` renders the text `In Progress` for the in-progress state
    - The file no longer renders the literal pill text for the prior label
    - The MasteryState import resolves from `#/schemas/progress`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MasteryBadge shows "In Progress" per the canonical vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client input → Zod parse | masteryState/source values validated by MasteryStateSchema before any persistence layer (05-04) trusts them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02a | Tampering | MasteryStateSchema | medium | mitigate | z.enum restricts masteryState to exactly three values; crafted values (e.g. "MASTERED", "hacked") rejected at parse — consumed by 05-04 server fn before DB write |
| T-05-02b | Tampering | source field | low | mitigate | source enum is `["manual","auto"]`; even though validated here, 05-04 hardcodes `"manual"` server-side and never reads source from client input |
</threat_model>

<verification>
- `npm test -- progress` green
- `npx tsc --noEmit` exits 0 across the repo (no consumer broke from the enum/type change)
- No `"learning"` value asserted valid anywhere
</verification>

<success_criteria>
Canonical `in-progress` vocabulary across schema, type, and badge; `source` field designed in; tests prove both; full type-check passes.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-02-SUMMARY.md` when done.
</output>
