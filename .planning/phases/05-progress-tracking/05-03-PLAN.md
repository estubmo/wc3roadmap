---
phase: 05-progress-tracking
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/schema.test.ts
autonomous: true
requirements: [PROG-01, PROG-02]
user_setup:
  - service: neon
    why: "Schema push targets the live Neon database"
    env_vars:
      - name: DATABASE_URL_DIRECT
        source: "Neon Console -> Connection Details -> Direct connection (non-pooled) string; stored in .env.local (already gitignored)"
must_haves:
  truths:
    - "A node_progress table exists in the Neon database with the columns userId, nodeId, masteryState, source, patchId, timestamps"
    - "The table enforces one row per (userId, nodeId) via a unique index — the upsert target for 05-04"
    - "Deleting a user cascades to their progress rows (FK onDelete cascade)"
  artifacts:
    - "src/db/schema.ts with nodeProgress table + nodeProgressRelations + usersRelations extension"
    - "node_progress table live in Neon (drizzle-kit push applied)"
  key_links:
    - "nodeProgress.userId → users.id FK (UUID progress key, AUTH-04/D-05)"
    - "uniqueIndex(userId,nodeId) → onConflictDoUpdate target in 05-04 setNodeMastery"
    - "nodeProgressRelations → db.query.nodeProgress.findMany works in 05-04 getUserProgress"
---

<objective>
Add the `nodeProgress` Drizzle table (surrogate text PK + unique index on `(userId, nodeId)`, FK to `users.id`, `source` and `patchId` columns), register its relations so `db.query.nodeProgress.findMany` resolves, and apply the schema to the live Neon database via the mandatory `[BLOCKING]` push.

Purpose: Persistent per-user progress (PROG-01, PROG-02) requires a real table. The unique index is the upsert target for 05-04. Designing `source` (D-04) and `patchId` (D-05) in now avoids a Phase-7 migration. Per the schema_push_requirement, type/build checks pass without the push, so the push is mandatory to avoid a false-positive verification.
Output: `nodeProgress` table in code + live in Neon.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@.planning/phases/05-progress-tracking/05-PATTERNS.md
@src/db/schema.ts
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nodeProgress table + relations to the Drizzle schema</name>
  <read_first>
    - src/db/schema.ts (the file being edited — users table lines 44-128 for column conventions; sessions lines 141-169 for the index-array pattern; FK cascade lines 163-166; usersRelations lines 245-248)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 1 nodeProgress table; §Pitfall 1 text vs pgEnum; §Pitfall 7 relations registration)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/db/schema.ts — surrogate PK + uniqueIndex + index; relations)
    - src/db/schema.test.ts (existing Phase-4 smoke test — extend, do not replace)
  </read_first>
  <action>
    In `src/db/schema.ts`: add `uniqueIndex` to the existing `drizzle-orm/pg-core` import line (alongside `index`, `pgTable`, `text`, `timestamp`, `boolean`). Add an exported `nodeProgress = pgTable("node_progress", {...}, (table) => [...])` with columns: `id: text("id").primaryKey()` (surrogate, matches project convention); `userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" })`; `nodeId: text("node_id").notNull()`; `masteryState: text("mastery_state").notNull()` (TEXT not pgEnum — Pitfall 1: the hyphen in `in-progress` breaks pgEnum DDL; validation lives in MasteryStateSchema at the app layer); `source: text("source").notNull().default("manual")` (D-04); `patchId: text("patch_id").notNull()` (D-05, stamped with CURRENT_PATCH.id by 05-04); `createdAt: timestamp("created_at").defaultNow().notNull()`; `updatedAt: timestamp("updated_at").defaultNow().$onUpdate(() => new Date()).notNull()`. Index array: `uniqueIndex("progress_user_node_unique").on(table.userId, table.nodeId)` (upsert target) and `index("progress_userId_idx").on(table.userId)` (bulk fetch). Add `export const nodeProgressRelations = relations(nodeProgress, ({ one }) => ({ user: one(users, { fields: [nodeProgress.userId], references: [users.id] }) }))` and extend `usersRelations` to add `nodeProgress: many(nodeProgress)` (Pitfall 7 — required for db.query.nodeProgress.findMany). Add a table-level JSDoc noting the surrogate-PK + unique-index upsert design and the D-04/D-05 forward-design. Do NOT use pgEnum for masteryState or source.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "nodeProgress = pgTable" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const nodeProgress = pgTable(`
    - The table uses `text("mastery_state")` (NOT pgEnum) and `text("source").notNull().default("manual")`
    - A `uniqueIndex("progress_user_node_unique").on(table.userId, table.nodeId)` is present
    - `nodeProgressRelations` is exported and `usersRelations` includes `nodeProgress: many(nodeProgress)`
    - `userId` references `users.id` with `{ onDelete: "cascade" }`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>nodeProgress table + relations defined with the surrogate-PK + unique-index upsert design.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the schema smoke test for nodeProgress</name>
  <read_first>
    - src/db/schema.test.ts (existing Phase-4 smoke test structure to mirror)
    - src/db/schema.ts (the table + columns to assert against)
  </read_first>
  <action>
    In `src/db/schema.test.ts`: add assertions that `nodeProgress` is defined and exposes the expected column accessors (`nodeProgress.id`, `nodeProgress.userId`, `nodeProgress.nodeId`, `nodeProgress.masteryState`, `nodeProgress.source`, `nodeProgress.patchId`). Keep it a lightweight structural smoke test in the same style as the existing Phase-4 assertions (no live DB connection — assert on the Drizzle column objects' names/presence). If the existing test imports `* as schema`, add the nodeProgress checks to the same import path.
  </action>
  <verify>
    <automated>npm test -- schema</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- schema` passes
    - The test references `nodeProgress` and at least the `source` and `patchId` columns
  </acceptance_criteria>
  <done>Smoke test confirms the nodeProgress table shape.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to Neon</name>
  <read_first>
    - drizzle.config.ts (uses DATABASE_URL_DIRECT non-pooled — required for push; Pitfall 5)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Environment Availability — DATABASE_URL_DIRECT must be set in .env.local)
    - src/db/schema.ts (the schema being pushed)
  </read_first>
  <action>
    [BLOCKING] After Task 1 and Task 2 are complete, run `npx drizzle-kit push` to apply the `node_progress` table to the live Neon database. This command reads `DATABASE_URL_DIRECT` from the environment (non-pooled direct connection per drizzle.config.ts). `node_progress` is a brand-new table so the push introduces no destructive column changes and is non-interactive; if drizzle-kit unexpectedly prompts for an unresolvable conflict, stop and surface it (this would require `autonomous: false` re-planning). Do NOT proceed to phase verification until this push succeeds — type and build checks pass without the live table, which would otherwise create a false-positive verification state.
  </action>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits 0
    - The `node_progress` table exists in the Neon database after the push (drizzle-kit reports the table created / no pending diff on a second run)
  </acceptance_criteria>
  <done>node_progress table is live in Neon; the upsert target index exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/server fn → database | progress rows written/read across the Drizzle+Neon boundary; FK + unique index enforce integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03a | Tampering | node_progress rows | high | mitigate | uniqueIndex(userId,nodeId) prevents duplicate/conflicting rows per user-node; upsert (05-04) is atomic via onConflictDoUpdate |
| T-05-03b | Information Disclosure | cross-user rows | high | mitigate | FK to users.id + 05-04 principal-keyed queries (D-06) ensure a user can only ever read/write rows for their own UUID; IDOR impossible by construction |
| T-05-03c | Denial of Service | unbounded progress rows | low | accept | one row per (user,node) capped by unique index; node corpus is small (~25-50) |
| T-05-SC | Tampering | drizzle-kit push to live DB | medium | mitigate | new table only, no destructive diff; DATABASE_URL_DIRECT kept in gitignored .env.local |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test -- schema` green
- `npx drizzle-kit push` exits 0 and node_progress exists in Neon
</verification>

<success_criteria>
node_progress table (surrogate PK, unique (userId,nodeId), FK cascade, source + patchId) defined in code, smoke-tested, and live in Neon.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-03-SUMMARY.md` when done.
</output>
