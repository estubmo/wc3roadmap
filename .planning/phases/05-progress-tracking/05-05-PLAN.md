---
phase: 05-progress-tracking
plan: 05
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/lib/local-progress.ts
  - src/lib/local-progress.test.ts
  - src/lib/progress-keys.ts
  - src/lib/graph-store.ts
autonomous: true
requirements: [PROG-01, PROG-03]
must_haves:
  truths:
    - "Signed-out progress reads/writes localStorage and is SSR-safe (returns {} when window is undefined)"
    - "A one-time merge flag (wc3rm:merged) tracks whether the first-sign-in merge has run"
    - "The graph store holds a masteryMap and exposes setNodeMastery + initMasteryMap for optimistic single-node updates"
    - "A typed progressKeys factory exists for TanStack Query invalidation"
  artifacts:
    - "src/lib/local-progress.ts + src/lib/local-progress.test.ts"
    - "src/lib/progress-keys.ts"
    - "src/lib/graph-store.ts extended with masteryMap slice"
  key_links:
    - "graph-store masteryMap → RoadmapGraph seam read (05-08) → single-node re-render (D-09)"
    - "local-progress (signed-out) → ProgressProvider init + merge payload (05-07)"
    - "progressKeys.byUser() → useQuery key (05-07) + invalidate in useProgressMutation (05-06)"
---

<objective>
Build the three client-side progress primitives: the SSR-safe `local-progress` localStorage store (signed-out source of truth + merge flag, D-08), the typed `progressKeys` TanStack Query key factory, and the additive `masteryMap` slice on the existing Zustand `graph-store` (the optimistic single-node update surface, D-09). These are pure client modules with no server or component dependencies beyond the `MasteryState` type.

Purpose: PROG-03 (localStorage store + merge flag) and the client half of PROG-01 (masteryMap that drives graph color). The masteryMap replaces the `getMockMastery` seam in 05-08; local-progress feeds the merge in 05-07; progressKeys wires query invalidation in 05-06.
Output: three lib modules + a test for local-progress.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@.planning/phases/05-progress-tracking/05-PATTERNS.md
@src/lib/graph-store.ts
@src/lib/patches.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSR-safe local-progress store module + tests</name>
  <read_first>
    - src/lib/mock-mastery.ts (private-backing-store + graceful-default pattern to adapt; getMockMastery default "untouched")
    - src/lib/patches.ts (module-constant + read-only accessor style to mirror)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 6 full module shape; §Pitfall 4 merge flag; §Security Domain localStorage poisoning)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/lib/local-progress.ts — exported fns + SSR guard)
    - src/schemas/progress.ts (MasteryState type — single source)
  </read_first>
  <behavior>
    - getLocalProgress() returns {} when typeof window === "undefined" (SSR)
    - getLocalProgress() returns {} when the stored JSON is malformed (try/catch)
    - setLocalMastery(nodeId, state) merges into the stored map; getLocalProgress reflects it
    - clearLocalProgress() removes the progress key
    - isAlreadyMerged() is false before markMerged(), true after
  </behavior>
  <action>
    Create `src/lib/local-progress.ts` with the SPDX header and a module JSDoc (signed-out store + one-time merge flag, D-07/D-08; client-only — never import in server fns). Define `const PROGRESS_KEY = "wc3rm:progress"` and `const MERGED_FLAG = "wc3rm:merged"`. Import `MasteryState` type from `#/schemas/progress`. Export: `getLocalProgress(): Record<string, MasteryState>` (guard `typeof window === "undefined"` → `{}`; try/catch JSON.parse of `localStorage.getItem(PROGRESS_KEY) ?? "{}"` → `{}` on error); `setLocalMastery(nodeId: string, state: MasteryState): void` (SSR guard; read current map, set key, write back); `clearLocalProgress(): void` (SSR guard; removeItem PROGRESS_KEY); `isAlreadyMerged(): boolean` (SSR guard → false; read MERGED_FLAG === "true"); `markMerged(): void` (SSR guard; setItem MERGED_FLAG "true"). Then create `src/lib/local-progress.test.ts` (`// @vitest-environment jsdom` at the top) that stubs/clears localStorage between tests and proves the five behaviors above, plus a test that with `window` present a malformed value returns `{}`.
  </action>
  <verify>
    <automated>npm test -- local-progress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/local-progress.ts` exports getLocalProgress, setLocalMastery, clearLocalProgress, isAlreadyMerged, markMerged
    - Every exported function guards with `typeof window === "undefined"` before touching localStorage
    - `src/lib/local-progress.test.ts` starts with `// @vitest-environment jsdom`
    - `npm test -- local-progress` passes including the SSR-guard and malformed-JSON cases
  </acceptance_criteria>
  <done>SSR-safe localStorage progress store + merge flag, fully tested.</done>
</task>

<task type="auto">
  <name>Task 2: progressKeys query-key factory</name>
  <read_first>
    - src/lib/patches.ts (typed const-object export style to mirror)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 5 query key factory)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/lib/progress-keys.ts)
  </read_first>
  <action>
    Create `src/lib/progress-keys.ts` with the SPDX header. Export `const progressKeys = { all: () => ["progress"] as const, byUser: () => [...progressKeys.all(), "byUser"] as const } as const;` with a short JSDoc explaining it enables typed selective invalidation (used by 05-06 useProgressMutation onSettled and 05-07 useQuery). No other exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "progressKeys" src/lib/progress-keys.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/progress-keys.ts` exports `progressKeys` with `all()` and `byUser()` returning readonly tuples
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Typed progressKeys factory available for query + invalidation wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Extend graph-store with masteryMap slice</name>
  <read_first>
    - src/lib/graph-store.ts (the file being edited — GraphStore interface ends at clearFilters line 143; create() body ends at clearFilters lines 193-195; setFilter set((state)=>) pattern lines 187-191)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§src/lib/graph-store.ts — interface + create() additions; useShallow consumer note)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 7 masteryMap; §Pitfall 2/3 useShallow + new-object semantics)
    - src/schemas/progress.ts (MasteryState type)
  </read_first>
  <action>
    In `src/lib/graph-store.ts`: add `import type { MasteryState } from "#/schemas/progress";` at the top. Add to the `GraphStore` interface (after `clearFilters`), under a new `// --- Phase 5: mastery state map ---` section comment: `masteryMap: Record<string, MasteryState>;`, `setNodeMastery: (nodeId: string, state: MasteryState) => void;`, `initMasteryMap: (map: Record<string, MasteryState>) => void;` each with a one-line JSDoc. Add to the `create<GraphStore>((set) => ({...}))` body (after the clearFilters implementation): `masteryMap: {}`, `setNodeMastery: (nodeId, state) => set((s) => ({ masteryMap: { ...s.masteryMap, [nodeId]: state } }))`, `initMasteryMap: (map) => set({ masteryMap: map })`. Do NOT change any existing Phase 2/3 field. Note in a comment that consumers MUST subscribe via `useGraphStore(useShallow((s) => s.masteryMap))` to avoid hover/selection re-renders (Pitfall 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "masteryMap" src/lib/graph-store.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/graph-store.ts` GraphStore interface declares `masteryMap`, `setNodeMastery`, `initMasteryMap`
    - The create() body initializes `masteryMap: {}` and implements both actions
    - Existing fields (hoveredNodeId, selectedNodeId, searchQuery, activeFilters, and their actions) are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>graph-store carries the masteryMap slice for optimistic single-node updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → app | signed-out progress + merge flag read from a user-writable client store |
| SSR ↔ client | localStorage access must be guarded (server has no window) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05a | Tampering | localStorage progress values | medium | mitigate | localStorage is client-trust only; the authoritative validation happens server-side at merge (05-04 MasteryStateSchema); signed-out marks are non-authoritative by design (D-08) |
| T-05-05b | Denial of Service | malformed localStorage JSON | low | mitigate | getLocalProgress try/catch returns {} — corrupted store degrades to empty, never throws |
| T-05-05c | Information Disclosure | SSR leaking client storage | low | mitigate | every accessor guards `typeof window === "undefined"` — no localStorage call on the server |
</threat_model>

<verification>
- `npm test -- local-progress` green
- `npx tsc --noEmit` exits 0
- graph-store retains all Phase 2/3 fields plus masteryMap
</verification>

<success_criteria>
SSR-safe local-progress store (with merge flag), typed progressKeys factory, and graph-store masteryMap slice all in place and type-clean; local-progress fully tested.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-05-SUMMARY.md` when done.
</output>
