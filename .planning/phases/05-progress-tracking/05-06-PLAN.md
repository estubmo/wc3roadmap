---
phase: 05-progress-tracking
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-04", "05-05"]
files_modified:
  - src/hooks/useProgressMutation.ts
  - src/components/graph/MasteryControls.tsx
  - src/components/graph/MasteryControls.test.tsx
autonomous: true
requirements: [PROG-04, PROG-05]
must_haves:
  truths:
    - "Marking a node updates the Zustand masteryMap immediately (optimistic), then persists async"
    - "On server error the prior masteryMap state is restored and an error toast appears"
    - "Signed-out marks write to localStorage; signed-in marks call setNodeMastery"
    - "MasteryControls renders exactly three state buttons and no count/aggregate/XP/streak text (PROG-05)"
  artifacts:
    - "src/hooks/useProgressMutation.ts"
    - "src/components/graph/MasteryControls.tsx + .test.tsx"
  key_links:
    - "MasteryControls onValueChange → useProgressMutation → graph-store optimistic + server fn / localStorage"
    - "useProgressMutation onError → graph-store rollback + sonner toast"
    - "useProgressMutation onSettled → invalidate progressKeys.byUser()"
---

<objective>
Build the marking interaction: `useProgressMutation` (optimistic Zustand update → async persist → rollback + toast on error, D-09) and `MasteryControls` (the three-state shadcn ToggleGroup that lives in the node detail panel, D-01/D-02). The hook branches on session: signed-in calls `setNodeMastery` (05-04); signed-out writes `setLocalMastery` (05-05). `MasteryControls` shows ONLY the per-node state — no counts, aggregates, XP, streaks, or leaderboards (D-10, PROG-05).

Purpose: PROG-04 (manual marking, full 3-state) and PROG-05 (no gamification). This is the user-facing write path; 05-08 mounts `MasteryControls` into the panel.
Output: hook + component + component test.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@.planning/phases/05-progress-tracking/05-PATTERNS.md
@.planning/phases/05-progress-tracking/05-UI-SPEC.md
@src/components/graph/MasteryBadge.tsx
@src/lib/auth-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useProgressMutation hook (optimistic + rollback + signed-out branch)</name>
  <read_first>
    - src/components/graph/NodePanelContent.tsx (lines 31-36 — the existing @tanstack/react-query import convention)
    - src/lib/graph-store.ts (masteryMap + setNodeMastery from 05-05 — the optimistic surface)
    - src/lib/progress-keys.ts (progressKeys.byUser from 05-05 — invalidation key)
    - src/lib/local-progress.ts (setLocalMastery from 05-05 — signed-out path)
    - src/server/progress.ts (setNodeMastery server fn from 05-04 — signed-in path)
    - src/lib/auth-client.ts (useSession — to branch signed-in vs signed-out)
    - src/components/ui/sonner.tsx (toast surface from 05-01)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 4 useMutation optimistic; §Pitfall 2/3 useShallow + new-object)
    - .planning/phases/05-progress-tracking/05-UI-SPEC.md (§Optimistic Update Flow; §Toast Specifications — error toast copy)
  </read_first>
  <action>
    Create `src/hooks/useProgressMutation.ts` (SPDX header; create the `src/hooks/` directory). Import `useMutation`, `useQueryClient` from `@tanstack/react-query`; `toast` from `sonner`; `useGraphStore` from `#/lib/graph-store`; `progressKeys` from `#/lib/progress-keys`; `setNodeMastery` from `#/server/progress`; `setLocalMastery` from `#/lib/local-progress`; `useSession` from `#/lib/auth-client`; `MasteryState` type from `#/schemas/progress`. Export `useProgressMutation()` returning a `useMutation` whose variables are `{ nodeId: string; masteryState: MasteryState }`. `mutationFn`: if `session` is present, call `setNodeMastery({ data: { nodeId, masteryState } })`; else call `setLocalMastery(nodeId, masteryState)` and resolve. `onMutate`: `await queryClient.cancelQueries({ queryKey: progressKeys.byUser() })`; snapshot `useGraphStore.getState().masteryMap[nodeId]` as `previousState`; call `useGraphStore.getState().setNodeMastery(nodeId, masteryState)` (optimistic single-node update, D-09); return `{ previousState, nodeId }`. `onError`: roll back via `useGraphStore.getState().setNodeMastery(ctx.nodeId, ctx.previousState ?? "untouched")`; fire `toast.error("Couldn't save your progress", { description: "Your selection has been reverted. Please try again.", ... })` with a Retry action that re-invokes the mutation for the same vars and `duration: Infinity` (UI-SPEC error toast). `onSettled`: only when signed-in, `queryClient.invalidateQueries({ queryKey: progressKeys.byUser() })`. Do NOT invalidate inside onMutate (Pitfall: full-graph invalidation). On the success path, no toast (UI-SPEC — silent success).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "onMutate\|onError\|onSettled" src/hooks/useProgressMutation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useProgressMutation.ts` exports `useProgressMutation`
    - `onMutate` calls `useGraphStore.getState().setNodeMastery(...)` and returns a `previousState` snapshot
    - `onError` restores `previousState` and calls `toast.error` with the UI-SPEC copy
    - `mutationFn` branches on `useSession()`: server fn when signed-in, `setLocalMastery` when signed-out
    - Success path fires no toast; invalidation happens only in `onSettled` (not `onMutate`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Optimistic mutation hook with rollback, error toast, and signed-out localStorage branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MasteryControls component + test</name>
  <read_first>
    - src/components/graph/MasteryBadge.tsx (mastery-state vocabulary + CSS-variable color conventions to mirror; MasteryState prop typing)
    - src/components/ui/toggle-group.tsx (ToggleGroup / ToggleGroupItem API from 05-01)
    - src/hooks/useProgressMutation.ts (the mutation the component fires on change)
    - src/lib/auth-client.ts (useSession — drives the source-attribution / signed-out hint line)
    - .planning/phases/05-progress-tracking/05-UI-SPEC.md (§MasteryControls Component — layout, button states, copy; §Copywriting Contract; §Mastery State Color Contract; §Anti-gamification enforcement)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§MasteryControls.tsx — props + ToggleGroup)
  </read_first>
  <behavior>
    - Renders three ToggleGroupItem buttons labeled "Untouched", "In Progress", "Mastered"
    - The button matching currentState is the active/pressed item
    - Selecting a different state calls the progress mutation with { nodeId, masteryState } for the new state
    - Signed-out: renders the hint "Sign in with Battle.net to save your progress across devices"
    - Renders NO count/percentage/XP/streak/leaderboard text (PROG-05)
  </behavior>
  <action>
    Create `src/components/graph/MasteryControls.tsx` (SPDX header). Props: `interface MasteryControlsProps { nodeId: string; currentState: MasteryState }` (`MasteryState` from `#/schemas/progress`). Render a `"Mastery"` section label (13px/600, subdued `#9998a8`), then a single-selection `ToggleGroup` (type `"single"`, `value={currentState}`, `aria-label="Mastery state"`) containing three `ToggleGroupItem`s with values `"untouched"`, `"in-progress"`, `"mastered"` and labels `"Untouched"`, `"In Progress"`, `"Mastered"` (UI-SPEC §Copywriting Contract). On `onValueChange`, ignore an empty/unchanged value, otherwise call the `useProgressMutation()` result's `.mutate({ nodeId, masteryState: value })`. Apply the active-state button colors from UI-SPEC §Mastery State Color Contract via CSS variables (untouched: obsidian-800 / body text; in-progress: rune-600 bg / rune-300 text; mastered: rune-500 bg / obsidian-950 text; inactive: obsidian-800 / subdued; full-width flex thirds, 4px gap, 36px height). Below the group: when `useSession()` has no session, render the caption "Sign in with Battle.net to save your progress across devices"; when signed-in, render no attribution line for an untouched state (UI-SPEC — source attribution line is optional polish; a simple "Marked manually" caption is acceptable but MUST NOT include any time-aggregate or count). Render NOTHING resembling a count, percentage, XP, streak, or leaderboard (D-10, PROG-05). Then create `src/components/graph/MasteryControls.test.tsx` (`// @vitest-environment jsdom`) mocking `useProgressMutation` (assert `.mutate` called with the new state on change) and `useSession`; prove: three labeled buttons render; the currentState button is pressed; changing selection fires the mutation with the correct masteryState; the signed-out hint renders when session is null; assert the rendered text contains no `%`, `XP`, `streak`, or `of ` count fragments.
  </action>
  <verify>
    <automated>npm test -- MasteryControls && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/MasteryControls.tsx` renders three ToggleGroupItems with labels Untouched / In Progress / Mastered
    - Selecting a new state calls the mutation with `{ nodeId, masteryState }` for that state
    - Signed-out renders the exact hint copy from UI-SPEC
    - The component renders no count/percentage/XP/streak/leaderboard text (asserted in the test)
    - `npm test -- MasteryControls` passes; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three-state MasteryControls wired to the mutation, gamification-free, signed-out aware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction → mutation | button selection becomes a masteryState write (server or localStorage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06a | Tampering | masteryState from UI | low | mitigate | ToggleGroup can only emit the three enum values; server (05-04) re-validates with MasteryStateSchema before persistence |
| T-05-06b | Repudiation | lost write on error | low | mitigate | onError rolls back optimistic state and surfaces an error toast so the user is never misled into thinking a failed save succeeded |
| T-05-06c | Information Disclosure | error toast internals | low | accept | toast body is fixed generic copy (UI-SPEC) — no server error detail exposed |
</threat_model>

<verification>
- `npm test -- MasteryControls` green
- `npx tsc --noEmit` exits 0
- No gamification text rendered (asserted)
</verification>

<success_criteria>
Optimistic mutation hook (rollback + toast + signed-out branch) and a gamification-free three-state MasteryControls, both tested.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-06-SUMMARY.md` when done.
</output>
