---
phase: 05-progress-tracking
plan: 09
type: execute
wave: 5
depends_on: ["05-07", "05-08"]
files_modified:
  - CONTEXT.md
  - docs/adr/009-progress-persistence.md
autonomous: false
requirements: [PROG-02, PROG-03, PROG-05]
must_haves:
  truths:
    - "The root CONTEXT.md glossary defines the Phase 5 domain terms (progress record, mastery source, local progress, merge-on-sign-in)"
    - "ADR 009 records the progress-table design (surrogate PK + unique index, text enum over pgEnum, source field, fill-gaps merge)"
    - "A human has verified the full marking + persistence + merge + no-gamification flow in the running app"
  artifacts:
    - "CONTEXT.md with Phase 5 terms"
    - "docs/adr/009-progress-persistence.md"
  key_links:
    - "ADR 009 → schema (05-03) + server fns (05-04) design rationale (audit trail)"
---

<objective>
Close the phase: extend the root `CONTEXT.md` domain glossary with the Phase 5 terms, author ADR 009 documenting the progress-persistence design decisions, and run the end-of-phase human-verify checkpoint covering all five ROADMAP success criteria (mark + persist, merge, single-node re-render, no gamification, server-as-source-of-truth).

Purpose: Architecture-discipline requirement (CONTEXT.md + docs/adr/ extended every phase) and the final acceptance gate. The checkpoint is the only place a human confirms the optimistic feel, the merge behavior, and the absence of gamification — things automated tests cannot fully assert.
Output: updated CONTEXT.md, ADR 009, human sign-off.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@CONTEXT.md
@docs/adr/008-betterauth-battlenet-integration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CONTEXT.md glossary + author ADR 009</name>
  <read_first>
    - CONTEXT.md (root domain glossary — existing term format; "Last updated: Phase 04" line to bump to Phase 05; append new terms in the established style)
    - docs/adr/008-betterauth-battlenet-integration.md (ADR file format/structure to mirror — status, context, decision, consequences)
    - docs/adr/007-authed-server-fn-authorization.md (the authorization ADR that ADR 009 builds on)
    - .planning/phases/05-progress-tracking/05-CONTEXT.md (decisions D-01..D-10 — the rationale source)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 1 surrogate PK; §Pitfall 1 text vs pgEnum; §Project Constraints — ADR for progress table design)
  </read_first>
  <action>
    In root `CONTEXT.md`: bump the "Last updated" line to Phase 05 and append glossary entries (matching the existing term/definition style) for: `progress record` (one user's mastery state on one node for one patch, persisted in node_progress, keyed by users.id UUID); `mastery source` (the `source` field — `manual` written in Phase 5, `auto` reserved for Phase 7/8 detection; a manual mark may override an auto state, D-04); `local progress` (signed-out mastery held in localStorage under `wc3rm:progress`, non-authoritative, D-08); `merge-on-sign-in` (the one-time fill-gaps merge — server wins, localStorage clears after, guarded by `wc3rm:merged`, D-07). Reaffirm `in-progress` as the canonical mastery-state value (D-03) if the glossary's mastery-state entry still says `learning`. Then create `docs/adr/009-progress-persistence.md` following the ADR-008 structure (Status: Accepted; Context; Decision; Consequences) recording: surrogate text PK + unique index on (userId, nodeId) as the upsert target (over composite PK); `text()` columns over `pgEnum` for masteryState/source (Pitfall 1 — hyphen DDL + future enum values); the forward-designed `source` field (D-04); patch stamping with CURRENT_PATCH.id (D-05); principal-keyed authedServerFn for all progress writes (builds on ADR 007, D-06); fill-gaps server-wins one-time merge (D-07); no-gamification / per-node-only constraint (D-10, PROG-05).
  </action>
  <verify>
    <automated>test -f docs/adr/009-progress-persistence.md && grep -ci "merge-on-sign-in\|mastery source\|progress record" CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `CONTEXT.md` defines `progress record`, `mastery source`, `local progress`, and `merge-on-sign-in`, and its "Last updated" reads Phase 05
    - `docs/adr/009-progress-persistence.md` exists with Status/Context/Decision/Consequences sections
    - ADR 009 names the surrogate-PK + unique-index choice, text-over-pgEnum, the source field, and the fill-gaps merge
  </acceptance_criteria>
  <done>Domain glossary + ADR 009 capture the Phase 5 vocabulary and design decisions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-of-phase human verification (5 ROADMAP criteria)</name>
  <action>Pause execution and request human verification of the running app against all five ROADMAP Phase 5 success criteria (see how-to-verify). Do not mark the phase complete until the human types "approved".</action>
  <what-built>
    End-to-end progress tracking: a three-state MasteryControls in the node detail panel (D-01/D-02), optimistic single-node graph updates with rollback (D-09), server-side persistence via principal-keyed authedServerFn (D-06), one-time localStorage fill-gaps merge on first sign-in (D-07), and a strictly no-gamification UI (D-10). Real masteryMap replaces the getMockMastery seam.
  </what-built>
  <how-to-verify>
    Run the app locally (`npm run dev`, http://localhost:3000) and verify each ROADMAP Phase 5 criterion:
    1. (Criterion 1) Sign in with Battle.net. Click a node → the panel shows "Mastery" with three buttons. Click "In Progress", then "Mastered" — the node's color/border/glow updates immediately in the graph. Refresh the browser → the state persists. (Optionally confirm on a second device/incognito signed in as the same account.)
    2. (Criterion 3) When you click a mastery button, ONLY the marked node re-renders/recolors — the rest of the graph does not flicker or reload, and there is no full-page reload.
    3. (Criterion 2 + 5) Sign out. Mark 2-3 nodes while signed out (these go to localStorage). Sign in for the first time → those marks appear on the account for nodes the account had no record for; a "Progress synced" toast shows. Then, as the signed-in user, open DevTools → Application → clear localStorage → reopen the app: the same server-persisted states still show (server is the source of truth, client is a cache).
    4. (Criterion 4 / PROG-05) Inspect the panel and the graph: confirm there is NO XP, no streak counter, no global leaderboard, no "X of Y mastered" count, and no completion percentage anywhere. The only progress signal is per-node color + the panel's three-state control.
    5. (Error path) Optionally simulate a failed save (e.g. offline) → the selection reverts and an error toast "Couldn't save your progress" appears with a Retry action.
  </how-to-verify>
  <resume-signal>Type "approved" if all five criteria pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → audit | ADR records the security/authorization design for future audit; no runtime boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09a | Repudiation | undocumented design | low | mitigate | ADR 009 records the surrogate-PK/text-enum/source/merge decisions so future phases inherit the rationale (audit trail, ADR discipline) |
| T-05-09b | Tampering | verification false-positive | medium | mitigate | the human-verify checkpoint exercises the live DB persistence + merge that automated type/build checks cannot confirm (false-positive guard per schema_push_requirement) |
</threat_model>

<verification>
- `docs/adr/009-progress-persistence.md` exists with the required sections
- `CONTEXT.md` carries the four Phase 5 terms
- Human approves all five ROADMAP criteria
</verification>

<success_criteria>
Domain glossary + ADR 009 updated; a human has confirmed marking, persistence, merge, single-node re-render, and the absence of gamification in the running app.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-09-SUMMARY.md` when done.
</output>
