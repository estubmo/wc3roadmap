---
phase: 05-progress-tracking
plan: 08
type: execute
wave: 4
depends_on: ["05-05", "05-06", "05-07"]
files_modified:
  - src/components/graph/RoadmapGraph.tsx
  - src/components/graph/NodePanelContent.tsx
autonomous: true
requirements: [PROG-01, PROG-04, PROG-05]
must_haves:
  truths:
    - "The graph reads mastery state from the Zustand masteryMap, not from getMockMastery"
    - "Marking a node re-renders only that node's color/border/glow — not the whole graph (D-09, ROADMAP criterion 3)"
    - "The node detail panel shows MasteryControls at the top, above How to Apply (D-01)"
    - "The panel's controls reflect the node's current mastery state"
  artifacts:
    - "src/components/graph/RoadmapGraph.tsx reading masteryMap via useShallow (getMockMastery seam removed)"
    - "src/components/graph/NodePanelContent.tsx mounting MasteryControls"
  key_links:
    - "masteryMap (05-05) → RoadmapGraph displayNodes/filteredDisplayNodes data.masteryState → GraphNode color"
    - "selectedNode masteryMap[nodeId] → MasteryControls currentState → useProgressMutation"
---

<objective>
Complete the integration: replace the `getMockMastery` seam in `RoadmapGraph` with reads from the Zustand `masteryMap` (so real per-user progress drives node color, D-09), and mount `MasteryControls` at the top of `NodePanelContent` (the marking UI lives in the panel, D-01). The masteryMap subscription uses `useShallow` so only the marked node re-renders (Pitfall 2; ROADMAP criterion 3 — single-node update, no full reload).

Purpose: PROG-01 (real mastery in the graph), PROG-04 (mark from the panel), and PROG-05 (panel shows per-node state only). This is the final wiring that makes marking visible end-to-end.
Output: edited RoadmapGraph + NodePanelContent.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-tracking/05-CONTEXT.md
@.planning/phases/05-progress-tracking/05-RESEARCH.md
@.planning/phases/05-progress-tracking/05-PATTERNS.md
@.planning/phases/05-progress-tracking/05-UI-SPEC.md
@src/components/graph/RoadmapGraph.tsx
@src/components/graph/NodePanelContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the getMockMastery seam in RoadmapGraph with masteryMap</name>
  <read_first>
    - src/components/graph/RoadmapGraph.tsx (line 50 getMockMastery import; lines 149-166 displayNodes useMemo with getMockMastery at 151; lines 200-205 existing useShallow filter subscription; lines 207-220 filteredDisplayNodes with getMockMastery at 211)
    - src/lib/graph-store.ts (masteryMap from 05-05 — read via useShallow)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§RoadmapGraph.tsx — exact seam swap at 151/211; useShallow subscription; dep-array additions; import swap)
    - .planning/phases/05-progress-tracking/05-RESEARCH.md (§Pattern 8 replace getMockMastery; §Pitfall 2/3 useShallow single-node re-render)
  </read_first>
  <action>
    In `src/components/graph/RoadmapGraph.tsx`: remove `import { getMockMastery } from "#/lib/mock-mastery";` (line 50) and add `import type { MasteryState } from "#/schemas/progress";`. Add a masteryMap subscription via the existing `useShallow` import — either extend the existing `useGraphStore(useShallow(...))` selector to also return `masteryMap`, or add a dedicated `const masteryMap = useGraphStore(useShallow((s) => s.masteryMap));` (place it near the existing filter-slice subscription around line 200). In the `displayNodes` useMemo (line 149-166): replace `const masteryState = getMockMastery(n.id);` with `const masteryState: MasteryState = masteryMap[n.id] ?? "untouched";` (preserve the graceful untouched default) and add `masteryMap` to that useMemo's dependency array. In the `filteredDisplayNodes` useMemo (line 207-220): replace `const mastery = getMockMastery(n.id);` with `const mastery: MasteryState = masteryMap[n.id] ?? "untouched";` and add `masteryMap` to that dependency array. Do not change the pathway-dim, filter-dim, fitView, or handler logic. The `useShallow` selector ensures only the changed masteryMap key triggers re-computation, so a single mark re-renders only the affected GraphNode (Pitfall 2/3).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c getMockMastery src/components/graph/RoadmapGraph.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/RoadmapGraph.tsx` contains zero references to `getMockMastery`
    - `masteryMap` is read via `useGraphStore(useShallow(...))` and added to both the `displayNodes` and `filteredDisplayNodes` dependency arrays
    - Both seam sites use `masteryMap[n.id] ?? "untouched"` (graceful default preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Graph node color is driven by real per-user masteryMap; mock seam removed.</done>
</task>

<task type="auto">
  <name>Task 2: Mount MasteryControls at the top of NodePanelContent</name>
  <read_first>
    - src/components/graph/NodePanelContent.tsx (success-state render block lines 322-417 — How to Apply section starts at 331; MasteryControls mounts ABOVE it; nodeId prop already available)
    - src/components/graph/MasteryControls.tsx (props nodeId + currentState from 05-06)
    - src/lib/graph-store.ts (masteryMap — read currentState via useShallow)
    - .planning/phases/05-progress-tracking/05-UI-SPEC.md (§MasteryControls Component placement — first element above How to Apply, 1px obsidian-600 border-bottom + 16px margin-bottom)
    - .planning/phases/05-progress-tracking/05-PATTERNS.md (§NodePanelContent.tsx — mount MasteryControls; read currentState from masteryMap)
  </read_first>
  <action>
    In `src/components/graph/NodePanelContent.tsx`: import `MasteryControls` from `./MasteryControls`, `useGraphStore` from `#/lib/graph-store`, and `useShallow` from `zustand/shallow`. Read the current node's state with `const currentState = useGraphStore(useShallow((s) => s.masteryMap[nodeId] ?? "untouched"))`. Inside the success-state content `<div>` (the block rendered when `node` is truthy, starting around line 322), insert `<MasteryControls nodeId={nodeId} currentState={currentState} />` as the FIRST child, before the "How to Apply in Your Next Game" `<section>` (D-01 — controls live in the panel, pinned above content). Wrap it with the UI-SPEC divider: a 1px `var(--color-obsidian-600)` border-bottom and 16px margin-bottom separating it from the How-to-Apply section. Do not change the existing How-to-Apply / body / citations / prerequisites ordering or the loading/error states.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "MasteryControls" src/components/graph/NodePanelContent.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/NodePanelContent.tsx` renders `<MasteryControls nodeId={nodeId} currentState={currentState} />` as the first element of the success content block, above the How-to-Apply section
    - `currentState` is read from `masteryMap[nodeId] ?? "untouched"` via a `useShallow` selector
    - The existing content sections and their order are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The detail panel shows the three-state MasteryControls at the top, reflecting the node's current state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → render | masteryMap drives node visual state; no new external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08a | Tampering | masteryMap render | low | mitigate | masteryMap is hydrated only from principal-keyed server data (05-07) or the user's own localStorage; rendering it exposes nothing cross-user |
| T-05-08b | Denial of Service | full-graph re-render | low | mitigate | useShallow masteryMap subscription + React.memo on GraphNode confine re-render to the changed node (ROADMAP criterion 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test` full suite green (no consumer regressed)
- Zero getMockMastery references remain in RoadmapGraph.tsx
- MasteryControls mounted above How-to-Apply in the panel
</verification>

<success_criteria>
Real masteryMap drives graph color with single-node re-render; the panel hosts the three-state MasteryControls at the top — marking is visible end-to-end.
</success_criteria>

<output>
Create `.planning/phases/05-progress-tracking/05-08-SUMMARY.md` when done.
</output>
