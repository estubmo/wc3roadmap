---
phase: 03-content-pipeline-node-panel
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/node-content-query.ts
  - src/lib/node-content-query.test.ts
autonomous: true
requirements: [GRAPH-03]
user_setup: []

must_haves:
  truths:
    - "Node content loads lazily per node by id — the panel fetches only the selected node's full content, with no full-page reload (success criterion 1)"
    - "The query is disabled until a node is selected and never stales (static build-time content)"
  artifacts:
    - "src/lib/node-content-query.ts exporting nodeContentQueryOptions(id)"
    - "a NodeFrontmatterWithMDX type covering the transform-added mdx + mdxHowToApply fields"
  key_links:
    - "queryFn resolves synchronously over the in-memory allNodes module — no network round-trip"
    - "enabled is false when nodeId is null so no query runs until a node is clicked"
---

<objective>
Implement the lazy-load data layer for the panel (D-02, GRAPH-03): a `nodeContentQueryOptions(id)` factory backed by TanStack Query over the in-memory `allNodes` module. This turns the `node-content-query.test.ts` scaffold from plan 03-01 green.

Purpose: ADR-002 mandates content loads lazily per node "via TanStack Query"; this module is the single seam the panel uses to fetch a node's full content (including the `mdx` + `mdxHowToApply` transform outputs) by id.
Output: `nodeContentQueryOptions`, a `NodeFrontmatterWithMDX` type, green query tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@docs/adr/002-content-graph-decoupling.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement nodeContentQueryOptions(id) over allNodes</name>
  <files>src/lib/node-content-query.ts, src/lib/node-content-query.test.ts</files>
  <read_first>
    - src/lib/node-content-query.test.ts (the RED scaffold from plan 03-01 — its assertions define the contract)
    - src/lib/mock-mastery.ts (analog: single-export accessor module over in-memory data, SPDX header, graceful default)
    - src/schemas/node.ts (the `NodeFrontmatter` type the panel renders; `mdx`/`mdxHowToApply` are transform outputs NOT on this type)
    - 03-PATTERNS.md §"src/lib/node-content-query.ts" (single export; `queryOptions`; `enabled: nodeId !== null` guard; `#/` alias)
    - 03-RESEARCH.md §Q2 "Query options module" (exact queryOptions shape; staleTime Infinity; type-cast note for the mdx fields)
  </read_first>
  <behavior>
    - `nodeContentQueryOptions(null).enabled === false` (disabled until a node is selected).
    - `nodeContentQueryOptions("map-control").queryKey` equals `["node-content", "map-control"]`.
    - For a non-null id, `enabled === true`.
    - `queryFn` returns the matching node (typed `NodeFrontmatterWithMDX`) for a known id and throws for an unknown id.
  </behavior>
  <action>
    Create `src/lib/node-content-query.ts` (SPDX header). Export a `nodeContentQueryOptions(nodeId: string | null)` factory using `queryOptions` from `@tanstack/react-query`: `queryKey: ["node-content", nodeId]`; a SYNCHRONOUS `queryFn` that does `allNodes.find(n => n.id === nodeId)` (import `allNodes` from `content-collections`), throwing `Node not found: <id>` when absent; `staleTime: Infinity` (static build-time content); `enabled: nodeId !== null`. Define and export a `NodeFrontmatterWithMDX` type = `NodeFrontmatter & { mdx: string; mdxHowToApply: string }` so the panel can read the transform-added fields without an `any` cast (the transform from plan 03-02 produces them). Use `#/` import aliases. This turns the plan 03-01 `node-content-query.test.ts` scaffold green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/node-content-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `nodeContentQueryOptions` is the single export factory with the queryKey/enabled/staleTime contract above
    - `enabled` is false for `null`, true for a real id
    - `queryFn` returns the node for a known id and throws for unknown
    - `NodeFrontmatterWithMDX` exported covering `mdx` + `mdxHowToApply`
    - `npx vitest run src/lib/node-content-query.test.ts` GREEN; `npx tsc --noEmit` clean for the file
  </acceptance_criteria>
  <done>Lazy per-node content query implemented and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory content bundle → panel | Static build-time content served synchronously to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-06 | Denial of Service | unknown-id query | low | mitigate | `queryFn` throws a clear error for unknown ids (surfaced as a query error state in the panel) rather than returning undefined and rendering a broken panel. No network round-trip — no external surface. |
</threat_model>

<verification>
- `npx vitest run src/lib/node-content-query.test.ts` green
- `npx tsc --noEmit` clean for node-content-query.ts
</verification>

<success_criteria>
Per-node lazy content query implemented over allNodes, disabled until selection, unit-green.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-05-SUMMARY.md` when done
</output>
