---
phase: 03-content-pipeline-node-panel
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - src/lib/graph-store.ts
  - src/lib/filter-utils.ts
  - src/lib/filter-utils.test.ts
autonomous: true
requirements: [GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "A user can filter nodes by skill type and mastery state and search by title/tags — the filter logic narrows matches deterministically (success criterion 5)"
    - "Panel-open and filter state live in the established Zustand store, decoupled from React Flow internals"
  artifacts:
    - "src/lib/graph-store.ts extended with selectedNodeId/setSelectedNode + searchQuery/activeFilters/setSearchQuery/setFilter/clearFilters and exported ActiveFilters"
    - "src/lib/filter-utils.ts with pure matchesFilter + isFilterActive"
  key_links:
    - "filter-utils.ts imports ActiveFilters from graph-store.ts and GraphDisplayNode (with skillType/tags) from graph.ts"
    - "matchesFilter must be pure — no React, DOM, or Zustand imports of state"
---

<objective>
Extend the Zustand `graph-store` with the panel-selection and filter state (D-02/D-09/D-10) and implement the pure `matchesFilter` / `isFilterActive` functions (D-08/D-10, GRAPH-04). This turns the `filter-utils.test.ts` scaffold from plan 03-01 green.

Purpose: Centralize selected-node + filter UI state in the established store (decoupled from React Flow), and provide a pure, unit-tested filter predicate the graph canvas and FilterBar consume.
Output: extended store with exported `ActiveFilters`, pure filter utilities, green filter-utils tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@.agents/skills/codebase-design/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend graph-store with panel-selection + filter state</name>
  <files>src/lib/graph-store.ts</files>
  <read_first>
    - src/lib/graph-store.ts (existing `GraphStore` interface + `create()` — lines 39-78; hover/ancestorEdge slice pattern)
    - 03-PATTERNS.md §"src/lib/graph-store.ts (modify — add panel + filter state)" (exact interface + create() additions; export ActiveFilters)
    - 03-RESEARCH.md §Q7 "graph-store.ts extension" (slice shapes; AND-across/OR-within facet semantics)
  </read_first>
  <action>
    Extend the existing `GraphStore` interface and `create()` implementation (do NOT rewrite the file). Add: `selectedNodeId: string | null` + `setSelectedNode(id)`; `searchQuery: string` + `setSearchQuery(q)`; `activeFilters: ActiveFilters` + `setFilter(facet, values)` + `clearFilters()`. Define and EXPORT an `ActiveFilters` interface with `race: string[]`, `skillType: string[]`, `difficulty: string[]`, `mastery: string[]` (imported by filter-utils.ts). Initialize `selectedNodeId: null`, `searchQuery: ""`, `activeFilters` with four empty arrays. `setFilter` replaces one facet's array immutably; `clearFilters` resets query + all facets. Keep the existing hover/ancestorEdge slice intact (D-02 live-inspector relies on `setSelectedNode`). Preserve the SPDX header.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "graph-store" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `GraphStore` exposes `selectedNodeId`, `setSelectedNode`, `searchQuery`, `setSearchQuery`, `activeFilters`, `setFilter`, `clearFilters`
    - `ActiveFilters` is exported with the four facet arrays
    - Existing hover/ancestorEdge state is unchanged
    - `npx tsc --noEmit` clean for graph-store.ts
  </acceptance_criteria>
  <done>Store carries selected-node + filter state; ActiveFilters exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure matchesFilter + isFilterActive</name>
  <files>src/lib/filter-utils.ts, src/lib/filter-utils.test.ts</files>
  <read_first>
    - src/lib/filter-utils.test.ts (the RED scaffold from plan 03-01 — its assertions define the contract)
    - src/lib/pathway-utils.ts (analog: pure-function module, SPDX header, deep-module JSDoc, no React/DOM)
    - src/lib/mock-mastery.ts (the mastery source the mastery facet reads — `getMockMastery` / `MasteryState` type)
    - 03-PATTERNS.md §"src/lib/filter-utils.ts" (header + pure-function pattern)
    - 03-RESEARCH.md §Q7 "Pure filter function" (exact matchesFilter / isFilterActive logic; AND across facets, OR within)
  </read_first>
  <behavior>
    - `matchesFilter(node, mastery, "", emptyFilters)` → true (nothing active).
    - Search: returns false unless `searchQuery` (lowercased) is a substring of `node.title` OR any `node.tags` entry (D-10, title+tags only).
    - Facets: AND across race/skillType/difficulty/mastery; OR within a facet (a non-empty facet array matches if it `includes` the node's value); the mastery facet compares against the supplied `mastery` arg (mock source).
    - `isFilterActive("", emptyFilters)` → false; true if query non-empty OR any facet array non-empty.
  </behavior>
  <action>
    Create `src/lib/filter-utils.ts` (deep module, SPDX header, JSDoc): export pure `matchesFilter(node: GraphDisplayNode, mastery: MasteryState, searchQuery: string, filters: ActiveFilters): boolean` and `isFilterActive(searchQuery: string, filters: ActiveFilters): boolean`. Implement title+tags substring search (D-10) and AND-across/OR-within facet logic (D-10) per RESEARCH §Q7. Import `GraphDisplayNode` from `#/schemas/graph`, `MasteryState` from `#/lib/mock-mastery`, `ActiveFilters` from `#/lib/graph-store` (type-only imports). No React, no DOM, no store-state access — pure logic only. This turns the plan 03-01 `filter-utils.test.ts` scaffold green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/filter-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `matchesFilter` and `isFilterActive` exported as pure functions (no React/DOM/store-state imports; type-only imports allowed)
    - AND-across-facets, OR-within-facet, and title+tags search behave per the `<behavior>` block
    - `npx vitest run src/lib/filter-utils.test.ts` is GREEN
    - `npx tsc --noEmit` clean for filter-utils.ts
  </acceptance_criteria>
  <done>Pure filter predicate implemented and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search input → filter logic | Free-text query consumed by string comparison only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-05 | Tampering / XSS | search query handling | low | mitigate | Search query is used only in `String.toLowerCase().includes()` comparisons and is never rendered as HTML (RESEARCH §Security Domain — safe by construction). |
</threat_model>

<verification>
- `npx vitest run src/lib/filter-utils.test.ts` green
- `npx tsc --noEmit` clean for graph-store.ts + filter-utils.ts
</verification>

<success_criteria>
Store carries panel + filter state; pure filter predicate implemented and unit-green.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-04-SUMMARY.md` when done
</output>
