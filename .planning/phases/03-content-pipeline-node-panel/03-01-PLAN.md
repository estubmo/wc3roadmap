---
phase: 03-content-pipeline-node-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/routes/__root.tsx
  - src/lib/filter-utils.test.ts
  - src/lib/node-content-query.test.ts
  - src/schemas/node.test.ts
autonomous: true
requirements: [CONT-01, CONT-03, GRAPH-03, GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "A QueryClient is available app-wide so the panel can lazy-load node content via TanStack Query without a full-page reload (success criterion 1)"
    - "Failing test scaffolds exist for the filter logic, the node-content query, and the citation discriminator before the feature waves implement them (Nyquist Wave 0)"
  artifacts:
    - "package.json with @tanstack/react-query and jsdom added"
    - "src/routes/__root.tsx wrapping children in QueryClientProvider"
    - "src/lib/filter-utils.test.ts (RED)"
    - "src/lib/node-content-query.test.ts (RED)"
    - "src/schemas/node.test.ts extended with citation kind discriminator cases (RED)"
  key_links:
    - "QueryClientProvider must wrap the route tree children but leave <Scripts /> outside it"
    - "Test scaffolds import the real module paths (#/lib/filter-utils, #/lib/node-content-query) so the feature waves turn them green by creating those modules"
---

<objective>
Establish the Phase 3 test + data-layer foundation: install `@tanstack/react-query` (the lazy-load data layer for the panel, GRAPH-03) and `jsdom`, mount a module-scope `QueryClient` via `QueryClientProvider` in the root document, and create the failing Nyquist test scaffolds named in 03-VALIDATION.md before any feature task depends on them.

Purpose: Wave 0 — every downstream feature task carries an automated `vitest` command; those test files and the QueryClient must exist first.
Output: Installed deps, QueryClientProvider wiring, three RED test files (`filter-utils.test.ts`, `node-content-query.test.ts`, extended `node.test.ts`).
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-VALIDATION.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-query + jsdom and add jsdom capability to Vitest</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - package.json (current deps; confirm `@tanstack/react-query` is absent, `jsdom` is absent — verified during planning)
    - vitest.config.ts (current `environment: "node"`)
    - 03-RESEARCH.md §"Standard Stack" / §"Package Legitimacy Audit" (react-query 5.x approved — SUS verdict is a false positive, no checkpoint) and §"Validation Architecture" (jsdom note)
  </read_first>
  <action>
    Install runtime dep `@tanstack/react-query` (5.x) and dev dep `jsdom` via the project package manager (npm). Keep versions pinned to the resolved 5.x / current jsdom per the pinned-stack discipline in CLAUDE.md.
    Keep `vitest.config.ts` default `environment: "node"` (pure-function tests in this phase run in node). Add jsdom *capability* so future React-component tests can opt in with a per-file `// @vitest-environment jsdom` directive — document this in a comment near the `environment` key in `vitest.config.ts`. Do NOT switch the global environment to jsdom (it would slow the schema/logic suite).
    Per RESEARCH Package Legitimacy Audit, `@tanstack/react-query` is pre-approved (58M weekly downloads, official TanStack monorepo) — no human checkpoint required (T-3-SC accepted).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['@tanstack/react-query']) throw new Error('react-query missing'); if(!(p.devDependencies&&p.devDependencies.jsdom)) throw new Error('jsdom missing'); console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` includes `@tanstack/react-query` at a 5.x version
    - `package.json` `devDependencies` includes `jsdom`
    - `vitest.config.ts` still has `environment: "node"` with a comment documenting the per-file jsdom opt-in directive
    - `npx vitest run` still collects and runs the existing suite without an environment error
  </acceptance_criteria>
  <done>react-query + jsdom installed; vitest config documents jsdom opt-in; existing suite still runs.</done>
</task>

<task type="auto">
  <name>Task 2: Mount module-scope QueryClient via QueryClientProvider in __root.tsx</name>
  <files>src/routes/__root.tsx</files>
  <read_first>
    - src/routes/__root.tsx (current `RootDocument` shell — lines 1-43)
    - 03-PATTERNS.md §"src/routes/__root.tsx (modify — QueryClient setup)" (exact placement: provider wraps `{children}`, `<Scripts />` stays outside)
    - 03-RESEARCH.md §Q2 "QueryClient setup" and §"Open Questions" #3 (client-only QueryClient; no dehydration this phase)
  </read_first>
  <action>
    Import `QueryClient` and `QueryClientProvider` from `@tanstack/react-query`. Create a single module-scope `queryClient = new QueryClient({ defaultOptions: { queries: { staleTime: 1000 * 60 * 5 } } })` (NOT inside the component — prevents re-creation on re-render). In `RootDocument`, wrap only `{children}` with `<QueryClientProvider client={queryClient}>`; keep `<Scripts />` OUTSIDE the provider (it emits script tags, not React components). No dehydration/hydration this phase (deferred to Phase 7 per RESEARCH Open Question 3). Keep the existing SPDX header.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "__root" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/__root.tsx` imports `QueryClient`, `QueryClientProvider` from `@tanstack/react-query`
    - `queryClient` is declared at module scope, not inside `RootDocument`
    - `{children}` is wrapped by `<QueryClientProvider>`; `<Scripts />` is rendered outside the provider
    - `npx tsc --noEmit` reports no error originating in `__root.tsx`
  </acceptance_criteria>
  <done>App-wide QueryClient available; no type errors in __root.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create RED Nyquist test scaffolds (filter-utils, node-content-query, citation discriminator)</name>
  <files>src/lib/filter-utils.test.ts, src/lib/node-content-query.test.ts, src/schemas/node.test.ts</files>
  <read_first>
    - src/lib/graph-layout.test.ts (analog test structure — fixtures + describe/it)
    - src/lib/mock-mastery.test.ts (analog for node-content-query test)
    - src/schemas/node.test.ts (existing citation tests at lines 38, 159, 304-335 — these reference the pre-discriminator shape and will be updated GREEN in plan 03-02)
    - 03-VALIDATION.md §"Wave 0 Requirements" and §"Per-Task Verification Map" (exact behaviors each test must assert)
    - 03-RESEARCH.md §Q3 (CitationSchema discriminator), §Q7 (matchesFilter / isFilterActive semantics), §Q2 (nodeContentQueryOptions)
  </read_first>
  <behavior>
    - filter-utils.test.ts: `matchesFilter` returns true when no query/filters active; matches title substring; matches tag substring; AND across facets (race AND skillType both must match); OR within a facet (any value in a facet array matches); mastery facet reads the supplied mastery arg. `isFilterActive` is false for empty query + all-empty facets, true otherwise.
    - node-content-query.test.ts: `nodeContentQueryOptions(null).enabled === false`; `nodeContentQueryOptions("map-control").queryKey` equals `["node-content","map-control"]`; `enabled === true` for a non-null id; `queryFn` returns the node for a known id and throws for an unknown id.
    - node.test.ts (added cases): `CitationSchema` accepts `kind: "science"` with source/applicationNote; accepts `kind: "creator"` with optional `quote`; rejects an unknown/missing `kind`; `quote` is optional only on the creator branch.
  </behavior>
  <action>
    Create `src/lib/filter-utils.test.ts` and `src/lib/node-content-query.test.ts` as new files, and APPEND new `describe` blocks to `src/schemas/node.test.ts`. Import from the real module paths using the `#/` alias: `#/lib/filter-utils`, `#/lib/node-content-query`, `#/schemas/node`. Write the assertions per the `<behavior>` block above. These are EXPECTED RED this wave — the modules/schema do not exist/changed yet; plans 03-02/03-04/03-05 turn them green. SPDX header on the two new files. Use shared fixtures shaped like `GraphDisplayNode` including `skillType` and `tags` (the post-ADR-006 shape). Do NOT add stub implementations of the production modules here — scaffolds only.
  </action>
  <verify>
    <automated>test -f src/lib/filter-utils.test.ts && test -f src/lib/node-content-query.test.ts && grep -q 'kind' src/schemas/node.test.ts && echo "scaffolds present"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/filter-utils.test.ts` exists, imports `matchesFilter`/`isFilterActive` from `#/lib/filter-utils`, asserts AND-across / OR-within facet semantics and title+tags search
    - `src/lib/node-content-query.test.ts` exists, imports `nodeContentQueryOptions` from `#/lib/node-content-query`, asserts enabled/disabled + queryKey + found/not-found
    - `src/schemas/node.test.ts` has new describe block(s) asserting the `kind` discriminator (science/creator, optional `quote`, invalid kind rejected)
    - Both new files carry the SPDX header
    - `npx vitest run src/lib/filter-utils.test.ts src/lib/node-content-query.test.ts` fails as RED (modules not yet implemented) — this is the expected Wave 0 state
  </acceptance_criteria>
  <done>Three test scaffolds exist and are RED, ready for feature waves to satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New package installs (`@tanstack/react-query`, `jsdom`) enter the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-SC | Tampering | npm installs (`@tanstack/react-query`, `jsdom`) | high | accept | RESEARCH §"Package Legitimacy Audit" pre-cleared both: react-query 58M wk downloads / official TanStack monorepo (SUS verdict = false positive on a 2-day-old patch), jsdom is the de-facto Vitest DOM env. No [SLOP]/[ASSUMED] package — no human checkpoint required. |
</threat_model>

<verification>
- `npx vitest run` collects all suites without an environment error
- `npx tsc --noEmit` shows no error in `__root.tsx`
- The three test scaffolds exist and are RED (expected this wave)
</verification>

<success_criteria>
react-query + jsdom installed; QueryClient mounted app-wide; the three Nyquist test scaffolds exist (RED) so later waves have automated green gates.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-01-SUMMARY.md` when done
</output>
