---
phase: 03-content-pipeline-node-panel
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/schemas/node.ts
  - content-collections.ts
  - src/schemas/node.test.ts
  - content/nodes/alpha-strike.mdx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03]
user_setup: []

must_haves:
  truths:
    - "A node MDX file whose citation is missing applicationNote, or whose body is missing the How to Apply section, fails the build (success criterion 3)"
    - "Citations carry a kind discriminator so science refs and creator wisdom can render distinctly (success criteria 1 and 4)"
    - "The compiled content exposes the How to Apply section separately from the body so the panel can pin it on top (success criterion 1, D-12)"
  artifacts:
    - "src/schemas/node.ts with CitationSchema as a z.discriminatedUnion('kind', [...]) and exported Citation/ScienceCitation/CreatorCitation types"
    - "content-collections.ts mirroring the discriminated union AND splitting the body / How-to-Apply into mdx + mdxHowToApply transform outputs"
    - "all 13 content/nodes/*.mdx citation blocks carrying kind: science|creator"
  key_links:
    - "CitationSchema must change in src/schemas/node.ts AND content-collections.ts in the same commit (parallel-schema sync)"
    - "The '## How to Apply' CI check must fire BEFORE the split so enforcement is preserved"
---

<objective>
Finalize the citation template and content pipeline that Phase 1 reserved: turn `CitationSchema` into a `kind`-discriminated union (D-07) in BOTH `src/schemas/node.ts` and `content-collections.ts` in one commit (parallel-schema sync), split the `## How to Apply` section out of the compiled body into a separate `mdxHowToApply` transform output (D-13), and migrate all 13 seed MDX citation blocks.

Purpose: This is the content/graph decoupling boundary's first content consumer surface — it makes science vs. creator citations renderable distinctly (CONT-01/CONT-03), keeps the How-to-Apply CI enforcement intact (CONT-02), and lets the panel pin How-to-Apply on top (D-12).
Output: discriminated `CitationSchema` + exported types, dual-`compileMDX` transform (`mdx` + `mdxHowToApply`), 13 migrated MDX files, green node schema tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@docs/adr/002-content-graph-decoupling.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CitationSchema discriminated union — node.ts + content-collections.ts (parallel-schema sync, one commit)</name>
  <files>src/schemas/node.ts, content-collections.ts, src/schemas/node.test.ts</files>
  <read_first>
    - src/schemas/node.ts (existing `CitationSchema` z.object — lines 82-95, and the PARALLEL-SCHEMA SYNC NOTE)
    - content-collections.ts (existing `citations:` array schema — lines 54-63, and the PARALLEL-SCHEMA SYNC NOTE at line 8)
    - src/schemas/node.test.ts (existing citation fixtures at line 38 and rejection tests at lines 304-335 — these use the pre-discriminator shape and must be updated; plus the RED `kind` cases added in plan 03-01)
    - 03-PATTERNS.md §"src/schemas/node.ts (modify — CitationSchema)" and §"content-collections.ts (modify — transform section)" (exact replacement shapes; Zod v4 `{ error: ... }` idiom)
    - 03-RESEARCH.md §Q3 (full discriminated-union shape; `quote` optional only on creator)
  </read_first>
  <behavior>
    - `CitationSchema.parse({ kind: "science", source, applicationNote })` succeeds.
    - `CitationSchema.parse({ kind: "creator", source, applicationNote, quote })` succeeds; `quote` may be omitted.
    - A citation object without `kind`, or with a `kind` not in {science, creator}, is rejected.
    - A citation missing/empty `applicationNote` is still rejected on both branches (D-03 enforcement preserved).
  </behavior>
  <action>
    In `src/schemas/node.ts`, replace the single `CitationSchema` z.object with two private branch schemas `ScienceCitationSchema` (kind literal "science"; source min(1); url optional; applicationNote min(1) with the existing D-03 error message) and `CreatorCitationSchema` (same plus optional `quote`), combined via `z.discriminatedUnion("kind", [ScienceCitationSchema, CreatorCitationSchema])`. Export `CitationSchema`, and `type Citation`, `type ScienceCitation`, `type CreatorCitation` via `z.infer`. Use the Zod v4 `{ error: "..." }` idiom (not `message`). Keep the SPDX header and the PARALLEL-SCHEMA SYNC NOTE.
    In `content-collections.ts`, mirror the exact same discriminated union inside the `citations: z.array(...)` field (per D-07; this is the parallel-schema sync — both files change in THIS task/commit). Keep the existing surrounding structure and the PARALLEL-SCHEMA SYNC NOTE.
    Update the existing pre-discriminator citation fixtures/tests in `src/schemas/node.test.ts` so they carry `kind` and pass (turning the plan 03-01 RED cases GREEN). Do not remove the applicationNote-rejection tests — they must still pass on both branches.
  </action>
  <verify>
    <automated>npx vitest run src/schemas/node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/schemas/node.ts` exports `CitationSchema` as a `z.discriminatedUnion("kind", ...)` plus `Citation`, `ScienceCitation`, `CreatorCitation` types
    - `content-collections.ts` `citations` array uses the field-for-field identical discriminated union (parallel-schema sync verified)
    - `npx vitest run src/schemas/node.test.ts` is GREEN, including the `kind` discriminator cases from plan 03-01 and the preserved applicationNote-rejection cases
    - `npx tsc --noEmit` reports no error in `node.ts` or `content-collections.ts`
  </acceptance_criteria>
  <done>CitationSchema is a discriminated union in both files; node schema tests green.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 transform-time split — dual compileMDX (mdx + mdxHowToApply) with CI enforcement preserved</name>
  <files>content-collections.ts</files>
  <read_first>
    - content-collections.ts (existing `transform` — lines 64-76: the `## How to Apply` CI check + single `compileMDX`)
    - 03-PATTERNS.md §"content-collections.ts (modify — transform section)" (exact D-13 extension: CI check first, split, body-empty guard, dual compileMDX)
    - 03-RESEARCH.md §Q1 (transform-time split rationale; partial-document compileMDX arg is [ASSUMED] A1 — validate it here) and §"Common Pitfalls" Pitfall 2 (empty body guard)
  </read_first>
  <action>
    Extend the existing `transform` (do NOT rewrite the file). Keep the existing `if (!document.content.includes("## How to Apply")) throw ...` check FIRST so CONT-02 CI enforcement fires before any split (success criterion 3). Then: match the heading with a `/^## How to Apply\s*/m` regex, compute `splitIdx = document.content.search(...)`, set `bodyRaw = content.slice(0, splitIdx).trim()` and `howToApplyRaw = content.slice(splitIdx).trim()` (keep the heading inside the howToApply chunk). Add a guard: if `bodyRaw.length === 0`, throw a clear "content body is empty before ## How to Apply" error (Pitfall 2). Call `compileMDX(context, { ...document, content: bodyRaw })` and `compileMDX(context, { ...document, content: howToApplyRaw })`, returning `{ ...document, mdx, mdxHowToApply }`. `mdx`/`mdxHowToApply` are transform-output fields (NOT frontmatter) — no `NodeFrontmatterSchema` change and no parallel-schema sync needed for them.
    Validate assumption A1 (the partial `{ ...document, content: substring }` arg is accepted by `compileMDX`) by running the content build (Task 3 verify covers all 13 nodes); if it fails, fall back to compiling on a cloned document object with only `content` replaced.
  </action>
  <verify>
    <automated>npm run build:content 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `## How to Apply` presence check still runs BEFORE the split (CI enforcement preserved)
    - `transform` returns both `mdx` and `mdxHowToApply` compiled strings
    - A body-empty guard throws when there is no prose before `## How to Apply`
    - `npm run build:content` regenerates the `allNodes` module with `mdxHowToApply` present on each item, with no compile error
  </acceptance_criteria>
  <done>Transform splits and compiles body + How-to-Apply separately; content build green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate all 13 seed MDX citation blocks to carry kind</name>
  <files>content/nodes/alpha-strike.mdx</files>
  <read_first>
    - one existing seed file (e.g. content/nodes/map-control.mdx) to see the current citation YAML shape
    - 03-RESEARCH.md §Q3 "Seed MDX migration — 13 files" (classification rule: paper/study → kind: science; named WC3 player/channel → kind: creator; optional `quote` on creator)
  </read_first>
  <action>
    For EVERY file in `content/nodes/*.mdx` (all 13 — listed individually in the executor's run; `alpha-strike.mdx` is the representative entry in `files_modified`), add a `kind:` field to each entry in the `citations:` frontmatter array: `kind: science` for peer-reviewed/academic sources, `kind: creator` for named WC3 players/guides/content-creators. Where a creator citation already contains or warrants a direct pull-quote, add an optional `quote:` field (D-05). Do not alter `source`, `url`, or `applicationNote` values. This is required because the discriminated union (Task 1) now rejects citations without `kind` at build time.
  </action>
  <verify>
    <automated>npm run build:content && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - Every citation block in all 13 `content/nodes/*.mdx` files has a `kind: science` or `kind: creator` field
    - At least the creator citations that include a quotable line carry a `quote:` field
    - `npm run build:content` succeeds (all citations satisfy the discriminated union)
    - `npm run validate` passes (How-to-Apply + referential integrity + patch checks remain green)
  </acceptance_criteria>
  <done>All 13 seed nodes validate against the new citation schema and rebuild cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX authors → build pipeline | Repo-controlled MDX content compiled at build time (trusted authors) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-02 | Tampering | `compileMDX` over node MDX | low | accept | MDX is repo-controlled (trusted authors), never user-supplied; `compileMDX` executes only build-time content. No user-generated MDX is ever passed to the compiler (documented constraint). |
| T-3-03 | Denial of Service | empty-body split edge case | low | mitigate | Body-empty guard throws a clear build error rather than emitting an empty/confusing panel (Pitfall 2). |
</threat_model>

<verification>
- `npx vitest run src/schemas/node.test.ts` green
- `npm run build:content && npm run validate` green across all 13 nodes
- `npx tsc --noEmit` clean for node.ts + content-collections.ts
</verification>

<success_criteria>
CitationSchema is a kind-discriminated union mirrored in both schema files; the transform exposes `mdx` + `mdxHowToApply`; all 13 seed nodes validate and rebuild.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-02-SUMMARY.md` when done
</output>
