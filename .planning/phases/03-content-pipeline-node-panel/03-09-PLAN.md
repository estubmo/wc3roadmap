---
phase: 03-content-pipeline-node-panel
plan: 09
type: execute
wave: 6
depends_on: ["03-04", "03-07", "03-08"]
files_modified:
  - src/components/graph/RoadmapGraph.tsx
  - src/components/graph/MobileNodeList.tsx
  - src/routes/index.tsx
autonomous: false
requirements: [GRAPH-03, GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "Clicking a node on the desktop graph (or tapping a mobile card) opens the detail panel (success criterion 1)"
    - "Typing in search or toggling a facet dims non-matching nodes in real time, mounted but de-emphasized, with no reflow or page reload (success criterion 5, D-08)"
    - "The filter dim composes with — does not replace — the Phase 2 pathway spotlight dim"
    - "The FilterBar lives in the top bar and the NodeDetailPanel is mounted once at the Home level inside ClientOnly"
  artifacts:
    - "src/components/graph/RoadmapGraph.tsx — onNodeClick wired + filteredDisplayNodes dim pass"
    - "src/components/graph/MobileNodeList.tsx — card tap wired to setSelectedNode"
    - "src/routes/index.tsx — FilterBar in top bar + NodeDetailPanel in ClientOnly"
  key_links:
    - "onNodeClick / onCardTap call useGraphStore.getState().setSelectedNode(node.id)"
    - "filteredDisplayNodes must be a useMemo feeding the existing setNodes effect (Pitfall 6 — no infinite loop)"
    - "The new filter dim is a second pass layered on the existing Phase 2 displayNodes dim, not a replacement"
---

<objective>
Wire the Phase 2 stubbed handlers and mount the new surfaces (GRAPH-03/GRAPH-04): replace `RoadmapGraph`'s no-op `onNodeClick` with `setSelectedNode`, add the filter-dim `useMemo` pass that composes with the existing pathway spotlight (D-08), wire `MobileNodeList` card taps, and mount `FilterBar` in the top bar + `NodeDetailPanel` in a single Home-level `<ClientOnly>`. Close with a human-verify checkpoint covering the manual-only interaction/visual criteria from 03-VALIDATION.md.

Purpose: This is the integration wave that turns the built pieces into the working click→panel and search/filter→dim experience.
Output: wired graph/list handlers, filter dim, mounted FilterBar + panel, verified end-to-end.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@.planning/phases/03-content-pipeline-node-panel/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RoadmapGraph onNodeClick + filter-dim pass (compose with pathway spotlight)</name>
  <files>src/components/graph/RoadmapGraph.tsx</files>
  <read_first>
    - src/components/graph/RoadmapGraph.tsx (the `handleNodeClick` stub lines 256-261; the existing `displayNodes` useMemo lines 147-164; the `setNodes` effect lines 194-196; hover handlers using getState() lines 240/247)
    - src/lib/filter-utils.ts (matchesFilter + isFilterActive from plan 03-04) and src/lib/mock-mastery.ts (getMockMastery)
    - 03-PATTERNS.md §"src/components/graph/RoadmapGraph.tsx (modify — wire onNodeClick)" (exact stub replacement; filter-dim useMemo; feed setNodes) and §"Shared Patterns → Spotlight/Dim Pattern" (compose, do NOT replace)
    - 03-RESEARCH.md §Q7 "Dim computation in GraphCanvas" + §"Common Pitfalls" Pitfall 3 (slice subscription) and Pitfall 6 (no infinite setNodes loop)
  </read_first>
  <action>
    Replace the `handleNodeClick` body with `useGraphStore.getState().setSelectedNode(node.id)` (keep the `useCallback([])` — getState is stable), wiring D-01/D-02. Add a slice subscription `useGraphStore(s => ({ searchQuery: s.searchQuery, activeFilters: s.activeFilters }))` (with `shallow`, Pitfall 3). Add a `filteredDisplayNodes` `useMemo` AFTER the existing `displayNodes` memo: when `isFilterActive` is false, return `displayNodes` unchanged; otherwise map and, for nodes failing `matchesFilter(rawNode, getMockMastery(id), searchQuery, activeFilters)`, layer an additional `style: { ...n.style, opacity: 0.15, pointerEvents: "none" }` ON TOP of the existing Phase 2 pathway-dim style (compose — do NOT replace the pathway spotlight; D-08). Feed `filteredDisplayNodes` (not `displayNodes`) into the existing `setNodes` effect and update its dependency array. Keep `filteredDisplayNodes` a memo so the effect does not loop (Pitfall 6).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `handleNodeClick` calls `setSelectedNode(node.id)` via `getState()`
    - A `filteredDisplayNodes` useMemo composes the filter dim on top of the existing pathway-dim style (existing Phase 2 dim preserved)
    - The `setNodes` effect consumes `filteredDisplayNodes`; no infinite render loop
    - Store read via shallow slice subscription
    - `npx vitest run && npx tsc --noEmit` green
  </acceptance_criteria>
  <done>Desktop node click opens the panel; filter dims non-matching nodes composing with the pathway spotlight.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MobileNodeList card tap to setSelectedNode</name>
  <files>src/components/graph/MobileNodeList.tsx</files>
  <read_first>
    - src/components/graph/MobileNodeList.tsx (the no-op onClick/onKeyDown lines 89-95)
    - 03-PATTERNS.md §"src/components/graph/MobileNodeList.tsx (modify — wire onClick)" (exact replacement; import useGraphStore; use getState())
  </read_first>
  <action>
    Replace the Phase 2 no-op `onClick` and `onKeyDown` (Enter/Space) bodies with `useGraphStore.getState().setSelectedNode(node.id)` (D-03 — opens the mobile bottom sheet). Add `import { useGraphStore } from "#/lib/graph-store"`. Use `getState()` (not a hook subscription) so the list does not re-render on unrelated store changes. Keep the existing accessible markup intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "MobileNodeList" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Card `onClick` and Enter/Space `onKeyDown` both call `setSelectedNode(node.id)` via `getState()`
    - `useGraphStore` imported via `#/` alias
    - `npx tsc --noEmit` clean for MobileNodeList.tsx
  </acceptance_criteria>
  <done>Mobile card tap/keyboard activation opens the bottom-sheet panel.</done>
</task>

<task type="auto">
  <name>Task 3: Mount FilterBar in the top bar + NodeDetailPanel at Home level</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (the 56px top-bar div lines 129-152; the two responsive divs lines 158-167; the loader projection already updated in plan 03-03)
    - src/components/graph/FilterBar.tsx (plan 03-07) and NodeDetailPanel.tsx (plan 03-08)
    - 03-PATTERNS.md §"src/routes/index.tsx (modify ...)" (FilterBar inside the top bar; ClientOnly panel mount after the two responsive divs)
    - 03-RESEARCH.md §Q5 "Recommended architecture" (Home owns the panel layer; single ClientOnly above both desktop + mobile)
  </read_first>
  <action>
    Render `<FilterBar />` inside the existing 56px top-bar div as a flex sibling to the brand span (D-09). After the existing desktop (`hidden md:block`) and mobile (`block md:hidden`) containers, mount `<ClientOnly fallback={null}><NodeDetailPanel /></ClientOnly>` once at the Home level (RESEARCH §Q5 recommended architecture — one panel layer above both breakpoints). Import `ClientOnly` from `@tanstack/react-router` (as used in RoadmapGraph) and the two new components. Do NOT re-touch the loader projection (done in plan 03-03). Keep all existing layout/styles.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build:content</automated>
  </verify>
  <acceptance_criteria>
    - `<FilterBar />` renders inside the 56px top bar; `<NodeDetailPanel />` mounted once inside a Home-level `<ClientOnly>`
    - Loader projection from plan 03-03 is unchanged
    - `npx vitest run && npx tsc --noEmit && npm run build:content` all green
  </acceptance_criteria>
  <done>FilterBar and the panel are mounted; the click→panel and filter→dim flow is wired end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — full click→panel + search/filter experience</name>
  <what-built>The full Phase 3 experience: click/tap a node → lazy-loading detail panel (desktop right drawer with dimmed-but-interactive graph; mobile ~80%-height bottom sheet with swipe-dismiss); How-to-Apply pinned at top with theory below; numbered science references + a distinct named pro-wisdom callout, each showing its applicationNote; clickable prerequisite chips that live-swap the panel; top-bar search + race/skillType/difficulty/mastery facet filters that dim non-matching nodes in place.</what-built>
  <how-to-verify>
    Run the dev server (`npm run dev`) and open the app.
    1. Desktop width: click a node → drawer slides in from the right, graph dims but stays clickable; click a second node → panel content swaps without closing; press Esc, then click a node and click the backdrop, then use the close control — all three dismiss the panel.
    2. Open a node that has both citation kinds → confirm "How to apply in your next game" is the first/highlighted block, theory is below it, science refs appear as a numbered References list at the bottom, and creator wisdom is a separate named callout (not a footnote) — each citation shows its applicationNote.
    3. Click a prerequisite chip → the panel swaps to that prerequisite node.
    4. Type in the search box and toggle a skillType and a mastery facet → non-matching nodes dim in place (no layout shift, no page reload); clear filters restores them.
    5. Mobile width (narrow the viewport): tap a card → bottom sheet rises to ~80% height; swipe down → it dismisses.
  </how-to-verify>
  <action>Pause for human verification. Run `npm run dev`, then walk through the five how-to-verify steps above against the running app. Do not mark the phase complete until the verifier confirms; record any reported issues as follow-up gaps.</action>
  <verify>
    <human-check>Verifier confirms all five interaction/visual criteria pass (drawer/sheet, dismissal paths, How-to-Apply pinned, citation surfaces with applicationNote, prereq swap, filter dim with no reflow).</human-check>
  </verify>
  <done>Verifier types "approved"; any reported issues captured as follow-up.</done>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph/list click → store | Node ids set into store selection (trusted, in-app ids) |
| filter state → setNodes | Derived dim styles applied to mounted React Flow nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-09 | Denial of Service | filteredDisplayNodes → setNodes effect | medium | mitigate | `filteredDisplayNodes` is a `useMemo` with a stable reference when inputs are unchanged, feeding the existing effect — prevents the infinite re-render loop (Pitfall 6). Store read via shallow slice (Pitfall 3) avoids hover-driven canvas re-renders. |
| T-3-01 | Tampering / XSS | citation links surfaced via the mounted panel | high | mitigate | Inherited from plans 03-06/03-08 — http(s)-only allowlist on every citation-URL render path is exercised once the panel is mounted here. Verified in the human-verify checkpoint. |
</threat_model>

<verification>
- `npx vitest run && npx tsc --noEmit && npm run build:content` all green (per-wave full gate)
- Human-verify checkpoint covers all 03-VALIDATION.md manual-only criteria (drawer/sheet, How-to-Apply pinned, citation surfaces, prereq swap, filter dim)
</verification>

<success_criteria>
Node click/tap opens the lazy panel on both breakpoints; search + four-facet filters dim non-matching nodes in place composing with the pathway spotlight; the full experience passes human verification.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-09-SUMMARY.md` when done
</output>
