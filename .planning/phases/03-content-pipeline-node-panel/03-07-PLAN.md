---
phase: 03-content-pipeline-node-panel
plan: 07
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - src/components/graph/PrerequisiteChips.tsx
  - src/components/graph/FilterBar.tsx
autonomous: true
requirements: [GRAPH-03, GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "A user can filter by skill type and mastery and search by title/tags from the always-visible top bar (success criterion 5, D-09)"
    - "Prerequisite chips in the panel are clickable and swap the panel to that prerequisite node (D-14, the in-panel learning path)"
  artifacts:
    - "src/components/graph/PrerequisiteChips.tsx — clickable chips wired to setSelectedNode"
    - "src/components/graph/FilterBar.tsx — search input + facet toggles in the top bar"
  key_links:
    - "Both components write via useGraphStore.getState() in handlers; FilterBar reads via slice subscription (shallow) to avoid hover-driven re-renders"
    - "FilterBar facets: race, skillType, difficulty, mastery; search is title+tags"
---

<objective>
Build the two interactive controls: `PrerequisiteChips` (D-14 — clickable prereq chips that swap the panel to that node) and `FilterBar` (D-09/D-10 — top-bar search input + race/skillType/difficulty/mastery facet toggles, GRAPH-04).

Purpose: The chips make the panel a continuous learning-path inspector (pairs with the live-swap drawer D-02); the FilterBar is the always-visible discovery surface that drives the graph's dim-non-matching behavior.
Output: `PrerequisiteChips.tsx`, `FilterBar.tsx`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@docs/adr/0001-visual-design-direction.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrerequisiteChips — clickable prereq chips that swap the panel</name>
  <files>src/components/graph/PrerequisiteChips.tsx</files>
  <read_first>
    - src/components/ui/badge.tsx (analog: CVA variant component + cn from #/lib/utils)
    - src/components/graph/MobileNodeList.tsx (lines 86-113: accessible role="button" + tabIndex + onKeyDown pattern)
    - 03-PATTERNS.md §"src/components/graph/PrerequisiteChips.tsx" (CVA chipVariants; getState().setSelectedNode click wiring) and §"Shared Patterns → Zustand getState()"
    - 03-RESEARCH.md §"specifics" (D-14 in-panel learning-path affordance)
  </read_first>
  <action>
    Create `src/components/graph/PrerequisiteChips.tsx` (SPDX header). Props: the current node's `prerequisites: string[]` (and a way to resolve a prereq id to its title — accept a `nodeTitles: Record<string,string>` map or look up via the loader data passed by the panel; keep the interface narrow). Render each prerequisite as a CVA-styled chip (rounded-full, obsidian-700 bg, rune-gold hover border per ADR 0001). Each chip is keyboard-accessible (`role="button"`, `tabIndex={0}`, `onKeyDown` Enter/Space) and on activation calls `useGraphStore.getState().setSelectedNode(prereqId)` (D-14 — swaps the panel; pairs with D-02 live inspector). Use `getState()` in the handler (NOT a hook subscription) so chips do not re-render on unrelated store changes. Render nothing when there are no prerequisites. Use `cn` + CVA; CSS-variable tokens for brand colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "PrerequisiteChips" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Each prerequisite renders as a keyboard-accessible chip (role/tabIndex/onKeyDown)
    - Activating a chip calls `setSelectedNode(prereqId)` via `getState()` (not a hook subscription)
    - Renders nothing when prerequisites is empty
    - CVA + cn used; CSS-variable tokens for brand colors
    - `npx tsc --noEmit` clean for PrerequisiteChips.tsx
  </acceptance_criteria>
  <done>Prereq chips swap the panel to the clicked prerequisite node.</done>
</task>

<task type="auto">
  <name>Task 2: FilterBar — top-bar search input + facet toggles</name>
  <files>src/components/graph/FilterBar.tsx</files>
  <read_first>
    - src/routes/index.tsx (the 56px top-bar div — lines 129-152 — FilterBar renders inside it as a flex sibling to the brand span)
    - 03-PATTERNS.md §"src/components/graph/FilterBar.tsx" (top-bar structure; Zustand slice subscription with shallow) and §"Common Pitfalls" Pitfall 3 (slice subscription, avoid whole-store)
    - 03-RESEARCH.md §Q7 "FilterBar in top bar" (search input → setSearchQuery; facet buttons toggle activeFilters[facet]; clear button when isFilterActive)
  </read_first>
  <action>
    Create `src/components/graph/FilterBar.tsx` (SPDX header). Render a controlled search `<input>` (placeholder reflecting title+tags search, D-10) whose `onChange` calls `setSearchQuery`. Render facet toggle controls for the four facets — race, skillType, difficulty, mastery (D-10) — each toggling a value in `activeFilters[facet]` via `setFilter`. Show a "Clear filters" affordance when `isFilterActive(searchQuery, activeFilters)` is true, calling `clearFilters`. Read state via a SLICE subscription `useGraphStore(s => ({ searchQuery: s.searchQuery, activeFilters: s.activeFilters }))` using the `shallow` comparator from `zustand/shallow` (Pitfall 3 — never subscribe to the whole store). Style to sit inside the existing 56px obsidian top bar (CSS-variable tokens, rune-gold accent for active facets). Mastery facet values correspond to the mock-mastery states (untouched / in-progress / mastered).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "FilterBar" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Controlled search input dispatches `setSearchQuery`; facet toggles dispatch `setFilter` for race/skillType/difficulty/mastery
    - "Clear filters" appears only when `isFilterActive` is true and calls `clearFilters`
    - State read via a `shallow` slice subscription (not the whole store)
    - Fits the 56px top bar with CSS-variable tokens
    - `npx tsc --noEmit` clean for FilterBar.tsx
  </acceptance_criteria>
  <done>Top-bar search + four-facet filter controls dispatch to the store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (search/facets) → store | UI-only state; consumed by pure filter logic, never rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-05 | Tampering / XSS | FilterBar search input | low | mitigate | Search value flows only into store state and `.includes()` comparison (plan 03-04); never rendered as HTML (RESEARCH §Security Domain — safe by construction). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both components
- (manual) typing/toggling in the top bar updates store state; clear resets it
</verification>

<success_criteria>
Prereq chips swap the panel and the top-bar FilterBar drives search + four-facet filter state via slice-subscribed store dispatches.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-07-SUMMARY.md` when done
</output>
