---
phase: 03-content-pipeline-node-panel
plan: 06
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/graph/CitationList.tsx
  - src/components/graph/ProWisdomCallout.tsx
autonomous: true
requirements: [CONT-01, CONT-03]
user_setup: []

must_haves:
  truths:
    - "Inline scientific citations link to real sources via a numbered References list at the panel bottom, each showing its applicationNote (success criterion 1, D-04/D-06)"
    - "Wisdom attributed to recognized WC3 creators is named visibly in a distinct callout, not buried in a footnote (success criterion 4, D-05)"
    - "A citation URL is only rendered as a link when its protocol is http(s) — javascript:/data: URLs never become anchors (ASVS V5, Pitfall 5)"
  artifacts:
    - "src/components/graph/CitationList.tsx — numbered science refs + applicationNote rows"
    - "src/components/graph/ProWisdomCallout.tsx — creator quote card with prominent name"
  key_links:
    - "Science indices derive from citations.filter(kind === 'science') array position — no React state for numbering"
    - "Both components must guard citation.url with an http(s)-only allowlist before emitting an anchor"
---

<objective>
Build the two citation-rendering components that make the panel's trust surface (CONT-01/CONT-03/CONT-05): `CitationList` renders science citations as numbered references with their `applicationNote` (D-04/D-06), and `ProWisdomCallout` renders creator wisdom as a distinct, prominently-named quote card (D-05). Both enforce an http(s)-only URL protocol allowlist before rendering any link (ASVS V5, Pitfall 5).

Purpose: Citations are the core trust surface — science visibly linked and creator wisdom never buried. Each citation visibly earns its place via its `applicationNote` (the anti-decorative-science stance, CONT-05).
Output: `CitationList.tsx`, `ProWisdomCallout.tsx`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@docs/adr/0001-visual-design-direction.md
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: CitationList — numbered science references with applicationNote + URL allowlist</name>
  <files>src/components/graph/CitationList.tsx</files>
  <read_first>
    - src/components/graph/MasteryBadge.tsx (analog: small typed display component, enum-driven rendering, CSS-var tokens)
    - src/schemas/node.ts (the `Citation` / `ScienceCitation` types from plan 03-02)
    - 03-PATTERNS.md §"src/components/graph/CitationList.tsx" (filter-by-kind for indices, URL security pattern) and §"Shared Patterns → URL Security (Citation Links)" + §"CSS Variable Tokens"
    - 03-RESEARCH.md §Q4 (rendering flow: science → [n] numbered refs) + §"Common Pitfalls" Pitfall 5 (URL allowlist)
  </read_first>
  <action>
    Create `src/components/graph/CitationList.tsx` (SPDX header). Props: `citations: Citation[]`. Derive science refs via `citations.filter((c): c is ScienceCitation => c.kind === "science")` and number them by array position (no React state — D-04). Render a "References" section (numbered list) where each row shows the index `[n]`, the `source`, and the `applicationNote` beneath it (D-06 — every citation visibly earns its place). Define a local `isSafeUrl(url?)` returning true only when the url starts with `http://` or `https://`; render `source` as an `<a target="_blank" rel="noopener noreferrer">` ONLY when `isSafeUrl` is true, otherwise render `source` as plain text (Pitfall 5). Use `var(--color-*)`/`var(--font-*)` tokens only (mono font for the index per ADR 0001); no hardcoded hex, no Tailwind brand-color classes. Render nothing if there are zero science citations.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "CitationList" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Science citations render as a numbered References list; each row shows source + applicationNote
    - Numbering derives from filtered array position (no useState for indices)
    - A citation whose `url` does not start with `http://`/`https://` renders as text, never as an anchor (manual + code-review verifiable)
    - Only CSS-variable design tokens are used (no raw hex / brand Tailwind classes)
    - `npx tsc --noEmit` clean for CitationList.tsx
  </acceptance_criteria>
  <done>Numbered science references render with applicationNote and a safe-URL guard.</done>
</task>

<task type="auto">
  <name>Task 2: ProWisdomCallout — distinct named creator quote card + URL allowlist</name>
  <files>src/components/graph/ProWisdomCallout.tsx</files>
  <read_first>
    - src/components/graph/PathwayBanner.tsx (analog: styled callout card with prominent text, CSS-var tokens)
    - src/schemas/node.ts (the `CreatorCitation` type from plan 03-02 — includes optional `quote`)
    - 03-PATTERNS.md §"src/components/graph/ProWisdomCallout.tsx" (card-in-obsidian styling; name prominent; URL security pattern)
    - 03-RESEARCH.md §Q3/§Q4 (creator → callout card with quote + applicationNote + source name + URL) + §"specifics" ("pro wisdom gets its own visual voice")
  </read_first>
  <action>
    Create `src/components/graph/ProWisdomCallout.tsx` (SPDX header). Props: `citations: Citation[]` (or the pre-filtered creator list). Filter `kind === "creator"` and render each as a distinct quote-card callout (D-05): creator `source` name PROMINENT (`var(--font-display)`, weight 600), the optional `quote` in italic body text, and the `applicationNote` as smaller muted text (D-06). This is visually separate from the numbered science refs — not a footnote. Reuse the same `isSafeUrl` http(s)-only guard before linking the creator `url` (Pitfall 5). CSS-variable tokens only (obsidian card + rune-gold accent per ADR 0001). Render nothing if there are zero creator citations.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "ProWisdomCallout" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Creator citations render as a distinct callout card with the creator name prominent (display font, weight 600) — not a footnote
    - Optional `quote` and the `applicationNote` are both shown
    - Creator `url` renders as a link only when http(s); otherwise text (Pitfall 5)
    - Only CSS-variable design tokens used
    - `npx tsc --noEmit` clean for ProWisdomCallout.tsx
  </acceptance_criteria>
  <done>Creator wisdom renders as a distinct, named, attributed callout with a safe-URL guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citation `url` field → rendered `<a href>` | Author-supplied URLs become clickable links in the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering / XSS | CitationList + ProWisdomCallout link rendering | high | mitigate | `isSafeUrl` protocol allowlist — render an anchor ONLY when the url starts with `http://`/`https://`; otherwise render source as plain text. Blocks `javascript:`/`data:` URL execution (ASVS V5, Pitfall 5). Links carry `rel="noopener noreferrer"`. |
| T-3-07 | Information Disclosure | citation `source` text | low | accept | `source` rendered as JSX text content (auto-escaped by React), never via `dangerouslySetInnerHTML`. Safe by construction. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for both components
- Code review confirms the http(s)-only allowlist on every citation-URL render path
- (manual) panel shows numbered science refs + distinct named creator callout, each with applicationNote
</verification>

<success_criteria>
Science citations render as numbered references and creator wisdom as a distinct named callout, each showing its applicationNote, with an http(s)-only URL allowlist enforced.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-06-SUMMARY.md` when done
</output>
