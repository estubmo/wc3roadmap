---
phase: 03-content-pipeline-node-panel
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/schemas/graph.ts
  - src/schemas/graph.test.ts
  - docs/adr/006-graph-display-node-skilltype-tags.md
  - src/routes/index.tsx
autonomous: true
requirements: [GRAPH-04]
user_setup: []

must_haves:
  truths:
    - "The graph layer receives skillType and tags so the user can filter by skill type and search by tag (success criterion 5)"
    - "The content/graph projection stays explicit and narrow — no full NodeFrontmatter leaks onto the graph layer (ADR-002/005 boundary preserved)"
  artifacts:
    - "src/schemas/graph.ts with GraphDisplayNodeSchema extended by skillType + tags"
    - "docs/adr/006-graph-display-node-skilltype-tags.md documenting the projection extension"
    - "src/routes/index.tsx loader projecting skillType + tags explicitly"
  key_links:
    - "Loader must project skillType and tags by explicit field assignment, never by spreading the full node (ADR-002)"
    - "Adding these two fields requires the new ADR-006 per the ADR-005 rule"
---

<objective>
Extend the `GraphDisplayNode` projection with `skillType` and `tags` (D-11) so the search/filter layer (GRAPH-04) has the fields it needs on the graph layer, and author ADR-006 to record the extension as the ADR-005 boundary rule requires. Update the route loader's explicit projection accordingly.

Purpose: GRAPH-04 filtering by skillType and search by tags require fields the graph layer does not currently receive; the projection must grow explicitly (not by leaking `NodeFrontmatter`), and the ADR-005 rule mandates a new ADR for any added field.
Output: extended schema + tests, ADR-006, updated loader projection.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-content-pipeline-node-panel/03-RESEARCH.md
@.planning/phases/03-content-pipeline-node-panel/03-PATTERNS.md
@docs/adr/005-graph-display-node.md
@docs/adr/002-content-graph-decoupling.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GraphDisplayNodeSchema with skillType + tags</name>
  <files>src/schemas/graph.ts, src/schemas/graph.test.ts</files>
  <read_first>
    - src/schemas/graph.ts (existing `GraphDisplayNodeSchema = NodeSummarySchema.extend({ difficulty })` — lines 31-38)
    - src/schemas/graph.test.ts (existing projection tests — extend with skillType/tags coverage)
    - src/schemas/node.ts (confirm `skillType` enum values macro|micro|mental and `tags: z.array(z.string())` already exist on NodeFrontmatter)
    - 03-PATTERNS.md §"src/schemas/graph.ts (modify — add skillType + tags)" (exact extension shape; reference ADR-006 in JSDoc)
    - 03-RESEARCH.md §Q6 (schema change + ADR-006 rationale)
  </read_first>
  <behavior>
    - `GraphDisplayNodeSchema.parse` accepts a node object with `skillType: "macro"|"micro"|"mental"` and `tags: string[]`.
    - It rejects a `skillType` outside the enum.
    - The projection still rejects unknown/extra content fields (boundary stays narrow).
  </behavior>
  <action>
    Extend `GraphDisplayNodeSchema` by adding `skillType: z.enum(["macro","micro","mental"])` and `tags: z.array(z.string())` after `difficulty` (per D-11). Update the schema's JSDoc to note these fields were added under ADR-006 for GRAPH-04 search/filter, and that any further field still requires a new ADR. Add `graph.test.ts` cases asserting the two new fields validate and that an invalid `skillType` is rejected.
  </action>
  <verify>
    <automated>npx vitest run src/schemas/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GraphDisplayNodeSchema` includes `skillType` (enum macro/micro/mental) and `tags` (string array)
    - JSDoc references ADR-006 and restates the "further fields need a new ADR" rule
    - `npx vitest run src/schemas/graph.test.ts` green including the new-field cases
    - `npx tsc --noEmit` clean for graph.ts
  </acceptance_criteria>
  <done>GraphDisplayNode carries skillType + tags; schema tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-006 documenting the projection extension</name>
  <files>docs/adr/006-graph-display-node-skilltype-tags.md</files>
  <read_first>
    - docs/adr/005-graph-display-node.md (the precedent ADR + its "further field requires new ADR" rule — match format exactly)
    - 03-RESEARCH.md §Q6 "ADR-006 summary to include" (status, fields, rationale, rule maintained)
  </read_first>
  <action>
    Create `docs/adr/006-graph-display-node-skilltype-tags.md` following the ADR-005 format. Status: Accepted. Record: fields added (`skillType: enum(macro/micro/mental)`, `tags: string[]`); rationale (GRAPH-04 needs skillType filtering + tag search; both are display-classification attributes equivalent in nature to `difficulty` from ADR-005, not content-body fields); decision that the projection remains explicit and narrow; and the maintained rule that any further `GraphDisplayNode` field requires a new ADR. Cite D-11 as the originating decision.
  </action>
  <verify>
    <automated>test -f docs/adr/006-graph-display-node-skilltype-tags.md && grep -qi "skillType" docs/adr/006-graph-display-node-skilltype-tags.md && grep -qi "Accepted" docs/adr/006-graph-display-node-skilltype-tags.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/006-graph-display-node-skilltype-tags.md` exists, Status: Accepted
    - Documents both added fields, the rationale tied to GRAPH-04/D-11, and the ADR-005 boundary rule restated
  </acceptance_criteria>
  <done>ADR-006 records the projection extension per the ADR-005 rule.</done>
</task>

<task type="auto">
  <name>Task 3: Project skillType + tags in the route loader</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (existing loader `GraphDisplayNodeSchema.safeParse({ ... })` explicit projection — lines 43-54)
    - 03-PATTERNS.md §"src/routes/index.tsx (modify — loader projection ...)" (add `skillType: n.skillType` and `tags: n.tags` after `difficulty`; NO spread of `n` — ADR-002 boundary)
    - 03-RESEARCH.md §Q6 "Loader update" (no seed migration needed — fields already on NodeFrontmatter)
  </read_first>
  <action>
    In the loader's `safeParse` projection object, add `skillType: n.skillType` and `tags: n.tags` immediately after `difficulty: n.difficulty`. Keep the projection explicit field-by-field — do NOT spread `n` (preserves the ADR-002 content/graph decoupling: only display-essential fields cross the boundary). No MDX migration is needed because `skillType` and `tags` already exist on every seed node's frontmatter (Phase 1). Touch only the loader projection in this task — FilterBar/panel mounting happens in plan 03-09.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | (grep -q "index.tsx" && exit 1; exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Loader projection includes explicit `skillType: n.skillType` and `tags: n.tags`
    - No `...n` spread is introduced into the projection (boundary preserved)
    - `npx tsc --noEmit` reports no error in `index.tsx`
    - `npm run validate` / loader still produces valid `GraphDisplayNode[]` (parse succeeds for all 13 nodes)
  </acceptance_criteria>
  <done>Loader projects the two new fields explicitly; types clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content schema → graph layer | The loader projects NodeFrontmatter down to the narrow GraphDisplayNode contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Information Disclosure | loader projection | low | mitigate | Explicit field-by-field projection (no `...n` spread) keeps content-body fields off the graph layer, preserving ADR-002 decoupling. Schema `safeParse` rejects extra fields. |
</threat_model>

<verification>
- `npx vitest run src/schemas/graph.test.ts` green
- ADR-006 present and Accepted
- `npx tsc --noEmit` clean for graph.ts + index.tsx
</verification>

<success_criteria>
GraphDisplayNode carries skillType + tags via an explicit projection, recorded in ADR-006, ready for the filter layer.
</success_criteria>

<output>
Create `.planning/phases/03-content-pipeline-node-panel/03-03-SUMMARY.md` when done
</output>
