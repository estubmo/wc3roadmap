---
phase: 07-w3champions-auto-detection
plan: 08
type: execute
wave: 4
depends_on: [07-03, 07-06, 07-07]
files_modified:
  - src/hooks/useSyncW3championsMutation.ts
  - src/components/profile/SyncW3championsButton.tsx
  - src/components/auth/UserDropdown.tsx
autonomous: true
requirements: [AUTO-01, AUTO-04, AUTO-05]
must_haves:
  truths:
    - "A signed-in user can trigger the sync from the profile surface (UserDropdown) and see 'Last synced Xm ago' (AUTO-01, criterion 1)"
    - "The button is always live (D-11) — pressing within TTL serves cached data silently; no disabled state, no countdown"
    - "On success the newly-advanced node ids are written to the graph store (setRecentlyAdvanced) to drive the D-07 pulse, and both progress + syncStatus queries are invalidated"
    - "Three tailored outcome messages: unreachable, rate-limited→showing recent data, no-data→play ranked (D-10); a 0-node success reads reassuring (D-08)"
    - "The sync UI is additive — no code path here disables/gates manual check-off or quizzes (AUTO-05, criterion 4)"
  artifacts:
    - "src/hooks/useSyncW3championsMutation.ts (mirrors useProgressMutation)"
    - "src/components/profile/SyncW3championsButton.tsx (always-live button + 'Last synced Xm ago')"
    - "UserDropdown.tsx mounting the sync item"
  key_links:
    - "syncW3champions + getW3championsSyncStatus (07-07) → mutation + status query"
    - "SYNC_TTL_MS (07-06) → staleTime mirror on the syncStatus query"
    - "setRecentlyAdvanced (07-03) ← mutation onSuccess (D-07 pulse trigger)"
    - "w3championsKeys.syncStatus() + progressKeys.byUser() → invalidated on settle (graph re-hydrates)"
---

<objective>
Wire the sync UX: a `useSyncW3championsMutation` hook (mirroring `useProgressMutation`) that calls the `syncW3champions` server fn, writes newly-advanced node ids to the graph store for the D-07 pulse, invalidates progress + syncStatus queries, and surfaces the D-08/D-10 outcome toasts; a `SyncW3championsButton` component ("Sync with w3champions" + "Last synced Xm ago") that is always live (D-11); and its mount inside the existing `UserDropdown` (whose comment reserves the sync item for Phase 7). Copy/styling are functional placeholders — exact wording/marker deferred to the UI-SPEC pass.

Purpose: Delivers criterion 1 (user-triggered sync + "Last synced Xm ago", no separate linking step — battleTag comes from the session), the D-11 always-live/silent-cache guard (AUTO-04 staleTime mirror), and the reassuring 0-node / three-bucket messaging (D-08/D-10) — all without ever gating manual/quiz tracking (AUTO-05).
Output: sync hook + button + UserDropdown mount.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-CONTEXT.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@src/hooks/useProgressMutation.ts
@src/components/auth/UserDropdown.tsx
@src/lib/progress-keys.ts
@src/lib/w3champions-keys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useSyncW3championsMutation hook</name>
  <files>src/hooks/useSyncW3championsMutation.ts</files>
  <read_first>
    - src/hooks/useProgressMutation.ts (analog — useMutation shape; onSettled invalidate; sonner toast.error with action:{label:"Retry"}, duration:Infinity)
    - src/lib/w3champions-keys.ts (w3championsKeys.syncStatus + SYNC_TTL_MS)
    - src/lib/progress-keys.ts (progressKeys.byUser)
    - src/lib/graph-store.ts (setRecentlyAdvanced — the D-07 pulse trigger to call on success)
    - src/server/w3champions.ts (syncW3champions server fn + its `{ status, advanced }` return shape)
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/hooks/useSyncW3championsMutation.ts — no signed-out branch; invalidate BOTH keys; three-bucket + reassurance toasts)
  </read_first>
  <action>
    Create `src/hooks/useSyncW3championsMutation.ts` (SPDX header). Implement `export function useSyncW3championsMutation()` mirroring `useProgressMutation`: `useMutation` whose `mutationFn` calls `syncW3champions()` (no client data — principal-keyed server-side; no signed-out branch, the button is only shown when authed). In `onSuccess`, branch on the returned `status`: `"ok"` with `advanced.length > 0` → call `setRecentlyAdvanced(advanced)` from the graph store (drives the D-07 pulse) and show a summary success toast ("N nodes advanced from your ladder data" — D-07, placeholder copy); `"ok"` with `advanced.length === 0` → a reassuring success toast, NOT an error (D-08, placeholder copy); `"rate-limited"` → a neutral "showing recent data" info toast (D-10b, not an error); `"unreachable"` → an error toast with a Retry action (D-10a); `"no-data"` → a reassuring "no ladder data found — play some ranked games" info toast (D-10c, reads as normal not broken). In `onSettled`, `queryClient.invalidateQueries` for BOTH `w3championsKeys.syncStatus()` (updates "Last synced Xm ago") and `progressKeys.byUser()` (graph re-hydrates with the new auto rows). Reuse the exact sonner `toast.*` + `action:{ label:"Retry", onClick }` shape from useProgressMutation for the D-10a/D-10b buckets only (D-08's 0-node case is a success, not a retry). Mark all copy as placeholder (JSDoc: final wording in the UI-SPEC pass). Do NOT add any logic that disables or short-circuits manual/quiz mutations (AUTO-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "invalidateQueries" src/hooks/useSyncW3championsMutation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useSyncW3championsMutation.ts` exports `useSyncW3championsMutation`
    - `mutationFn` calls `syncW3champions` with no client-supplied userId/data
    - `onSuccess` calls `setRecentlyAdvanced(advanced)` for the ok+advanced case and branches the D-08/D-10 outcomes
    - `onSettled` invalidates BOTH `w3championsKeys.syncStatus()` and `progressKeys.byUser()`
    - No code disables manual/quiz mutation paths
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sync mutation hook: triggers server fn, drives D-07 pulse, invalidates both caches, surfaces D-08/D-10 outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Create SyncW3championsButton (always-live + "Last synced Xm ago")</name>
  <files>src/components/profile/SyncW3championsButton.tsx</files>
  <read_first>
    - src/hooks/useSyncW3championsMutation.ts (the hook created in Task 1)
    - src/lib/w3champions-keys.ts (w3championsKeys.syncStatus + SYNC_TTL_MS for staleTime)
    - src/server/w3champions.ts (getW3championsSyncStatus — the read fn for the timestamp)
    - src/components/auth/UserDropdown.tsx (existing DropdownMenuItem styling conventions to match)
    - .planning/phases/07-w3champions-auto-detection/07-CONTEXT.md (D-11 always-live, no disabled state/countdown; D-07/D-08/D-10 UX; exact copy DEFERRED to UI-SPEC)
  </read_first>
  <action>
    Create `src/components/profile/SyncW3championsButton.tsx` (SPDX header). Implement `export function SyncW3championsButton()`: use `useSyncW3championsMutation()` for the trigger and a `useQuery({ queryKey: w3championsKeys.syncStatus(), queryFn: getW3championsSyncStatus, staleTime: SYNC_TTL_MS })` for the last-synced timestamp (staleTime mirrors the DB TTL — AUTO-04/criterion 3, prevents redundant same-tab refetch). Render a "Sync with w3champions" action (placeholder copy) that is ALWAYS clickable — D-11: no disabled state, no countdown; a mid-flight `isPending` may show a spinner but the button is never gated by TTL. Render "Last synced Xm ago" derived from the query's `lastSyncedAt` (relative time; placeholder formatting). Keep styling minimal/placeholder — final visual polish is the UI-SPEC pass. The component assumes it is only rendered in the authed surface (no signed-out branch needed).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "staleTime" src/components/profile/SyncW3championsButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/profile/SyncW3championsButton.tsx` exports `SyncW3championsButton`
    - Uses `useSyncW3championsMutation` for the trigger and `useQuery` with `staleTime: SYNC_TTL_MS` for the last-synced status
    - The button has NO TTL-based disabled state or countdown (D-11 always-live)
    - Renders a "Last synced Xm ago" indicator from the status query
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Always-live sync button with a TTL-mirroring last-synced indicator.</done>
</task>

<task type="auto">
  <name>Task 3: Mount SyncW3championsButton in UserDropdown</name>
  <files>src/components/auth/UserDropdown.tsx</files>
  <read_first>
    - src/components/auth/UserDropdown.tsx (the file being edited — the DropdownMenuContent body where the "Sign out" item lives; the header comment noting the w3champions sync item is deferred to Phase 7)
    - src/components/profile/SyncW3championsButton.tsx (the component to mount)
  </read_first>
  <action>
    In `src/components/auth/UserDropdown.tsx`: add the `SyncW3championsButton` into the `DropdownMenuContent` (above the existing "Sign out" item) so a signed-in user can trigger the sync + see "Last synced Xm ago" from their profile surface (criterion 1). Update the header comment that currently says the sync item is "intentionally absent — deferred to Phase 7" to reflect that it is now present. Keep the existing avatar/BattleTag trigger and Sign-out item unchanged. The dropdown is only rendered when signed in, satisfying the authed-only assumption of the button. Do not introduce a separate linking step — the BattleTag is already in the session (AUTO-01/no separate linking).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "SyncW3championsButton" src/components/auth/UserDropdown.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `UserDropdown.tsx` imports and renders `SyncW3championsButton` inside `DropdownMenuContent`
    - The existing Sign-out item and avatar trigger are unchanged
    - The stale "deferred to Phase 7" comment is updated
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sync surface is reachable from the signed-in profile dropdown — no separate linking step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → sync server fn | button triggers the authed server fn; all authorization is server-side (07-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08a | Information Disclosure | error toasts leaking upstream detail | medium | mitigate | toasts render only the opaque bucket copy (D-10) from the server status — no raw upstream error text/status codes |
| T-07-08b | Denial of Service | always-live button spam | low | mitigate | D-11 button is live but the server-side SYNC_TTL_MS gate (07-07) + staleTime mirror bound actual upstream calls; no client-side loop |
| T-07-08c | Elevation of Privilege | sync UI blocking/altering manual paths | high | mitigate | hook is purely additive — invalidates caches, never disables setNodeMastery/recordQuizPass (AUTO-05) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- SyncW3championsButton mounted in UserDropdown (grep); staleTime: SYNC_TTL_MS on the status query
- Full suite green: `npm test`
- Manual (UI-SPEC-deferred, per VALIDATION.md Manual-Only): log in, open profile dropdown, click Sync, confirm "Last synced Xm ago" updates and advanced nodes pulse/label distinct
</verification>

<success_criteria>
A signed-in user triggers the sync from the profile dropdown, sees "Last synced Xm ago", gets reassuring/tailored outcome messages, and freshly-advanced nodes pulse + label distinct — the button is always live and never blocks manual/quiz tracking.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-08-SUMMARY.md` when done.
</output>
