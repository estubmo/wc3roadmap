---
phase: 07-w3champions-auto-detection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/graph-store.ts
  - src/components/graph/MasteryBadge.tsx
  - src/components/graph/GraphNode.tsx
autonomous: true
requirements: [AUTO-02]
must_haves:
  truths:
    - "An auto-advanced node is labeled distinct from manual and quiz — panel badge reads its auto source, canvas carries a distinct marker (D-09, criterion 2)"
    - "The graph store holds a transient set of recently auto-advanced node ids that drives a pulse highlight (D-07)"
    - "Auto labeling applies at the in-progress state (D-04 ceiling) — a new branch, since in-progress had no source-conditional text before"
  artifacts:
    - "src/lib/graph-store.ts with recentlyAdvancedNodeIds + setRecentlyAdvanced"
    - "MasteryBadge.tsx source === 'auto' branch on the in-progress pill"
    - "GraphNode.tsx distinct canvas marker + Motion pulse for recentlyAdvancedNodeIds"
  key_links:
    - "setRecentlyAdvanced ← useSyncW3championsMutation onSuccess (07-08) — the highlight trigger"
    - "sourceMap[nodeId] === 'auto' → MasteryBadge/GraphNode auto branch (reads progress rows stamped source:'auto' by 07-07)"
---

<objective>
Implement the distinct-source labeling mechanism and the transient graph-highlight slice so auto-advanced MECHANIC nodes read as visibly different from manual check-off and quiz mastery (criterion 2 / D-09), and newly-advanced nodes can pulse on the graph when the user returns (D-07). This is functional/mechanism work only — exact copy and marker styling are DEFERRED to the UI-SPEC pass (CONTEXT.md); build placeholders + the wiring, not final polish.

Purpose: Criterion 2 requires the auto change be "visible as distinct from manual check-off." This extends the existing Phase-6 `MasteryBadge` `source` + `GraphNode` `sourceMap` mechanism with an `"auto"` branch, and adds the store slice the sync mutation (07-08) writes to drive the D-07 pulse. Built independently of the server fn: it only reacts to `source === "auto"` progress rows and a store setter.
Output: graph-store highlight slice + auto-source render branches.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-CONTEXT.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@src/lib/graph-store.ts
@src/components/graph/MasteryBadge.tsx
@src/components/graph/GraphNode.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recentlyAdvancedNodeIds highlight slice to the graph store</name>
  <files>src/lib/graph-store.ts</files>
  <read_first>
    - src/lib/graph-store.ts (the file being edited — `sourceMap`/`setSource`/`setSourceMap` slice lines ~191-298 for the exact set-immutably pattern to mirror; `GraphStore` interface lines ~74+)
    - .planning/phases/07-w3champions-auto-detection/07-CONTEXT.md (D-07 transient highlight marker)
  </read_first>
  <action>
    In `src/lib/graph-store.ts` (SPDX header intact): extend the `GraphStore` interface with `recentlyAdvancedNodeIds: ReadonlySet<string>` (transient — the set of node ids advanced by the most recent sync, D-07) and `setRecentlyAdvanced: (nodeIds: string[]) => void`. Add to the store body `recentlyAdvancedNodeIds: new Set<string>()` and `setRecentlyAdvanced: (nodeIds) => set({ recentlyAdvancedNodeIds: new Set(nodeIds) })`, mirroring the immutable `set(...)` style used by the existing `setSourceMap`. Document with JSDoc that this is a transient UI-only marker (not persisted) consumed by GraphNode's pulse highlight and written by `useSyncW3championsMutation` onSuccess (07-08). Do NOT change existing `masteryMap`/`sourceMap` slices.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "recentlyAdvancedNodeIds" src/lib/graph-store.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/graph-store.ts` exports the extended store with `recentlyAdvancedNodeIds` and `setRecentlyAdvanced`
    - `setRecentlyAdvanced` replaces the set immutably via `set({ recentlyAdvancedNodeIds: new Set(...) })`
    - Existing `sourceMap`/`masteryMap` slices are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Graph store carries the transient recently-advanced marker for the D-07 pulse.</done>
</task>

<task type="auto">
  <name>Task 2: Add the source === "auto" branch to MasteryBadge</name>
  <files>src/components/graph/MasteryBadge.tsx</files>
  <read_first>
    - src/components/graph/MasteryBadge.tsx (the file being edited — in-progress pill lines ~33-52 which currently has NO source-conditional text; the quiz branch at line ~72 for the `source ===` conditional pattern to mirror)
    - .planning/phases/07-w3champions-auto-detection/07-CONTEXT.md (D-09 panel label "In progress · from w3champions"; exact copy DEFERRED to UI-SPEC)
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§Source-labeling UI — auto is a NEW in-progress branch, not a modification of quiz logic)
  </read_first>
  <action>
    In `src/components/graph/MasteryBadge.tsx`: in the `state === "in-progress"` render branch (which currently renders unconditional "In Progress"), add a conditional so that when `source === "auto"` the label reads a placeholder distinct-source text (e.g. `"In progress · from w3champions"` per D-09) and otherwise keeps the existing "In Progress" text. Keep the existing `mastered` + `source === "quiz"` branch untouched. Mark the copy as placeholder (JSDoc note: exact wording finalized in the UI-SPEC pass per CONTEXT.md). Do not add new colors/accents — reuse the existing in-progress pill styling (rune-600 bg / rune-300 text).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "auto" src/components/graph/MasteryBadge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MasteryBadge.tsx` renders a distinct label when `state === "in-progress"` and `source === "auto"`
    - The existing `source === "quiz"` mastered branch is unchanged
    - The in-progress pill styling is reused (no new accent color introduced)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Panel badge distinguishes auto-detected in-progress from manual in-progress and quiz mastery.</done>
</task>

<task type="auto">
  <name>Task 3: Add the distinct auto canvas marker + Motion pulse to GraphNode</name>
  <files>src/components/graph/GraphNode.tsx</files>
  <read_first>
    - src/components/graph/GraphNode.tsx (the file being edited — `masterySource` subscription line ~187; the quiz canvas marker slot lines ~237-254; the right-side marker slot rendering)
    - src/lib/graph-store.ts (the new `recentlyAdvancedNodeIds` slice to subscribe to)
    - .planning/phases/07-w3champions-auto-detection/07-CONTEXT.md (D-07 pulse; D-09 distinct canvas marker parallel to quiz ◆)
    - ./CLAUDE.md ("What NOT to Use" — import from `motion/react`, NOT framer-motion)
  </read_first>
  <action>
    In `src/components/graph/GraphNode.tsx`: (1) In the right-side marker slot, add a distinct auto-source canvas marker rendered when `masterySource === "auto" && masteryState === "in-progress"` (D-09), parallel to the existing quiz ◆ marker branch — use a visually distinct glyph placeholder (final glyph style DEFERRED to UI-SPEC; JSDoc note). (2) Subscribe to `recentlyAdvancedNodeIds` via `useGraphStore((s) => s.recentlyAdvancedNodeIds)` and, when this node's id is in that set, wrap the node in a brief Motion pulse/highlight (import from `motion/react` per CLAUDE.md — NOT framer-motion; a short one-shot scale/opacity or box-shadow pulse). Keep the pulse transient and non-blocking — it must not interfere with existing hover/selection behavior. Do not alter the existing quiz marker or mastery badge wiring.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "motion/react" src/components/graph/GraphNode.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GraphNode.tsx` renders a distinct canvas marker when `masterySource === "auto"` at in-progress state (parallel to quiz ◆)
    - `GraphNode.tsx` subscribes to `recentlyAdvancedNodeIds` and applies a Motion pulse for nodes in that set
    - Motion is imported from `motion/react` (grep confirms), NOT `framer-motion`
    - Existing quiz marker + MasteryBadge wiring unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Canvas visibly distinguishes auto-advanced nodes and pulses freshly-advanced ones on return (D-07/D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → render | transient highlight set + sourceMap drive purely client-side rendering; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03a | Tampering | client-only render state | low | accept | recentlyAdvancedNodeIds is transient UI state; no persistence, no auth surface |
| T-07-03b | Denial of Service | Motion pulse on large graphs | low | mitigate | one-shot transient pulse scoped to the small recently-advanced set (not all nodes); reuses existing memoized GraphNode |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- MasteryBadge auto branch + GraphNode auto marker + Motion pulse present (grep)
- Full suite still green: `npm test`
</verification>

<success_criteria>
Auto-advanced nodes are labeled and marked distinct from manual/quiz, and the graph pulses freshly-advanced nodes; final copy/glyph styling left as placeholders for the UI-SPEC pass.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-03-SUMMARY.md` when done.
</output>
