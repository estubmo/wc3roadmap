---
phase: 07-w3champions-auto-detection
plan: 07
type: tdd
wave: 3
depends_on: [07-02, 07-04, 07-05, 07-06]
files_modified:
  - src/server/w3champions.ts
  - src/server/w3champions.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05]
must_haves:
  truths:
    - "syncW3champions is principal-keyed via authMiddleware — battleTag/gateway/userId come from context.principal, NO userId client-input channel (ADR 007, AUTO-01, criterion 1)"
    - "source:'auto', masteryState:'in-progress', patchId:CURRENT_PATCH.id are ALL server-stamped (never from client or upstream API)"
    - "Auto write is a plain insert (NOT onConflictDoUpdate) — cannot overwrite/downgrade an existing row (D-04 ceiling, D-05 untouched-only, D-06 monotonic)"
    - "Two syncs within SYNC_TTL_MS make exactly ONE upstream fetch — the DB w3championsSync row is the authoritative TTL gate (AUTO-04, criterion 3)"
    - "A failed/unreachable/rate-limited/no-data sync returns a bucket status and advances nothing — manual/quiz paths untouched (AUTO-05, criterion 4)"
    - "getW3championsSyncStatus returns the principal's last-synced timestamp for the 'Last synced Xm ago' UI"
  artifacts:
    - "src/server/w3champions.ts exporting syncW3championsHandler + syncW3champions + getW3championsSyncStatusHandler + getW3championsSyncStatus"
    - "src/server/w3champions.test.ts covering authorization, TTL single-fetch, ceiling, monotonic (Wave-0 test)"
  key_links:
    - "context.principal (authMiddleware) → battleTag/gateway/id (ADR 007/008)"
    - "w3championsSync row + SYNC_TTL_MS → TTL gate (07-02, 07-06)"
    - "fetchW3championsSignals (07-06) → signals on TTL miss"
    - "detectMasterySignals (07-05) → candidate node ids from allNodes + existing progress"
    - "plain insert into nodeProgress source:'auto' (07-02 nodeProgress unchanged from Phase 5)"
---

<objective>
Build the sync server function (`src/server/w3champions.ts`) — the orchestrating deep module that wraps the pure detection function with authorization, the DB-TTL cache gate, the outbound fetch, and the additive auto-write. It mirrors `src/server/quiz.ts`'s authorization + server-stamping contract exactly, with the critical deviation that the auto-write is a plain `insert` (never `onConflictDoUpdate`) so it is a one-way additive ratchet (D-04/D-05/D-06). Also expose a principal-keyed `getW3championsSyncStatus` read for the "Last synced Xm ago" UI.

Purpose: This is where every AUTO requirement converges. It must be principal-keyed (ADR 007, AUTO-01), cap at in-progress (D-04), touch only untouched nodes (D-05), never downgrade (D-06), fetch at most once per TTL (AUTO-04/criterion 3), and never break manual/quiz on failure (AUTO-05). All four invariants are proven by the Wave-0 server test.
Output: sync + status server fns + Wave-0 server test.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-RESEARCH.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@src/server/quiz.ts
@src/server/progress.ts
@src/lib/auth-middleware.ts
@src/lib/node-content-query.ts
@docs/adr/007-authed-server-fn-authorization.md
@docs/adr/009-progress-persistence.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 server-fn test (RED) — authorization, TTL, ceiling, monotonic</name>
  <files>src/server/w3champions.test.ts</files>
  <read_first>
    - src/server/quiz.test.ts (the exact vi.doMock + vi.resetModules + dynamic import() convention to mirror; the cross-user authorization test shape)
    - src/server/progress.test.ts (principal-keyed query test patterns)
    - .planning/phases/07-w3champions-auto-detection/07-VALIDATION.md (rows D-04 "ceiling", D-06 "monotonic", AUTO-04 "TTL", ADR 007 "authorization")
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Pattern 4 server-fn sketch; §Validation Architecture)
  </read_first>
  <behavior>
    - "authorization": the handler reads userId/battleTag/gateway ONLY from context.principal — invoking with a principal never reads any userId from `data`; a second principal only ever touches its own rows (no cross-user write path exists)
    - "TTL": with a fresh w3championsSync row (lastSyncedAt within SYNC_TTL_MS), the handler does NOT call fetchW3championsSignals (mock asserts 0 upstream calls); with an expired/missing row it calls it exactly once — two back-to-back syncs => exactly ONE fetch (AUTO-04)
    - "ceiling": every nodeProgress insert issued by the handler uses masteryState "in-progress" — never "mastered" (D-04)
    - "monotonic": the auto-write path uses a plain insert with NO onConflictDoUpdate — a node already present in existing progress is filtered out by detectMasterySignals and never re-written/downgraded (D-06); assert no onConflictDoUpdate is invoked on the auto path
    - a non-ok fetch bucket (unreachable/rate-limited/no-data) returns `{ status, advanced: [] }` and issues ZERO nodeProgress inserts (AUTO-05)
    - a rate-limited result serves the cached row's signals (does not error)
  </behavior>
  <action>
    Create `src/server/w3champions.test.ts` (SPDX header). Follow the quiz.test.ts convention exactly: `vi.resetModules()` + `vi.doMock("#/lib/db", ...)`, `vi.doMock("#/lib/w3champions-client", ...)` (to count/stub fetch calls), `vi.doMock("content-collections", ...)` (to supply a small allNodes fixture with MECHANIC autoDetect nodes + a CONCEPTUAL node), then `const { syncW3championsHandler } = await import("#/server/w3champions")` per test. Call the exported handler directly with an injected `{ context: { principal: { id, battleTag, gateway } } }`. Name tests to match the VALIDATION command map: "authorization", "TTL", "ceiling", "monotonic". Assert the fetch-call count for the TTL cases, the masteryState literal on inserts for ceiling, the absence of onConflictDoUpdate for monotonic, and zero inserts on non-ok buckets. Run to confirm RED.
  </action>
  <verify>
    <automated>npx vitest run src/server/w3champions.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/w3champions.test.ts` exists with tests named "authorization", "TTL", "ceiling", "monotonic"
    - Uses vi.doMock + resetModules + dynamic import (mirrors quiz.test.ts), mocks db + w3champions-client + content-collections
    - Running the test fails (RED) — handler not yet present
  </acceptance_criteria>
  <done>Failing Wave-0 server test encodes the four invariants (principal-keyed, single-fetch TTL, in-progress ceiling, monotonic plain-insert).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement syncW3champions + getW3championsSyncStatus (GREEN)</name>
  <files>src/server/w3champions.ts</files>
  <read_first>
    - src/server/quiz.ts (recordQuizPassHandler — the authorization + server-stamped write template; the createServerFn(...).middleware([authMiddleware]).validator(...).handler(...) wiring; the compiler-visibility constraint)
    - src/server/progress.ts (getUserProgressHandler principal-keyed read; mergeProgressOnSignInHandler existingSet/.filter() pattern for untouched-only)
    - src/lib/auth-middleware.ts (AuthedContext; context.principal carries id/battleTag/gateway; createServerFn must be lexically direct — never wrapped in a factory)
    - src/lib/node-content-query.ts (how allNodes is imported from content-collections — reuse the same import to map {id, nodeType, autoDetect})
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/server/w3champions.ts — full pattern incl. the plain-insert deviation)
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Pattern 4)
    - src/lib/w3champions-keys.ts (SYNC_TTL_MS), src/lib/w3champions-client.ts (fetchW3championsSignals), src/lib/detect-mastery-signals.ts (detectMasterySignals)
  </read_first>
  <action>
    Create `src/server/w3champions.ts` (SPDX header). Export `async function syncW3championsHandler({ context }: AuthedContext)`: read `const { principal } = context` (id/battleTag/gateway — NEVER from data, ADR 007). Load the cache row `db.query.w3championsSync.findFirst({ where: eq(w3championsSync.userId, principal.id) })`; compute `withinTtl = cached && Date.now() - cached.lastSyncedAt.getTime() < SYNC_TTL_MS`. On TTL hit, use the cached signals (status "cached"/"ok") and DO NOT call fetch (AUTO-04/criterion 3). On TTL miss, call `fetchW3championsSignals(principal.battleTag, principal.gateway)`; on `status === "ok"` upsert the w3championsSync row (`onConflictDoUpdate` target `w3championsSync.userId`, set mmrTier/gamesPlayed/lastSyncedAt=now); on `rate-limited` fall back to the cached row's signals if present (D-10b, not an error); on `unreachable`/`no-data` return `{ status, advanced: [] }` with ZERO progress writes (AUTO-05/criterion 4). When signals are available: load `existing = db.query.nodeProgress.findMany({ where: eq(nodeProgress.userId, principal.id) })`, build `existingSet = new Set(existing.map(r => r.nodeId))`, import `allNodes` (same path as node-content-query.ts) and map to `{ id, nodeType, autoDetect }`, then `candidates = detectMasterySignals(mapped, signals, existingSet)`. For each candidate, issue a PLAIN `db.insert(nodeProgress).values({ id: crypto.randomUUID(), userId: principal.id, nodeId, masteryState: "in-progress" (D-04 ceiling — hardcoded), source: "auto" (server-stamped — hardcoded), patchId: CURRENT_PATCH.id })` — NO onConflictDoUpdate (D-05/D-06: untouched-only guarantees no conflict; auto is additive-only; optionally `.onConflictDoNothing()` for defensive concurrency, but NEVER DoUpdate). Return `{ status: "ok", advanced: candidates.map(c => c.nodeId) }`. Also export `async function getW3championsSyncStatusHandler({ context }: AuthedContext)` returning the principal's w3championsSync row (or null) — principal-keyed, for the "Last synced Xm ago" UI. Wire both: `export const syncW3champions = createServerFn({ method: "POST" }).middleware([authMiddleware]).handler(syncW3championsHandler)` and `export const getW3championsSyncStatus = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(getW3championsSyncStatusHandler)` — createServerFn calls lexically direct at module scope (compiler-visibility constraint; never wrap in a factory). The sync fn takes no meaningful client input (all keying is principal-derived); add a no-op/empty validator only if TanStack Start requires one for the method (follow the repo convention).
  </action>
  <verify>
    <automated>npx vitest run src/server/w3champions.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/server/w3champions.test.ts` exits 0 (GREEN) — authorization, TTL, ceiling, monotonic all pass
    - `w3champions.ts` exports `syncW3championsHandler`, `syncW3champions`, `getW3championsSyncStatusHandler`, `getW3championsSyncStatus`
    - The file contains `authMiddleware`; no code path reads userId/battleTag/gateway from `data` (principal-keyed only)
    - The auto write is a plain `insert` with `masteryState: "in-progress"` and `source: "auto"` hardcoded — NO `onConflictDoUpdate` on the nodeProgress auto path
    - The fetch is skipped when the cache row is within SYNC_TTL_MS (TTL test proves single fetch)
    - createServerFn calls are lexically direct at module scope (not wrapped in a factory)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Principal-keyed sync + status server fns green: single-fetch TTL gate, in-progress ceiling, additive plain-insert ratchet, failure-safe for manual/quiz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → sync server fn | authenticated request; principal derived server-side, no userId body channel |
| server fn → external w3champions API | untrusted upstream response (classified in 07-06) |
| server fn → database | principal-keyed cache + progress writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07a | Spoofing / Elevation of Privilege | cross-user write via forged userId | high | mitigate | principal-keyed via authMiddleware (ADR 007); userId/battleTag/gateway read only from context.principal — no client input channel; "authorization" unit test proves it |
| T-07-07b | Tampering | overwriting/downgrading manual or quiz progress | high | mitigate | plain insert (no onConflictDoUpdate) + detectMasterySignals untouched-only filter (D-05/D-06); "monotonic" unit test proves no DoUpdate on the auto path |
| T-07-07c | Denial of Service / cost amplification | sync-spam upstream calls | high | mitigate | DB w3championsSync TTL gate (SYNC_TTL_MS) skips the outbound fetch within the window regardless of click frequency (D-11); "TTL" unit test proves single fetch |
| T-07-07d | Information Disclosure | leaking raw upstream errors to client | medium | mitigate | handler returns only opaque bucket statuses from the 07-06 classifier — no upstream status codes/strings surfaced |
| T-07-07e | Tampering | server-stamped fields sourced from data/upstream | high | mitigate | masteryState/source/patchId/userId are hardcoded/principal-derived literals, never read from client or upstream JSON (ADR 009) |
| T-07-07-SC | Tampering | no new packages | low | accept | phase adds no npm dependencies (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/server/w3champions.test.ts` green (authorization / TTL / ceiling / monotonic)
- `npx tsc --noEmit` exits 0
- Full suite green: `npm test`
- Grep confirms no `onConflictDoUpdate` on the nodeProgress auto-write path in w3champions.ts
</verification>

<success_criteria>
Principal-keyed sync + status server functions with a single-fetch DB TTL gate, in-progress ceiling, additive plain-insert (one-way ratchet), and failure buckets that never disturb manual/quiz progress.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-07-SUMMARY.md` when done.
</output>
