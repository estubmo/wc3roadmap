---
phase: 07-w3champions-auto-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/schema.test.ts
autonomous: true
requirements: [AUTO-04]
user_setup:
  - service: neon
    why: "Schema push targets the live Neon database (new w3champions_sync cache table)"
    env_vars:
      - name: DATABASE_URL_DIRECT
        source: "Neon Console -> Connection Details -> Direct connection (non-pooled) string; stored in .env.local (already gitignored)"
must_haves:
  truths:
    - "A w3champions_sync table exists in Neon storing one cache row per user (mmrTier, gamesPlayed, lastSyncedAt)"
    - "The table enforces exactly one row per userId (unique index on userId alone — single-row cache, unlike node_progress)"
    - "Deleting a user cascades to their sync cache row (FK onDelete cascade)"
    - "lastSyncedAt is the durable TTL source of truth surviving across devices/tabs (AUTO-04 / criterion 3)"
  artifacts:
    - "src/db/schema.ts with w3championsSync table + w3championsSyncRelations + usersRelations extension"
    - "w3champions_sync table live in Neon (drizzle-kit push applied)"
  key_links:
    - "w3championsSync.userId → users.id FK (principal-keyed cache, ADR 007)"
    - "uniqueIndex(userId) → onConflictDoUpdate target in 07-07 syncW3championsHandler"
    - "w3championsSyncRelations → db.query.w3championsSync.findFirst resolves in 07-07 TTL gate"
---

<objective>
Add the `w3championsSync` Drizzle cache table (surrogate text PK, unique index on `userId` alone, FK cascade to `users.id`, nullable `mmrTier`, non-null `gamesPlayed`, `lastSyncedAt` timestamp), register its relations, and apply it to the live Neon database via the mandatory `[BLOCKING]` push. This is the durable TTL source of truth behind criterion 3 / AUTO-04.

Purpose: The DB row — not TanStack `staleTime` — is the authoritative rate-limit gate; it must survive across browser tabs and devices (RESEARCH: Alternatives Considered). Per schema_push_requirement, type/build checks pass without the live table (types come from schema.ts, not the live DB), so the push is mandatory to avoid a false-positive verification. This is a brand-new table only — `nodeProgress` already carries `source`+`patchId` from Phase 5 and MUST NOT be re-migrated.
Output: `w3championsSync` table in code + live in Neon.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-RESEARCH.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@.planning/phases/05-progress-tracking/05-03-PLAN.md
@src/db/schema.ts
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add w3championsSync table + relations to the Drizzle schema</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (the file being edited — `nodeProgress` table lines ~294-349 for the surrogate-PK + FK-cascade + index-array pattern; `text`-over-`pgEnum` comment lines ~279-282; `usersRelations` for the `many()` extension)
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/db/schema.ts — structural pattern; unique index on userId ALONE, single-row cache)
    - .planning/phases/05-progress-tracking/05-03-PLAN.md (the nodeProgress precedent to mirror, incl. the [BLOCKING] push task)
  </read_first>
  <action>
    In `src/db/schema.ts` (keep the SPDX header intact): add an exported `w3championsSync = pgTable("w3champions_sync", { ... }, (table) => [ ... ])` with columns: `id: text("id").primaryKey()` (surrogate); `userId: text("user_id").notNull().references(() => users.id, { onDelete: "cascade" })`; `mmrTier: text("mmr_tier")` (NULLABLE — null = unranked / no ladder data this season, D-10c); `gamesPlayed: integer("games_played").notNull().default(0)`; `lastSyncedAt: timestamp("last_synced_at").notNull()` (TTL source of truth, AUTO-04); plus `createdAt`/`updatedAt` timestamps following the nodeProgress convention. Index array: `uniqueIndex("w3c_sync_user_unique").on(table.userId)` — ONE row per user (this is a single-row cache, NOT per-nodeId like node_progress, so the unique key is `userId` alone). Import `integer` from `drizzle-orm/pg-core` if not already imported. Add `export const w3championsSyncRelations = relations(w3championsSync, ({ one }) => ({ user: one(users, { fields: [w3championsSync.userId], references: [users.id] }) }))` and extend `usersRelations` with `w3championsSync: many(w3championsSync)` (required so `db.query.w3championsSync.findFirst` resolves in 07-07). Add a table-level JSDoc noting: single-row-per-user cache, `lastSyncedAt` is the durable TTL gate (criterion 3 / AUTO-04), `mmrTier` nullable for the D-10c no-data case. Do NOT touch or re-migrate the existing `nodeProgress` table (source+patchId already present from Phase 5).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "w3championsSync = pgTable" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `export const w3championsSync = pgTable(`
    - `mmrTier` column is nullable (`text("mmr_tier")` with no `.notNull()`); `gamesPlayed` is `integer(...).notNull()`
    - A `uniqueIndex("w3c_sync_user_unique").on(table.userId)` is present (userId alone)
    - `w3championsSyncRelations` is exported and `usersRelations` includes `w3championsSync: many(w3championsSync)`
    - `userId` references `users.id` with `{ onDelete: "cascade" }`
    - `nodeProgress` table is unchanged (no re-migration)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>w3championsSync single-row cache table + relations defined with userId-unique TTL design.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the schema smoke test for w3championsSync</name>
  <files>src/db/schema.test.ts</files>
  <read_first>
    - src/db/schema.test.ts (existing smoke test structure — assert on Drizzle column objects, no live DB)
    - src/db/schema.ts (the new table + columns to assert against)
  </read_first>
  <action>
    In `src/db/schema.test.ts`: add assertions that `w3championsSync` is defined and exposes the expected column accessors (`w3championsSync.id`, `.userId`, `.mmrTier`, `.gamesPlayed`, `.lastSyncedAt`). Match the existing lightweight structural style (no live connection — assert on the Drizzle column objects' presence). Use the same `* as schema` import path the existing tests use.
  </action>
  <verify>
    <automated>npm test -- schema</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- schema` passes
    - The test references `w3championsSync` and at least the `mmrTier`, `gamesPlayed`, and `lastSyncedAt` columns
  </acceptance_criteria>
  <done>Smoke test confirms the w3championsSync table shape.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to Neon</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - drizzle.config.ts (uses `DATABASE_URL_DIRECT` non-pooled — required for push)
    - .planning/phases/05-progress-tracking/05-03-PLAN.md (Task 3 — the exact [BLOCKING] push precedent)
    - src/db/schema.ts (the schema being pushed)
  </read_first>
  <action>
    [BLOCKING] After Task 1 and Task 2 complete, run `npx drizzle-kit push` to apply the `w3champions_sync` table to the live Neon database. This reads `DATABASE_URL_DIRECT` (non-pooled direct connection per drizzle.config.ts), NOT the pooled `DATABASE_URL`. `w3champions_sync` is a brand-new table so the push introduces no destructive column changes and is non-interactive; if drizzle-kit unexpectedly prompts for an unresolvable conflict, STOP and surface it (would require `autonomous: false` re-planning). Do NOT proceed to phase verification until this push succeeds — type/build checks pass without the live table, which would otherwise create a false-positive verification state. This push is for the NEW table only; do NOT re-migrate `node_progress`.
  </action>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits 0
    - The `w3champions_sync` table exists in Neon after the push (a second run reports no pending diff)
    - `node_progress` was not altered by the push
  </acceptance_criteria>
  <done>w3champions_sync table is live in Neon; the userId-unique upsert target exists for 07-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server fn → database | sync cache rows written/read across the Drizzle+Neon boundary; FK + unique index enforce integrity |
| operator → live DB | drizzle-kit push mutates production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02a | Tampering | w3champions_sync rows | high | mitigate | uniqueIndex(userId) enforces one cache row per user; 07-07 upsert is principal-keyed and atomic (onConflictDoUpdate) |
| T-07-02b | Information Disclosure | cross-user cache rows | high | mitigate | FK to users.id + 07-07 principal-keyed queries (ADR 007) — a user can only read/write their own sync row; IDOR impossible by construction |
| T-07-02c | Denial of Service | unbounded cache rows | low | accept | one row per user capped by unique index |
| T-07-02-SC | Tampering | drizzle-kit push to live DB | medium | mitigate | new table only, no destructive diff; DATABASE_URL_DIRECT kept in gitignored .env.local |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test -- schema` green
- `npx drizzle-kit push` exits 0 and `w3champions_sync` exists in Neon
</verification>

<success_criteria>
w3champions_sync cache table (surrogate PK, unique userId, FK cascade, nullable mmrTier, gamesPlayed, lastSyncedAt) defined in code, smoke-tested, and live in Neon.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-02-SUMMARY.md` when done.
</output>
