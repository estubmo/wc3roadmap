---
phase: 07-w3champions-auto-detection
plan: 05
type: tdd
wave: 2
depends_on: [07-01]
files_modified:
  - src/lib/detect-mastery-signals.ts
  - src/lib/detect-mastery-signals.test.ts
autonomous: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "detectMasterySignals is a PURE function — zero I/O (no fetch, no DB, no auth)"
    - "Only nodeType === 'MECHANIC' nodes are ever emitted — CONCEPTUAL nodes never advance (AUTO-03, criterion 5)"
    - "Only nodes with NO existing progress row are emitted (D-05 untouched-only)"
    - "Only nodes whose single signal+threshold criterion is met are emitted (D-02)"
    - "mmrTier === null (unranked) never satisfies an mmrTier criterion (D-10c no-data)"
  artifacts:
    - "src/lib/detect-mastery-signals.ts exporting detectMasterySignals + AutoDetectableNode + W3cSignals"
    - "src/lib/detect-mastery-signals.test.ts covering AUTO-03 + D-05 + threshold correctness (Wave-0 test)"
  key_links:
    - "tierIndex (07-01) → ordinal mmrTier gte comparison"
    - "detectMasterySignals ← called inside syncW3championsHandler (07-07) after fetch + existing-progress load"
---

<objective>
Create the pure, zero-I/O `detectMasterySignals(nodes, signals, existingProgressNodeIds)` function that emits auto-advance candidate node ids. It is the structural guarantee for AUTO-03 (MECHANIC-only) and D-05 (untouched-only), and applies the single signal+threshold comparison (D-02) using the ordinal `tierIndex` from the tier registry. This is the canonical deep module of the phase: a 3-argument pure interface hiding the MECHANIC-filter + untouched-filter + threshold logic.

Purpose: Keeping detection pure and unit-testable is the criterion-5 requirement and the deep-module discipline mandate — the server fn (07-07) wraps it with fetch + cache + persistence, but the eligibility logic is provable in isolation.
Output: `detect-mastery-signals.ts` + its Wave-0 unit test.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-CONTEXT.md
@.planning/phases/07-w3champions-auto-detection/07-RESEARCH.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@src/lib/mmr-tiers.ts
@.agents/skills/codebase-design/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 detect-mastery-signals unit test (RED)</name>
  <files>src/lib/detect-mastery-signals.test.ts</files>
  <read_first>
    - src/server/quiz.test.ts (describe/it conventions; pure-fn tests need NO mocking)
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Pattern 3 reference impl; §Validation Architecture rows AUTO-03 / D-05)
    - .planning/phases/07-w3champions-auto-detection/07-VALIDATION.md (rows AUTO-03 "CONCEPTUAL", D-05 "untouched")
    - src/lib/mmr-tiers.ts (TIER_IDS / tierIndex for constructing test fixtures)
  </read_first>
  <behavior>
    - "CONCEPTUAL": a CONCEPTUAL node whose criterion is trivially met is NEVER emitted (AUTO-03 / criterion 5)
    - "untouched": a MECHANIC node whose id is in existingProgressNodeIds is NEVER emitted, even when its criterion is met (D-05)
    - A MECHANIC node with no autoDetect field is never emitted (graceful default)
    - gamesPlayed: emitted iff signals.gamesPlayed >= criterion.gte (boundary inclusive)
    - mmrTier: emitted iff tierIndex(signals.mmrTier) >= tierIndex(criterion.gte); a node requiring "gold" is emitted for a "gold"/"diamond" signal, not for "silver"
    - mmrTier with signals.mmrTier === null (unranked) is never emitted regardless of criterion (D-10c)
    - Output shape is `{ nodeId }[]` containing only qualifying MECHANIC untouched node ids
  </behavior>
  <action>
    Create `src/lib/detect-mastery-signals.test.ts` (SPDX header). Import `{ detectMasterySignals }` from `./detect-mastery-signals`. Build fixtures covering: a CONCEPTUAL node (must never emit), a touched MECHANIC node (in existingProgressNodeIds — must never emit), a MECHANIC node with no autoDetect (never emits), a MECHANIC gamesPlayed node at/above/below threshold, a MECHANIC mmrTier node at/above/below the ordinal `gte`, and an mmrTier node under a `null` (unranked) signal. Name the CONCEPTUAL test `-t "CONCEPTUAL"` and the untouched test `-t "untouched"` to match the VALIDATION.md command map. Run to confirm RED.
  </action>
  <verify>
    <automated>npx vitest run src/lib/detect-mastery-signals.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/detect-mastery-signals.test.ts` exists with tests named including "CONCEPTUAL" and "untouched"
    - SPDX GPL-3.0-or-later header present
    - Running the test fails (RED) — implementation not yet present
  </acceptance_criteria>
  <done>Failing Wave-0 test encodes the MECHANIC-only + untouched-only + threshold contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detect-mastery-signals.ts pure function (GREEN)</name>
  <files>src/lib/detect-mastery-signals.ts</files>
  <read_first>
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Pattern 3 — copy field names AutoDetectableNode / W3cSignals; the exact filter chain)
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/lib/detect-mastery-signals.ts — filter discipline mirrors mergeProgressOnSignInHandler's existingSet/.filter())
    - src/lib/mmr-tiers.ts (tierIndex for ordinal comparison)
    - src/schemas/node.ts (nodeType enum + the AutoDetect criteria shape the fn's types must align with)
  </read_first>
  <action>
    Create `src/lib/detect-mastery-signals.ts` (SPDX header). Define `interface AutoDetectableNode { id: string; nodeType: "MECHANIC" | "CONCEPTUAL"; autoDetect?: { signal: "mmrTier"; gte: string } | { signal: "gamesPlayed"; gte: number } }` and `interface W3cSignals { mmrTier: string | null; gamesPlayed: number }` (mmrTier null = unranked / no ladder data, D-10c). Implement `export function detectMasterySignals(nodes: AutoDetectableNode[], signals: W3cSignals, existingProgressNodeIds: ReadonlySet<string>): { nodeId: string }[]` as a pure filter chain: filter `nodeType === "MECHANIC"` (AUTO-03) → filter `autoDetect !== undefined` → filter `!existingProgressNodeIds.has(n.id)` (D-05 untouched-only) → filter `meetsThreshold(n.autoDetect, signals)` → map to `{ nodeId: n.id }`. Implement a private `meetsThreshold`: for `gamesPlayed`, `signals.gamesPlayed >= criteria.gte`; for `mmrTier`, return false when `signals.mmrTier === null`, else `tierIndex(signals.mmrTier) >= tierIndex(criteria.gte)`. NO I/O — no fetch, no db, no auth imports. Add a JSDoc stating the AUTO-03 structural guarantee and D-04/D-05/D-06 relationship (this fn only ever proposes untouched → advance; the caller caps state and never overwrites).
  </action>
  <verify>
    <automated>npx vitest run src/lib/detect-mastery-signals.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/detect-mastery-signals.test.ts` exits 0 (GREEN)
    - `detect-mastery-signals.ts` exports `detectMasterySignals`, `AutoDetectableNode`, `W3cSignals`
    - The module imports NOTHING from db, fetch, or auth layers (pure — only `./mmr-tiers`)
    - CONCEPTUAL nodes and touched nodes are provably never emitted (tests green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pure detection function green: MECHANIC-only, untouched-only, ordinal-threshold candidate emission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure function) | zero I/O; all inputs supplied by the caller (07-07) which owns fetch/auth/DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05a | Elevation of Privilege | CONCEPTUAL node advancing from a sync | high | mitigate | structural MECHANIC-only filter (AUTO-03) proven by the "CONCEPTUAL" unit test — CONCEPTUAL can never be emitted |
| T-07-05b | Tampering | overwriting a manual/quiz progress row | high | mitigate | untouched-only filter (D-05) proven by the "untouched" unit test; fn only proposes ids absent from existingProgressNodeIds |
</threat_model>

<verification>
- `npx vitest run src/lib/detect-mastery-signals.test.ts` green
- `npx tsc --noEmit` exits 0
- Module has no db/fetch/auth imports (pure)
</verification>

<success_criteria>
detectMasterySignals is a pure, unit-tested deep module emitting only MECHANIC, untouched, threshold-qualifying candidate node ids.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-05-SUMMARY.md` when done.
</output>
