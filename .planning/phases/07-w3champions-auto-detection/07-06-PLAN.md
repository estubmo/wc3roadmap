---
phase: 07-w3champions-auto-detection
plan: 06
type: tdd
wave: 2
depends_on: [07-01]
files_modified:
  - src/lib/w3champions-client.ts
  - src/lib/w3champions-client.test.ts
  - src/lib/w3champions-keys.ts
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-04]
must_haves:
  truths:
    - "The client fetches coarse signals (mmrTier via tierForMmr, gamesPlayed) for a BattleTag+gateway from the w3champions public API"
    - "BattleTag is always encodeURIComponent-ed before URL interpolation (the '#' discriminator, Pitfall 3)"
    - "gateway us|eu → Europe/America; kr → America ([ASSUMED], documented inline, Pitfall 4)"
    - "The status classifier maps 200-populated→ok, 200-empty/all-zero→no-data, 404→no-data, network-throw/timeout→unreachable, 429→rate-limited (D-10)"
    - "SYNC_TTL_MS is a single named constant (~15 min, RESEARCH A2) — the shared TTL for the DB gate and staleTime mirror"
  artifacts:
    - "src/lib/w3champions-client.ts exporting fetchW3championsSignals + classifyW3championsResponse + W3C_BASE_URL"
    - "src/lib/w3champions-keys.ts exporting w3championsKeys + SYNC_TTL_MS"
    - "src/lib/w3champions-client.test.ts covering the D-10 status→bucket classifier (Wave-0 test)"
  key_links:
    - "tierForMmr (07-01) → mmrTier derivation from the raw mmr integer"
    - "fetchW3championsSignals ← called by syncW3championsHandler on TTL miss (07-07)"
    - "SYNC_TTL_MS → DB TTL gate (07-07) AND staleTime mirror (07-08)"
---

<objective>
Build the w3champions HTTP client (`src/lib/w3champions-client.ts`): a native-`fetch` wrapper that resolves the current season, fetches career games volume + current-season MMR for a BattleTag, derives coarse `W3cSignals` (`mmrTier` via `tierForMmr`, `gamesPlayed` as the sum of `winLosses[].games`), and classifies every outcome into the D-10 buckets. Also create `src/lib/w3champions-keys.ts` holding the `w3championsKeys` query-key factory and the single named `SYNC_TTL_MS` constant. No new npm packages — native `fetch` only.

Purpose: This is the phase's only outbound integration and the source of the three-bucket error UX (D-10). Keeping the status classifier a pure, testable function (mock `fetch`) satisfies the Nyquist D-10 test and the deep-module discipline; the server fn (07-07) consumes a clean tagged result and never re-parses HTTP.
Output: fetch client + status classifier + query-keys/TTL module + Wave-0 classifier test.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-w3champions-auto-detection/07-RESEARCH.md
@.planning/phases/07-w3champions-auto-detection/07-PATTERNS.md
@src/lib/mmr-tiers.ts
@src/lib/progress-keys.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create w3champions-keys.ts (query-key factory + SYNC_TTL_MS)</name>
  <files>src/lib/w3champions-keys.ts</files>
  <read_first>
    - src/lib/progress-keys.ts (analog — copy the readonly-tuple factory shape verbatim, adapted)
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/lib/w3champions-keys.ts)
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Assumptions A2 — 15-minute TTL [ASSUMED])
  </read_first>
  <action>
    Create `src/lib/w3champions-keys.ts` (SPDX header). Export `const w3championsKeys = { all: () => ["w3champions"] as const, syncStatus: () => [...w3championsKeys.all(), "syncStatus"] as const } as const` mirroring `progressKeys`. Export `export const SYNC_TTL_MS = 15 * 60 * 1000` with a JSDoc citing RESEARCH A2: 15-minute conservative default, `[ASSUMED]` (no authoritative w3champions rate limit exists for player-stat endpoints — only replay endpoints are code-rate-limited); this single constant is the shared source for BOTH the DB TTL gate (07-07) and the TanStack `staleTime` mirror (07-08), so criterion 3 holds across tabs/devices. This module is client-safe (pure constants, no fetch, no db) — keep it separate from w3champions-client.ts so the hook can import SYNC_TTL_MS without bundling fetch code.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "SYNC_TTL_MS" src/lib/w3champions-keys.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/w3champions-keys.ts` exports `w3championsKeys` (with `all` + `syncStatus`) and `SYNC_TTL_MS`
    - `SYNC_TTL_MS === 15 * 60 * 1000` with a JSDoc citing RESEARCH A2 / [ASSUMED]
    - The module imports no fetch/db code (client-safe)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Query-key factory + single named TTL constant available to server fn and hook.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Wave-0 status-classifier test (RED)</name>
  <files>src/lib/w3champions-client.test.ts</files>
  <read_first>
    - src/server/quiz.test.ts (vi.doMock / resetModules conventions for mocking module-level deps; adapt to mock global fetch)
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Code Examples live request/response pairs — 200 populated, 200 [], 404, 429; §Common Pitfalls 1-3; §Validation row D-10)
    - .planning/phases/07-w3champions-auto-detection/07-VALIDATION.md (row D-10)
  </read_first>
  <behavior>
    - 200 with a populated game-mode-stats array + non-zero games → status "ok", signals.mmrTier derived via tierForMmr(mmr), signals.gamesPlayed = sum of winLosses[].games
    - 200 with an empty array / all-zero winLosses → status "no-data" (mmrTier null, gamesPlayed 0) — NOT an error (Pitfall 1, D-10c)
    - 404 (unknown/never-onboarded BattleTag) → status "no-data" (Pitfall 2, D-10c) — NOT "unreachable"
    - fetch throws / times out → status "unreachable" (D-10a)
    - 429 → status "rate-limited" (D-10b) — does not depend on a Retry-After header (none is sent)
    - the request URL uses encodeURIComponent on the BattleTag (assert the mocked fetch was called with the '#' percent-encoded)
    - gateway "kr" maps to the America gateway param (Pitfall 4 [ASSUMED])
  </behavior>
  <action>
    Create `src/lib/w3champions-client.test.ts` (SPDX header). Mock global `fetch` (via `vi.stubGlobal("fetch", ...)` or `vi.doMock`-style per the repo convention) to return the RESEARCH live-verified response bodies for each case. Assert `classifyW3championsResponse` (or the exported result of `fetchW3championsSignals`) yields the correct `{ status, signals? }` for each of the six behaviors. Assert the outbound URL contains the percent-encoded BattleTag (`%23`) and the mapped gateway. Run to confirm RED (module not yet present).
  </action>
  <verify>
    <automated>npx vitest run src/lib/w3champions-client.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/w3champions-client.test.ts` exists, mocks `fetch`, and asserts all five buckets (ok / no-data for both empty-200 and 404 / unreachable / rate-limited)
    - Asserts encodeURIComponent on the BattleTag and the kr→America gateway mapping
    - Running the test fails (RED) — implementation not yet present
  </acceptance_criteria>
  <done>Failing Wave-0 test encodes the D-10 status→bucket contract + URL-encoding + gateway mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement w3champions-client.ts (GREEN)</name>
  <files>src/lib/w3champions-client.ts</files>
  <read_first>
    - .planning/phases/07-w3champions-auto-detection/07-PATTERNS.md (§src/lib/w3champions-client.ts — request/response contract, tagged-result discriminant, lazy-singleton discipline from db.ts)
    - .planning/phases/07-w3champions-auto-detection/07-RESEARCH.md (§Code Examples — exact endpoints/params; §Pitfalls 3/4/5; §Anti-Patterns — do NOT fetch league-constellation; derive mmrTier from raw mmr via tierForMmr)
    - src/lib/mmr-tiers.ts (tierForMmr)
    - src/lib/db.ts (lazy-singleton / no top-level side-effects discipline)
  </read_first>
  <action>
    Create `src/lib/w3champions-client.ts` (SPDX header). Define `const W3C_BASE_URL = "https://website-backend.w3champions.com"` (hardcoded constant, never client-configurable — SSRF guard). Implement a private gateway mapper: `us|eu` → the w3champions `GateWay` string param (`America`/`Europe`), `kr` → `America` with an inline `[ASSUMED]` comment citing Pitfall 4 (WC3 Asian playerbase clusters on America; wrong-case degrades gracefully to no-data). Implement `export async function fetchW3championsSignals(battleTag: string, gateway: string): Promise<{ status: "ok" | "no-data" | "unreachable" | "rate-limited"; signals?: { mmrTier: string | null; gamesPlayed: number } }>`: resolve the current season from `GET /api/ladder/seasons` (first/descending element); fetch `GET /api/players/{enc}` (career games = sum of `winLosses[].games`) and `GET /api/players/{enc}/game-mode-stats?gateWay={mappedGateway}&season={season}` where `enc = encodeURIComponent(battleTag)`. Derive `mmrTier = tierForMmr(mmr)` from the highest-rankingPoints entry's raw `mmr` integer (NEVER leagueId/League names — RESEARCH Anti-Patterns/Pitfall 5); `mmrTier = null` when the array is empty. Export a `classifyW3championsResponse` helper (or make the status mapping a directly-testable pure function) implementing D-10: 200-populated→ok, 200-empty/all-zero→no-data, 404→no-data, thrown/timeout→unreachable, 429→rate-limited. Validate the parsed JSON shape with Zod before use (ASVS V5, defense against malformed upstream). Wrap the fetch in try/catch; NO retry/backoff (Don't-Hand-Roll — degrades to D-10 messaging). No top-level fetch/side effects.
  </action>
  <verify>
    <automated>npx vitest run src/lib/w3champions-client.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/w3champions-client.test.ts` exits 0 (GREEN)
    - `w3champions-client.ts` exports `fetchW3championsSignals` and the status classifier; `W3C_BASE_URL` is a hardcoded constant
    - mmrTier is derived from the raw `mmr` integer via `tierForMmr` (no `league-constellation` fetch, no leagueId use)
    - BattleTag is encodeURIComponent-ed; `kr` maps to America with an inline [ASSUMED] comment
    - No retry/backoff loop; parsed JSON validated with Zod
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>w3champions client green: coarse signal fetch + D-10 status classification, native fetch only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → external w3champions API | untrusted upstream JSON crosses into the app; the base URL is fixed, the BattleTag is server-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06a | Tampering | SSRF via URL construction | medium | mitigate | W3C_BASE_URL is a hardcoded constant; the only interpolated value is a server-read BattleTag, encodeURIComponent-ed (ASVS V5) |
| T-07-06b | Tampering | malformed/hostile upstream JSON | medium | mitigate | Zod-validate the parsed response before deriving signals (ASVS V5) |
| T-07-06c | Information Disclosure | leaking raw upstream error text | medium | mitigate | classifier returns only opaque bucket tags (ok/no-data/unreachable/rate-limited) — no status codes or upstream strings surfaced |
| T-07-06d | Denial of Service | sync-spam cost amplification | high | mitigate | SYNC_TTL_MS gate (consumed by 07-07) bounds upstream calls to once per TTL per user; no retry/backoff loop here |
</threat_model>

<verification>
- `npx vitest run src/lib/w3champions-client.test.ts` green
- `npx tsc --noEmit` exits 0
- Classifier covers all five buckets; URL uses encodeURIComponent; SYNC_TTL_MS named once
</verification>

<success_criteria>
Native-fetch w3champions client derives coarse signals and classifies every outcome into the D-10 buckets; the shared SYNC_TTL_MS constant and w3championsKeys factory exist.
</success_criteria>

<output>
Create `.planning/phases/07-w3champions-auto-detection/07-06-SUMMARY.md` when done.
</output>
