---
phase: 04-auth-database
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/db/schema.ts
  - src/lib/db.ts
  - src/lib/db.test.ts
  - src/db/migrations/
autonomous: true
requirements: [AUTH-01, AUTH-04]
user_setup:
  - service: neon
    why: "Schema push (Task 3) writes the user/session/account/verification tables to the live Neon dev branch; DATABASE_URL_DIRECT must be set before this plan runs"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Connection string (pooled) -> dev branch"
      - name: DATABASE_URL_DIRECT
        source: "Neon Console -> Connection string -> Direct connection -> dev branch (non-pooled)"

must_haves:
  truths:
    - "The users table has a stable internal primary key (the progress key) that is NOT the BattleTag (AUTH-04)"
    - "The users table persists battleTag (canonical Name#1234), gateway, avatarUrl (nullable), and bnetSub (D-05/D-06)"
    - "better-auth's four tables (user, session, account, verification) exist in the schema and in the live Neon DB"
    - "The db singleton connects via the neon-http driver and is importable server-side"
  artifacts:
    - "src/db/schema.ts"
    - "src/lib/db.ts"
    - "src/lib/db.test.ts"
  key_links:
    - "schema.ts users.id -> the UUID that becomes ProgressRecordSchema.userId in Phase 5"
    - "db.ts -> DATABASE_URL (pooled, neon-http)"
    - "drizzle.config.ts (04-01) -> schema.ts (push reads it)"
---

<objective>
Define the Drizzle schema (the four better-auth tables plus the custom identity profile fields), the database singleton, a connection smoke test, and push the schema to the live Neon database.

Purpose: This establishes AUTH-04's stable-UUID progress key and the D-05/D-06 identity profile (BattleTag + gateway + bnetSub) that Phases 7/8 query w3champions by. The schema-push gate is mandatory — type/build checks pass off Drizzle config alone and would otherwise produce a false-positive verification with no real tables.
Output: `src/db/schema.ts`, `src/lib/db.ts`, `src/lib/db.test.ts`, generated migration files, and live tables in Neon.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-database/04-RESEARCH.md
@.planning/phases/04-auth-database/04-PATTERNS.md
@src/schemas/progress.ts

**Path alias:** Use `#/` not `@/` (RESEARCH samples show `@/`). e.g. `import * as schema from "#/db/schema"`.
**SPDX header:** two-line GPL header on schema.ts, db.ts, db.test.ts.
**better-auth column authority:** The four better-auth tables (user, session, account, verification) have exact column expectations owned by better-auth, NOT by us. Fetch the current better-auth Drizzle schema via Context7 (`better-auth` library, query "drizzle adapter schema tables columns") and match column names/types exactly. The RESEARCH §"Code Examples" schema is illustrative; the Context7 schema is authoritative. Custom user fields (battleTag, gateway, avatarUrl, bnetSub) are added on top of better-auth's user table.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Drizzle schema with better-auth tables + custom identity fields</name>
  <read_first>
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Code Examples" -> Drizzle Schema, §"Open Questions" #4 (id type — UUID vs cuid)
    - .planning/phases/04-auth-database/04-PATTERNS.md §"src/db/schema.ts" (JSDoc-per-field style from progress.ts)
    - src/schemas/progress.ts (header + per-field JSDoc analog)
    - Context7: better-auth drizzle adapter schema (authoritative column set)
  </read_first>
  <files>src/db/schema.ts</files>
  <action>
    Author `src/db/schema.ts` with GPL header + module JSDoc (state Phase 4 scope: identity tables only; Phase 5 writes progress). Export four pgTable named exports: `users` (better-auth maps to table name "user"), `sessions` ("session"), `accounts` ("account"), `verifications` ("verification"). Match better-auth's required columns exactly (from Context7). On the user table add custom NOT-NULL columns `battleTag` (canonical "Name#1234" — JSDoc the D-06 w3champions-key constraint), `gateway` ("us" | "eu" | "kr" — D-05/D-07), `bnetSub` (stable Battle.net sub for dedup), and nullable `avatarUrl` (Battle.net has no avatar endpoint — Pitfall 2). The primary key `id` is the stable internal account UUID (AUTH-04) — if better-auth's default id is not a DB-generated UUID, set a Drizzle default of `sql\`gen_random_uuid()\`` per Open Question #4 so the key is stable and never the BattleTag. Per-table and per-non-obvious-column JSDoc. Named exports only so `import * as schema` in db.ts collects them.
    Do NOT place any literal that a later negative-grep gate targets into comment prose.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/db/schema.ts','utf8');for(const c of ['battleTag','gateway','bnetSub','avatarUrl']){if(!s.includes(c)){console.error('missing '+c);process.exit(1)}};['users','sessions','accounts','verifications'].forEach(t=>{if(!s.includes('export const '+t)){console.error('missing export '+t);process.exit(1)}});console.log('schema ok')"</automated>
  </verify>
  <acceptance_criteria>
    schema.ts exports users/sessions/accounts/verifications; users has battleTag, gateway, bnetSub (NOT NULL) and nullable avatarUrl; id is a stable UUID-style key; typecheck passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database singleton + connection smoke test</name>
  <read_first>
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Pattern 4" (db.ts neon-http)
    - .planning/phases/04-auth-database/04-PATTERNS.md §"src/lib/db.ts" and §"Vitest Test Structure"
    - src/schemas/progress.test.ts (vitest structure analog)
  </read_first>
  <files>src/lib/db.ts, src/lib/db.test.ts</files>
  <action>
    Create `src/lib/db.ts`: GPL header + JSDoc (neon-http works on Vercel Node.js + edge; migrations use DATABASE_URL_DIRECT). `const sql = neon(process.env.DATABASE_URL!)`; `export const db = drizzle({ client: sql, schema })` importing `* as schema from "#/db/schema"`. Module-scope singleton (patches.ts analog) — never recreated per request.
    Create `src/lib/db.test.ts`: a structural smoke test that does NOT require a live DB connection — assert `db` is defined, exposes `.query.users` (schema wired), and is a singleton (two imports are referentially equal). Mock `@neondatabase/serverless` `neon` so the test runs offline in CI (a real round-trip belongs in manual verification). This satisfies the VALIDATION.md Wave 0 `db.test.ts` requirement without a network dependency.
  </action>
  <verify>
    <automated>npm test -- db.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    db.ts exports a singleton `db` wired to the schema; `npm test -- db.test` is green; the test runs offline (neon mocked), asserting db.query.users exists.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to the live Neon database</name>
  <read_first>
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Pitfall 5" (non-pooled connection for drizzle-kit)
    - drizzle.config.ts (from 04-01 — reads DATABASE_URL_DIRECT)
  </read_first>
  <files>src/db/migrations/</files>
  <action>
    With DATABASE_URL_DIRECT set to the Neon dev-branch direct (non-pooled) connection, run `npx drizzle-kit generate` to emit committed migration SQL under `src/db/migrations/` (repo history), then run `npx drizzle-kit push` to apply the schema to the live Neon dev branch. This is the mandatory schema-push gate — it must run AFTER schema.ts is final (Task 1) and BEFORE phase verification. On a fresh empty Neon branch there are no existing tables, so push is non-interactive (no destructive prompt). If push reports a destructive prompt it cannot suppress, STOP and surface it for manual confirmation (the plan is otherwise autonomous). Do not run drizzle-kit against the pooled DATABASE_URL — pgbouncer breaks migration transactions (Pitfall 5).
  </action>
  <verify>
    <automated>ls src/db/migrations/*.sql >/dev/null 2>&1 && echo "migration generated" || { echo "no migration sql"; exit 1; }</automated>
    <human-check>Confirm in the Neon Console (dev branch -> Tables) that the user, session, account, and verification tables exist with the custom user columns (battleTag, gateway, bnetSub, avatarUrl). This requires the live Neon connection and cannot be asserted in CI.</human-check>
  </verify>
  <acceptance_criteria>
    A migration SQL file exists under src/db/migrations/; `npx drizzle-kit push` completed without error; the four tables are present in the Neon dev branch.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Neon Postgres | All user identity data crosses here; connection strings are secrets |
| drizzle-kit -> Neon | Schema-altering operations require the privileged direct connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02a | Tampering | src/db/schema.ts (SQL) | high | mitigate | Drizzle parameterized queries only; no raw SQL string interpolation in app code (V5/SQLi) |
| T-04-02b | Information Disclosure | DATABASE_URL / DATABASE_URL_DIRECT | high | mitigate | Connection strings are server-only env vars (.env.local / Vercel secrets); never imported into client bundle; db.ts is server-side only |
| T-04-02c | Spoofing | users.id as progress key | medium | mitigate | AUTH-04: id is a stable internal UUID, decoupled from the mutable BattleTag, so display-name changes cannot hijack or orphan a progress record |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm test -- db.test` green (offline smoke)
- Migration SQL generated; schema pushed to Neon dev branch (human-confirmed)
</verification>

<success_criteria>
The four better-auth tables plus the custom identity columns exist in the schema and the live DB; the stable-UUID progress key (AUTH-04) and the D-05/D-06 w3champions identity fields are in place; db singleton importable server-side.
</success_criteria>

<artifacts_produced>
- Symbol: `users`, `sessions`, `accounts`, `verifications` (Drizzle pgTable exports in `src/db/schema.ts`)
- Symbol: `db` (Drizzle singleton in `src/lib/db.ts`)
- Drizzle table names: `user`, `session`, `account`, `verification`
- Migration: `src/db/migrations/*.sql`
- Live tables in Neon dev branch
</artifacts_produced>

<output>
Create `.planning/phases/04-auth-database/04-02-SUMMARY.md` when done
</output>
