---
phase: 04-auth-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components.json
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/dialog.tsx
  - drizzle.config.ts
  - .env.example
  - .gitignore
  - .claude/CLAUDE.md
  - docs/adr/001-stack-choice.md
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: neon
    why: "PostgreSQL database for users/sessions (consumed by plan 04-02 schema push)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Connection string (pooled) -> dev branch"
      - name: DATABASE_URL_DIRECT
        source: "Neon Console -> Connection string -> Direct connection toggle -> dev branch (non-pooled; required for drizzle-kit)"
  - service: battlenet
    why: "Battle.net OAuth client (consumed by plan 04-03 auth wiring and end-of-phase sign-in verification)"
    env_vars:
      - name: BNET_CLIENT_ID
        source: "https://develop.battle.net/access/clients -> create client -> Client ID"
      - name: BNET_CLIENT_SECRET
        source: "https://develop.battle.net/access/clients -> Client Secret"
      - name: BETTER_AUTH_SECRET
        source: "generate locally: openssl rand -base64 32"
    dashboard_config:
      - task: "Set Redirect URIs on the Battle.net OAuth client to http://localhost:3000/api/auth/callback/battlenet (dev) and https://<vercel-domain>/api/auth/callback/battlenet (prod)"
        location: "https://develop.battle.net/access/clients"

must_haves:
  truths:
    - "better-auth, drizzle-orm, drizzle-kit, and @neondatabase/serverless are installed at the RESEARCH-pinned versions"
    - "shadcn dropdown-menu, avatar, and dialog primitives exist under src/components/ui/"
    - "drizzle.config.ts points migrations at DATABASE_URL_DIRECT (non-pooled)"
    - ".env.example documents every secret a contributor must supply"
  artifacts:
    - "drizzle.config.ts"
    - ".env.example"
    - "src/components/ui/dropdown-menu.tsx"
    - "src/components/ui/avatar.tsx"
    - "src/components/ui/dialog.tsx"
  key_links:
    - "drizzle.config.ts -> DATABASE_URL_DIRECT env var (push/migrate use non-pooled connection)"
    - "package.json pinned versions -> better-auth drizzle adapter compatibility"
---

<objective>
Bootstrap every dependency, UI primitive, config file, and contributor-onboarding artifact the rest of Phase 4 builds on. No auth logic, DB schema, or UI behavior here — this is the foundation wave so later plans run cleanly in parallel.

Purpose: Establish the exact pinned toolchain (resolving the drizzle version drift in Pitfall 6), the three new shadcn primitives the auth UI needs, and a complete `.env.example` so an OSS contributor can set up Neon + Battle.net locally without guessing.
Output: Installed packages, shadcn primitives, `drizzle.config.ts`, `.env.example`, and a CLAUDE.md/ADR-001 changelog note recording the version pins.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-database/04-RESEARCH.md
@.planning/phases/04-auth-database/04-PATTERNS.md
@components.json
@.claude/CLAUDE.md

**CRITICAL — path alias:** This repo's TypeScript path alias is `#/` (tsconfig `"#/*": ["./src/*"]`), NOT `@/`. RESEARCH.md and its code samples use `@/` — translate every `@/` to `#/` when writing imports. shadcn aliases (components.json) already use `#/components`, `#/lib/utils`.

**SPDX header:** Every hand-authored source file starts with the two-line GPL header (see `src/schemas/progress.ts` lines 1-2). shadcn-generated `ui/*.tsx` files are exempt (generated, like routeTree.gen.ts).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned auth + database packages and update version records</name>
  <read_first>
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Standard Stack" (exact versions) and §"Common Pitfalls" Pitfall 6 (drizzle version drift)
    - package.json (current dependency block, confirm none already present)
    - .claude/CLAUDE.md §"Recommended Stack" version table; docs/adr/001-stack-choice.md
  </read_first>
  <files>package.json, package-lock.json, .claude/CLAUDE.md, docs/adr/001-stack-choice.md</files>
  <action>
    Install runtime deps: `better-auth`, `drizzle-orm@0.45.2`, `@neondatabase/serverless` (latest 1.x). Install dev dep: `drizzle-kit@0.31.10`. Pin drizzle-orm to 0.45.2 and drizzle-kit to 0.31.10 exactly per Pitfall 6 — do NOT accept the CLAUDE.md 0.44.x/0.25.x values, which are stale and conflict with the better-auth adapter. Run `npm view <pkg> version` first to confirm better-auth and @neondatabase/serverless current majors match RESEARCH (better-auth 1.6.x, @neondatabase/serverless 1.x); pin the confirmed exact versions in package.json.
    After install, update the CLAUDE.md "Recommended Stack" / "Version Compatibility" rows for drizzle-orm and drizzle-kit to the installed versions, and append a one-line changelog note to docs/adr/001-stack-choice.md recording the drift correction (drizzle-orm 0.44.x -> 0.45.2, drizzle-kit 0.25.x -> 0.31.10) per the package-legitimacy audit in RESEARCH (both drizzle packages and better-auth were SUS false-positives, all Approved).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};for(const k of ['better-auth','drizzle-orm','@neondatabase/serverless','drizzle-kit']){if(!d[k]){console.error('MISSING '+k);process.exit(1)}};if(!d['drizzle-orm'].includes('0.45')){console.error('drizzle-orm not 0.45.x');process.exit(1)};if(!d['drizzle-kit'].includes('0.31')){console.error('drizzle-kit not 0.31.x');process.exit(1)};console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    All four packages appear in package.json; drizzle-orm resolves to 0.45.x and drizzle-kit to 0.31.x; `npm ls better-auth drizzle-orm drizzle-kit @neondatabase/serverless` reports no UNMET; CLAUDE.md and ADR-001 reflect the pinned versions.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add shadcn dropdown-menu, avatar, and dialog primitives</name>
  <read_first>
    - components.json (style "new-york", aliases use `#/`, baseColor neutral)
    - src/components/ui/button.tsx (existing primitive — confirms install location + style)
    - .planning/phases/04-auth-database/04-UI-SPEC.md §"Component Inventory" -> "Install Commands"
  </read_first>
  <files>src/components/ui/dropdown-menu.tsx, src/components/ui/avatar.tsx, src/components/ui/dialog.tsx, package.json</files>
  <action>
    Run `npx shadcn add dropdown-menu avatar dialog` (non-interactive; the project is already initialized — components.json present). These land in `src/components/ui/` per the `ui` alias. Do not hand-edit the generated files. If shadcn add prompts to overwrite existing files, decline for button/badge/tooltip. Confirm radix-ui peer (already in package.json `radix-ui ^1.6.0`) satisfies the new primitives; shadcn may add `@radix-ui/react-*` subpackages — accept those.
  </action>
  <verify>
    <automated>test -f src/components/ui/dropdown-menu.tsx && test -f src/components/ui/avatar.tsx && test -f src/components/ui/dialog.tsx && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    The three files exist under src/components/ui/; `npm run typecheck` passes with the new primitives present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author drizzle.config.ts and a complete .env.example</name>
  <read_first>
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Pattern 5" (drizzle.config), §"Pitfall 5" (non-pooled migration connection), §"OSS Contributor Dev DB Recommendation" (.env.example contents)
    - .gitignore (confirm .env / .env.local already ignored; .env.example must NOT be ignored)
  </read_first>
  <files>drizzle.config.ts, .env.example, .gitignore</files>
  <action>
    Create root `drizzle.config.ts` via drizzle-kit `defineConfig`: schema `./src/db/schema.ts`, out `./src/db/migrations`, dialect `postgresql`, `dbCredentials.url = process.env.DATABASE_URL_DIRECT!`. Add a comment that DIRECT (non-pooled) is mandatory for drizzle-kit (Pitfall 5) — pooled pgbouncer connections fail migrations/push.
    Create `.env.example` listing every secret with placeholder values and inline source comments: `BETTER_AUTH_SECRET` (openssl rand -base64 32), `BETTER_AUTH_URL` (http://localhost:3000), `BNET_CLIENT_ID`, `BNET_CLIENT_SECRET` (develop.battle.net), `DATABASE_URL` (pooled, app queries), `DATABASE_URL_DIRECT` (non-pooled, drizzle-kit), `VITE_APP_URL` (http://localhost:3000). Match the keys exactly to RESEARCH §"OSS Contributor Dev DB Recommendation".
    Verify .gitignore ignores `.env` and `.env.local` (add if missing) but never `.env.example`. Never commit a real secret — `.env.example` holds placeholders only.
  </action>
  <verify>
    <automated>test -f drizzle.config.ts && grep -q 'DATABASE_URL_DIRECT' drizzle.config.ts && for v in BETTER_AUTH_SECRET BNET_CLIENT_ID BNET_CLIENT_SECRET DATABASE_URL DATABASE_URL_DIRECT VITE_APP_URL; do grep -q "$v" .env.example || { echo "MISSING $v in .env.example"; exit 1; }; done && grep -qE '^\.env(\.local)?$|^\.env$' .gitignore && echo "config ok"</automated>
  </verify>
  <acceptance_criteria>
    drizzle.config.ts uses DATABASE_URL_DIRECT and dialect postgresql; .env.example contains all six+ documented keys with placeholders (no real secrets); .gitignore excludes .env/.env.local and includes .env.example.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local toolchain | Installing third-party auth/DB packages introduces supply-chain trust |
| repo -> git history | Secrets must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm installs (better-auth, drizzle-orm, drizzle-kit, @neondatabase/serverless) | high | mitigate | All four vetted in RESEARCH §"Package Legitimacy Audit": the two SUS flags are documented false-positives, all Approved with download/age evidence. Pin exact versions; no [ASSUMED]/[SLOP] packages remain, so no blocking human checkpoint is required. |
| T-04-01 | Information Disclosure | .env / secrets | high | mitigate | .env.example holds placeholders only; .gitignore excludes real env files; BETTER_AUTH_SECRET/BNET_CLIENT_SECRET never committed |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm test` passes (no test changes this plan; suite stays green)
- Package versions pinned per RESEARCH; no UNMET peer deps
</verification>

<success_criteria>
better-auth + drizzle stack installed at pinned versions; three shadcn primitives present; drizzle.config.ts and .env.example authored; no secrets in git.
</success_criteria>

<artifacts_produced>
- File: `drizzle.config.ts` (drizzle-kit config, DATABASE_URL_DIRECT)
- File: `.env.example` (contributor secret template)
- Files: `src/components/ui/dropdown-menu.tsx`, `src/components/ui/avatar.tsx`, `src/components/ui/dialog.tsx`
- Env vars established (documented, not valued): `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `BNET_CLIENT_ID`, `BNET_CLIENT_SECRET`, `DATABASE_URL`, `DATABASE_URL_DIRECT`, `VITE_APP_URL`
</artifacts_produced>

<output>
Create `.planning/phases/04-auth-database/04-01-SUMMARY.md` when done
</output>
