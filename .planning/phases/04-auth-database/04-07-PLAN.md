---
phase: 04-auth-database
plan: 07
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - CONTEXT.md
  - docs/adr/007-authed-server-fn-authorization.md
  - docs/adr/008-betterauth-battlenet-integration.md
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "Root CONTEXT.md gains the Phase 4 domain terms: principal, session, BattleTag, gateway/region, account UUID (D-14)"
    - "An ADR records the authedServerFn principal-keyed authorization convention (D-11/D-12) as the mandated pattern for all later user-data server functions"
    - "An ADR records the better-auth + Battle.net integration decisions (generic-OAuth, region-capture workaround, no-avatar workaround, 30-day rolling sessions)"
  artifacts:
    - "docs/adr/007-authed-server-fn-authorization.md"
    - "docs/adr/008-betterauth-battlenet-integration.md"
  key_links:
    - "ADR 007 -> src/lib/auth-middleware.ts (the convention it documents)"
    - "CONTEXT.md -> domain language consumed by Phases 5-8"
---

<objective>
Record the architecture-discipline deliverables for Phase 4 (D-14): extend the project glossary with the new identity/auth domain terms, and write the two ADRs that lock the authorization convention and the Battle.net integration choices.

Purpose: The `authedServerFn` convention is the security pattern Phases 5-8 copy verbatim; an ADR makes it the auditable, citable standard (not tribal knowledge). CONTEXT.md keeps the ubiquitous language precise. This satisfies the cross-cutting deep-module + ADR discipline.
Output: Updated `CONTEXT.md` and ADRs 007 + 008.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CONTEXT.md
@docs/adr/006-graph-display-node-skilltype-tags.md
@.planning/phases/04-auth-database/04-CONTEXT.md
@.planning/phases/04-auth-database/04-RESEARCH.md

**ADR numbering:** The substantive ADR sequence is 001-006 (3-digit). Continue with 007 and 008. Match the existing ADR file structure/format of `docs/adr/006-graph-display-node-skilltype-tags.md` (status, context, decision, consequences).
**Accuracy:** Write the ADRs AFTER the auth-middleware module (04-04) exists so they describe the shipped interface, not a guess. Cite `src/lib/auth-middleware.ts` and `src/server/user-profile.ts` by path.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend root CONTEXT.md with Phase 4 domain terms</name>
  <read_first>
    - CONTEXT.md (existing structure, "Last updated: Phase 02" header, Core Terms format)
    - .planning/phases/04-auth-database/04-CONTEXT.md §"decisions" D-04..D-12 (term definitions)
  </read_first>
  <files>CONTEXT.md</files>
  <action>
    Append new glossary entries in the existing Core Terms style (one `### term` heading + concise definition each): `principal` (the authenticated session user injected by authMiddleware; the only trusted identity source in a server function), `session` (better-auth 30-day rolling cookie-backed auth state), `BattleTag` (canonical "Name#1234" Battle.net display identity; the w3champions lookup key — D-06; mutable, refreshed each login), `gateway` / `region` (us | eu | kr; the Battle.net server a player signs in through; persisted for Phase 7/8), `account UUID` (the stable internal users.id; the immutable progress key — AUTH-04, never the BattleTag). Update the "Last updated" line to "Phase 04". Do not alter existing terms.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('CONTEXT.md','utf8');for(const t of ['principal','session','BattleTag','gateway','account UUID','Phase 04']){if(!s.includes(t)){console.error('missing '+t);process.exit(1)}};console.log('context ok')"</automated>
  </verify>
  <acceptance_criteria>
    CONTEXT.md contains the five new terms with definitions and shows "Last updated: Phase 04"; existing terms unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ADR 007 (authedServerFn authorization) + ADR 008 (better-auth + Battle.net)</name>
  <read_first>
    - docs/adr/006-graph-display-node-skilltype-tags.md (ADR format)
    - .planning/phases/04-auth-database/04-CONTEXT.md §"D-11..D-13" + §"D-05..D-08"
    - .planning/phases/04-auth-database/04-RESEARCH.md §"Pattern 3", §"Pitfall 1/2/3/7", §"Security Domain"
    - src/lib/auth-middleware.ts (the shipped interface)
  </read_first>
  <files>docs/adr/007-authed-server-fn-authorization.md, docs/adr/008-betterauth-battlenet-integration.md</files>
  <action>
    Write `docs/adr/007-authed-server-fn-authorization.md`: status Accepted; context (server functions are HTTP endpoints reachable by curl; beforeLoad is not a boundary — Pitfall 7; client-supplied userId is untrusted); decision (the `authedServerFn` deep module is mandatory for all user-data server functions; queries are principal-keyed by construction (D-12); authMiddleware throws 401 on no session (D-11); the D-13 cross-user test is the regression lock); consequences (every later phase copies this; one file to audit). Cite src/lib/auth-middleware.ts and src/server/user-profile.ts.
    Write `docs/adr/008-betterauth-battlenet-integration.md`: status Accepted; context (Battle.net via generic-OAuth; no community-validated example — LOW-MEDIUM confidence; userinfo returns only sub/id/battletag); decision (generic-OAuth global endpoint with A1 per-region fallback noted; gateway captured via pre-redirect region selector + sessionStorage — Pitfall 1; avatar generated client-side since Battle.net has none — Pitfall 2; overrideUserInfo refreshes BattleTag while UUID stays stable — D-08/Pitfall 3; 30-day rolling always-persistent sessions — D-09/D-10; auth.ts is server-only, auth-client.ts client-only); consequences (Phase 7/8 resolve w3champions by the stored BattleTag+gateway with no identity migration).
  </action>
  <verify>
    <automated>test -f docs/adr/007-authed-server-fn-authorization.md && test -f docs/adr/008-betterauth-battlenet-integration.md && grep -qi 'principal' docs/adr/007-authed-server-fn-authorization.md && grep -qi 'battle.net\|battlenet' docs/adr/008-betterauth-battlenet-integration.md && echo "adrs ok"</automated>
  </verify>
  <acceptance_criteria>
    ADR 007 documents the principal-keyed authedServerFn convention citing the shipped files; ADR 008 documents the better-auth + Battle.net integration decisions including the region/avatar/overrideUserInfo workarounds; both follow the existing ADR format.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future implementers | ADRs codify the security convention later phases must follow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07a | Repudiation | undocumented authorization convention | medium | mitigate | ADR 007 makes the principal-keyed rule explicit and auditable, preventing a later phase from silently adding a userId-from-client server function |
</threat_model>

<verification>
- CONTEXT.md updated with the five terms (Phase 04)
- ADRs 007 + 008 exist in the established format and cite the shipped modules
</verification>

<success_criteria>
The domain language and the authorization + integration decisions are recorded so Phases 5-8 inherit the convention without re-deriving it (D-14).
</success_criteria>

<artifacts_produced>
- File: `docs/adr/007-authed-server-fn-authorization.md`
- File: `docs/adr/008-betterauth-battlenet-integration.md`
- Updated: `CONTEXT.md` (terms: principal, session, BattleTag, gateway/region, account UUID)
</artifacts_produced>

<output>
Create `.planning/phases/04-auth-database/04-07-SUMMARY.md` when done
</output>
