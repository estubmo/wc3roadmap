---
phase: 04-auth-database
plan: 06
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - src/components/SiteHeader.tsx
  - src/routes/__root.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: battlenet
    why: "End-of-phase human verification of the full OAuth round-trip requires real Battle.net credentials + redirect URIs registered"
    dashboard_config:
      - task: "Confirm Redirect URIs include http://localhost:3000/api/auth/callback/battlenet"
        location: "https://develop.battle.net/access/clients"

must_haves:
  truths:
    - "A fixed top header renders the SignInButton when signed out and the UserDropdown when signed in, switching on the live session (D-01/D-02/D-03)"
    - "The header overlays the full-screen graph without obstructing it; page content clears the 48px bar"
    - "Refreshing the browser keeps a signed-in user signed in — the header shows the dropdown immediately, no flash of signed-out state (AUTH-02)"
    - "Signing out returns the header to the SignInButton with no page reload and no data loss (D-03)"
  artifacts:
    - "src/components/SiteHeader.tsx"
  key_links:
    - "SiteHeader -> useSession (auth-client) -> SignInButton | UserDropdown"
    - "__root.tsx RootDocument -> <SiteHeader/> above {children} with padding-top offset"
    - "SignInButton -> RegionSelector open state owned by SiteHeader"
---

<objective>
Compose the auth components into a fixed site header, mount it in the root layout, and wire the sign-in open/close flow. Then verify the complete Battle.net OAuth round-trip and session persistence end-to-end.

Purpose: This delivers the visible AUTH-01 entry point and AUTH-02 session persistence as experienced by the user — the header is the phase's primary focal point (UI-SPEC). Sign-in stays purely additive (D-03): the graph remains fully usable signed-out.
Output: `src/components/SiteHeader.tsx` and the `__root.tsx` mount.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth-database/04-UI-SPEC.md
@src/routes/__root.tsx
@src/components/auth/SignInButton.tsx
@src/components/auth/UserDropdown.tsx
@src/components/auth/RegionSelector.tsx

**Path alias:** Use `#/` not `@/`.
**SPDX header:** two-line GPL header on SiteHeader.tsx; preserve the existing header on __root.tsx.
**Client-safe imports only:** SiteHeader imports `useSession` from `#/lib/auth-client`, never `#/lib/auth`.
**SSR/hydration:** __root.tsx RootDocument already wraps {children} in QueryClientProvider and renders <Scripts/> OUTSIDE the provider — do not disturb that structure. The header mounts inside the provider, above {children}. Session must derive from the cookie (SSR-safe) so there is no flash of signed-out state on refresh (UI-SPEC §"Session Persistence").
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader — fixed bar switching SignInButton/UserDropdown on session</name>
  <read_first>
    - .planning/phases/04-auth-database/04-UI-SPEC.md §"<SiteHeader>" + §"Interaction Patterns" + §"OAuth Error Handling" + §"Accessibility Notes"
    - src/components/auth/SignInButton.tsx, UserDropdown.tsx, RegionSelector.tsx (from 04-05)
  </read_first>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    Create `src/components/SiteHeader.tsx`: GPL header + JSDoc. Named export `SiteHeader`. Render a `<header role="banner">` fixed bar: top/left/right 0, height 48px, z-index 50, background `var(--color-obsidian-900)`, bottom border 1px `var(--color-obsidian-600)`, padding 0 16px, flex align-center justify-end. Read `useSession()` from `#/lib/auth-client`. When no session -> render `<SignInButton>` (owning the RegionSelector open state here: SiteHeader holds `regionOpen` useState, passes onOpenRegion to SignInButton and open/onOpenChange to RegionSelector). When session present -> render `<UserDropdown>`. Left region empty (future wordmark). On OAuth error state, show the inline message "Sign-in failed — check your connection and try again." (14px/400) per UI-SPEC; never a blocking modal (D-03). Avoid a flash of signed-out content: while session is pending, render nothing in the right slot (not the SignInButton) so a refreshed signed-in user does not flash the CTA.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/components/SiteHeader.tsx','utf8');for(const t of ['useSession','SignInButton','UserDropdown','RegionSelector','role=\"banner\"']){if(!s.includes(t)){console.error('missing '+t);process.exit(1)}};if(/from ['\"]#\/lib\/auth['\"]/.test(s)){console.error('must use auth-client');process.exit(1)};console.log('header ok')"</automated>
  </verify>
  <acceptance_criteria>
    SiteHeader renders the fixed 48px banner, switches SignInButton/UserDropdown on useSession, owns the RegionSelector open state, shows inline OAuth error copy, and avoids a signed-out flash; typecheck passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount SiteHeader in the root layout</name>
  <read_first>
    - src/routes/__root.tsx (RootDocument structure — QueryClientProvider wraps {children})
    - .planning/phases/04-auth-database/04-UI-SPEC.md §"<SiteHeader>" (mounts above {children}; {children} wrapper gets padding-top 48px)
  </read_first>
  <files>src/routes/__root.tsx</files>
  <action>
    In RootDocument, import `SiteHeader` from `#/components/SiteHeader` and render `<SiteHeader/>` inside `<QueryClientProvider>`, above `{children}`. Wrap `{children}` so it clears the fixed bar — add padding-top: 48px on the content wrapper so the graph canvas is not hidden behind the header. Do not move `<Scripts/>` (stays outside the provider). Preserve the existing GPL header, head config, and QueryClient singleton exactly.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/routes/__root.tsx','utf8');if(!s.includes('SiteHeader')){console.error('SiteHeader not mounted');process.exit(1)};if(!/padding-top|paddingTop|pt-/.test(s)){console.error('content offset for fixed header missing');process.exit(1)};console.log('root ok')" && npm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    __root.tsx renders SiteHeader inside the provider above {children}; content is offset 48px; Scripts/QueryClient untouched; typecheck + full test suite green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: End-to-end OAuth + session-persistence verification</name>
  <read_first>
    - .planning/phases/04-auth-database/04-UI-SPEC.md §"Interaction Patterns" (Sign-in / Sign-out / Session Persistence flows)
    - .planning/phases/04-auth-database/04-VALIDATION.md §"Manual-Only Verifications"
    - .planning/ROADMAP.md §"Phase 4" success criteria 1-2
  </read_first>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    No new code unless the manual check surfaces a defect. This task is the end-of-phase verification of the full OAuth round-trip (which cannot run in CI — requires live Blizzard credentials per VALIDATION.md). Ensure `.env.local` has real BNET_CLIENT_ID/SECRET, BETTER_AUTH_SECRET, DATABASE_URL(_DIRECT), and the dev server runs. If the round-trip exposes the A1 regional-routing risk (EU/KR players not reaching their region via the global endpoint — Open Question #1), capture the finding for a per-region genericOAuth fallback and note it in the SUMMARY (do not silently swallow it).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -8</automated>
    <human-check>
      Run `npm run dev`. (1) Signed out: header shows the gold "Sign in with Battle.net" CTA over the graph; the graph is fully usable without signing in (D-03). (2) Click it -> region dialog -> choose Americas -> complete Battle.net auth -> return to app: header now shows your BattleTag + avatar (success criterion 1). (3) Refresh the browser: you stay signed in, dropdown shows immediately with no flash of the CTA (success criterion 2 / AUTH-02). (4) Open the dropdown -> Sign out -> header returns to the CTA, no reload. (5) Confirm a users row was written in Neon with your battleTag, gateway, and bnetSub.
    </human-check>
  </verify>
  <acceptance_criteria>
    Full suite green; the five human-check steps pass (sign-in recognized by BattleTag, session survives refresh, sign-out returns to CTA, additive signed-out usage, users row persisted). Any regional-routing limitation is documented, not hidden.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session cookie -> header render | Header UI state derives from the server-resolved session, not client-spoofable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06a | Spoofing | client-faked signed-in header | low | accept | The header is presentational; all protected data goes through authMiddleware (04-04). A faked client session shows a name but cannot read another user's data — no security impact |
| T-04-06b | Information Disclosure | flash of wrong auth state on refresh | low | mitigate | Session derives from the httpOnly cookie (SSR-safe); pending state renders an empty slot, not the CTA, avoiding state leakage/flicker |
</threat_model>

<verification>
- `npm run typecheck` + full `npm test` suite green (phase Nyquist gate)
- Human-check: OAuth round-trip, session-refresh persistence, sign-out, additive signed-out usage, users-row persistence
</verification>

<success_criteria>
The header presents Battle.net sign-in over the graph, recognizes the returning user by BattleTag, persists the session across refresh, signs out cleanly, and keeps the app fully usable signed-out (AUTH-01, AUTH-02, D-01/D-02/D-03).
</success_criteria>

<artifacts_produced>
- Symbol: `SiteHeader` (`src/components/SiteHeader.tsx`)
- Modified: `src/routes/__root.tsx` (header mount + content offset)
</artifacts_produced>

<output>
Create `.planning/phases/04-auth-database/04-06-SUMMARY.md` when done
</output>
