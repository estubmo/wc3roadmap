---
phase: 06-self-assessment-quizzes
plan: 04
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/quiz-grading.ts
  - src/lib/quiz-grading.test.ts
autonomous: true
requirements: [QUIZ-02]
must_haves:
  truths:
    - "Grading is a pure, deterministic function — auto-graded, objective pass/fail (D-02): gradeQuiz(3,3)=true and gradeQuiz(3,2)=false (3q requires 3/3, NOT 2/3 — D-05)"
    - "gradeQuiz(4,3)=true/gradeQuiz(4,2)=false and gradeQuiz(5,4)=true/gradeQuiz(5,3)=false (D-05 thresholds)"
    - "gradeAnswers maps selected option indexes against shuffled questions via isCorrect (no canonical-index remapping)"
    - "shuffle/prepareQuiz never mutate the source array (MDX content is effectively frozen)"
  artifacts:
    - path: "src/lib/quiz-grading.ts"
      provides: "PASS_THRESHOLD, gradeQuiz, gradeAnswers, shuffle, prepareQuiz"
      contains: "PASS_THRESHOLD"
    - path: "src/lib/quiz-grading.test.ts"
      provides: "boundary + purity tests for the grading engine"
      contains: "gradeQuiz"
  key_links:
    - from: "src/lib/quiz-grading.ts gradeAnswers"
      to: "src/schemas/node.ts QuizQuestion"
      via: "type-only import of QuizQuestion"
      pattern: "QuizQuestion"
---

<objective>
Build the deep-module grading engine for QUIZ-02 as a pure, dependency-free utility: a `PASS_THRESHOLD` lookup, `gradeQuiz`, `gradeAnswers`, and Fisher-Yates `shuffle`/`prepareQuiz`. The pass rule is an EXPLICIT table (3q→3/3, 4q→3/4, 5q→4/5) — the 3-question case requires all three correct, NOT 2/3 (Pitfall 2). TDD: tests first.

Purpose: A simple interface (`gradeAnswers(questions, selections) → {passed, correctCount, missedIndexes}`) hiding the pass-rule + shuffle substance — a candidate deep module (codebase-design discipline). Client-side grading keeps the answer key off the server (the server fn receives only `nodeId`).
Output: `src/lib/quiz-grading.ts` + colocated tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/lib/patches.ts
@src/schemas/node.ts
@.agents/skills/codebase-design/SKILL.md
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); `PASS_THRESHOLD`/`gradeQuiz`/`gradeAnswers`/`shuffle`/`prepareQuiz` (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); quiz UI components + radio-group; extended `MasteryBadge`/`GraphNode`; demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing grading tests (RED)</name>
  <files>src/lib/quiz-grading.test.ts</files>
  <read_first>
    - src/lib/patches.test.ts (pure-module test style — describe/it, no jsdom)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 381–471 (Pattern 4/5 grading + shuffle), lines 670–674 (Pitfall 2)
  </read_first>
  <behavior>
    - gradeQuiz(3,3)=true, gradeQuiz(3,2)=false (3q boundary — the critical Pitfall-2 case).
    - gradeQuiz(4,3)=true, gradeQuiz(4,2)=false.
    - gradeQuiz(5,4)=true, gradeQuiz(5,3)=false.
    - gradeQuiz throws for counts outside {3,4,5} (e.g. gradeQuiz(2,2), gradeQuiz(6,6)).
    - gradeAnswers over a 3-question fixture: all-correct selections → passed true, correctCount 3, missedIndexes []; one wrong → passed false (3q), missedIndexes lists the wrong index; null (unanswered) counts as missed.
    - shuffle returns a permutation with the same elements and does not mutate the input (input array identity/contents unchanged after call).
    - prepareQuiz returns questions with shuffled options whose isCorrect flags are preserved and never mutates the source questions.
  </behavior>
  <action>
    Create `src/lib/quiz-grading.test.ts` (SPDX header line 1). Import the not-yet-existing exports from `#/lib/quiz-grading`. Write `describe`/`it` cases for every behavior above. For shuffle purity, snapshot a copy of the input before calling and deep-equal the original afterward. For gradeAnswers, build QuizQuestion fixtures inline (text, options with isCorrect, explanation) typed via the `QuizQuestion` type from `#/schemas/node`. This task is RED — the module does not exist yet, so the run fails to import; that is expected.
  </action>
  <verify>
    <automated>npx vitest run src/lib/quiz-grading.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/quiz-grading.test.ts` exists and references `gradeQuiz`, `gradeAnswers`, `shuffle`, `prepareQuiz`.
    - Running the file fails on missing module/exports (RED state confirmed).
  </acceptance_criteria>
  <done>Failing tests pin the grading contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the grading engine (GREEN)</name>
  <files>src/lib/quiz-grading.ts</files>
  <read_first>
    - src/lib/patches.ts lines 1–62 (pure module structure: SPDX, typed const tables, JSDoc-per-export)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 304–325 (file structure), lines 447–471 (shuffle-by-value insight)
  </read_first>
  <action>
    Create `src/lib/quiz-grading.ts` (SPDX `GPL-3.0-or-later` header line 1; copyright line 2). Type-only import `QuizQuestion` from `#/schemas/node`. Export `PASS_THRESHOLD: Record<number, number>` = `{ 3: 3, 4: 3, 5: 4 }` with a JSDoc explaining the 3q→3/3 (NOT 2/3) decision per D-05. Export `gradeQuiz(total, correctCount): boolean` that throws when `PASS_THRESHOLD[total]` is undefined and otherwise returns `correctCount >= threshold`. Export `gradeAnswers(shuffledQuestions, selectedOptionIndexes): { passed; correctCount; missedIndexes }` that, per question, treats it correct only when the selected index is non-null and `options[selectedIdx].isCorrect === true`, accumulating correctCount and missedIndexes, then returns `gradeQuiz(shuffledQuestions.length, correctCount)` as `passed`. Export `shuffle<T>(arr): T[]` as Fisher-Yates on a COPY (`const a = [...arr]`) and `prepareQuiz(questions): QuizQuestion[]` that returns `shuffle(questions).map(q => ({ ...q, options: shuffle(q.options) }))`. Never mutate inputs. Keep the interface minimal (deep module).
  </action>
  <verify>
    <automated>npx vitest run src/lib/quiz-grading.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/quiz-grading.ts` exports `PASS_THRESHOLD`, `gradeQuiz`, `gradeAnswers`, `shuffle`, `prepareQuiz`.
    - `npx vitest run src/lib/quiz-grading.test.ts` exits 0 (all behaviors GREEN, including gradeQuiz(3,2)=false).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The grading engine passes its full test suite, with the 3-question 3/3 rule enforced and pure shuffles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (client-side grading) | Grading runs client-side; the server fn (06-05) is invoked only on a true pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | client-side quiz bypass (call pass without earning it) | low | accept | Documented tradeoff (ADR 010): quiz is self-honesty, not security-critical (no money/competitive consequence); server cannot verify grading without leaking the answer key. The UI gate (06-09) only fires the mutation when `passed===true`, tested there |
| T-06-08 | Information Disclosure | shuffle predictability | low | accept | Answers live in open-source GPL MDX content; there is no server-side secret to protect |
</threat_model>

<verification>
- `npx vitest run src/lib/quiz-grading.test.ts` passes.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
A pure grading engine deterministically enforces the D-05 pass thresholds (3/3, 3/4, 4/5), grades answers against shuffled questions by value, and shuffles without mutation. (QUIZ-02)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-04-SUMMARY.md` when done.
</output>
