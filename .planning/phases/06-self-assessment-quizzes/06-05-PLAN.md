---
phase: 06-self-assessment-quizzes
plan: 05
type: tdd
wave: 2
depends_on: [06-02]
files_modified:
  - src/server/quiz.ts
  - src/server/quiz.test.ts
autonomous: true
requirements: [QUIZ-02]
must_haves:
  truths:
    - "recordQuizPass stamps masteryState=\"mastered\" + source=\"quiz\" + patchId=CURRENT_PATCH server-side and writes userId from principal.id — never from client data (D-12/D-13)"
    - "Forged source/userId/masteryState/patchId in the request body are ignored (RecordQuizPassInput has only nodeId; Zod strips the rest)"
    - "A pass upserts BOTH nodeProgress (mastery) and quizProgress (passed=true, attemptCount+1, lastAttemptAt) keyed on (userId,nodeId)"
    - "recordQuizAttempt increments lapseCount only on a fail AFTER a prior pass (D-08, Pitfall 6)"
  artifacts:
    - path: "src/server/quiz.ts"
      provides: "RecordQuizPassInput, recordQuizPassHandler, recordQuizPass, RecordQuizAttemptInput, recordQuizAttemptHandler, recordQuizAttempt"
      contains: "authMiddleware"
    - path: "src/server/quiz.test.ts"
      provides: "server-stamp + input-rejection regression tests"
      contains: "recordQuizPassHandler"
  key_links:
    - from: "src/server/quiz.ts"
      to: "src/db/schema.ts quizProgress"
      via: "db.insert(quizProgress).onConflictDoUpdate target (userId,nodeId)"
      pattern: "quizProgress"
    - from: "src/server/quiz.ts"
      to: "src/lib/auth-middleware.ts"
      via: ".middleware([authMiddleware]) principal-keyed write (ADR 007)"
      pattern: "authMiddleware"
---

<objective>
Build the quiz-mastery write path as a principal-keyed `authedServerFn` (ADR 007), a direct parallel to `setNodeMasteryHandler`. `recordQuizPass` stamps `mastered` + `source:"quiz"` + `CURRENT_PATCH` server-side (D-12/D-13), upserting both `nodeProgress` (the mastery record) and `quizProgress` (the SRS signals). `recordQuizAttempt` handles the fail path so `lapseCount` is tracked (D-08, Pitfall 6). TDD: server-stamp regression tests first.

Purpose: The server fn must never trust client input for `userId`/`source`/`masteryState`/`patchId`. This is the security spine of QUIZ-02 — regression-tested exactly like `progress.test.ts`.
Output: `src/server/quiz.ts` + colocated security tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/server/progress.ts
@src/server/progress.test.ts
@src/lib/auth-middleware.ts
@src/lib/patches.ts
@docs/adr/007-authed-server-fn-authorization.md
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); `RecordQuizPassInput`/`recordQuizPassHandler`/`recordQuizPass`/`RecordQuizAttemptInput`/`recordQuizAttemptHandler`/`recordQuizAttempt` (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); quiz UI components + radio-group; extended `MasteryBadge`/`GraphNode`; demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing server-stamp tests (RED)</name>
  <files>src/server/quiz.test.ts</files>
  <read_first>
    - src/server/progress.test.ts lines 1–113 (fixtures + beforeEach vi.resetModules + vi.doMock wiring), lines 146–231 (stamps/ignores-forged-fields tests)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 274–301 (test copy guidance for two-table upsert)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 808–809 (QUIZ-02 security test rows)
  </read_first>
  <behavior>
    - recordQuizPassHandler stamps source="quiz" regardless of any source supplied in data.
    - recordQuizPassHandler stamps masteryState="mastered" regardless of any masteryState in data.
    - recordQuizPassHandler stamps patchId=CURRENT_PATCH.id regardless of any patchId in data.
    - recordQuizPassHandler writes userId from principal.id, ignoring any forged userId field in data.
    - A pass produces TWO insert/values calls (nodeProgress then quizProgress); quizProgress values carry passed=true and attemptCount=1 on first insert.
    - recordQuizAttemptHandler with passed=false, when a prior passed row exists, increments lapseCount; with no prior pass it does not.
  </behavior>
  <action>
    Create `src/server/quiz.test.ts` (SPDX header). Copy the `principalA` fixture and the `beforeEach` `vi.resetModules()` + `vi.doMock()` block from `progress.test.ts` verbatim, but mock `#/db/schema` to expose BOTH `nodeProgress` and `quizProgress`, and import the handler from `#/server/quiz`. Capture every `db.insert().values()` call (extend the captured-values array to length 2). Write `it` cases for each behavior above, passing intentionally forged `data` objects (`{ nodeId, userId: "forged", source: "manual", masteryState: "in-progress", patchId: "forged" }`) and asserting the captured nodeProgress write contains principal.id / "mastered" / "quiz" / CURRENT_PATCH.id. RED state expected (module absent).
  </action>
  <verify>
    <automated>npx vitest run src/server/quiz.test.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/quiz.test.ts` exists, uses `vi.doMock` + `vi.resetModules` (not hoisted `vi.mock`), and references `recordQuizPassHandler`.
    - Running the file fails on missing `#/server/quiz` (RED confirmed).
  </acceptance_criteria>
  <done>Failing security tests pin the server-stamp + input-rejection + two-table contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement quiz server functions (GREEN)</name>
  <files>src/server/quiz.ts</files>
  <read_first>
    - src/server/progress.ts lines 1–35 (module JSDoc + imports), lines 44–164 (SetNodeMasteryInput + handler + createServerFn chain — the exact template)
    - src/lib/auth-middleware.ts lines 60–108 (authedServerFn + the "createServerFn must be lexically visible" rule)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 195–270 (server fn template, two-table upsert), lines 611–628 (auth/server-stamp shared patterns)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 262–335 (Pattern 2), lines 694–698 (Pitfall 6 lapseCount)
  </read_first>
  <action>
    Create `src/server/quiz.ts` (SPDX header; module JSDoc copying the progress.ts authorization-contract style). Import `{ db }`, `{ nodeProgress, quizProgress }` from `#/db/schema`, `sql`, `crypto`, `createServerFn`, `authMiddleware`, `AuthedContext`, `CURRENT_PATCH`, `z`. Define `RecordQuizPassInput = z.object({ nodeId: z.string().min(1) })` — NO source/userId/masteryState/patchId fields. Implement `recordQuizPassHandler({ context, data })`: re-parse data with `RecordQuizPassInput.parse(data)`; upsert `nodeProgress` with `id: crypto.randomUUID()`, `userId: principal.id`, `nodeId`, `masteryState: "mastered"`, `source: "quiz"`, `patchId: CURRENT_PATCH.id`, `onConflictDoUpdate` target `[nodeProgress.userId, nodeProgress.nodeId]` setting masteryState/source/patchId/updatedAt via `sql` literals (mirror progress.ts); then upsert `quizProgress` with `passed: true`, `lastAttemptAt: new Date()`, `attemptCount: 1`, `lapseCount: 0`, `onConflictDoUpdate` target `[quizProgress.userId, quizProgress.nodeId]` setting `passed: sql\`true\``, `lastAttemptAt: sql\`now()\``, `attemptCount: sql\`${quizProgress.attemptCount} + 1\``, `updatedAt: sql\`now()\``. Return `{ ok: true }`. Also define `RecordQuizAttemptInput = z.object({ nodeId: z.string().min(1), passed: z.boolean() })` and `recordQuizAttemptHandler`: on `passed===true` delegate to the pass path; on `passed===false` upsert quizProgress incrementing `attemptCount` and conditionally incrementing `lapseCount` only when a prior `passed=true` row exists (use a SQL CASE or a read-then-write — `lapseCount = sql\`${quizProgress.lapseCount} + (case when ${quizProgress.passed} then 1 else 0 end)\``), and do NOT touch nodeProgress (a fail never changes mastery, D-12). Export `recordQuizPass` and `recordQuizAttempt` via `createServerFn({ method: "POST" }).middleware([authMiddleware]).validator(...).handler(...)` with the chain lexically visible (no factory wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/server/quiz.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/quiz.ts` contains `export const recordQuizPass = createServerFn(` and `.middleware([authMiddleware])`.
    - `RecordQuizPassInput` has only a `nodeId` field (no source/userId/masteryState/patchId).
    - `npx vitest run src/server/quiz.test.ts` exits 0 (all stamp + forge-rejection + lapseCount cases GREEN).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The principal-keyed quiz-pass write stamps mastered/quiz/CURRENT_PATCH, upserts both tables, rejects forged fields, and tracks lapseCount on fail-after-pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server fn | Untrusted request body crosses here; only `nodeId` is honored, identity comes from the session |
| server fn → Neon DB | Authorized, server-stamped writes to nodeProgress + quizProgress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Tampering | forged source/masteryState/patchId in body | high | mitigate | `RecordQuizPassInput` omits these fields; Zod strips extras; handler hardcodes `source:"quiz"`, `masteryState:"mastered"`, `patchId:CURRENT_PATCH.id`; regression-tested |
| T-06-05-02 | Elevation of Privilege | IDOR — write another user's progress via forged userId | high | mitigate | `userId` comes from `principal.id` (session via authMiddleware), never from data (ADR 007); regression-tested |
| T-06-05-03 | Tampering | calling recordQuizPass without completing the quiz | low | accept | Server cannot verify grading without the answer key; client gate (06-09) only fires on pass; documented in ADR 010 (self-honesty, not security-critical) |
| T-06-05-04 | Repudiation | quiz attempt history | low | accept | Minimal-data stance (D-07): only passed/lastAttemptAt/attemptCount/lapseCount stored; no per-attempt audit log by design (no-gamification) |
</threat_model>

<verification>
- `npx vitest run src/server/quiz.test.ts` passes (server-stamp, forge-rejection, two-table upsert, lapseCount).
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
Passing a quiz can only ever set the caller's own node to mastered with source "quiz" at the current patch, persisting SRS signals; forged identity/source/state are ignored. (QUIZ-02 / D-12 / D-13)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-05-SUMMARY.md` when done.
</output>
