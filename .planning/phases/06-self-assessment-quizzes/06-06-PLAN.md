---
phase: 06-self-assessment-quizzes
plan: 06
type: execute
wave: 2
depends_on: [06-03]
files_modified:
  - src/components/graph/MasteryBadge.tsx
  - src/components/graph/GraphNode.tsx
autonomous: true
requirements: [QUIZ-02]
must_haves:
  truths:
    - "When a node is mastered via quiz, the MasteryBadge reads 'Mastered · via quiz' instead of plain 'Mastered' (D-14 panel surfacing)"
    - "A quiz-mastered node shows a subtle rune-gold marker on the graph canvas, distinct from manual/auto mastery (D-14 canvas surfacing) — NOT a star/trophy/XP badge (anti-gamification)"
    - "The source distinction reads from the store sourceMap; quiz content never enters the GraphDisplayNode projection (ADR 002/005)"
  artifacts:
    - path: "src/components/graph/MasteryBadge.tsx"
      provides: "optional source prop + 'via quiz' label branch"
      contains: "via quiz"
    - path: "src/components/graph/GraphNode.tsx"
      provides: "sourceMap subscription + subtle quiz-mastered marker"
      contains: "sourceMap"
  key_links:
    - from: "src/components/graph/GraphNode.tsx"
      to: "src/lib/graph-store.ts sourceMap"
      via: "useGraphStore selector on sourceMap[d.id]"
      pattern: "sourceMap"
---

<objective>
Surface the quiz mastery source in two places (D-14): the detail-panel `MasteryBadge` gains an optional `source` prop that renders "Mastered · via quiz", and `GraphNode` subscribes to the store `sourceMap` to render a subtle rune-gold marker for quiz-mastered nodes on the canvas. The marker stays a source DISTINCTION, never a reward/score (anti-gamification, PROG-05).

Purpose: Criterion 2 requires the graph (not just the panel) to reflect that mastery came from a quiz, distinct from manual. The visual is the lightest possible indicator within the obsidian/rune-gold system.
Output: extended `MasteryBadge` and `GraphNode` with quiz-source visuals reading from the sourceMap.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/components/graph/MasteryBadge.tsx
@src/components/graph/GraphNode.tsx
@src/lib/graph-store.ts
@docs/adr/0001-visual-design-direction.md
@docs/adr/005-graph-display-node.md
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); quiz UI components + radio-group; extended `MasteryBadge` (source prop) + `GraphNode` (quiz marker); demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add 'via quiz' label to MasteryBadge</name>
  <files>src/components/graph/MasteryBadge.tsx</files>
  <read_first>
    - src/components/graph/MasteryBadge.tsx lines 15–72 (full component; the `state === "mastered"` branch)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 488–508 (props + label extension template)
  </read_first>
  <action>
    Add an optional `source?: string` prop to `MasteryBadgeProps`. In the `state === "mastered"` branch, render the label text as "Mastered · via quiz" when `source === "quiz"`, otherwise "Mastered". Keep all existing styles, the untouched/in-progress branches, and the component's render shape unchanged. Update the JSDoc to note D-14. SPDX header already present.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/MasteryBadge.tsx` accepts a `source` prop and renders the quiz-source label only when mastered + source quiz.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The panel badge distinguishes quiz mastery from other sources when mastered.</done>
</task>

<task type="auto">
  <name>Task 2: Add subtle quiz-mastered marker to GraphNode</name>
  <files>src/components/graph/GraphNode.tsx</files>
  <read_first>
    - src/components/graph/GraphNode.tsx lines 40 (MasteryBadge import), 98–123 (masteryStyles map + CVA variants), 180–232 (memo wrapper, data cast, top-right badge slot)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 512–548 (sourceMap subscription + marker template + design guardrail)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 910–913 (D-14 visual recommendation)
  </read_first>
  <action>
    Inside the `GraphNode` component, subscribe to the store for this node's source: `const masterySource = useGraphStore((s) => s.sourceMap[d.id])` (use the existing `useGraphStore` import; add it if absent). Do NOT add source to the node-data projection — read it from the store only (ADR 002/005). In the top-right slot alongside `<MasteryBadge>`, when `masterySource === "quiz" && masteryState === "mastered"`, render a single subtle rune marker: a `◆` glyph in a `<span aria-label="Mastered via quiz">` styled at ~9px, `color: var(--color-rune-400)`, `lineHeight: 1`, `flexShrink: 0`. Do NOT add a fourth masteryState/CVA variant — reuse the existing `mastered` styling and layer only the marker. Keep `React.memo` and existing rendering intact. The marker must remain a source distinction, never a star/trophy/score (PROG-05 anti-gamification).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/GraphNode.tsx` subscribes to `sourceMap` and conditionally renders the marker only when mastered + source quiz.
    - No new value is added to `MasteryStateSchema` / no `quiz` field added to the GraphDisplayNode projection.
    - `npx tsc --noEmit` exits 0; existing graph tests pass: `npx vitest run src/lib/graph-layout.test.ts` exits 0.
  </acceptance_criteria>
  <done>Quiz-mastered nodes carry a subtle, non-gamified canvas marker driven by the store sourceMap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation only; derives from already-authorized store state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Information Disclosure | quiz content leaking into graph projection | low | mitigate | GraphNode reads source from the store sourceMap only; quiz questions never enter GraphDisplayNode (ADR 002/005) |
| T-06-10 | (design) | gamification drift in the D-14 marker | low | mitigate | Marker constrained to a single 9px rune-gold glyph; explicit guardrail against star/trophy/XP (PROG-05); confirmed at end-of-phase human verify (06-10) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx vitest run src/lib/graph-layout.test.ts` passes (no graph regression).
- Quiz-source label + canvas marker render only for mastered + quiz source.
</verification>

<success_criteria>
A quiz-mastered node is visually distinguishable from manual mastery in both the panel badge and on the graph canvas, using a subtle non-gamified source distinction. (QUIZ-02 criterion 2 / D-14)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-06-SUMMARY.md` when done.
</output>
