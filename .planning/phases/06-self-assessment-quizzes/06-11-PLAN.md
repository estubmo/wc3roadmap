---
phase: 06-self-assessment-quizzes
plan: 11
type: execute
wave: 5
depends_on: [06-10, 06-06, 06-03]
files_modified:
  - src/components/graph/NodePanelContent.tsx
  - CONTEXT.md
  - docs/adr/010-quiz-mastery-design.md
autonomous: false
requirements: [QUIZ-01, QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "A CONCEPTUAL node panel shows the Take/Retake assessment CTA grouped with MasteryControls; a MECHANIC node panel shows no CTA (criterion 1)"
    - "Launching the quiz replaces the panel body so the node's learning prose/citations are NOT readable while answering (D-09 active recall, in-panel takeover)"
    - "On pass, the node becomes mastered with source labeled 'quiz' in the panel badge AND the graph node updates without a page reload (criterion 2)"
    - "Phase domain language + the quiz-mastery design decision are recorded (CONTEXT.md terms, ADR 010 incl. the documented quiz-bypass tradeoff)"
  artifacts:
    - path: "src/components/graph/NodePanelContent.tsx"
      provides: "QuizCTA mount + AnimatePresence in-panel takeover + MasteryBadge source"
      contains: "QuizTakeover"
    - path: "docs/adr/010-quiz-mastery-design.md"
      provides: "ADR for quiz-mastery design (source precedence, client grading tradeoff, forward-SRS)"
      contains: "ADR 010"
    - path: "CONTEXT.md"
      provides: "Phase-6 domain terms (quiz, assessment, active recall, quiz source, pass threshold, quiz mastery, lapse)"
      contains: "active recall"
  key_links:
    - from: "src/components/graph/NodePanelContent.tsx"
      to: "src/components/graph/quiz/QuizCTA.tsx + QuizTakeover.tsx"
      via: "renders QuizCTA (gated) and swaps body to QuizTakeover on start"
      pattern: "QuizTakeover"
    - from: "src/components/graph/NodePanelContent.tsx"
      to: "src/lib/graph-store.ts sourceMap"
      via: "reads currentSource for MasteryBadge + CTA label"
      pattern: "sourceMap"
---

<objective>
Wire the quiz into the live node panel and document the phase. In `NodePanelContent`: read `currentSource` from the store `sourceMap`, pass it to `MasteryBadge` (so a quiz-mastered node reads "Mastered · via quiz"), render `QuizCTA` grouped with `MasteryControls` (gated to CONCEPTUAL+quiz — criterion 1), and add an AnimatePresence in-panel takeover that REPLACES the panel body with `QuizTakeover` while answering so the node's learning prose/citations are not readable (D-09 active recall). Then record the Phase-6 domain terms in root `CONTEXT.md` and author `docs/adr/010-quiz-mastery-design.md`. Finish with an end-to-end human verification of criteria 1 and 2.

Purpose: This is the integration capstone that makes the quiz reachable and proves the no-reload mastery update (criterion 2). The ADR captures the source-precedence model and the accepted client-grading tradeoff.
Output: integrated panel, CONTEXT.md terms, ADR 010, and a human-verified end-to-end flow.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/components/graph/NodePanelContent.tsx
@src/components/graph/NodeDetailPanel.tsx
@src/components/graph/MasteryControls.tsx
@src/components/graph/MasteryBadge.tsx
@CONTEXT.md
@docs/adr/009-progress-persistence.md
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); `RadioGroup`/`RadioGroupItem`, `QuizQuestion`/`QuizStepper`/`QuizResults`/`QuizTakeover`/`QuizCTA` (src/components/*); extended `MasteryBadge`/`GraphNode`; demo quiz on content/nodes/tech-timing.mdx; `docs/adr/010-quiz-mastery-design.md`; Phase-6 CONTEXT.md terms; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Integrate QuizCTA + in-panel takeover into NodePanelContent</name>
  <files>src/components/graph/NodePanelContent.tsx</files>
  <read_first>
    - src/components/graph/NodePanelContent.tsx lines 220–431 (panel content; MasteryControls at ~339; the store selectors at ~227–229)
    - src/components/graph/NodeDetailPanel.tsx lines 40–158 (motion/AnimatePresence import + cross-fade pattern)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 426–463 (takeover + CTA placement + currentSource selector)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 497–536 (Pattern 7 in-panel takeover, D-09 active recall)
  </read_first>
  <action>
    Add local `const [quizOpen, setQuizOpen] = useState(false)`. Extend the existing store selector to also read `const currentSource = useGraphStore(useShallow((s) => s.sourceMap[nodeId]))`. Pass `source={currentSource}` to the existing `MasteryBadge` render. Wrap the panel body in `<AnimatePresence mode="wait">`: when `quizOpen` is true render a `motion.div key="quiz"` (opacity cross-fade, ~0.2s) containing `<QuizTakeover quiz={node.quiz} nodeId={nodeId} onClose={() => setQuizOpen(false)} />` so the node prose/citations are NOT mounted while answering (D-09); otherwise render a `motion.div key="content"` with the existing panel body PLUS `<QuizCTA nodeId={nodeId} nodeType={node.nodeType} hasQuiz={!!node.quiz} currentState={currentState} currentSource={currentSource} onStart={() => setQuizOpen(true)} />` placed immediately after `<MasteryControls>` (grouped, D-11). Import `motion`/`AnimatePresence` from `motion/react` (match NodeDetailPanel). Do not change unrelated panel behavior; keep `React.memo`/existing structure.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `NodePanelContent` renders `QuizCTA` after `MasteryControls` and swaps the body to `QuizTakeover` (inside AnimatePresence) when quizOpen.
    - `MasteryBadge` receives `source={currentSource}`.
    - While quizOpen, the node body/citations are not rendered (takeover replaces them — D-09).
    - `npx tsc --noEmit` exits 0; full `npx vitest run` exits 0; `npm run build:content` exits 0.
  </acceptance_criteria>
  <done>The quiz is reachable from CONCEPTUAL panels, the takeover hides the node text while answering, and the badge labels quiz mastery.</done>
</task>

<task type="auto">
  <name>Task 2: Record CONTEXT.md domain terms + author ADR 010</name>
  <files>CONTEXT.md, docs/adr/010-quiz-mastery-design.md</files>
  <read_first>
    - CONTEXT.md (root — existing domain-language structure / Phase-5 additions)
    - docs/adr/009-progress-persistence.md (ADR format + the source-field precedence prior art)
    - .planning/phases/06-self-assessment-quizzes/06-CONTEXT.md lines 99–102 (terms + ADR-010 instruction)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 884–888 (terms to add), lines 850–858 (quiz-bypass tradeoff for the ADR)
  </read_first>
  <action>
    Append Phase-6 domain terms to root `CONTEXT.md` matching the existing entry style: `quiz`, `assessment`, `active recall`, `quiz source`, `pass threshold`, `quiz mastery`, `lapse` (each a one-line definition grounded in this phase). Author `docs/adr/010-quiz-mastery-design.md` following the ADR format used by 009: Status (Accepted), Context, Decision, Consequences. Capture: the quiz-mastery write reuses the principal-keyed authedServerFn stamping mastered+source:"quiz"+CURRENT_PATCH (D-12/D-13); latest-write-wins source precedence (quiz only ever sets mastered; manual can override to any state); the separate `quizProgress` table with forward-SRS signals (passed/lastAttemptAt/attemptCount/lapseCount) and the deferred scheduler (D-08); the deliberate `lapseCount` addition; and the ACCEPTED client-side-grading bypass tradeoff (self-honesty, not security-critical — server cannot verify without leaking the answer key). SPDX/license note not required for markdown docs.
  </action>
  <verify>
    <automated>test -f docs/adr/010-quiz-mastery-design.md && grep -qi "active recall" CONTEXT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/010-quiz-mastery-design.md` exists with Status/Context/Decision/Consequences and documents the client-grading tradeoff + source precedence.
    - `CONTEXT.md` contains the seven Phase-6 terms including "active recall" and "lapse".
  </acceptance_criteria>
  <done>Phase-6 domain language and the quiz-mastery design rationale are recorded for downstream phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human verification (criteria 1 and 2)</name>
  <action>Pause execution and request human verification of the running app against ROADMAP Phase 6 criteria 1 (CONCEPTUAL-only CTA) and 2 (pass → mastered/quiz, no reload), per the how-to-verify steps. Do not mark the phase complete until the human types "approved".</action>
  <what-built>The full quiz flow wired into the node panel: a gated Take/Retake assessment CTA, an in-panel takeover that hides node text during answering, client-side grading, and a quiz pass that drives mastery to mastered with source "quiz" — reflected in the panel badge and on the graph canvas without a page reload.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the graph.
    2. Open a MECHANIC node (e.g. army-positioning) — confirm NO "Take Assessment" button appears (criterion 1).
    3. Open the tech-timing CONCEPTUAL node — confirm a "Take Assessment" button appears grouped with the mastery controls (criterion 1).
    4. Click it — confirm the node's learning prose/citations are no longer readable while the quiz is open (D-09 active recall), questions appear one at a time, and Next is disabled until you pick an answer.
    5. Answer to PASS — confirm: results reveal any missed questions with explanations (no score/points shown); the node's mastery badge now reads "Mastered · via quiz"; the graph node shows the subtle quiz marker; and NONE of this required a page reload (criterion 2).
    6. Fail a retake — confirm mastery does NOT downgrade (D-12) and a reshuffle occurs on retry (D-06).
    7. Reopen the panel — confirm the CTA now reads "Retake assessment" and is still available (D-15).
  </how-to-verify>
  <resume-signal>Type "approved" if criteria 1 and 2 hold (CONCEPTUAL-only CTA + pass→mastered/quiz with no reload), or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → server fn | The panel mounts the takeover which triggers the principal-keyed mutation on pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Information Disclosure | node answers readable during quiz (defeats active recall) | medium | mitigate | In-panel takeover REPLACES (does not overlay) the body — node prose/citations are unmounted while quizOpen (D-09); verified at the human checkpoint |
| T-06-21 | Tampering | CTA shown on ineligible nodes | medium | mitigate | QuizCTA self-gates on CONCEPTUAL+quiz (06-10, unit-tested); confirmed in the panel at the human checkpoint |
| T-06-22 | (correctness) | mastery update requires reload | high | mitigate | Optimistic single-node re-render via the hook (06-08) + sourceMap; verified no-reload at the human checkpoint (criterion 2) |
</threat_model>

<verification>
- `npx tsc --noEmit`, full `npx vitest run`, and `npm run build:content` all pass.
- Human verify confirms criterion 1 (CONCEPTUAL-only CTA) and criterion 2 (pass → mastered/quiz, no reload).
- ADR 010 + CONTEXT.md terms recorded.
</verification>

<success_criteria>
The end-to-end quiz experience is reachable, hides node text during answering, and drives a CONCEPTUAL node to mastered with source "quiz" — reflected in panel and canvas without a reload, with the design recorded in ADR 010. (QUIZ-01, QUIZ-02, QUIZ-03)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-11-SUMMARY.md` when done.
</output>
