---
phase: 06-self-assessment-quizzes
plan: 07
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - content/nodes/tech-timing.mdx
autonomous: false
requirements: [QUIZ-01, QUIZ-03]
must_haves:
  truths:
    - "At least one CONCEPTUAL node (tech-timing) ships a real, application-framed quiz proving the end-to-end flow (D-01 demo)"
    - "The demo quiz validates against QuizSchema (3–5 questions, exactly-one-correct, required explanation) and the build passes (QUIZ-01)"
    - "Each question is scenario/application-framed — a player cannot answer correctly by re-reading the node's surface text; an SME agrees it tests understanding (QUIZ-03, criterion 3, non-inferable → human review)"
  artifacts:
    - path: "content/nodes/tech-timing.mdx"
      provides: "authored quiz frontmatter (3–5 scenario questions w/ explanations)"
      contains: "quiz:"
  key_links:
    - from: "content/nodes/tech-timing.mdx quiz"
      to: "content-collections.ts + src/schemas/node.ts QuizSchema"
      via: "build-time validation of the authored quiz"
      pattern: "quiz"
---

<objective>
Author a real demonstration quiz in the `tech-timing` CONCEPTUAL node's MDX frontmatter to prove the end-to-end flow (D-01) and to anchor QUIZ-03. The questions must be application/scenario-framed ("opponent does X — what do you do?") so they cannot be answered by re-reading the node's surface prose, and each must carry a substantive `explanation`. A human SME confirms the questions test understanding, not surface recall (criterion 3 — non-inferable, requires expert judgment).

Purpose: Phase 6 builds the engine + schema + ONE demo quiz, not the full corpus (bulk authoring pairs with the Phase 9 content gate). This node is the proof that authoring → validation → quiz-taking → mastery works.
Output: a validated, SME-approved quiz on `content/nodes/tech-timing.mdx`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@content/nodes/tech-timing.mdx
@src/schemas/node.ts
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); quiz UI components + radio-group; extended `MasteryBadge`/`GraphNode`; demo quiz authored on content/nodes/tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author the tech-timing demo quiz</name>
  <files>content/nodes/tech-timing.mdx</files>
  <read_first>
    - content/nodes/tech-timing.mdx (full file — frontmatter + body prose so questions can be written to be NON-inferable from the surface text)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 704–725 (example quiz frontmatter shape + scenario framing)
    - .planning/phases/06-self-assessment-quizzes/06-CONTEXT.md lines 136–144 (application-framing requirement)
  </read_first>
  <action>
    Add a `quiz:` field to the tech-timing frontmatter with 3 to 4 multiple-choice questions. Each question: a `text` that poses an in-game SCENARIO (e.g. "Your scout reveals the opponent has skipped Tier 2 and is massing Tier-1 units while expanding — what does correct tech timing dictate?") rather than a definition; an `options` list (3–4 options) with exactly ONE `isCorrect: true`; and a non-empty `explanation` that justifies WHY the correct option is right in terms of the leverage/asymmetry concepts from the node body. Ensure the correct answer requires applying the concept, NOT matching a phrase verbatim from the prose. Keep YAML valid and consistent with the existing frontmatter indentation. Do not alter the existing node body or other frontmatter fields.
  </action>
  <verify>
    <automated>npm run build:content</automated>
  </verify>
  <acceptance_criteria>
    - `content/nodes/tech-timing.mdx` contains a `quiz:` block with 3–4 questions, each with exactly one correct option and a non-empty explanation.
    - `npm run build:content` exits 0 (quiz validates against the schema and transform count guard).
    - `npm run validate` exits 0.
  </acceptance_criteria>
  <done>The demo quiz is authored and passes build-time validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SME review of demo quiz (criterion 3)</name>
  <action>Pause execution and request a subject-matter review that each demo quiz question is application/scenario-framed and cannot be answered by re-reading the node's surface text (criterion 3 / QUIZ-03). Do not proceed until the human types "approved" or lists revisions.</action>
  <what-built>An application-framed quiz on the tech-timing CONCEPTUAL node (3–4 scenario questions, each with one correct answer + explanation), validated by the content pipeline.</what-built>
  <how-to-verify>
    1. Open `content/nodes/tech-timing.mdx` and read the `quiz:` block alongside the node body prose.
    2. For each question, confirm: it poses a scenario/decision (not a definition), and the correct answer CANNOT be found by string-matching the node's surface text — it requires applying the concept.
    3. Confirm each `explanation` genuinely explains why the answer is right (not a restatement of the question).
    4. As a subject-matter check (criterion 3): would a knowledgeable WC3 player agree these test understanding rather than recall? If any question is answerable by skimming the prose, note which one and what to change.
  </how-to-verify>
  <resume-signal>Type "approved" if the quiz tests understanding (criterion 3 satisfied), or list the questions to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX author → build pipeline | Quiz text is version-controlled content, same trust level as existing citations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | XSS via quiz question/option text | low | accept | Quiz text is authored in version-controlled MDX (not user-submitted); same trust + compile pipeline as citation text; no extra sanitization needed |
| T-06-12 | (quality) | surface-recall question that fails QUIZ-03 | medium | mitigate | Blocking human SME checkpoint reviews each question for application-framing before approval |
</threat_model>

<verification>
- `npm run build:content` passes (quiz schema + count guard).
- `npm run validate` passes.
- Human SME approves the questions test understanding (criterion 3).
</verification>

<success_criteria>
The tech-timing node ships a validated, SME-approved quiz of scenario-framed questions that cannot be answered by re-reading the node — proving the authoring-to-mastery flow end to end. (QUIZ-01, QUIZ-03)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-07-SUMMARY.md` when done.
</output>
