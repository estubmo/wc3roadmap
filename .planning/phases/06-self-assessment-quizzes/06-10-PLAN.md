---
phase: 06-self-assessment-quizzes
plan: 10
type: execute
wave: 4
depends_on: [06-09, 06-04, 06-08, 06-03]
files_modified:
  - src/components/graph/quiz/QuizCTA.tsx
  - src/components/graph/quiz/QuizCTA.test.tsx
  - src/components/graph/quiz/QuizTakeover.tsx
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "QuizCTA renders ONLY for CONCEPTUAL nodes that have a quiz; MECHANIC nodes and quiz-less CONCEPTUAL nodes render nothing (D-04, criterion 1)"
    - "The CTA label is 'Take Assessment', becoming 'Retake assessment' once the node is mastered via quiz; it is available regardless of mastery state (D-11/D-15)"
    - "QuizTakeover steps through shuffled questions one at a time, grades on completion, reveals missed+explanations, and fires the quiz-pass mutation ONLY on a true pass (Pitfall 5)"
    - "Retry reshuffles question and answer order (D-06)"
  artifacts:
    - path: "src/components/graph/quiz/QuizCTA.tsx"
      provides: "self-gating Take/Retake assessment CTA (D-04/D-11/D-15)"
      contains: "QuizCTA"
    - path: "src/components/graph/quiz/QuizCTA.test.tsx"
      provides: "D-04 gating tests (MECHANIC/no-quiz/CONCEPTUAL+quiz)"
      contains: "QuizCTA"
    - path: "src/components/graph/quiz/QuizTakeover.tsx"
      provides: "stepper→grade→results flow host + mutation on pass"
      contains: "useQuizPassMutation"
  key_links:
    - from: "src/components/graph/quiz/QuizTakeover.tsx"
      to: "src/lib/quiz-grading.ts + src/hooks/useQuizPassMutation.ts"
      via: "prepareQuiz/gradeAnswers then mutate({ nodeId }) only when passed"
      pattern: "useQuizPassMutation"
---

<objective>
Assemble the stateful quiz flow: `QuizCTA` (self-gating "Take Assessment"/"Retake assessment" button — D-04/D-11/D-15) with colocated gating tests, and `QuizTakeover` (the in-panel takeover host that shuffles questions, steps through them one at a time, grades on completion, reveals missed questions + explanations, supports retry-with-reshuffle, and fires `useQuizPassMutation` ONLY on a true pass — Pitfall 5).

Purpose: This is where the engine (grading), the persistence seam (mutation), and the primitives (stepper/question/results) come together into the active-recall experience. The CTA gating directly realizes criterion 1; the pass-only mutation gate protects criterion 2.
Output: `QuizCTA.tsx` + `QuizCTA.test.tsx` + `QuizTakeover.tsx`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/components/graph/MasteryControls.tsx
@src/components/graph/MasteryControls.test.tsx
@src/lib/quiz-grading.ts
@src/hooks/useQuizPassMutation.ts
@src/schemas/node.ts
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); `RadioGroup`/`RadioGroupItem`, `QuizQuestion`/`QuizStepper`/`QuizResults`/`QuizTakeover`/`QuizCTA` (src/components/*); extended `MasteryBadge`/`GraphNode`; demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build QuizCTA (self-gating) + gating tests</name>
  <files>src/components/graph/quiz/QuizCTA.tsx, src/components/graph/quiz/QuizCTA.test.tsx</files>
  <read_first>
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 467–485 (CTA label logic + button style), lines 444–457 (gating condition)
    - src/components/graph/MasteryControls.test.tsx (component test style: `@vitest-environment jsdom` directive, render + query)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 815–817 (D-04 component test rows)
  </read_first>
  <behavior>
    - nodeType="MECHANIC" → renders nothing (null), regardless of quiz presence.
    - nodeType="CONCEPTUAL" + hasQuiz=false → renders nothing.
    - nodeType="CONCEPTUAL" + hasQuiz=true → renders a button.
    - currentState="mastered" + currentSource="quiz" → label "Retake assessment"; otherwise "Take Assessment".
    - Button is rendered regardless of currentState (untouched/in-progress/mastered) when CONCEPTUAL + hasQuiz (D-15).
    - Clicking the button calls onStart.
  </behavior>
  <action>
    Create `src/components/graph/quiz/QuizCTA.tsx` (SPDX header). Props: `nodeId: string`, `nodeType: "MECHANIC" | "CONCEPTUAL"`, `hasQuiz: boolean`, `currentState: MasteryState`, `currentSource?: string`, `onStart: () => void`. Return `null` unless `nodeType === "CONCEPTUAL" && hasQuiz` (D-04). Compute `const hasQuizPassed = currentState === "mastered" && currentSource === "quiz"` and label the button "Retake assessment" when true, else "Take Assessment" (D-11/D-15). Style the button with obsidian/rune tokens to read as a distinct CTA grouped with mastery controls (copy the inactive-toggle inline-style token approach from MasteryControls). Call `onStart` on click. Create `src/components/graph/quiz/QuizCTA.test.tsx` with the `@vitest-environment jsdom` directive at file top; cover every behavior above using render + `queryByRole("button")`/`getByText` assertions and a click → onStart spy.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/quiz/QuizCTA.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `QuizCTA` returns null for MECHANIC and for CONCEPTUAL-without-quiz; renders a button for CONCEPTUAL-with-quiz.
    - Label switches to "Retake assessment" only when mastered via quiz.
    - `npx vitest run src/components/graph/quiz/QuizCTA.test.tsx` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The CTA self-gates to CONCEPTUAL+quiz nodes (criterion 1) with the correct dynamic label, proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build QuizTakeover flow host</name>
  <files>src/components/graph/quiz/QuizTakeover.tsx</files>
  <read_first>
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 497–536 (Pattern 7 in-panel takeover), lines 688–692 (Pitfall 5 pass-only mutation)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 556–563 (QuizTakeover sketch)
    - src/components/graph/quiz/QuizStepper.tsx + QuizResults.tsx (consumed primitives), src/lib/quiz-grading.ts, src/hooks/useQuizPassMutation.ts
  </read_first>
  <action>
    Create `src/components/graph/quiz/QuizTakeover.tsx` (SPDX header). Props: `quiz: Quiz` (the node's questions), `nodeId: string`, `onClose: () => void`. On mount/start, compute `const [shuffled, setShuffled] = useState(() => prepareQuiz(quiz))`. Hold `currentIndex` and `selectedOptionIndexes: (number|null)[]` (init nulls) and a `phase: "quiz" | "results"` state. Render `QuizStepper` for the current question (wire selectedIndex/onSelect/onNext; advancing past the last question computes the grade). On reaching results: call `gradeAnswers(shuffled, selectedOptionIndexes)` → `{ passed, correctCount, missedIndexes }`; if `passed` call `useQuizPassMutation().mutate({ nodeId })` EXACTLY ONCE (Pitfall 5 — never call on fail); render `QuizResults` with `passed`, `correctCount`, `total`, the missed questions mapped from `missedIndexes` to `{ text, explanation }`, and `onRetry` that re-runs `prepareQuiz(quiz)` (reshuffle, D-06), resets indexes/selections, and returns to phase "quiz". Provide a close affordance calling `onClose`. Keep all node learning prose out of this component (it is rendered in place of the panel body by 06-11 — D-09 active recall). Use CSS-variable tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `QuizTakeover` uses `prepareQuiz` + `gradeAnswers` and calls `useQuizPassMutation().mutate` only when `passed === true`.
    - Retry reshuffles via `prepareQuiz` and resets selections.
    - `npx tsc --noEmit` exits 0; `npx vitest run` (full suite) exits 0.
  </acceptance_criteria>
  <done>The takeover host runs the one-at-a-time graded flow, reveals missed+explanations, reshuffles on retry, and persists mastery only on a true pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → server fn | The takeover decides pass/fail client-side and triggers the principal-keyed mutation only on pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Tampering | mutation fired on a failed quiz | medium | mitigate | QuizTakeover calls `mutate` only inside the `if (passed)` branch; grading is deterministic (06-04); reinforced by Pitfall-5 awareness; the server still stamps mastered unconditionally so this client gate is the protection |
| T-06-18 | (correctness) | double-fire of the pass mutation | low | mitigate | Mutation fires once on transition into results; guarded by phase state so re-renders don't re-trigger |
| T-06-19 | Tampering | MECHANIC/quiz-less node showing a CTA | medium | mitigate | QuizCTA self-gates on `CONCEPTUAL && hasQuiz` (D-04), unit-tested |
</threat_model>

<verification>
- `npx vitest run src/components/graph/quiz/QuizCTA.test.tsx` passes (D-04 gating).
- `npx tsc --noEmit` and full `npx vitest run` pass.
</verification>

<success_criteria>
The CTA appears only for CONCEPTUAL quiz nodes with the correct dynamic label, and the takeover runs a shuffled, one-at-a-time graded quiz that reveals explanations and persists mastery only on a genuine pass. (QUIZ-01, QUIZ-02, QUIZ-03)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-10-SUMMARY.md` when done.
</output>
