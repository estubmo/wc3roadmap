---
phase: 06-self-assessment-quizzes
plan: 09
type: execute
wave: 3
depends_on: [06-01]
files_modified:
  - src/components/ui/radio-group.tsx
  - src/components/graph/quiz/QuizQuestion.tsx
  - src/components/graph/quiz/QuizStepper.tsx
  - src/components/graph/quiz/QuizResults.tsx
autonomous: true
requirements: [QUIZ-01, QUIZ-03]
must_haves:
  truths:
    - "A single quiz question renders its options as an accessible radio group (one selectable answer, arrow-key nav) — one question at a time (D-10)"
    - "The stepper shows progress ('Question i of N') with Next disabled until an option is selected; no per-question score is revealed mid-quiz (D-10)"
    - "The results screen reveals which questions were missed plus their explanations and offers a retry; it shows NO score percentage/points (D-06, anti-gamification)"
  artifacts:
    - path: "src/components/ui/radio-group.tsx"
      provides: "shadcn RadioGroup/RadioGroupItem (Radix)"
      contains: "RadioGroup"
    - path: "src/components/graph/quiz/QuizQuestion.tsx"
      provides: "accessible MCQ question (fieldset/legend + RadioGroup)"
      contains: "RadioGroup"
    - path: "src/components/graph/quiz/QuizStepper.tsx"
      provides: "one-question-at-a-time stepper (D-10)"
      contains: "QuizStepper"
    - path: "src/components/graph/quiz/QuizResults.tsx"
      provides: "missed-questions + explanations + retry (D-06)"
      contains: "QuizResults"
  key_links:
    - from: "src/components/graph/quiz/QuizQuestion.tsx"
      to: "src/components/ui/radio-group.tsx"
      via: "imports RadioGroup/RadioGroupItem for option selection"
      pattern: "radio-group"
---

<objective>
Build the presentational quiz primitives: install the shadcn `radio-group` (Radix, no new npm install), then `QuizQuestion` (one accessible MCQ via RadioGroup), `QuizStepper` (one-question-at-a-time progress with a gated Next — D-10), and `QuizResults` (reveal missed questions + explanations + retry — D-06, no score/points). These are pure presentational components (props in, callbacks out); the stateful host wires them in 06-10.

Purpose: Keep the quiz UI as small, testable, accessible primitives consuming CSS-variable tokens — the input layer of the active-recall surface.
Output: `radio-group.tsx` + `QuizQuestion.tsx` + `QuizStepper.tsx` + `QuizResults.tsx`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/components/graph/MasteryControls.tsx
@src/schemas/node.ts
@docs/adr/0001-visual-design-direction.md
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); `RadioGroup`/`RadioGroupItem` (src/components/ui/radio-group.tsx), `QuizQuestion`/`QuizStepper`/`QuizResults`/`QuizTakeover`/`QuizCTA` (src/components/graph/quiz/*); extended `MasteryBadge`/`GraphNode`; demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn radio-group + build QuizQuestion</name>
  <files>src/components/ui/radio-group.tsx, src/components/graph/quiz/QuizQuestion.tsx</files>
  <read_first>
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 538–574 (Pattern 8 accessible RadioGroup), lines 99–106 (shadcn add command, no npm install)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 572–579 (QuizQuestion sketch), lines 658–666 (CSS token convention)
    - src/components/graph/MasteryControls.tsx lines 73–144 (inline-style CSS-variable token pattern)
  </read_first>
  <action>
    Run `npx shadcn@latest add radio-group` to write `src/components/ui/radio-group.tsx` (no npm install — `@radix-ui/react-radio-group` is already in node_modules). If the CLI declines non-interactively, author the component manually from the Radix RadioGroup primitive matching the existing shadcn component style in this repo. Then create `src/components/graph/quiz/QuizQuestion.tsx` (SPDX header). Props: `question: QuizQuestion`, `selectedIndex: number | null`, `onSelect: (i: number) => void`. Render a `<fieldset>` with a `<legend>` for `question.text` and a `RadioGroup` (`aria-label={question.text}`, `value` = the selected index as string or undefined, `onValueChange` → `onSelect(Number(v))`) mapping `question.options` to `RadioGroupItem` + a plain `<label htmlFor>` (do NOT install the shadcn label component — use a plain label, per RESEARCH Open Question 3). Use CSS-variable tokens only (no hardcoded hex). Type-import `QuizQuestion` from `#/schemas/node`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/radio-group.tsx` exists exporting `RadioGroup` + `RadioGroupItem`.
    - `src/components/graph/quiz/QuizQuestion.tsx` renders a fieldset/legend + RadioGroup with one selectable option and an aria-label.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>An accessible single-question MCQ component renders options as a Radix radio group.</done>
</task>

<task type="auto">
  <name>Task 2: Build QuizStepper and QuizResults</name>
  <files>src/components/graph/quiz/QuizStepper.tsx, src/components/graph/quiz/QuizResults.tsx</files>
  <read_first>
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 565–589 (QuizStepper + QuizResults sketches; styling/anti-gamification notes)
    - .planning/phases/06-self-assessment-quizzes/06-CONTEXT.md lines 45–48 (D-09/D-10 one-at-a-time + results-at-end)
    - src/schemas/node.ts (QuizQuestion type for props)
  </read_first>
  <action>
    Create `src/components/graph/quiz/QuizStepper.tsx` (SPDX header). Props: `question`, `questionIndex`, `totalQuestions`, `selectedIndex`, `onSelect`, `onNext`. Render `QuizQuestion` plus a progress label "Question {questionIndex+1} of {totalQuestions}" and a Next button DISABLED until `selectedIndex !== null`. On the last question the Next button label becomes "See results". No per-question correctness reveal mid-quiz (D-10). Create `src/components/graph/quiz/QuizResults.tsx` (SPDX header). Props: `passed: boolean`, `correctCount: number`, `total: number`, `missedQuestions: { text: string; explanation: string }[]`, `onRetry: () => void`. Render a pass/fail headline (text only — NO percentage, NO points, NO streak — anti-gamification PROG-05), the list of missed questions with each `explanation`, and a "Retry" button calling `onRetry`. Do NOT display the correct-answer text (active-recall principle — show only the explanation). Use CSS-variable tokens only.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `QuizStepper` renders a single question with a Next button disabled until a selection is made and a "Question i of N" progress label.
    - `QuizResults` lists missed questions + explanations and a Retry button, with no score percentage/points text.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The stepper (one-at-a-time) and results (missed + explanations + retry, no score) presentational components compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational components; no I/O, no auth, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | (a11y/quality) | inaccessible custom radio | low | mitigate | Use Radix RadioGroup (role=radio, aria-checked, arrow-key nav) via shadcn; fieldset/legend per question |
| T-06-16 | (design) | gamification drift in results | low | mitigate | QuizResults shows no score/percentage/points; explicit anti-gamification constraint (PROG-05) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- radio-group, QuizQuestion, QuizStepper, QuizResults compile and follow token + accessibility conventions.
</verification>

<success_criteria>
The quiz input layer renders one accessible question at a time with a gated Next, and a results screen that reveals missed questions + explanations with a retry and no score. (QUIZ-01 / D-06 / D-10)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-09-SUMMARY.md` when done.
</output>
