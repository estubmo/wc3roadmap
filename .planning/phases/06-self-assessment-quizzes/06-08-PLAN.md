---
phase: 06-self-assessment-quizzes
plan: 08
type: execute
wave: 3
depends_on: [06-05, 06-03]
files_modified:
  - src/hooks/useQuizPassMutation.ts
autonomous: true
requirements: [QUIZ-02]
must_haves:
  truths:
    - "On a quiz pass the hook optimistically sets the node to mastered AND sets its source to quiz, re-rendering only that node — no page reload (criterion 2)"
    - "Signed-in passes persist via recordQuizPass (authedServerFn); signed-out passes persist mastered via setLocalMastery (mirrors useProgressMutation dual path)"
    - "On error the optimistic mastery AND source are rolled back and an error toast is shown; on settle the per-user progress query is invalidated (signed-in only)"
  artifacts:
    - path: "src/hooks/useQuizPassMutation.ts"
      provides: "useQuizPassMutation hook"
      contains: "useQuizPassMutation"
  key_links:
    - from: "src/hooks/useQuizPassMutation.ts"
      to: "src/server/quiz.ts recordQuizPass"
      via: "mutationFn calls recordQuizPass({ data: { nodeId } }) when signed-in"
      pattern: "recordQuizPass"
    - from: "src/hooks/useQuizPassMutation.ts"
      to: "src/lib/graph-store.ts setSource/setNodeMastery"
      via: "optimistic onMutate + rollback onError"
      pattern: "setSource"
---

<objective>
Wire the quiz pass to the UI state with a TanStack Query mutation that mirrors the Phase-5 `useProgressMutation` optimistic pattern, adding the `source` dimension. On pass: optimistically set the node to `mastered` and its source to `quiz` (single-node re-render, no reload — criterion 2), persist via `recordQuizPass` when signed-in or `setLocalMastery` when signed-out, roll back both on error, and invalidate the per-user progress query on settle.

Purpose: This is the no-reload seam of criterion 2. The dual signed-in/signed-out path matches the established mastery-write hook so quiz mastery behaves identically to manual mastery, plus the quiz source label.
Output: `src/hooks/useQuizPassMutation.ts`.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-assessment-quizzes/06-CONTEXT.md
@.planning/phases/06-self-assessment-quizzes/06-RESEARCH.md
@.planning/phases/06-self-assessment-quizzes/06-PATTERNS.md
@src/hooks/useProgressMutation.ts
@src/lib/graph-store.ts
@src/server/quiz.ts
@src/lib/progress-keys.ts
@src/lib/local-progress.ts
</context>

<artifacts_produced>
This phase introduces (drift-exclusion list): quiz schemas + `quiz?` (src/schemas/node.ts) + mirror (content-collections.ts); `source` value `"quiz"` (src/schemas/progress.ts); `quizProgress` table (src/db/schema.ts); grading engine (src/lib/quiz-grading.ts); quiz server fns (src/server/quiz.ts); `sourceMap`/`setSource`/`initSourceMap` (src/lib/graph-store.ts); `useQuizPassMutation` (src/hooks/useQuizPassMutation.ts); quiz UI components + radio-group; extended `MasteryBadge`/`GraphNode`; demo quiz on tech-timing.mdx; ADR 010; quiz tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Implement useQuizPassMutation</name>
  <files>src/hooks/useQuizPassMutation.ts</files>
  <read_first>
    - src/hooks/useProgressMutation.ts lines 72–146 (the exact dual-path optimistic template: mutationFn session branch, onMutate snapshot+update, onError rollback+toast, onSettled invalidate)
    - .planning/phases/06-self-assessment-quizzes/06-PATTERNS.md lines 367–400 (useQuizPassMutation deltas), lines 630–656 (shared optimistic/rollback/invalidation patterns)
    - .planning/phases/06-self-assessment-quizzes/06-RESEARCH.md lines 727–765 (hook example)
  </read_first>
  <action>
    Create `src/hooks/useQuizPassMutation.ts` (SPDX header). Mirror `useProgressMutation` structure with these deltas: (1) mutation vars are `{ nodeId: string }` only; (2) `mutationFn` branches on `useSession()` — signed-in calls `recordQuizPass({ data: { nodeId } })` (no masteryState/source passed); signed-out calls `setLocalMastery(nodeId, "mastered")` and returns; (3) `onMutate` cancels `progressKeys.byUser()`, snapshots BOTH `previousState = masteryMap[nodeId]` and `previousSource = sourceMap[nodeId]`, then calls `setNodeMastery(nodeId, "mastered")` and `setSource(nodeId, "quiz")`, returning `{ previousState, previousSource, nodeId }`; (4) `onError` rolls back `setNodeMastery(nodeId, previousState ?? "untouched")` and `setSource(nodeId, previousSource ?? <unset>)`, then shows `toast.error("Couldn't save quiz result", { description: "Your mastery state has been reverted." })` (copy the toast pattern, keep an optional Retry action like the analog); (5) `onSettled` invalidates `progressKeys.byUser()` only when signed-in. Note in a comment that signed-out source persistence is session-only (local-progress does not track source — out of scope this phase, mirrors the existing local mastery model).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useQuizPassMutation.ts` exports `useQuizPassMutation`, calls `recordQuizPass` (signed-in) and `setLocalMastery` (signed-out), and optimistically calls both `setNodeMastery(...,"mastered")` and `setSource(...,"quiz")`.
    - `onError` rolls back both mastery and source; `onSettled` invalidates `progressKeys.byUser()` when signed-in.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The quiz-pass mutation optimistically masters + sources the node with no reload and rolls back cleanly on error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server fn | Hook sends only `{ nodeId }` to the principal-keyed recordQuizPass; identity/source/state are server-stamped (06-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Tampering | client supplies extra fields to recordQuizPass | low | mitigate | Hook passes only `{ nodeId }`; server validator strips extras and server-stamps the rest (06-05) |
| T-06-14 | (correctness) | optimistic state stuck after failed write | medium | mitigate | onError rolls back both masteryMap and sourceMap to the snapshot; onSettled re-syncs from server truth (signed-in) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Optimistic update + rollback verified end-to-end at the 06-10 human checkpoint.
</verification>

<success_criteria>
A quiz pass updates the affected node to mastered with source quiz immediately (no reload), persists per session state, and reverts safely on failure. (QUIZ-02 criterion 2)
</success_criteria>

<output>
Create `.planning/phases/06-self-assessment-quizzes/06-08-SUMMARY.md` when done.
</output>
