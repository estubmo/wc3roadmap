---
phase: 02-graph-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components.json
  - src/styles/app.css
  - src/components/ui/button.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/tooltip.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-06]
user_setup: []

must_haves:
  truths:
    - "@xyflow/react, @dagrejs/dagre, zustand, motion, lucide-react are installed at the RESEARCH-pinned versions and resolve from src/."
    - "shadcn/ui is initialized (new-york, Tailwind v4) with button, badge, tooltip components present under src/components/ui/."
    - "@xyflow/react base CSS is imported globally so graph nodes/edges render styled (Pitfall 6)."
  artifacts:
    - package.json
    - components.json
    - src/components/ui/button.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/tooltip.tsx
    - src/styles/app.css
  key_links:
    - "app.css @import '@xyflow/react/dist/style.css' placed before @import 'tailwindcss' so Tailwind theme overrides win."
    - "shadcn init must NOT overwrite src/styles/app.css (canonical token file, ADR 0001)."
  prohibitions:
    - "Do NOT use the old `reactflow` package — only `@xyflow/react`."
    - "Do NOT use `framer-motion` — only `motion` (import from `motion/react`)."
    - "Do NOT install `elkjs` — layout engine is `@dagrejs/dagre` (RESEARCH decision)."
    - "Do NOT let shadcn regenerate or replace app.css design tokens."
---

<objective>
Install the Phase 2 graph stack at pinned versions, initialize shadcn/ui (new-york / Tailwind v4) for the three primitives the UI-SPEC uses, and wire the @xyflow/react base stylesheet into the global CSS. This is the tooling substrate every later Phase 2 plan builds on.

Purpose: Without these packages, the canvas, layout, animation, hover store, and node primitives cannot be authored. The CSS import prevents the "unstyled boxes / invisible handles" failure mode (Pitfall 6).
Output: Updated package manifest + lockfile, components.json, three shadcn primitives, and a one-line app.css import.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned graph dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (§Standard Stack, §Installation, version-verification block: @xyflow/react 12.11.1, @dagrejs/dagre 3.0.0, zustand 5.0.14, motion 12.42.0; lucide-react latest)
    - package.json (existing deps — confirm none already present; note exact-version pinning convention used by Phase 1, e.g. "zod": "4.4.3" without caret)
    - .claude/CLAUDE.md (§"What NOT to Use" — reactflow/framer-motion/elkjs are banned)
  </read_first>
  <action>
    Run `npm install` adding runtime deps: `@xyflow/react@12.11.1`, `@dagrejs/dagre@3.0.0`, `zustand@5.0.14`, `motion@12.42.0`, `lucide-react` (latest). Match the Phase 1 pinning style (exact versions, no caret) for the four version-locked packages. Do NOT add `elkjs`, `reactflow`, or `framer-motion`. Confirm `@dagrejs/graphlib` arrives transitively as a dagre peer dep. Verify the install did not alter unrelated dependency versions.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; for (const k of ['@xyflow/react','@dagrejs/dagre','zustand','motion','lucide-react']) if(!p[k]) {console.error('MISSING '+k); process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains all five packages; banned packages (reactflow, framer-motion, elkjs) absent.
    - `node -e "require('@xyflow/react'); require('@dagrejs/dagre'); require('zustand')"` exits 0.
    - package-lock.json updated and committed.
  </acceptance_criteria>
  <done>All five packages install at pinned versions, resolve at runtime, and no banned package is present.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add button, badge, tooltip</name>
  <files>components.json, src/components/ui/button.tsx, src/components/ui/badge.tsx, src/components/ui/tooltip.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Design System "shadcn init note"; §Registry Safety — button/badge/tooltip uses)
    - src/styles/app.css (canonical @theme token block — must remain the source of truth; shadcn must not overwrite it)
    - tsconfig.json (path alias `#/*` → ./src/* — components.json aliases must align)
  </read_first>
  <action>
    Run `npx shadcn init`; select new-york style and Tailwind v4 mode. Configure `components.json` so the CSS file points at the existing `src/styles/app.css` and the import alias matches the project (`#/*` → `src/*`); set the components alias to `#/components`. If init attempts to rewrite `src/styles/app.css`, decline / restore it from git so the ADR 0001 token block is preserved verbatim. Then run `npx shadcn add button badge tooltip`. Ensure generated files carry the SPDX header convention if the project lints for it (add `// SPDX-License-Identifier: GPL-3.0-or-later` line if missing — note these are generated, mirror the Phase 1 routeTree exemption decision if lint excludes them).
  </action>
  <verify>
    <automated>test -f components.json && test -f src/components/ui/button.tsx && test -f src/components/ui/badge.tsx && test -f src/components/ui/tooltip.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists; its tailwind.css points to src/styles/app.css and aliases resolve via the `#/` path.
    - button.tsx, badge.tsx, tooltip.tsx exist under src/components/ui/.
    - `git diff src/styles/app.css` shows the @theme token block unchanged by shadcn (only the Task 3 xyflow import differs).
  </acceptance_criteria>
  <done>shadcn is initialized and the three primitives exist without clobbering the canonical token file.</done>
</task>

<task type="auto">
  <name>Task 3: Import @xyflow/react base CSS into global stylesheet</name>
  <files>src/styles/app.css</files>
  <read_first>
    - src/styles/app.css (existing @import order: fontsource imports, then `@import "tailwindcss"`, then @theme)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pitfall 6 + Open Question 1: import xyflow CSS before `@import "tailwindcss"`)
  </read_first>
  <action>
    Add `@import "@xyflow/react/dist/style.css";` to src/styles/app.css positioned before the `@import "tailwindcss";` line (so Tailwind v4 @theme overrides take precedence over xyflow defaults). Do not modify the @theme token block or any existing rule.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/styles/app.css','utf8');const x=c.indexOf('@xyflow/react/dist/style.css');const t=c.indexOf('@import \"tailwindcss\"');process.exit(x>-1 && x<t ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains the xyflow CSS import and it appears before the tailwindcss import.
    - The @theme token block and prefers-reduced-motion rule are byte-identical to the prior version.
  </acceptance_criteria>
  <done>The xyflow base stylesheet is globally imported ahead of Tailwind so graph elements render styled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party package code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (@xyflow/react, dagre, zustand, motion, lucide) | high | accept | RESEARCH §Package Legitimacy Audit verified all packages (millions of weekly downloads, official orgs). The SUS flags on @xyflow/react and motion are false positives from recent-release dates only; no [ASSUMED]/[SLOP] packages, so no blocking-human checkpoint required. Pinned exact versions + committed lockfile prevent drift. |
| T-02-01 | Tampering | shadcn init overwriting app.css | low | mitigate | Decline/restore app.css; verify token block unchanged in acceptance. |
</threat_model>

<verification>
- `npm install` clean; all five packages resolve.
- shadcn primitives present; app.css tokens intact.
- xyflow CSS imported before Tailwind.
</verification>

<success_criteria>
The graph stack is installed at pinned versions, shadcn primitives exist, and the xyflow base stylesheet is globally imported — later plans can author canvas, nodes, edges, and animation without environment gaps.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-01-SUMMARY.md` when done.
</output>
