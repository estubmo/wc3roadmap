---
phase: 02-graph-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/nodes/
  - src/lib/mock-mastery.ts
  - src/lib/mock-mastery.test.ts
autonomous: true
requirements: [GRAPH-02]
user_setup: []

must_haves:
  truths:
    - "10–15 MDX seed nodes exist, each passing the content-collections schema + the '## How to Apply' transform, forming an acyclic prerequisite DAG."
    - "The seed corpus contains at least 8 nodes that can form a pathway, both MECHANIC and CONCEPTUAL nodes, and a spread of beginner/intermediate/advanced difficulty."
    - "mock-mastery.ts maps node IDs to one of untouched | in-progress | mastered, covering all three states across the corpus."
  artifacts:
    - content/nodes/*.mdx
    - src/lib/mock-mastery.ts
  key_links:
    - "Seed node IDs are the referents for pathways/beginner-fundamentals.json (plan 05) and the mock-mastery map keys."
    - "MasteryState type exported from mock-mastery.ts is consumed by GraphNode, MobileNodeList, and MasteryBadge."
  prohibitions:
    - "Mastery is MOCKED only — no persistence, no DB, no server write. This file is explicitly replaced by real persistence in Phase 5."
    - "All seed content is race: agnostic (faction tints stay dormant in v1)."
    - "Do not author real citations/pedagogy here beyond the one real node — placeholder content is acceptable for mock nodes (real authoring is Phase 9)."
---

<objective>
Author enough seed content to exercise the graph: 10–15 MDX nodes forming a real prerequisite DAG (the existing `map-control` node plus mocked ones), and a `mock-mastery.ts` map that assigns the three mastery states across them. This is the data the canvas, pathway spotlight, and mobile list render against this phase.

Purpose: Only `map-control.mdx` exists. The pathway needs 8–12 nodes (success criterion 3), and the graph must show all three mastery states (criterion 2) plus both node shapes (D-06). Real MDX nodes (not in-memory mocks) exercise the Phase 1 content pipeline and CI under realistic conditions.
Output: ~12 MDX seed nodes + a mocked mastery map with tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@content/nodes/map-control.mdx
@content-collections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 10–15 MDX seed nodes forming a DAG</name>
  <files>content/nodes/</files>
  <read_first>
    - content/nodes/map-control.mdx (the one real node — copy its frontmatter shape exactly: id, title, nodeType, skillType, difficulty, race, tags, prerequisites, patchId, patch_context, last_reviewed, meta_volatile, citations + a "## How to Apply" body section)
    - content-collections.ts (the per-document schema + the transform that THROWS if "## How to Apply" is missing — every node must include that heading)
    - src/schemas/node.ts (NodeFrontmatterSchema — all required fields)
    - src/lib/patches.ts (valid patchId values: patch-1.36.1 / patch-1.36.2)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Open Question 2 — create 10–15 minimal seed nodes; Pitfall 7 — prerequisites direction)
  </read_first>
  <action>
    Create ~12 new MDX files under content/nodes/ (kebab-case filenames matching their `id`). Cover WC3 fundamentals (e.g. supply-management, creep-routing, hero-leveling, expansion-timing, scouting, micro-focus-fire, hotkey-discipline, army-positioning, tech-timing, resource-banking, base-defense, harassment). For each: set `race: agnostic`; assign `nodeType` mixing MECHANIC and CONCEPTUAL (at least 3 CONCEPTUAL so rounded shapes appear); spread `difficulty` across beginner/intermediate/advanced; set valid `patchId`, a non-empty `patch_context`, a `last_reviewed` YYYY-MM-DD, `meta_volatile` boolean, `tags: []`, and at least one citation with a non-empty `applicationNote`. Wire `prerequisites` to form an acyclic top-to-bottom DAG (beginner roots like map-control/supply-management at the top; advanced nodes depend on earlier ones). Every prerequisite ID MUST reference an existing node (this corpus or map-control). Give each a short MDX body including a `## How to Apply` section (placeholder prose acceptable for mock nodes).
  </action>
  <verify>
    <automated>npm run build:content && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:content` compiles all nodes (no missing "## How to Apply" throw).
    - `npm run validate` passes: all prerequisite IDs resolve, all patchIds valid, graph acyclic.
    - `ls content/nodes/*.mdx | wc -l` is between 11 and 16 inclusive.
    - The corpus includes ≥3 CONCEPTUAL nodes and ≥8 nodes reachable as a connected pathway.
  </acceptance_criteria>
  <done>A validated, acyclic seed corpus of 10–15 nodes exists, mixing node types and difficulties.</done>
</task>

<task type="auto">
  <name>Task 2: Mocked mastery map + tests</name>
  <files>src/lib/mock-mastery.ts, src/lib/mock-mastery.test.ts</files>
  <read_first>
    - src/lib/patches.ts (analog: private const + public read-only view + typed accessor — PATTERNS §mock-mastery.ts)
    - src/lib/patches.test.ts (colocated test pattern, SPDX + vitest)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Mastery State Encoding — untouched / in-progress ("Learning") / mastered ("Mastered"))
    - content/nodes/ (the node IDs created in Task 1 — keys for the map)
  </read_first>
  <action>
    Create src/lib/mock-mastery.ts with SPDX header and a module JSDoc marked `// Phase 2 only — replaced by real persistence in Phase 5`. Export `type MasteryState = "untouched" | "in-progress" | "mastered"`. Define a private `_MOCK_MASTERY` Record<string, MasteryState> keyed by the seed node IDs, assigning values so all three states appear (several untouched, a few in-progress, a few mastered — including at least one mastered node that is a prerequisite of an in-progress node, to exercise the three-gold hierarchy). Export a read-only `MOCK_MASTERY` view and `getMockMastery(nodeId: string): MasteryState` that returns "untouched" for unknown IDs (graceful default). Write mock-mastery.test.ts asserting: the type/accessor returns expected states for known IDs, defaults to "untouched" for unknown IDs, and all three states are present in the map.
  </action>
  <verify>
    <automated>npm test -- src/lib/mock-mastery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/lib/mock-mastery.test.ts` passes.
    - MOCK_MASTERY contains at least one of each of untouched, in-progress, mastered.
    - getMockMastery returns "untouched" for an unknown node ID.
    - Module JSDoc states it is Phase-2-only / replaced in Phase 5.
  </acceptance_criteria>
  <done>A mocked mastery source-of-truth exists, covers all three states, and is test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static MDX content → content-collections Zod | Frontmatter validated at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | malformed seed frontmatter | low | mitigate | content-collections Zod + CI validate reject malformed nodes before build. |
| T-02-05 | Denial-of-Service | cyclic prerequisites crash layout | low | mitigate | detectCycles in `npm run validate` enforces DAG invariant. |
</threat_model>

<verification>
- build:content + validate green with the expanded corpus.
- mock-mastery tests pass and cover all three states.
</verification>

<success_criteria>
The project has a realistic, validated seed corpus and a mocked mastery map that together let the graph demonstrate the pathway count, all three mastery states, and both node shapes.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-03-SUMMARY.md` when done.
</output>
