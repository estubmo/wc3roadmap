---
phase: 02-graph-engine
plan: 05
type: execute
wave: 2
depends_on: ["02-02", "02-03"]
files_modified:
  - pathways/beginner-fundamentals.json
  - scripts/validate-pathway.ts
  - scripts/validate-pathway.test.ts
  - scripts/validate-content.ts
autonomous: true
requirements: [GRAPH-01]
user_setup: []

must_haves:
  truths:
    - "pathways/beginner-fundamentals.json exists, validates against PathwaySchema, and lists 8–12 ordered step IDs that all resolve to real seed nodes."
    - "A referential-integrity validator fails CI if any pathway step ID does not exist in the node corpus (mirrors the Phase-1 prerequisite validator)."
    - "`npm run validate` runs the pathway integrity check as part of the existing content validation pass (no CI workflow edit needed)."
  artifacts:
    - pathways/beginner-fundamentals.json
    - scripts/validate-pathway.ts
  key_links:
    - "validate-pathway exports a pure validator (returns string[]) consumed by scripts/validate-content.ts — same aggregation pattern as validatePrerequisiteIds."
    - "The route loader (plan 10) parses this same JSON with PathwaySchema at load time."
  prohibitions:
    - "Do not add a new package.json script or edit .github/workflows/ci.yml — extend the existing validate-content.ts orchestrator instead (PATTERNS option)."
    - "Pathway content (titles, real ordering rationale, progress bar) stays minimal — Phase 9 owns real pathway content."
---

<objective>
Create the Beginner Fundamentals pathway data file and the CI referential-integrity check that guarantees every step ID resolves to a real node. Wire the check into the existing `npm run validate` orchestrator so it runs in CI without touching the workflow file.

Purpose: D-10 requires the pathway as a standalone, Zod-validated data file with a CI integrity check mirroring the Phase-1 prerequisite validator. This is what makes the guided-pathway view (success criterion 3) data-driven and safe — a typo in a step ID fails the build instead of silently dropping a node at runtime.
Output: pathway JSON + validate-pathway.ts (pure validator) + test, plus a one-call extension of validate-content.ts.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-PATTERNS.md
@scripts/validate-content.ts
@scripts/lib/validators.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Beginner Fundamentals pathway JSON</name>
  <files>pathways/beginner-fundamentals.json</files>
  <read_first>
    - src/schemas/pathway.ts (PathwaySchema shape: id, title, subtitle, steps[])
    - content/nodes/ (the seed node IDs created in plan 03 — steps must reference these)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Copywriting Contract — title "Beginner Fundamentals", subline "8 foundational skills — start here")
  </read_first>
  <action>
    Create pathways/beginner-fundamentals.json with `id: "beginner-fundamentals"`, `title: "Beginner Fundamentals"`, `subtitle: "8 foundational skills — start here"`, and `steps`: an ordered array of 8–12 node IDs drawn from the seed corpus (start with map-control / supply-management roots, progress through intermediate fundamentals). Order the steps as a sensible learning sequence following the prerequisite DAG. Every ID must exactly match a content/nodes file id.
  </action>
  <verify>
    <automated>node -e "const p=require('./pathways/beginner-fundamentals.json'); if(!Array.isArray(p.steps)||p.steps.length<8||p.steps.length>12){console.error('step count out of range');process.exit(1)}; console.log('ok',p.steps.length)"</automated>
  </verify>
  <acceptance_criteria>
    - The JSON parses and has id/title/subtitle/steps.
    - steps has 8–12 entries, all kebab-case node IDs present in content/nodes/.
    - subtitle matches the copywriting contract.
  </acceptance_criteria>
  <done>The pathway data file exists with a valid ordered 8–12 step list referencing real nodes.</done>
</task>

<task type="auto">
  <name>Task 2: Pathway integrity validator + test</name>
  <files>scripts/validate-pathway.ts, scripts/validate-pathway.test.ts</files>
  <read_first>
    - scripts/lib/validators.ts (validatePrerequisiteIds — the exact pure-validator pattern to mirror: build a Set of node ids, push a clear error string per unresolved reference, return string[])
    - scripts/lib/validators.test.ts (test style for a pure validator)
    - src/schemas/pathway.ts (PathwaySchema — parse the file before integrity-checking)
    - .planning/phases/02-graph-engine/02-PATTERNS.md (§validate-pathway.ts — orchestrator/validator pattern)
  </read_first>
  <action>
    Create scripts/validate-pathway.ts with SPDX header + JSDoc. Export a pure function `validatePathwayStepIds(pathway: { id: string; steps: readonly string[] }, nodeIds: ReadonlySet<string>): string[]` that returns one clear error string per step ID not in nodeIds (e.g. `Pathway "beginner-fundamentals": step "typo-id" does not reference an existing node`), and `[]` when all resolve. Keep it decoupled from content-collections and the filesystem (accept plain inputs) so it is unit-testable, exactly like validatePrerequisiteIds. Write scripts/validate-pathway.test.ts: passes when all steps resolve; returns errors for an unknown step ID; handles an empty steps array.
  </action>
  <verify>
    <automated>npm test -- scripts/validate-pathway.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- scripts/validate-pathway.test.ts` passes.
    - validatePathwayStepIds returns string[] and is pure (no fs/content-collections import).
    - An unknown step ID produces a descriptive error including the pathway id and the bad step id.
  </acceptance_criteria>
  <done>A pure, tested referential-integrity validator for pathway steps exists.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pathway validation into validate-content orchestrator</name>
  <files>scripts/validate-content.ts</files>
  <read_first>
    - scripts/validate-content.ts (the main() orchestrator: imports allNodes, runs validators, aggregates errors[], exits non-zero on failure)
    - src/schemas/pathway.ts (PathwaySchema for parsing the file)
    - scripts/validate-pathway.ts (the validator from Task 2)
  </read_first>
  <action>
    Extend scripts/validate-content.ts main(): after the existing three validators, read pathways/beginner-fundamentals.json (node:fs readFileSync + JSON.parse), parse it with `PathwaySchema.safeParse` (push a clear error and skip integrity if parse fails), build the node-id Set from `allNodes`, call `validatePathwayStepIds(pathway, nodeIds)`, and push results into the same `errors[]` array so the existing exit-non-zero-on-error logic covers it. Update the module JSDoc to mention the fourth (pathway integrity) check. Do not add a new npm script or edit CI — `npm run validate` already runs this file.
  </action>
  <verify>
    <automated>npm run build:content && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run validate` passes with the valid pathway file and reports the pathway check ran.
    - Temporarily injecting a bogus step ID causes `npm run validate` to exit non-zero with the descriptive error (revert after confirming).
    - No new script in package.json; .github/workflows/ci.yml unchanged.
  </acceptance_criteria>
  <done>The pathway integrity check runs inside the existing validate pass and blocks CI on a bad step reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static pathway JSON → Zod parse + integrity check | Build-time validation of pathway data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial-of-Service | dangling pathway step at runtime | medium | mitigate | CI referential-integrity check fails the build on any unresolved step id. |
| T-02-08 | Tampering | malformed pathway JSON | low | mitigate | PathwaySchema.safeParse at validation + load time; descriptive error, no crash. |
</threat_model>

<verification>
- Pathway JSON valid, 8–12 real steps.
- validate-pathway tests pass.
- `npm run validate` exercises the integrity check and fails on a bad ID.
</verification>

<success_criteria>
The Beginner Fundamentals pathway is a standalone, schema-validated data file whose step references are guaranteed real by a CI check wired into the existing validate pass.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-05-SUMMARY.md` when done.
</output>
