---
phase: 02-graph-engine
plan: 07
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/graph/MobileNodeList.tsx
  - src/components/graph/PathwayBanner.tsx
autonomous: true
requirements: [GRAPH-05, GRAPH-02]
user_setup: []

must_haves:
  truths:
    - "MobileNodeList renders a vertical, scrollable, SSR-safe list of node cards: a 'Your Pathway' section (pathway nodes in order) then an 'All Nodes' section (D-11/D-12)."
    - "Each mobile card (≥72px) shows type icon + title + mastery badge; the whole row is a ≥44px tap target; tap is a no-op in Phase 2."
    - "PathwayBanner renders the pathway title (Space Grotesk), subtitle, step count, and an 'Explore full map' control."
  artifacts:
    - src/components/graph/MobileNodeList.tsx
    - src/components/graph/PathwayBanner.tsx
  key_links:
    - "MobileNodeList is rendered inside a `block md:hidden` wrapper (plan 10) — pure SSR HTML, no @xyflow/react, no window check."
    - "Both components reuse MasteryBadge (plan 06) and mock-mastery; PathwayBanner's 'Explore full map' button handler is wired by the canvas/route layer."
  prohibitions:
    - "No @xyflow/react import in either component — mobile fallback is pure HTML (GRAPH-05)."
    - "No window.innerWidth / JS viewport check — responsive switching is CSS-only (Pitfall 4)."
    - "Tap handler is a silent no-op — no navigation, no toast (Phase 3 wires the detail panel)."
---

<objective>
Build the two non-canvas chrome components: `MobileNodeList` (the SSR-rendered mobile simplified form) and `PathwayBanner` (the pathway-name overlay with the "Explore full map" CTA). Both are plain HTML/React — no graph library — so they render server-side without hydration risk.

Purpose: Mobile readability is success criterion 4. The list is the desktop-first graph's "readable simplified form" and must be SSR-safe. The pathway banner surfaces the guided-pathway identity and the primary action that reveals the full map (criterion 3).
Output: MobileNodeList.tsx + PathwayBanner.tsx.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@src/schemas/graph.ts
@src/schemas/pathway.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MobileNodeList (SSR-safe node card list)</name>
  <files>src/components/graph/MobileNodeList.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Mobile Simplified Form D-11/D-12: sections "Your Pathway" / "All Nodes", card layout 72px, icon+title+badge, tap no-op; §Color obsidian-900 background; §Copywriting section labels)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (§Mobile Fallback — `block md:hidden`, no window check)
    - src/schemas/graph.ts (GraphDisplayNode), src/schemas/pathway.ts (Pathway)
    - src/lib/mock-mastery.ts (getMockMastery), src/components/graph/MasteryBadge.tsx
  </read_first>
  <action>
    Create src/components/graph/MobileNodeList.tsx (SPDX header, named export). Props: `{ nodes: GraphDisplayNode[]; pathway: Pathway }`. Render an obsidian-900 scrollable container with a sticky "Beginner Fundamentals" header (Space Grotesk 22px/600). Section 1 "Your Pathway": map pathway.steps in order to the matching nodes; Section 2 "All Nodes": the remaining nodes not in the pathway. Each card: ≥72px tall flex row, 16px horizontal padding, left = 20px lucide type icon (Sword/BookOpen) + title (15px/600, 1-line truncate), right = `<MasteryBadge state={getMockMastery(node.id)} />`. The full row is the tap target with `pointer-events: auto` and an `onClick` that is an explicit no-op (Phase 2). Section separator: 1px obsidian-700 divider. Token-driven colors only. This component must not import anything from @xyflow/react.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - `grep -n "@xyflow/react" src/components/graph/MobileNodeList.tsx` returns nothing.
    - `grep -n "window" src/components/graph/MobileNodeList.tsx` returns nothing (no JS viewport check).
    - Renders both "Your Pathway" and "All Nodes" sections; pathway nodes appear in pathway order.
  </acceptance_criteria>
  <done>A pure-HTML, SSR-safe mobile node list renders pathway-first then all nodes, with no-op taps.</done>
</task>

<task type="auto">
  <name>Task 2: PathwayBanner overlay</name>
  <files>src/components/graph/PathwayBanner.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Control Bar, §Guided-Pathway View Model banner, §Copywriting "Explore full map"/"Back to pathway"/"{N} of {total} nodes", §Typography Pathway heading Space Grotesk 22px/600)
    - src/components/ui/button.tsx (shadcn button for the CTA)
    - src/schemas/pathway.ts (Pathway)
  </read_first>
  <action>
    Create src/components/graph/PathwayBanner.tsx (SPDX header, named export). Props: `{ pathway: Pathway; totalNodes: number; exploring: boolean; onToggleExplore: () => void }`. Render the pathway title (Space Grotesk 22px/600), subtitle, and a step-count label "{pathway.steps.length} of {totalNodes} nodes". Render a shadcn button using outline style with a rune-500 ring: label "Explore full map" when `exploring` is false, "Back to pathway" when true; onClick calls onToggleExplore. Token-driven styling per the accent-reserved list (rune-gold only on the CTA ring). No @xyflow/react import.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - Button label toggles between "Explore full map" and "Back to pathway" based on the `exploring` prop.
    - Step-count label uses pathway.steps.length and totalNodes.
    - No hardcoded hex (`grep -nE "#[0-9a-fA-F]{3,6}" src/components/graph/PathwayBanner.tsx` returns nothing).
  </acceptance_criteria>
  <done>The pathway banner shows identity + progress count + a toggling explore/back CTA, token-driven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node/pathway data → React render (SSR) | Strings rendered server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | XSS via node title in list | low | mitigate | React escapes children; no dangerouslySetInnerHTML. |
| T-02-11 | Denial-of-Service | hydration mismatch from JS viewport check | low | mitigate | CSS-only responsive switching; component never reads window. |
</threat_model>

<verification>
- typecheck passes.
- No @xyflow/react or window usage in mobile list.
- Banner CTA toggles label by prop.
- Visual confirmation deferred to the preview-route checkpoint (plan 10).
</verification>

<success_criteria>
The mobile simplified form and pathway banner exist as SSR-safe, token-driven chrome — the readable mobile fallback (criterion 4) and the guided-pathway identity + explore action (criterion 3) are in place.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-07-SUMMARY.md` when done.
</output>
