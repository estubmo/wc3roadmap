---
phase: 02-graph-engine
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/graph-layout.ts
  - src/lib/graph-layout.test.ts
  - src/lib/pathway-utils.ts
  - src/lib/pathway-utils.test.ts
autonomous: true
requirements: [GRAPH-01, GRAPH-06]
user_setup: []

must_haves:
  truths:
    - "Node positions are auto-computed from the prerequisites[] DAG — no hand-authored coordinates (D-01)."
    - "computeLayout(nodes, 'TB') returns React Flow Node[] + Edge[] with deterministic positions for every input node, top-to-bottom — fundamentals at top, advanced below (D-02; y increases with prerequisite depth)."
    - "Edges run prereq → dependent (g.setEdge(prereqId, n.id)) so arrows point at the dependent node and fundamentals sit at the top (Pitfall 7)."
    - "computeAncestorEdgeIds(nodeId, edges) returns the Set of edge IDs on the full transitive prerequisite chain of nodeId."
  artifacts:
    - src/lib/graph-layout.ts
    - src/lib/pathway-utils.ts
  key_links:
    - "computeLayout is called once per node-array change via useMemo inside the client-only canvas (plan 09) — synchronous dagre, deterministic so SSR/hydration agree."
    - "computeAncestorEdgeIds is invoked by the Zustand store (plan 08) on hover to drive edge highlight."
  prohibitions:
    - "Layout must be a PURE function — no DOM access, no React, no side effects (so it is unit-testable in the node vitest environment)."
    - "Do not reverse edge direction (no g.setEdge(dependent, prereq)) — that flips the tree."
---

<objective>
Implement the two pure algorithms the canvas depends on: `computeLayout` (dagre top-to-bottom DAG layout auto-computed from `prerequisites[]` per D-01/D-02, GraphDisplayNode[] → React Flow Node[]/Edge[]) and `computeAncestorEdgeIds` (BFS up the prerequisite chain for edge highlight). Both are pure, deterministic, and fully unit-tested in the node environment.

Purpose: Deterministic synchronous layout is what makes the client-only canvas hydration-safe and memoization-friendly (GRAPH-06). The ancestor-chain BFS is the engine behind the interactive "show me the learning path" highlight (D-03). Isolating both as pure functions keeps them testable without a DOM.
Output: graph-layout.ts + pathway-utils.ts with colocated tests.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-PATTERNS.md
@src/schemas/graph.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dagre layout pure function + tests</name>
  <files>src/lib/graph-layout.ts, src/lib/graph-layout.test.ts</files>
  <read_first>
    - scripts/lib/validators.ts (analog: deep-module JSDoc + pure typed function returning a value, no side effects — PATTERNS §graph-layout.ts)
    - scripts/lib/detectCycles.ts (the project's existing graph traversal style + minimal local interface convention)
    - src/schemas/graph.ts (GraphDisplayNode type — the input element type)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 1 full dagre code; Pitfall 7 edge direction; NODE_WIDTH 160 / NODE_HEIGHT 80 from UI-SPEC)
    - src/lib/patches.test.ts (colocated pure-function test pattern)
  </read_first>
  <behavior>
    - Given a small fixed DAG (e.g. a → b, a → c, b → d), every input node appears in the output with a numeric position.
    - rankdir 'TB': a node's y is strictly greater than each of its prerequisites' y (top-to-bottom depth ordering).
    - One Edge is produced per prerequisite link, id `${prereqId}->${n.id}`, source=prereqId, target=n.id, type 'prerequisite'.
    - Output is identical across repeated calls with the same input (determinism).
    - Node `type` is 'mechanic' for MECHANIC and 'conceptual' for CONCEPTUAL; node `data` carries the GraphDisplayNode fields.
  </behavior>
  <action>
    Create src/lib/graph-layout.ts with SPDX header + deep-module JSDoc (one sentence on interface/implementation ratio: simple signature hiding dagre rank assignment). Import `dagre` from "@dagrejs/dagre" and `Node`/`Edge` types from "@xyflow/react", and `GraphDisplayNode` from "#/schemas/graph". Define module-scope `NODE_WIDTH = 160`, `NODE_HEIGHT = 80`. Implement `export function computeLayout(nodes: GraphDisplayNode[], direction: 'TB' | 'LR' = 'TB'): { nodes: Node[]; edges: Edge[] }` per RESEARCH Pattern 1: build a dagre graph (rankdir, ranksep 80, nodesep 40), set each node with width/height, add one dagre edge per prerequisite with `g.setEdge(prereqId, n.id)` AND collect a parallel React Flow Edge, run `dagre.layout(g)`, then map to React Flow Node[] centering position (x - NODE_WIDTH/2, y - NODE_HEIGHT/2) and setting `type` from nodeType and `data` to the node fields. Write graph-layout.test.ts asserting the behavior block on a small fixed fixture.
  </action>
  <verify>
    <automated>npm test -- src/lib/graph-layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/lib/graph-layout.test.ts` passes.
    - A determinism test asserts two calls produce deeply-equal output.
    - A direction test asserts dependent.y > prerequisite.y for a known pair.
    - `grep -n "setEdge(" src/lib/graph-layout.ts` shows prereq-first argument order.
  </acceptance_criteria>
  <done>computeLayout deterministically lays out the DAG top-to-bottom with correctly-directed edges, verified by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ancestor-chain BFS pure function + tests</name>
  <files>src/lib/pathway-utils.ts, src/lib/pathway-utils.test.ts</files>
  <read_first>
    - scripts/lib/validators.ts (minimal local interface convention — don't import the full schema; PATTERNS §pathway-utils.ts)
    - scripts/lib/detectCycles.ts (BFS/DFS traversal style in this codebase)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 5 — computeAncestorEdgeIds; §Pitfall 5 — returned Set is consumed via .has())
    - src/lib/patches.test.ts (colocated test pattern)
  </read_first>
  <behavior>
    - For node d in DAG a→b→d, a→c: computeAncestorEdgeIds('d', edges) returns the edge ids on d's ancestor chain {a->b, b->d} (and not a->c, which is not an ancestor of d).
    - A root node with no prerequisites returns an empty Set.
    - An unknown node id returns an empty Set (no throw).
    - Handles diamond shapes without infinite loops (visited tracking).
  </behavior>
  <action>
    Create src/lib/pathway-utils.ts with SPDX header + JSDoc (pure function, no side effects, drives D-03 edge highlight). Import the `Edge` type from "@xyflow/react" (type-only). Implement `export function computeAncestorEdgeIds(nodeId: string, edges: Edge[]): Set<string>`: BFS upward from nodeId following edges whose `target === current`, collecting each traversed edge's `id` and enqueuing its `source`, with a visited set to avoid revisiting in diamonds/cycles. Return the accumulated Set. Write pathway-utils.test.ts covering the behavior block.
  </action>
  <verify>
    <automated>npm test -- src/lib/pathway-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/lib/pathway-utils.test.ts` passes.
    - A test confirms only ancestor-chain edges are returned (sibling branch excluded).
    - A test confirms empty Set for a root node and for an unknown id.
    - A diamond-DAG test completes without timeout (visited guard works).
  </acceptance_criteria>
  <done>computeAncestorEdgeIds returns exactly the prerequisite-chain edge set for any node, verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory node array → layout/BFS | Pure computation; no external input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Denial-of-Service | unbounded BFS on cyclic input | low | mitigate | visited-set guard in computeAncestorEdgeIds; corpus is CI-verified acyclic anyway. |
</threat_model>

<verification>
- Both pure-function suites pass in the node vitest environment.
- Edge direction and determinism explicitly asserted.
</verification>

<success_criteria>
The canvas has a deterministic, test-covered layout function and a correct ancestor-chain BFS — the algorithmic core of the interactive graph and its edge highlight.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-04-SUMMARY.md` when done.
</output>
