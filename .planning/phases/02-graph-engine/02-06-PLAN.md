---
phase: 02-graph-engine
plan: 06
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/graph/GraphNode.tsx
  - src/components/graph/MasteryBadge.tsx
autonomous: true
requirements: [GRAPH-02, GRAPH-06]
user_setup: []

must_haves:
  truths:
    - "GraphNode is React.memo-wrapped from its first commit (GRAPH-06) and renders the D-04 node face: type icon, title (2-line ellipsis), difficulty dots, and a mastery badge."
    - "MECHANIC nodes render with 4px sharp corners, CONCEPTUAL with 16px rounded corners (D-06), legible at ~50% zoom."
    - "Three mastery states are visually distinct via fill+glow progression AND a status badge (D-05): untouched (dim obsidian, no badge), in-progress (rune-600 ring + soft glow + 'Learning'), mastered (rune-500 fill+strong glow + 'Mastered')."
    - "A faction-tint hook exists but is dormant for race: agnostic (D-07)."
  artifacts:
    - src/components/graph/GraphNode.tsx
    - src/components/graph/MasteryBadge.tsx
  key_links:
    - "GraphNode is registered in the module-scope nodeTypes const (plan 09) for both 'mechanic' and 'conceptual' keys; it switches shape on data.nodeType."
    - "Mastery state comes from node data.masteryState (sourced from mock-mastery in the canvas/route layer)."
    - "All color comes from CSS variables / Tailwind theme tokens (var(--color-rune-*), --color-obsidian-*) — never hardcoded hex."
  prohibitions:
    - "No detail-panel content, no lazy content load, no citations — node face is intentionally lightweight (Phase 3 owns the panel)."
    - "Do NOT store rich content in node data — display-essential fields only."
    - "Do NOT apply faction color to agnostic nodes — build the hook dormant."
    - "Mastery glow/border must use the three-gold hierarchy (area vs perimeter vs line; rune-500/600/400) so it stays distinct from edge highlight (F-01)."
---

<objective>
Build the memoized custom node component (`GraphNode`) and its mastery badge — the visual heart of the graph. One component serves both node types, switching shape (sharp vs rounded) and rendering the D-04 face (icon, title, difficulty dots, badge) with the D-05 three-state fill+glow+badge mastery encoding.

Purpose: This is success criterion 2 (three visually distinct mastery states) and part of criterion 5 (React.memo from first commit). The component must be memoized at birth — not retrofitted — and consume only display-essential data.
Output: GraphNode.tsx + MasteryBadge.tsx, styled entirely from design tokens.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@src/schemas/graph.ts
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: MasteryBadge component</name>
  <files>src/components/graph/MasteryBadge.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Mastery State Encoding badge specs; §Copywriting: "Learning"/"Mastered", no badge when untouched; §Color badge token roles)
    - src/components/ui/badge.tsx (shadcn badge primitive to compose on)
    - src/lib/mock-mastery.ts (MasteryState type)
    - src/styles/app.css (rune/obsidian token names)
  </read_first>
  <action>
    Create src/components/graph/MasteryBadge.tsx (SPDX header, named export, typed props `{ state: MasteryState }`). Render nothing when state is "untouched". For "in-progress": a pill using `--color-rune-600` background and `--color-rune-300` text, label "Learning". For "mastered": a pill using `--color-rune-500` background and `--color-obsidian-950` text, label "Mastered". Use the shadcn badge primitive or a styled span; sizing per UI-SPEC (11px/600, badge inner padding xs). Use Tailwind theme classes / CSS-variable refs — no hardcoded hex.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - Returns null for "untouched"; renders "Learning"/"Mastered" pills for the other states with the specified token roles.
    - No hardcoded hex color literals (`grep -nE "#[0-9a-fA-F]{3,6}" src/components/graph/MasteryBadge.tsx` returns nothing).
  </acceptance_criteria>
  <done>The mastery badge renders the correct labelled pill per state and is token-driven.</done>
</task>

<task type="auto">
  <name>Task 2: GraphNode memoized custom node</name>
  <files>src/components/graph/GraphNode.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 3 — memo(function GraphNode...), NodeProps, Handle/Position, CVA variants; §Pitfall 1 memoization rationale)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Node Anatomy, §Node Shapes by Type, §Node Icons by Type — Sword/BookOpen, §Mastery State Encoding, §Difficulty Markers 3-dot, §Accessibility aria-labels)
    - src/schemas/graph.ts (GraphDisplayNode — node data shape)
    - src/lib/mock-mastery.ts (MasteryState)
    - src/styles/app.css (obsidian/rune/faction tokens)
    - src/components/graph/MasteryBadge.tsx (from Task 1)
  </read_first>
  <action>
    Create src/components/graph/GraphNode.tsx (SPDX header). Export `const GraphNode = memo(function GraphNode({ data }: NodeProps) { ... })` — named inner function for DevTools, React.memo from the first commit (GRAPH-06). Type the node data as `GraphDisplayNode & { masteryState: MasteryState }`. Render a 160×80 face: top-left lucide `Sword` (MECHANIC) or `BookOpen` (CONCEPTUAL) icon with the aria-label from the copywriting contract; top-right `<MasteryBadge state={masteryState} />`; title at 15px/600 truncated to 2 lines; bottom-left a 3-dot difficulty row (1/2/3 filled for beginner/intermediate/advanced) with the difficulty aria-label. Shape: rounded-sm (4px) for MECHANIC, rounded-2xl (16px) for CONCEPTUAL. Use class-variance-authority for the 3 mastery × shape variants applying the D-05 background/border/glow tokens (untouched: obsidian-800 + 1px obsidian-600, no glow; in-progress: 2px rune-600 + soft glow; mastered: rune-500 tinted fill + 2px rune-500 + strong glow). Add invisible target `Handle` (top) and source `Handle` (bottom) per Pattern 3. Add a dormant faction-tint hook: a code path that would apply `--color-faction-*` keyed on data.race but is a no-op for "agnostic" (D-07) — leave a JSDoc note that v1 content is all agnostic. Use only token-based colors.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - `grep -n "memo(" src/components/graph/GraphNode.tsx` confirms React.memo wrapping in the first commit.
    - Component imports `Sword` and `BookOpen` from lucide-react and renders both Handles.
    - CVA (or equivalent typed variant map) drives the three mastery states; no hardcoded hex (`grep -nE "#[0-9a-fA-F]{3,6}" src/components/graph/GraphNode.tsx` returns nothing).
    - A faction-tint branch exists keyed on data.race and is inert for "agnostic".
  </acceptance_criteria>
  <done>GraphNode renders the full D-04 face with distinct shapes and the three D-05 mastery states, memoized from the first commit, token-driven, with a dormant faction hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node data → React render | Node title/strings rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | XSS via node title | low | mitigate | React escapes string children by default; no dangerouslySetInnerHTML in node face. |
</threat_model>

<verification>
- typecheck passes.
- memo wrapping, both icons, both Handles, CVA variants, token-only colors, dormant faction hook all present.
- Visual confirmation deferred to the preview-route checkpoint (plan 10).
</verification>

<success_criteria>
A memoized custom node component renders three visually distinct mastery states and two distinct shapes from display-essential data only, satisfying success criteria 2 and part of 5.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-06-SUMMARY.md` when done.
</output>
