---
phase: 02-graph-engine
plan: 08
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - src/lib/graph-store.ts
  - src/components/graph/GraphEdge.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-06]
user_setup: []

must_haves:
  truths:
    - "A Zustand store holds hoveredNodeId + a precomputed ancestorEdgeIds Set; setHoveredNode(nodeId, edges) recomputes the Set via computeAncestorEdgeIds (or clears it on null)."
    - "GraphEdge is React.memo-wrapped and reads ONLY a per-edge boolean via the selector `(s) => s.ancestorEdgeIds.has(id)` (Pitfall 5) so only affected edges re-render."
    - "A highlighted edge animates to rune-400 / 3px / opacity 1 via motion.path (spring on enter, 0.2s on exit); at rest it is obsidian-700 / 1px / 0.5 opacity (D-03)."
  artifacts:
    - src/lib/graph-store.ts
    - src/components/graph/GraphEdge.tsx
  key_links:
    - "GraphEdge is registered in the module-scope edgeTypes const (plan 09) under 'prerequisite'."
    - "Node hover handlers in the canvas (plan 09) call store.setHoveredNode with the current edges to populate ancestorEdgeIds."
    - "Edge path geometry comes from getBezierPath(@xyflow/react); stroke color comes from var(--color-rune-400)/var(--color-obsidian-700)."
  prohibitions:
    - "Do NOT fork or mutate React Flow's internal Zustand store — keep hover state in a separate store (anti-pattern)."
    - "Do NOT use a broad selector returning the whole Set — must be a `.has(id)` boolean selector (Pitfall 5)."
    - "Do NOT animate the stroke with pure CSS transitions — use motion.path for cross-browser SVG stroke animation."
    - "Edge highlight is stroke-only — no glow on edges (preserves the three-gold hierarchy, F-01)."
---

<objective>
Build the edge-highlight subsystem: a lightweight Zustand store (`hoveredNodeId` + precomputed `ancestorEdgeIds`) and the memoized animated custom edge (`GraphEdge`) that reads a per-edge boolean and animates its stroke to rune-gold on the hovered node's prerequisite chain.

Purpose: This is the primary "show me the learning path" affordance (D-03) standing in for the not-yet-built detail panel, and a memoization-correctness surface (GRAPH-06). Per-edge boolean selectors keep re-renders isolated; motion.path gives reliable cross-browser stroke animation.
Output: graph-store.ts + GraphEdge.tsx.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
@src/lib/pathway-utils.ts
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand graph-store for hover/highlight state</name>
  <files>src/lib/graph-store.ts</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 5 — store shape: hoveredNodeId, ancestorEdgeIds, setHoveredNode; §Pitfall 5)
    - src/lib/pathway-utils.ts (computeAncestorEdgeIds signature from plan 04)
    - src/lib/patches.ts (project convention: private impl + public surface — PATTERNS §graph-store.ts)
  </read_first>
  <action>
    Create src/lib/graph-store.ts (SPDX header + JSDoc noting it is a SEPARATE store from React Flow's internal store). Import `create` from "zustand", the `Edge` type from "@xyflow/react", and `computeAncestorEdgeIds` from "#/lib/pathway-utils". Define the store interface `{ hoveredNodeId: string | null; ancestorEdgeIds: Set<string>; setHoveredNode: (nodeId: string | null, edges: Edge[]) => void }`. Implement `useGraphStore = create<GraphStore>((set) => ({ ... }))`: setHoveredNode clears to `{ hoveredNodeId: null, ancestorEdgeIds: new Set() }` when nodeId is null, otherwise sets hoveredNodeId and `ancestorEdgeIds: computeAncestorEdgeIds(nodeId, edges)`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - Store exposes hoveredNodeId, ancestorEdgeIds (a Set), and setHoveredNode.
    - setHoveredNode delegates to computeAncestorEdgeIds and resets to an empty Set on null.
    - Does not import or mutate React Flow's internal store.
  </acceptance_criteria>
  <done>A standalone Zustand store drives edge highlight from hovered-node ancestor edges.</done>
</task>

<task type="auto">
  <name>Task 2: GraphEdge memoized animated custom edge</name>
  <files>src/components/graph/GraphEdge.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 4 — memo(GraphEdge), getBezierPath, motion.path animate stroke/strokeWidth/opacity, spring vs 0.2 transition; §Pitfall 5 per-edge `.has(id)` selector)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Edge Visual Language: rest obsidian-700/1px/0.5, hovered rune-400/3px/1.0, stroke-only no glow; §Animation Contract spring 300/30)
    - src/lib/graph-store.ts (useGraphStore from Task 1)
    - src/styles/app.css (--color-rune-400 / --color-obsidian-700)
  </read_first>
  <action>
    Create src/components/graph/GraphEdge.tsx (SPDX header). Export `const GraphEdge = memo(function GraphEdge(props: EdgeProps) { ... })`. Read `const isHighlighted = useGraphStore((s) => s.ancestorEdgeIds.has(props.id))` — a per-edge boolean selector. Compute the path with `getBezierPath` from "@xyflow/react" using the edge's source/target coords + positions. Render a `motion.path` (from "motion/react") with `fill="none"`, animating `stroke` between `var(--color-rune-400)` (highlighted) and `var(--color-obsidian-700)` (rest), `strokeWidth` 3 vs 1, `opacity` 1 vs 0.5; transition is `{ type: 'spring', stiffness: 300, damping: 30 }` on enter (highlighted) and `{ duration: 0.2 }` on exit. No box-shadow/glow on the edge. Token-driven colors only.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck passes.
    - `grep -n "memo(" src/components/graph/GraphEdge.tsx` confirms React.memo.
    - `grep -n "ancestorEdgeIds.has(" src/components/graph/GraphEdge.tsx` confirms the per-edge boolean selector.
    - Imports motion from "motion/react" (not "framer-motion") and getBezierPath from "@xyflow/react".
    - No glow/box-shadow on the edge; no hardcoded hex.
  </acceptance_criteria>
  <done>GraphEdge animates its stroke to rune-gold only when on the hovered node's prerequisite chain, with isolated per-edge re-renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client hover events → store → edge render | Local UI state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Denial-of-Service | re-render storm from broad selector | low | mitigate | Per-edge `.has(id)` boolean selector confines re-renders to affected edges (Pitfall 5). |
</threat_model>

<verification>
- typecheck passes.
- memo + per-edge selector + motion/react + getBezierPath confirmed.
- Visual confirmation deferred to the preview-route checkpoint (plan 10).
</verification>

<success_criteria>
Hovering a node highlights exactly its prerequisite-chain edges in animated rune-gold with isolated re-renders — the interactive learning-path affordance (D-03) and a clean memoization surface.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-08-SUMMARY.md` when done.
</output>
