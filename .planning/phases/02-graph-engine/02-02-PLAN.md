---
phase: 02-graph-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/graph.ts
  - src/schemas/graph.test.ts
  - src/schemas/pathway.ts
  - src/schemas/pathway.test.ts
  - docs/adr/005-graph-display-node.md
  - CONTEXT.md
autonomous: true
requirements: [GRAPH-01, GRAPH-02]
user_setup: []

must_haves:
  truths:
    - "GraphDisplayNodeSchema = NodeSummarySchema.extend({ difficulty }) — the ONLY field the graph adds beyond NodeSummary; no NodeFrontmatter content field crosses the boundary (ADR 002)."
    - "PathwaySchema validates { id, title, subtitle, steps[] } with non-empty strings and a non-empty steps array."
    - "ADR 005 records why difficulty is the sole graph-boundary addition and why it does not violate ADR 002."
  artifacts:
    - src/schemas/graph.ts
    - src/schemas/pathway.ts
    - docs/adr/005-graph-display-node.md
    - CONTEXT.md
  key_links:
    - "GraphDisplayNode is the graph engine's typed import — components type their props to it, TypeScript enforces the ADR 002 boundary."
    - "PathwaySchema is consumed by the route loader (parse-at-load) and scripts/validate-pathway.ts (CI integrity)."
  prohibitions:
    - "graph.ts must NOT import NodeFrontmatter, citations, patch_context, or any content-only field."
    - "Schemas must use Zod v4 idioms: z.enum([...]) not z.nativeEnum, { error } not { message }, .min(1) on required strings."
---

<objective>
Author the two new Zod schemas that form Phase 2's data contract: `GraphDisplayNodeSchema` (the graph-display projection that adds `difficulty` to `NodeSummary`) and `PathwaySchema` (the standalone pathway data shape, D-10). Document the projection as ADR 005 and extend the domain glossary.

Purpose: The graph face needs `difficulty` (D-04) which lives on `NodeFrontmatter`, but the engine must not import `NodeFrontmatter` (ADR 002). A narrow `.extend()` projection resolves this without breaking decoupling. The pathway schema gives the guided-view its validated, CI-checked data structure.
Output: graph.ts, pathway.ts, their tests, ADR 005, CONTEXT.md glossary additions.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-PATTERNS.md
@src/schemas/node.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphDisplayNodeSchema projection + tests</name>
  <files>src/schemas/graph.ts, src/schemas/graph.test.ts</files>
  <read_first>
    - src/schemas/node.ts (NodeSummarySchema fields: id, title, nodeType, race, prerequisites; the `.extend()` + `z.infer` convention at lines 46-70 and 110-164)
    - src/schemas/mastery.ts (analog: SPDX header, module JSDoc, single export + inferred type — PATTERNS §graph.ts)
    - src/schemas/node.test.ts (test fixture + describe/it accept|reject structure, lines 1-55)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 6 + §GraphDisplayNode Projection)
  </read_first>
  <behavior>
    - Accepts a NodeSummary object augmented with difficulty: "beginner" | "intermediate" | "advanced".
    - Rejects an object missing difficulty.
    - Rejects an invalid difficulty value (e.g. "expert").
    - Rejects a missing NodeSummary field (e.g. nodeType).
    - The inferred GraphDisplayNode type carries id, title, nodeType, race, prerequisites, difficulty and NOTHING else (no citations/patch fields).
  </behavior>
  <action>
    Create src/schemas/graph.ts with SPDX header + Copyright line and a 3-5 line module JSDoc stating: it is the graph-display projection (D-04), it adds only `difficulty` to NodeSummary, and it must never import NodeFrontmatter (ADR 002 / ADR 005). Import `NodeSummarySchema` from "./node". Export `GraphDisplayNodeSchema = NodeSummarySchema.extend({ difficulty: z.enum(["beginner","intermediate","advanced"]) })` and `export type GraphDisplayNode = z.infer<typeof GraphDisplayNodeSchema>` immediately after. Write graph.test.ts mirroring node.test.ts structure: a valid fixture, accept case, and reject cases for missing/invalid difficulty and a missing summary field.
  </action>
  <verify>
    <automated>npm test -- src/schemas/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/schemas/graph.test.ts` passes.
    - `grep -nE "NodeFrontmatter|citations|patch_context|meta_volatile" src/schemas/graph.ts` returns no matches (boundary preserved).
    - graph.ts exports GraphDisplayNodeSchema and GraphDisplayNode.
  </acceptance_criteria>
  <done>The projection schema validates difficulty-augmented summaries, rejects malformed input, and imports nothing from the content surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PathwaySchema + tests</name>
  <files>src/schemas/pathway.ts, src/schemas/pathway.test.ts</files>
  <read_first>
    - src/schemas/mastery.ts (analog file shape — PATTERNS §pathway.ts)
    - src/schemas/node.ts (CitationSchema lines 82-95 for the `.min(1)` + `{ error }` Zod v4 idioms)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 7 pathway shape)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Guided-Pathway View Model "Pathway data contract": { id, title, subtitle, steps[] })
  </read_first>
  <behavior>
    - Accepts { id, title, subtitle, steps: ["map-control", ...] } with a non-empty steps array.
    - Rejects an empty steps array.
    - Rejects empty-string id/title/subtitle.
    - Rejects a missing field.
  </behavior>
  <action>
    Create src/schemas/pathway.ts with SPDX header + module JSDoc (D-10: standalone pathway data file structure; Phase 9 extends it with progress/content). Define `PathwaySchema = z.object({ id: z.string().min(1), title: z.string().min(1), subtitle: z.string().min(1), steps: z.array(z.string()).min(1) })` and `export type Pathway = z.infer<typeof PathwaySchema>`. Write pathway.test.ts with accept + reject cases per the behavior block.
  </action>
  <verify>
    <automated>npm test -- src/schemas/pathway.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/schemas/pathway.test.ts` passes.
    - pathway.ts exports PathwaySchema and Pathway.
    - steps uses `.min(1)`; string fields use `.min(1)`.
  </acceptance_criteria>
  <done>PathwaySchema validates the pathway data contract and rejects empty/malformed input.</done>
</task>

<task type="auto">
  <name>Task 3: ADR 005 + CONTEXT.md glossary extension</name>
  <files>docs/adr/005-graph-display-node.md, CONTEXT.md</files>
  <read_first>
    - docs/adr/002-content-graph-decoupling.md (the boundary rule ADR 005 extends; mirror its heading/format)
    - docs/adr/0001-visual-design-direction.md (ADR format reference)
    - CONTEXT.md (existing domain-term entries — match the format when adding terms)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (§GraphDisplayNode Projection — rationale text for ADR 005)
  </read_first>
  <action>
    Create docs/adr/005-graph-display-node.md following the project ADR format (Status: Accepted; Context; Decision; Consequences). Record: the graph face needs `difficulty` (D-04); `NodeSummary` lacks it and `NodeFrontmatter` is off-limits (ADR 002); the decision is a single-field `.extend()` projection `GraphDisplayNode`; the consequence is that adding any further field requires a new ADR so the boundary stays explicit. Extend CONTEXT.md with the new domain terms introduced this phase: GraphDisplayNode (graph projection), pathway step, ancestor chain / prerequisite-chain highlight, spotlight (guided-pathway camera framing), mastery encoding (fill+glow+badge). Keep each entry one line in the existing glossary style.
  </action>
  <verify>
    <automated>test -f docs/adr/005-graph-display-node.md && grep -q "GraphDisplayNode" docs/adr/005-graph-display-node.md && grep -q "GraphDisplayNode" CONTEXT.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - ADR 005 exists with Context/Decision/Consequences sections referencing ADR 002 and D-04.
    - CONTEXT.md contains the five new graph/pathway terms.
  </acceptance_criteria>
  <done>The projection decision is recorded as ADR 005 and the glossary covers the new graph vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static content/pathway data → Zod parse | Untrusted-shaped JSON/frontmatter validated at parse time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | GraphDisplayNode boundary leak | medium | mitigate | Schema adds only `difficulty`; acceptance greps for forbidden NodeFrontmatter imports. |
| T-02-03 | Denial-of-Service | malformed pathway JSON | low | mitigate | PathwaySchema rejects empty/missing fields at parse; consumers fall back to empty state (handled in later plans). |
</threat_model>

<verification>
- Both schema test suites pass.
- graph.ts contains no content-surface imports.
- ADR 005 + CONTEXT.md updated.
</verification>

<success_criteria>
The graph engine has a typed, boundary-safe data contract (GraphDisplayNode) and a validated pathway shape (Pathway), each test-covered, with the projection decision documented.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-02-SUMMARY.md` when done.
</output>
