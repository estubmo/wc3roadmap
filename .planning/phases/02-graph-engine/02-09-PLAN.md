---
phase: 02-graph-engine
plan: 09
type: execute
wave: 4
depends_on: ["02-04", "02-06", "02-08"]
files_modified:
  - src/components/graph/RoadmapGraph.tsx
autonomous: true
requirements: [GRAPH-01, GRAPH-05, GRAPH-06]
user_setup: []

must_haves:
  truths:
    - "The full <ReactFlow> canvas is wrapped in <ClientOnly> (post-hydration only); a placeholder fallback renders during SSR (Pitfall 2)."
    - "Layout is computed once via useMemo(() => computeLayout(rawNodes,'TB'), [rawNodes]) — deterministic, synchronous dagre."
    - "nodeTypes ({mechanic,conceptual}→GraphNode) and edgeTypes ({prerequisite}→GraphEdge) are defined at MODULE SCOPE (Pitfall 1)."
    - "Memoization conventions are present in this first commit: nodesDraggable={false}, onlyRenderVisibleElements, all handlers useCallback (GRAPH-06)."
    - "First load mounts ALL nodes but dims non-pathway nodes to opacity 0.2 (pointer-events off) and fitView-frames the pathway; 'Explore full map' un-dims and refits to all nodes (D-08/D-09)."
  artifacts:
    - src/components/graph/RoadmapGraph.tsx
  key_links:
    - "Node hover/mouse-enter/leave handlers call useGraphStore.setHoveredNode(id, edges) to drive edge highlight (plan 08)."
    - "fitView is scoped to pathway.steps on mount via the fitViewOptions/useEffect path (Pitfall 3); explore handler refits to all nodes with duration 800."
    - "Renders PathwayBanner (plan 07) with exploring state + onToggleExplore; consumes mastery from node data."
  prohibitions:
    - "Do NOT SSR the ReactFlow canvas — it must be inside ClientOnly (no DOM measurement on server)."
    - "Do NOT define nodeTypes/edgeTypes inline in the component (forces remount of all nodes)."
    - "Do NOT call fitView during render — only in useEffect or an event handler."
    - "Do NOT use window.innerWidth for the mobile decision (that is the route wrapper's CSS job, plan 10)."
    - "No detail panel, no search/filter — click handler on nodes is a no-op stub (Phase 3)."
---

<objective>
Assemble the interactive canvas: `RoadmapGraph` wraps `<ReactFlow>` in `<ClientOnly>`, lays out nodes with the pure dagre function via `useMemo`, registers the memoized node/edge components from module scope, applies the full memoization conventions in this first commit, and implements the guided-pathway spotlight (dim non-pathway + fitView to pathway, with an "Explore full map" toggle).

Purpose: This is the core of success criteria 1 (pan/zoom/click, no frame drops), 3 (guided pathway default + explore action), and 5 (memoization from first commit). All the algorithmic and component pieces from earlier waves come together here.
Output: RoadmapGraph.tsx.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
@src/lib/graph-layout.ts
@src/lib/graph-store.ts
@src/schemas/graph.ts
@src/schemas/pathway.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoadmapGraph client-only canvas + memoization conventions</name>
  <files>src/components/graph/RoadmapGraph.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 2 ClientOnly+useMemo+ReactFlowProvider; §Pitfall 1 module-scope nodeTypes/edgeTypes; §Pitfall 2 ClientOnly; §Profiler Verification Strategy — what makes re-renders leak)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Graph Canvas Layout: nodesDraggable false, onlyRenderVisibleElements, minZoom 0.25, maxZoom 2.0, colorMode dark, canvas height calc(100dvh - 56px))
    - src/lib/graph-layout.ts (computeLayout), src/components/graph/GraphNode.tsx (GraphNode), src/components/graph/GraphEdge.tsx (GraphEdge), src/lib/mock-mastery.ts (getMockMastery)
  </read_first>
  <action>
    Create src/components/graph/RoadmapGraph.tsx (SPDX header, named export `RoadmapGraph`). At MODULE SCOPE define `const nodeTypes = { mechanic: GraphNode, conceptual: GraphNode }` and `const edgeTypes = { prerequisite: GraphEdge }`. Props interface `{ nodes: GraphDisplayNode[]; pathway: Pathway }`. Inner `GraphCanvas` component: compute `{ nodes, edges } = useMemo(() => computeLayout(rawNodes, 'TB'), [rawNodes])`, then inject masteryState into each node's data from getMockMastery(node.id) and the pathway-dim style (non-pathway → `{ opacity: 0.2, pointerEvents: 'none' }`) using a `pathwaySet = new Set(pathway.steps)` — keep these derivations memoized so node data references stay stable (Profiler note). Render `<ReactFlowProvider><ReactFlow nodes edges nodeTypes edgeTypes nodesDraggable={false} onlyRenderVisibleElements colorMode="dark" minZoom={0.25} maxZoom={2.0} fitView fitViewOptions={{ nodes: pathway.steps.map(id => ({ id })), padding: 0.2 }} ...handlers /></ReactFlowProvider>`. Wrap GraphCanvas export in `<ClientOnly fallback={<div className="h-full bg-obsidian-950 animate-pulse" />}>{() => <GraphCanvas {...props} />}</ClientOnly>` (ClientOnly from "@tanstack/react-router"). All event handlers (onNodeMouseEnter, onNodeMouseLeave, onNodeClick, handleExplore) use useCallback. onNodeMouseEnter calls `useGraphStore.getState().setHoveredNode(node.id, edges)`; onNodeMouseLeave calls setHoveredNode(null, edges); onNodeClick is a no-op stub. Import "@xyflow/react/dist/style.css" is already global (plan 01) — do not re-import per-component unless needed.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - typecheck + build pass.
    - `grep -n "ClientOnly" src/components/graph/RoadmapGraph.tsx` confirms the canvas is client-only.
    - nodeTypes and edgeTypes are declared before/outside the component function (module scope).
    - `nodesDraggable={false}` and `onlyRenderVisibleElements` are present; handlers are wrapped in useCallback.
    - `grep -n "useMemo" src/components/graph/RoadmapGraph.tsx` confirms layout is memoized on [rawNodes].
  </acceptance_criteria>
  <done>The client-only canvas renders the laid-out graph with all memoization conventions in its first commit and hover-driven edge highlight wired.</done>
</task>

<task type="auto">
  <name>Task 2: Guided-pathway spotlight + Explore full map toggle</name>
  <files>src/components/graph/RoadmapGraph.tsx</files>
  <read_first>
    - .planning/phases/02-graph-engine/02-RESEARCH.md (Pattern 8 fitView spotlight; §Guided-Pathway Spotlight Implementation table — initial vs after-explore opacity/camera/button; §Pitfall 3 fitView timing)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Guided-Pathway View Model; §Animation Contract fitView duration 800, explore reveal duration 0.4)
    - src/components/graph/PathwayBanner.tsx (exploring + onToggleExplore props)
  </read_first>
  <action>
    Extend RoadmapGraph (same file): add `exploring` state (default false). On mount (`useEffect([])`), call `useReactFlow().fitView({ nodes: pathway.steps.map(id => ({ id })), padding: 0.2, duration: 800 })`. `handleExplore` (useCallback): toggle `exploring`; when entering explore mode, set all node styles to opacity 1 / pointer-events auto and `fitView({ duration: 800, padding: 0.15 })`; when returning, restore the non-pathway dim and refit to the pathway. Drive node opacity via the memoized style derivation keyed on `exploring` (avoid recreating node data objects unnecessarily). Render `<PathwayBanner pathway={pathway} totalNodes={nodes.length} exploring={exploring} onToggleExplore={handleExplore} />` as a fixed overlay above the canvas.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - typecheck + build pass.
    - On-mount fitView is scoped to pathway.steps (not all nodes); called from useEffect, never during render.
    - handleExplore toggles between pathway-framed/dimmed and full-map/un-dimmed states and refits the camera.
    - PathwayBanner is rendered with the exploring state and toggle handler.
  </acceptance_criteria>
  <done>First load frames and spotlights the pathway; "Explore full map" reveals and refits the whole graph, satisfying criterion 3 by D-09 intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static node/pathway props → client canvas | Pre-validated data rendered client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Denial-of-Service | hydration mismatch crashing the page | medium | mitigate | Canvas inside ClientOnly; SSR renders a static placeholder (Pitfall 2). |
| T-02-14 | Denial-of-Service | re-render storm from inline nodeTypes | low | mitigate | Module-scope nodeTypes/edgeTypes consts (Pitfall 1). |
</threat_model>

<verification>
- typecheck + build pass.
- ClientOnly wrap, module-scope types, memoization flags, useCallback handlers, scoped fitView, explore toggle all confirmed.
- Profiler-based re-render verification is manual (plan 10 checkpoint).
</verification>

<success_criteria>
A performant, hydration-safe interactive canvas renders the laid-out graph with first-commit memoization conventions, hover edge highlight, and a guided-pathway spotlight with an explore toggle.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-09-SUMMARY.md` when done.
</output>
