---
phase: 02-graph-engine
plan: 10
type: execute
wave: 5
depends_on: ["02-05", "02-07", "02-09"]
files_modified:
  - src/routes/index.tsx
  - src/routes/preview/pathway.tsx
  - src/routes/preview/mastery-states.tsx
  - src/routes/preview/full-map.tsx
  - src/routes/preview/mobile.tsx
autonomous: false
requirements: [GRAPH-01, GRAPH-05]
user_setup: []

must_haves:
  truths:
    - "The index route loader projects the content-collections corpus to GraphDisplayNode[] (difficulty included, no NodeFrontmatter content) and loads pathways/beginner-fundamentals.json parsed by PathwaySchema."
    - "The index route renders the desktop canvas in a `hidden md:block` wrapper and MobileNodeList in a `block md:hidden` wrapper — CSS-only responsive switch, both SSR-rendered (Pitfall 4)."
    - "Four /preview routes render the four acceptance surfaces (pathway, mastery-states, full-map, mobile) reachable via npm run dev."
    - "A human verifies the four preview routes against success criteria 1–4 (pan/zoom/click <3 re-renders, three mastery states, guided pathway + explore, mobile readable)."
  artifacts:
    - src/routes/index.tsx
    - src/routes/preview/pathway.tsx
    - src/routes/preview/mastery-states.tsx
    - src/routes/preview/full-map.tsx
    - src/routes/preview/mobile.tsx
  key_links:
    - "Loader uses allNodes from content-collections, mapping each to { id,title,nodeType,race,prerequisites,difficulty } — the GraphDisplayNode projection (ADR 005 boundary)."
    - "Responsive wrapper: canvas div `hidden md:block` height calc(100dvh - 56px); mobile div `block md:hidden`."
    - "Preview routes import RoadmapGraph / MobileNodeList with mocked mastery for isolated visual acceptance."
  prohibitions:
    - "Loader must NOT pass NodeFrontmatter content fields (citations, patch_context, body) to the graph — projection only (ADR 002)."
    - "No window-based mobile switch — CSS hidden/block only."
    - "Preview routes are dev acceptance surfaces — no production nav links required; tap/click stay no-ops."
---

<objective>
Wire the graph into the application: the index route's loader projects nodes to `GraphDisplayNode[]` and loads the pathway, then renders the desktop canvas and mobile list behind a CSS-only responsive switch. Add the four `/preview/*` acceptance routes, and gate the phase on a human visual verification of the success criteria.

Purpose: This makes the graph reachable (success criteria 1, 3, 4 on the real route) and provides the isolated preview surfaces the UI-SPEC mandates as the visual acceptance bar. The checkpoint covers the manual-only verifications from 02-VALIDATION.md (Profiler re-renders, mastery distinctness, mobile readability).
Output: index.tsx (loader + responsive render) + four preview routes + a human-verify checkpoint.
</objective>

<execution_context>
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/workflows/execute-plan.md
@/home/eirikmo/projects/wc3roadmap/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-engine/02-UI-SPEC.md
@.planning/phases/02-graph-engine/02-RESEARCH.md
@.planning/phases/02-graph-engine/02-VALIDATION.md
@src/routes/index.tsx
@content-collections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index route loader + responsive desktop/mobile render</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (current createFileRoute("/") shape — extend with a loader; PATTERNS §index.tsx modify)
    - content-collections.ts (the nodes collection; allNodes import from "content-collections" — see scripts/validate-content.ts line 24 for the import form)
    - src/schemas/graph.ts (GraphDisplayNodeSchema projection), src/schemas/pathway.ts (PathwaySchema)
    - .planning/phases/02-graph-engine/02-RESEARCH.md (§Mobile Fallback — hidden md:block / block md:hidden wrapper)
    - src/components/graph/RoadmapGraph.tsx, src/components/graph/MobileNodeList.tsx
  </read_first>
  <action>
    Extend src/routes/index.tsx: add a `loader` to createFileRoute("/") that imports `allNodes` from "content-collections", maps each node to the GraphDisplayNode projection (id, title, nodeType, race, prerequisites, difficulty — NO content fields), validates the array with GraphDisplayNodeSchema (or .safeParse per element), reads + JSON.parses pathways/beginner-fundamentals.json and validates with PathwaySchema, and returns `{ nodes, pathway }`. The component reads loader data via the route hook and renders: a 56px control bar, then a `<div className="hidden md:block" style={{ height: 'calc(100dvh - 56px)' }}><RoadmapGraph nodes pathway /></div>` and a `<div className="block md:hidden"><MobileNodeList nodes pathway /></div>`. Both divs are always rendered (SSR); CSS hides the inappropriate one. Handle the empty-pathway/empty-nodes case with the UI-SPEC empty-state copy.
  </action>
  <verify>
    <automated>npm run generate-routes && npm run build:content && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - generate-routes, build:content, typecheck, and build all pass.
    - Loader returns GraphDisplayNode[] (no citations/patch_context/body fields) + a PathwaySchema-validated pathway.
    - Both responsive wrappers are present with `hidden md:block` and `block md:hidden`.
    - `grep -nE "innerWidth|window\." src/routes/index.tsx` returns nothing.
  </acceptance_criteria>
  <done>The home route loads projected nodes + pathway and renders the canvas (desktop) and node list (mobile) via a CSS-only switch.</done>
</task>

<task type="auto">
  <name>Task 2: Four /preview acceptance routes</name>
  <files>src/routes/preview/pathway.tsx, src/routes/preview/mastery-states.tsx, src/routes/preview/full-map.tsx, src/routes/preview/mobile.tsx</files>
  <read_first>
    - src/routes/index.tsx (route shell pattern; PATTERNS §preview routes — identical createFileRoute shell)
    - .planning/phases/02-graph-engine/02-UI-SPEC.md (§Preview Artifacts — exact demonstration checklist for each of the four routes)
    - src/lib/mock-mastery.ts (mocked mastery for isolated previews), src/components/graph/RoadmapGraph.tsx, src/components/graph/MobileNodeList.tsx
  </read_first>
  <action>
    Create four routes under src/routes/preview/ (each: SPDX header, createFileRoute("/preview/<name>"), named component). Each loads the seed corpus (via the same loader projection or a shared helper) + the pathway and renders the relevant surface per the UI-SPEC Preview Artifacts section: `/preview/pathway` — guided-pathway default view (non-pathway dimmed, camera framed, banner, explore button); `/preview/mastery-states` — at least one untouched + one in-progress + one mastered node with both shapes and an edge highlight on hover; `/preview/full-map` — explore-mode full graph at 100% opacity, "Back to pathway" visible, full pan/zoom/click; `/preview/mobile` — MobileNodeList in a constrained (mobile-width) container showing "Your Pathway" then "All Nodes" with all three mastery states across cards. Reuse mock-mastery so all three states appear.
  </action>
  <verify>
    <automated>npm run generate-routes && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - generate-routes, typecheck, build pass with the four routes registered in routeTree.gen.ts.
    - Each route renders its target surface per the UI-SPEC Preview Artifacts checklist.
    - All three mastery states and both node shapes appear across the previews.
  </acceptance_criteria>
  <done>Four reachable /preview routes demonstrate the pathway, mastery states, full-map, and mobile surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of success criteria 1–4</name>
  <action>
    Pause for human verification. Claude has automated everything buildable; the human now runs `npm run dev` and confirms the four preview routes against success criteria 1–4 (Profiler re-renders, mastery-state distinctness, guided-pathway + explore, mobile readability) per the steps below, then approves or lists issues.
  </action>
  <what-built>
    The interactive graph engine: client-only @xyflow/react canvas with dagre layout, three-state mastery nodes, animated prerequisite-chain edge highlight, guided-pathway spotlight + "Explore full map", a CSS-only mobile node-card list, and four /preview acceptance routes.
  </what-built>
  <verify>
    <human-check>Human confirms criteria 1–4 across the four /preview routes (see how-to-verify).</human-check>
  </verify>
  <how-to-verify>
    Run `npm run dev` and verify each acceptance criterion:
    1. Visit http://localhost:3000/preview/full-map. Open React DevTools → Profiler, record a 200px pan drag, stop. Confirm any GraphNode renders ≤2 times across the recording (success criterion 1 / GRAPH-06). Confirm pan, zoom (scroll), and clicking a node all work without visible frame drops.
    2. Visit http://localhost:3000/preview/mastery-states. Confirm three visually distinct states are legible: untouched (dim obsidian, no badge), in-progress (rune-600 ring + soft glow + "Learning"), mastered (rune-500 fill + strong glow + "Mastered"). Confirm MECHANIC (sharp) vs CONCEPTUAL (rounded) shapes differ. Hover a node and confirm its prerequisite-chain edges animate to rune-gold (criterion 2 + D-03).
    3. Visit http://localhost:3000/preview/pathway (and the home route /). Confirm first load frames ~8–12 highlighted pathway nodes with non-pathway nodes dimmed; click "Explore full map" and confirm the camera zooms out and all nodes un-dim, with a "Back to pathway" control (criterion 3).
    4. Visit http://localhost:3000/preview/mobile (and resize the browser below 768px on /). Confirm the canvas is replaced by a readable scrollable node-card list with "Your Pathway" then "All Nodes" sections, each card showing icon + title + mastery badge, without layout breakage (criterion 4).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-collections corpus + pathway JSON → loader (SSR) | Static data projected/validated at load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Information Disclosure | content fields leaking to the graph client | low | mitigate | Loader projects to GraphDisplayNode only; acceptance greps confirm no content fields. |
| T-02-16 | Denial-of-Service | malformed pathway JSON at load | low | mitigate | PathwaySchema.safeParse in loader → empty-state fallback, not a crash. |
</threat_model>

<verification>
- generate-routes + build:content + typecheck + build all pass.
- Loader projection boundary preserved; CSS-only responsive switch.
- Human checkpoint confirms criteria 1–4 against the preview routes.
</verification>

<success_criteria>
The graph is reachable on the home route (desktop canvas + mobile list via CSS switch), four preview routes demonstrate every acceptance surface, and a human has confirmed success criteria 1–4.
</success_criteria>

<output>
Create `.planning/phases/02-graph-engine/02-10-SUMMARY.md` when done.
</output>
