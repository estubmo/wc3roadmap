---
phase: 08-replay-parsing
plan: 03
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/lib/replay-parser.ts
  - src/lib/replay-parser.test.ts
autonomous: true
requirements: [REPLAY-01]
must_haves:
  truths:
    - "parseReplay(buffer) returns a ParserOutput exposing buildNumber, version, matchup, duration, and per-player build order / apm / groupHotkeys / heroCollector / actions"
    - "A malformed or non-.w3g buffer fails safely (rejects/returns an opaque error) rather than crashing or leaking internals"
  artifacts:
    - src/lib/replay-parser.ts
    - src/lib/replay-parser.test.ts
  key_links:
    - "parseReplay is the single server-side I/O wrapper around w3gjs — the ONLY place w3gjs is imported for parsing; the pure signal layer (08-05) consumes its typed output"
---

<objective>
Wrap w3gjs behind a thin, server-only deep module `parseReplay(buffer): Promise<ParserOutput>` — the one place the untrusted `.w3g` binary is turned into typed w3gjs output (REPLAY-01). Establish the fail-safe try/catch discipline so a malformed/adversarial replay returns an opaque error instead of crashing the server function (RESEARCH Security Domain — untrusted binary parsing / DoS).

Purpose: Isolate the binary-parse concern (Node `Buffer`) so the pure semantic layer and the server orchestration never touch w3gjs internals directly.
Output: `src/lib/replay-parser.ts` + tests against a real fixture replay.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-replay-parsing/08-RESEARCH.md
@src/lib/w3champions-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseReplay w3gjs wrapper</name>
  <files>src/lib/replay-parser.ts, src/lib/replay-parser.test.ts</files>
  <read_first>
    - .planning/phases/08-replay-parsing/08-RESEARCH.md ("Code Examples → Parsing a .w3g buffer with w3gjs"; ParserOutput/Player field list)
    - src/lib/w3champions-client.ts (module-header + opaque-failure discipline: "no upstream error strings ever leave this module")
    - src/lib/__fixtures__/README.md + the committed sample `.w3g` (from 08-01)
  </read_first>
  <behavior>
    - parseReplay(<valid fixture buffer>) resolves with buildNumber > 0, a version string, players.length >= 2
    - The returned object exposes per-player build order (units/buildings/upgrades/items .order arrays of {id, ms}), apm, groupHotkeys ({assigned, used}), heroCollector, and actions
    - parseReplay(<garbage buffer>) rejects/returns an opaque failure — never throws an unhandled error with internal parser detail
  </behavior>
  <action>
    Create `src/lib/replay-parser.ts` (SPDX header + module JSDoc stating: server-only, the sole w3gjs import site, untrusted-binary DoS guard). Import default `W3GReplay` from "w3gjs" and re-export the `ParserOutput` and `Player` types. Implement `export async function parseReplay(buffer: Buffer): Promise<ParserOutput>` that `new W3GReplay().parse(buffer)` inside a try/catch; on any parse failure, throw a single opaque `ReplayParseError` (no upstream/internal strings) — mirror the "no error strings leave this module" discipline from w3champions-client.ts. Keep this module free of db/auth/fetch imports. In `replay-parser.test.ts`, parse the real fixture from 08-01 (the Nyquist real-binary vehicle) and assert the field shape; add a garbage-buffer case asserting a clean opaque rejection.
  </action>
  <verify>
    <automated>npx vitest run src/lib/replay-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Valid fixture parses; asserted fields present (buildNumber, version, players[].apm, players[].groupHotkeys, players[].units.order).
    - Garbage buffer produces an opaque `ReplayParseError`, not an unhandled crash or leaked internal message.
    - Module imports nothing from db/auth/fetch layers.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| uploaded/downloaded bytes → parser | Untrusted binary crosses into w3gjs |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03a | Denial of Service | parseReplay(untrusted buffer) | medium | mitigate | try/catch → opaque `ReplayParseError`; a malformed replay fails cleanly. Size cap (ADR 011) enforced upstream in 08-11/08-12 before parse is attempted |
| T-08-03b | Information Disclosure | parser error surface | low | mitigate | No upstream/internal error strings leave the module (w3champions-client.ts T-07-06c precedent) |
</threat_model>

<verification>
- `npx vitest run src/lib/replay-parser.test.ts` green against the real fixture.
</verification>

<success_criteria>
The `.w3g` binary is parseable through a single fail-safe server-only seam that exposes every REPLAY-01 signal field.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `parseReplay(buffer: Buffer): Promise<ParserOutput>` (`src/lib/replay-parser.ts`)
- **Error type:** `ReplayParseError`
- **Re-exported types:** `ParserOutput`, `Player` (from w3gjs)

<output>
Create `.planning/phases/08-replay-parsing/08-03-SUMMARY.md` when done.
</output>
