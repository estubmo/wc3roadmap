---
phase: 08-replay-parsing
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/schema.test.ts
autonomous: true
requirements: [REPLAY-08, REPLAY-05]
must_haves:
  truths:
    - "A replayAnalysis cache table exists, keyed uniquely by gameId, storing signals (JSON text), patchId, and the raw WC3 buildNumber"
    - "The live database has the replayAnalysis table after drizzle-kit push (types alone do not prove this)"
    - "The nodeProgress.source column comment documents 'replay' as a valid value (D-01)"
  artifacts:
    - src/db/schema.ts
  key_links:
    - "replayAnalysis.gameId unique index is the D-17 cache-gate target (global cache, not per-user)"
    - "buildNumber + patchId are both stored (D-12) — raw header build number AND resolved patch"
    - "[BLOCKING] drizzle-kit push must run AFTER the table edit and BEFORE phase verification"
---

<objective>
Add the `replayAnalysis` gameId-keyed cache table (D-17): signals-only (never raw `.w3g` bytes), storing the resolved `patchId` and the raw WC3 `buildNumber` (D-12). Push the additive schema change to the live database so the phase does not verify on a false-positive (types come from Drizzle config, not the live DB).

Purpose: The cache gate ("a replay with a known gameId is never re-parsed", auto-pull dedupe) requires this table to physically exist before the server write path (08-11) runs.
Output: `replayAnalysis` table in `src/db/schema.ts`, pushed to the database; `nodeProgress.source` comment updated for `"replay"`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/db/schema.ts
@.planning/phases/08-replay-parsing/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add replayAnalysis cache table + update source comment</name>
  <files>src/db/schema.ts, src/db/schema.test.ts</files>
  <read_first>
    - src/db/schema.ts (`w3championsSync` table — surrogate-text-PK + single-column `uniqueIndex` shape to mirror; `nodeProgress.source` text() column + its comment)
    - .planning/phases/08-replay-parsing/08-PATTERNS.md ("replayAnalysis cache table (D-17)" section — exact column guidance)
    - docs/adr/009-progress-persistence.md (text()-over-pgEnum convention; source column is already text(), no DDL change)
  </read_first>
  <action>
    In `src/db/schema.ts`, add `export const replayAnalysis = pgTable("replay_analysis", {...})` mirroring the `w3championsSync` surrogate-PK shape: `id: text("id").primaryKey()`, `gameId: text("game_id").notNull()`, `signals: text("signals").notNull()` (JSON-stringified `ReplaySignals`, following the text()-over-structured convention — do NOT store raw `.w3g` bytes, RESEARCH anti-pattern), `patchId: text("patch_id").notNull()`, `buildNumber: integer("build_number").notNull()` (D-12 raw header build number), plus `createdAt`/`updatedAt` timestamps like the analog. Key the unique index on `gameId` ALONE (global cache per D-17, not per-user): `uniqueIndex("replay_analysis_game_id_unique").on(table.gameId)`. Update the `nodeProgress.source` column comment to list `"replay"` as a valid value (D-01) — no DDL change to that column (already text()). Add a `src/db/schema.test.ts` assertion (or extend existing) that the exported `replayAnalysis` object has the gameId unique index and the buildNumber/patchId/signals columns.
  </action>
  <verify>
    <automated>npx vitest run src/db/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `replayAnalysis` exported with columns: id, gameId, signals, patchId, buildNumber, createdAt, updatedAt.
    - Unique index on `gameId` only (D-17 global cache).
    - No raw-bytes column; `nodeProgress.source` comment mentions `"replay"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push additive schema to the database</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - drizzle.config.ts (DATABASE_URL_DIRECT non-pooled connection — pooled pgbouncer breaks drizzle-kit, per STATE decision)
    - package.json (drizzle-kit 0.31.10)
  </read_first>
  <action>
    After Task 1 is complete, run `npx drizzle-kit push` to apply the additive `replay_analysis` table to the live database. This change is additive (new table + a comment-only change on an existing text column) so no interactive prompt should appear. If drizzle-kit presents a destructive/rename prompt (it should not), STOP and flag for manual intervention — do not force. Confirm the table exists after push.
  </action>
  <verify>
    <automated>npx drizzle-kit push && echo "PUSH_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes without an interactive/destructive prompt and prints success.
    - The `replay_analysis` table physically exists in the database (verifiable via drizzle-kit or a `select` against it).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server → database | New cache table stores derived signals (never raw replay bytes) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06a | Information Disclosure | replayAnalysis storage | low | mitigate | Signals-only cache (D-17); no raw `.w3g` bytes → no redistribution/PII-in-binary ambiguity, bounded storage growth |
| T-08-06b | Elevation of Privilege | global gameId cache read | medium | accept | Cache is keyed by gameId (public match id), not user-scoped; it holds only derived signals. All USER progress writes remain principal-keyed in 08-11 |
</threat_model>

<verification>
- `npx vitest run src/db/schema.test.ts` green.
- `npx drizzle-kit push` succeeds; `replay_analysis` table present in DB.
</verification>

<success_criteria>
The gameId-keyed signals cache table exists in the live database, storing buildNumber + patchId + signals — the prerequisite for the cache-gate and auto-pull dedupe.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Table:** `replayAnalysis` (`replay_analysis`) — gameId-keyed signals cache (`src/db/schema.ts`)
- **Index:** `replay_analysis_game_id_unique`
- **Schema comment:** `nodeProgress.source` documents `"replay"` (D-01)

<output>
Create `.planning/phases/08-replay-parsing/08-06-SUMMARY.md` when done. Note the push result explicitly.
</output>
