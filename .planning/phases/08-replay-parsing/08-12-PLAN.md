---
phase: 08-replay-parsing
plan: 12
type: execute
wave: 4
depends_on: [08-08, 08-11]
files_modified:
  - src/hooks/useUploadReplayMutation.ts
  - src/hooks/usePullReplaysMutation.ts
  - src/routes/replays.tsx
autonomous: false
requirements: [REPLAY-04, REPLAY-05, REPLAY-07]
must_haves:
  truths:
    - "A signed-in user can visit /replays, drag-drop a .w3g (size-capped client-side), and see an actionable per-signal report ('you did X at Y; target is Z') with which nodes advanced"
    - "A 'Pull recent replays from w3champions' action triggers auto-pull and surfaces the same report"
    - "Newly-advanced nodes pulse on graph return via setRecentlyAdvanced (D-05); progress cache is invalidated so the graph re-hydrates"
  artifacts:
    - src/routes/replays.tsx
    - src/hooks/useUploadReplayMutation.ts
    - src/hooks/usePullReplaysMutation.ts
  key_links:
    - "upload hook enforces the client-side size cap from ADR 011 BEFORE calling the server (Pitfall 1)"
    - "both hooks call setRecentlyAdvanced(result.advanced) and invalidate progressKeys.byUser() (D-05 pulse + graph re-hydrate)"
    - "report renders each signal as actual+target tied to a node (REPLAY-07) — never a bare stat dump"
---

<objective>
Ship the dedicated Replay Analysis surface (D-13): the `/replays` route with a drag-drop uploader and a "Pull recent replays" action, plus the two TanStack Query mutation hooks. Render the actionable report (REPLAY-07) and wire the D-05 graph pulse. Reuses the Phase 7 sync-hook + button precedent almost exactly.

Purpose: The user-facing entry point for REPLAY-04/05 and the surface where actionable feedback (REPLAY-07) is presented.
Output: `useUploadReplayMutation`, `usePullReplaysMutation`, and the `/replays` route with uploader + pull CTA + report.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/hooks/useSyncW3championsMutation.ts
@src/components/profile/SyncW3championsButton.tsx
@src/routes/preview/auto-advance.tsx
@src/lib/graph-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useUploadReplayMutation + usePullReplaysMutation hooks</name>
  <files>src/hooks/useUploadReplayMutation.ts, src/hooks/usePullReplaysMutation.ts</files>
  <read_first>
    - src/hooks/useSyncW3championsMutation.ts (full shape: useMutation, mutateRef forward-ref, status-bucket switch + sonner toasts, `setRecentlyAdvanced` on advanced ids, `onSettled` dual-invalidation of syncStatus + `progressKeys.byUser()`)
    - src/lib/graph-store.ts (`setRecentlyAdvanced` action — the D-05 pulse mechanism)
    - src/lib/replay-keys.ts, src/lib/progress-keys.ts (invalidation targets)
    - docs/adr/011-replay-parse-architecture.md (the client-side upload size cap to enforce)
  </read_first>
  <action>
    Create `useUploadReplayMutation.ts`: a `useMutation<ReplayReport, Error, FormData>` whose `mutationFn` first enforces the ADR 011 client-side file-size cap (reject oversized files with a clear toast BEFORE calling the server — Pitfall 1) then calls `uploadReplay({ data })`. On result, `switch` over the `ReplayStatus` buckets with per-bucket sonner toasts (mirror the sync hook), call `useGraphStore.getState().setRecentlyAdvanced(result.advanced)` (D-05 pulse), and in `onSettled` invalidate `replayKeys.analysisStatus()` AND `progressKeys.byUser()`. Create `usePullReplaysMutation.ts` matching `useSyncW3championsMutation` almost exactly (zero-arg principal-keyed `pullReplays()` call, same bucket switch + pulse + dual-invalidation). No signed-out branch (authed-only surface).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "useUploadReplayMutation|usePullReplaysMutation" || echo "hooks typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - Upload hook rejects files over the ADR 011 cap before any server call.
    - Both hooks call `setRecentlyAdvanced(result.advanced)` and invalidate `progressKeys.byUser()`.
    - Bucket-branched toasts mirror the sync hook; no signed-out branch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: /replays route — uploader + pull CTA + report</name>
  <files>src/routes/replays.tsx</files>
  <read_first>
    - src/routes/preview/auto-advance.tsx (`createFileRoute` route-file shape + graph-store import usage)
    - src/components/profile/SyncW3championsButton.tsx (mutation-trigger + `useQuery` status-display combo)
    - src/lib/replay-keys.ts (query keys for the analysis status query)
  </read_first>
  <action>
    Create `src/routes/replays.tsx` with `createFileRoute("/replays")({ component: ReplaysRoute })` (SPDX header). Build: (a) a drag-drop `.w3g` uploader wired to `useUploadReplayMutation` (accept `.w3g`, show the size-cap rejection inline); (b) a "Pull recent replays from w3champions" button wired to `usePullReplaysMutation` with a pending spinner (mirror `SyncW3championsButton`'s mutation+useQuery combo); (c) a report section that renders each signal from the result as an actionable line — "you did {actual} at {time}; target for {nodeTitle} is {target}" — tied to its node, plus a list of nodes advanced this analysis (REPLAY-07 / D-16). Import `allNodes` from `content-collections` for node-title lookups. Never render a bare number without its node + target. Keep exact copy/marker styling deferred to UI-SPEC — use functional, unstyled-but-clear placeholders.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "build exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `/replays` route builds and mounts the uploader + pull CTA + report.
    - Report renders each signal as actual+target tied to a node title (never a bare stat).
    - Route compiles into the generated route tree (build succeeds).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the end-to-end replay analysis flow</name>
  <action>
    Pause for human verification. Claude has automated everything buildable (uploader, pull action, report, graph pulse wiring); the human now runs the app, signs in, and exercises the upload + auto-pull + report + pulse flow per the steps below, then approves or lists issues.
  </action>
  <what-built>The /replays route: drag-drop uploader (client size-capped), a "Pull recent replays" action, and an actionable per-signal report with graph pulse on advanced nodes.</what-built>
  <how-to-verify>
    1. Run the app locally and sign in with Battle.net.
    2. Visit `/replays`. Drag-drop a real `.w3g` 1v1 replay.
    3. Confirm the report lists each signal as "you did X at Y; target is Z" tied to a node, and lists which nodes advanced.
    4. Return to the graph; confirm newly-advanced nodes pulse and show "Mastered · from replay" labeling.
    5. Click "Pull recent replays"; confirm it fetches, reports, and that re-pulling a known gameId is fast (cached, not re-parsed).
    6. Try an oversized/non-.w3g file; confirm a clear client-side rejection.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → uploadReplay | File selection + client-side size pre-check before the server parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12a | Denial of Service | oversized upload | medium | mitigate | Client-side size cap (ADR 011) rejects before upload; server re-validates (08-11) — never trust the client check alone |
| T-08-12b | Information Disclosure | report rendering | low | mitigate | Report shows only the principal's own derived signals (server read is principal-keyed); no other user's data reachable |
</threat_model>

<verification>
- `npm run build` succeeds with the `/replays` route present.
- Hooks typecheck; human verification of the end-to-end flow approved.
</verification>

<success_criteria>
A signed-in user can upload or auto-pull a replay on /replays and receive actionable, node-tied feedback with newly-advanced nodes pulsing on the graph.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Hook:** `useUploadReplayMutation` (`src/hooks/useUploadReplayMutation.ts`)
- **Hook:** `usePullReplaysMutation` (`src/hooks/usePullReplaysMutation.ts`)
- **Route:** `/replays` (`src/routes/replays.tsx`) — uploader + pull CTA + actionable report

<output>
Create `.planning/phases/08-replay-parsing/08-12-SUMMARY.md` when done.
</output>
