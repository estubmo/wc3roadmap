---
phase: 08-replay-parsing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mastery-ordinal.ts
  - src/lib/mastery-ordinal.test.ts
  - src/schemas/progress.ts
autonomous: true
requirements: [REPLAY-06]
must_haves:
  truths:
    - "masteryStateIndex('untouched'|'in-progress'|'mastered') returns 0|1|2 matching MasteryStateSchema enum order"
    - "The progress source enum accepts 'replay' as a valid value (D-01)"
  artifacts:
    - src/lib/mastery-ordinal.ts
    - src/lib/mastery-ordinal.test.ts
  key_links:
    - "mastery-ordinal ordering MUST match MasteryStateSchema.options order (drift guard, Pitfall 5)"
    - "source enum extension is the Zod-layer half of D-01; the DB text() column needs no DDL change"
---

<objective>
Create the ordinal registry helper that makes the monotonic-max upsert (D-04) expressible, and extend the mastery `source` enum with `"replay"` (D-01, Zod layer). These are the two smallest, purely-additive foundations the replay write path depends on.

Purpose: `masteryStateIndex()` gives the write path a single, drift-guarded comparison idiom (mirrors `tierIndex()`), and the `source` enum extension lets the server stamp `source:"replay"`.
Output: `src/lib/mastery-ordinal.ts` + tests; `source` enum in `src/schemas/progress.ts` extended to include `"replay"`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/mmr-tiers.ts
@src/schemas/progress.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mastery-ordinal.ts ordinal registry</name>
  <files>src/lib/mastery-ordinal.ts, src/lib/mastery-ordinal.test.ts</files>
  <read_first>
    - src/lib/mmr-tiers.ts (the private-array + public-readonly-view + `tierIndex()` ordinal-registry pattern to mirror exactly)
    - src/schemas/progress.ts (MasteryStateSchema — the authoritative enum order `untouched`,`in-progress`,`mastered`)
  </read_first>
  <behavior>
    - masteryStateIndex("untouched") === 0
    - masteryStateIndex("in-progress") === 1
    - masteryStateIndex("mastered") === 2
    - masteryStateIndex(unknown) === -1
    - The registry array order equals MasteryStateSchema.options (assert at runtime to catch drift)
  </behavior>
  <action>
    Create `src/lib/mastery-ordinal.ts` with the SPDX header. Mirror `mmr-tiers.ts`: a private `_MASTERY_STATES` array (`{ id: "untouched", order: 0 }`, `in-progress`=1, `mastered`=2) `as const satisfies`, a public readonly `MASTERY_STATES` view, and `export function masteryStateIndex(id: string): number` via `findIndex` (returns -1 for unknown). Add a top doc-comment explicitly linking the array order to `MasteryStateSchema` in `src/schemas/progress.ts` as the drift-prevention doc (Pitfall 5), mirroring mmr-tiers.ts's own rationale-comment style. In `mastery-ordinal.test.ts`, include a test asserting the registry ids equal `MasteryStateSchema.options` so a future schema reorder fails loudly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/mastery-ordinal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - masteryStateIndex returns 0/1/2 for the three states and -1 for unknown.
    - A test asserts the ordinal order matches `MasteryStateSchema.options`.
    - File carries the SPDX `GPL-3.0-or-later` header (line 1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend progress source enum with "replay" (D-01)</name>
  <files>src/schemas/progress.ts</files>
  <read_first>
    - src/schemas/progress.ts (ProgressRecordSchema `source: z.enum(["manual","auto","quiz"]).default("manual")` at ~line 95)
    - docs/adr/009-progress-persistence.md (forward-designed text() source field — no pgEnum migration needed)
  </read_first>
  <action>
    In `src/schemas/progress.ts`, change the `source` enum from `z.enum(["manual", "auto", "quiz"])` to `z.enum(["manual", "auto", "quiz", "replay"])` (keep `.default("manual")`). Update the adjacent JSDoc to document `replay` per D-01: server-stamped only, the strongest evidence, the only source permitted to reach `mastered`, applied via monotonic-max upsert (never latest-write-wins). Do NOT change `src/db/schema.ts` here — the DB `source` column is already `text()` and requires no DDL change (D-01); its comment is updated in plan 08-06.
  </action>
  <verify>
    <automated>npx vitest run src/schemas 2>/dev/null; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i progress.ts || echo "no type errors in progress.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `ProgressRecordSchema.shape.source` parses `"replay"` as valid and still defaults to `"manual"`.
    - JSDoc documents the `replay` source semantics (server-stamped, mastered-capable, monotonic-max).
    - No DDL / schema.ts change made in this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure schema/lib additions; no I/O, no user input crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02a | Tampering | source enum widening | low | accept | Adding `"replay"` is additive; `source` remains server-stamped (never client-supplied) at every write site — enforced in 08-11 |
</threat_model>

<verification>
- `npx vitest run src/lib/mastery-ordinal.test.ts` green.
- `source` enum accepts `"replay"`.
</verification>

<success_criteria>
Ordinal helper and source enum extension exist and are tested — the two smallest foundations for the monotonic-max replay write.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `masteryStateIndex(id: string): number` (`src/lib/mastery-ordinal.ts`)
- **Constant:** `MASTERY_STATES` readonly ordinal registry
- **Schema change:** `ProgressRecordSchema.source` enum + `"replay"` (D-01)

<output>
Create `.planning/phases/08-replay-parsing/08-02-SUMMARY.md` when done.
</output>
