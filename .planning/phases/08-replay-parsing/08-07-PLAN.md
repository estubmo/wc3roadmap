---
phase: 08-replay-parsing
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/node.ts
  - src/schemas/node.test.ts
  - content-collections.ts
autonomous: true
requirements: [REPLAY-06, REPLAY-08]
must_haves:
  truths:
    - "A MECHANIC node can declare a single replay-criteria signal+threshold in frontmatter (buildOrderTiming | eapm | controlGroupUsage | heroTiming | expansionTiming)"
    - "The replay-criteria schema is defined once in src/schemas/node.ts and mirrored field-for-field in content-collections.ts"
  artifacts:
    - src/schemas/node.ts
    - content-collections.ts
  key_links:
    - "ReplayCriteriaSchema in node.ts MUST stay field-for-field identical to its inline mirror in content-collections.ts (parallel-schema-sync) — build fails otherwise"
    - "single signal+threshold per node (D-02 precedent), NOT a compound rule engine"
---

<objective>
Extend node frontmatter with a per-node replay-mastery criterion (REPLAY-06/08), mirroring the existing `autoDetect` discriminated-union + parallel-schema-sync pattern. Authors declare a single replay signal + threshold per MECHANIC node; the pure threshold detector (08-09) reads it.

Purpose: Replay thresholds live in content (like `autoDetect`), so authors add/tune eligible nodes without touching detection code, and the four canonical build-order nodes (08-10) have a schema to fill.
Output: `ReplayCriteriaSchema` in `src/schemas/node.ts` (optional field on `NodeFrontmatterSchema`) + its field-for-field inline mirror in `content-collections.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/schemas/node.ts
@content-collections.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReplayCriteriaSchema in node.ts</name>
  <files>src/schemas/node.ts, src/schemas/node.test.ts</files>
  <read_first>
    - src/schemas/node.ts (`AutoDetectCriteriaSchema` discriminated union + its PARALLEL-SCHEMA SYNC NOTE + `NodeFrontmatterSchema.autoDetect` optional-field wiring — the exact pattern to parallel)
    - .planning/phases/08-replay-parsing/08-PATTERNS.md ("node.ts + content-collections.ts — replay criteria frontmatter" — field candidates)
    - .planning/phases/08-replay-parsing/08-CONTEXT.md (D-09 signal list; Claude's Discretion: threshold location — per-node frontmatter chosen here per RESEARCH)
  </read_first>
  <behavior>
    - A node frontmatter with { replayCriteria: { signal: "buildOrderTiming", beforeMs: 300000 } } parses valid
    - A node with { replayCriteria: { signal: "eapm", gte: 150 } } parses valid
    - An unknown signal discriminant fails parsing
    - replayCriteria is optional — nodes without it parse fine (graceful default, never advance from replay)
  </behavior>
  <action>
    In `src/schemas/node.ts`, add `export const ReplayCriteriaSchema = z.discriminatedUnion("signal", [...])` directly above where it will be referenced, carrying a PARALLEL-SCHEMA SYNC NOTE identical in style to `AutoDetectCriteriaSchema`'s. Model one variant per D-09 race-agnostic signal, single signal+threshold each: `buildOrderTiming` (a target `beforeMs` for a keyed build step), `eapm` (`gte` number), `controlGroupUsage` (`gte` number), `heroTiming` (`beforeMs`), `expansionTiming` (`beforeMs`). Add optional `replayCriteria: ReplayCriteriaSchema.optional()` to `NodeFrontmatterSchema` with a JSDoc mirroring the `autoDetect` field note (MECHANIC-only, graceful-absent default, MUST stay mirrored). Export the inferred `ReplayCriteria` type. Cover valid/invalid/absent cases in `node.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/schemas/node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ReplayCriteriaSchema` parses each of the five signal variants; rejects unknown discriminants.
    - `NodeFrontmatterSchema` accepts nodes with and without `replayCriteria`.
    - Field carries a PARALLEL-SCHEMA SYNC NOTE referencing content-collections.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mirror replayCriteria in content-collections.ts</name>
  <files>content-collections.ts</files>
  <read_first>
    - content-collections.ts (inline `autoDetect` discriminated-union mirror + its PARALLEL-SCHEMA SYNC NOTE — the exact spot and style to copy)
    - src/schemas/node.ts (the `ReplayCriteriaSchema` just authored — the source of truth to mirror field-for-field)
  </read_first>
  <action>
    In `content-collections.ts`, add an inline `replayCriteria: z.discriminatedUnion("signal", [...]).optional()` mirror immediately alongside the existing `autoDetect` inline schema, field-for-field identical to `ReplayCriteriaSchema` in `src/schemas/node.ts`. Include a PARALLEL-SCHEMA SYNC NOTE pointing back to node.ts (both directions), matching the existing `autoDetect` mirror note. Do not add transform logic — this is a validation-surface mirror only.
  </action>
  <verify>
    <automated>npm run build:content 2>/dev/null || npx content-collections build 2>/dev/null; echo "content build exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The inline `replayCriteria` union is field-for-field identical to `ReplayCriteriaSchema` (same signals, same threshold field names/types).
    - Content build succeeds with the mirrored field present.
    - Both definitions carry cross-referencing PARALLEL-SCHEMA SYNC NOTES.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content authoring → build | Node frontmatter is validated at build time by Zod |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07a | Tampering | node frontmatter criteria | low | mitigate | Zod discriminated-union validation at build time (both surfaces); malformed criteria fail the content build, never reach runtime |
</threat_model>

<verification>
- `npx vitest run src/schemas/node.test.ts` green.
- Content build succeeds with the mirrored `replayCriteria` field.
</verification>

<success_criteria>
MECHANIC nodes can declare a patch-aware replay criterion in content, validated identically at runtime and build time.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Schema:** `ReplayCriteriaSchema` (`src/schemas/node.ts`) + inferred `ReplayCriteria` type
- **Frontmatter field:** `NodeFrontmatterSchema.replayCriteria` (optional)
- **Mirror:** inline `replayCriteria` union in `content-collections.ts`

<output>
Create `.planning/phases/08-replay-parsing/08-07-SUMMARY.md` when done.
</output>
