---
phase: 08-replay-parsing
plan: 09
type: execute
wave: 2
depends_on: [08-02, 08-04, 08-05, 08-07]
files_modified:
  - src/lib/replay-thresholds.ts
  - src/lib/replay-thresholds.test.ts
autonomous: true
requirements: [REPLAY-06, REPLAY-07]
must_haves:
  truths:
    - "detectReplaySignals(nodes, signals, patchId) emits, for each MECHANIC node whose replayCriteria the signals meet, a per-node target masteryState plus the actual value and target (for feedback)"
    - "Only MECHANIC nodes with replayCriteria are evaluated; a met threshold yields target 'mastered' (D-02 — replay is mastered-capable)"
    - "The detector is pure (no db/fetch/auth) and NOT untouched-only (D-11) — it may target nodes that already have progress"
  artifacts:
    - src/lib/replay-thresholds.ts
    - src/lib/replay-thresholds.test.ts
  key_links:
    - "each emitted result carries { nodeId, targetState, actual, target, signal } so REPLAY-07 feedback is 'you did X (actual) at Y; target is Z' by construction"
    - "patch-aware: criteria/object-id resolution keys off patchId via object-id-maps (08-04) and patches.ts"
---

<objective>
Build the pure patch-aware threshold detector (REPLAY-06): `detectReplaySignals(nodes, signals, patchId)` maps derived replay signals against each MECHANIC node's `replayCriteria` and emits per-node target `masteryState` — plus the actual measured value and the target, so the feedback layer (REPLAY-07) can render "you did X at Y; target is Z" without re-deriving anything. Mirrors `detect-mastery-signals.ts` discipline but is mastered-capable (D-02) and NOT untouched-only (D-11).

Purpose: The single pure decision function the server write path calls; keeps threshold logic independently unit-testable across patch/threshold edges.
Output: `src/lib/replay-thresholds.ts` (pure) + tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/detect-mastery-signals.ts
@src/lib/replay-signals.ts
@src/lib/object-id-maps/index.ts
@src/lib/mastery-ordinal.ts
@.planning/phases/08-replay-parsing/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detectReplaySignals pure threshold detector</name>
  <files>src/lib/replay-thresholds.ts, src/lib/replay-thresholds.test.ts</files>
  <read_first>
    - src/lib/detect-mastery-signals.ts (filter-chain-then-map shape + `meetsThreshold` — the exact structure to mirror; note the untouched-only filter that MUST NOT be carried over here per D-11)
    - src/lib/replay-signals.ts (`ReplaySignals` input contract)
    - src/schemas/node.ts (`ReplayCriteria` discriminated union — the per-node criterion to evaluate)
    - src/lib/object-id-maps/index.ts (`objectIdMapVersionForPatch`/`resolveObjectId` for patch-correct build-order identity)
  </read_first>
  <behavior>
    - A MECHANIC node with { signal: "buildOrderTiming", beforeMs } whose signals hit the keyed step before beforeMs → emits { nodeId, targetState: "mastered", actual: <ms>, target: beforeMs, signal }
    - A node whose signals miss the threshold → not emitted
    - A CONCEPTUAL node, or a node without replayCriteria → never emitted
    - eapm/controlGroupUsage/heroTiming/expansionTiming variants each evaluate their own actual-vs-threshold and carry actual+target
    - Pure: identical input → identical output; no db/fetch/auth imports; NOT filtered by existing progress
  </behavior>
  <action>
    Create `src/lib/replay-thresholds.ts` (SPDX header + module JSDoc STRUCTURAL GUARANTEES: MECHANIC-only, replayCriteria-required, D-02 mastered-capable, D-11 NOT untouched-only, "NO I/O" line). Define a minimal `ReplayThresholdInput` node subset (`id`, `nodeType`, `replayCriteria`, `patchId`) and an output `ReplayNodeResult { nodeId; targetState: MasteryState; actual: number | null; target: number; signal: string }`. Implement `export function detectReplaySignals(nodes: ReplayThresholdInput[], signals: ReplaySignals, patchId: string): ReplayNodeResult[]` as a filter chain: `nodeType === "MECHANIC"` → `replayCriteria !== undefined` → `meetsReplayThreshold(criteria, signals, patchId)` → map to a result carrying `targetState: "mastered"` (D-02), the actual measured value, and the target. Factor `meetsReplayThreshold` as a per-signal switch that uses `object-id-maps` (`objectIdMapVersionForPatch(patchId)` + `resolveObjectId`) to identify the correct build-order/expansion object for the replay's patch (patch-aware, REPLAY-08). Do NOT include any `existingProgressNodeIds` filter (D-11). Cover every signal variant + boundary + miss + non-MECHANIC in tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/replay-thresholds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five signal variants has a passing and a failing test asserting emit/no-emit.
    - Every emitted result carries `actual`, `target`, and `signal` (REPLAY-07 feedback data).
    - Emitted `targetState` is `"mastered"` (D-02); CONCEPTUAL and criteria-less nodes never emit.
    - Purity + no-I/O asserted; no untouched-only filter present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure decision function over already-derived signals |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09a | Tampering | threshold evaluation | low | mitigate | Pure + deterministic; only MECHANIC nodes with valid criteria emit; the 1v1 gate (08-05) and principal-keyed write (08-11) bound whether a result becomes a real advancement |
</threat_model>

<verification>
- `npx vitest run src/lib/replay-thresholds.test.ts` green (patch/threshold edges covered).
</verification>

<success_criteria>
A pure, patch-aware detector turns signals into per-node mastered-capable targets carrying the exact actual+target values REPLAY-07 needs.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `detectReplaySignals(nodes, signals, patchId): ReplayNodeResult[]`
- **Function:** `meetsReplayThreshold(criteria, signals, patchId): boolean`
- **Types:** `ReplayThresholdInput`, `ReplayNodeResult`

<output>
Create `.planning/phases/08-replay-parsing/08-09-SUMMARY.md` when done.
</output>
