---
phase: 08-replay-parsing
plan: 13
type: execute
wave: 5
depends_on: [08-03, 08-05, 08-11, 08-12]
files_modified:
  - src/lib/wc3v/README.md
  - src/lib/replay-advanced.ts
  - src/lib/replay-advanced.test.ts
  - src/server/replay-advanced.ts
  - src/routes/replays.tsx
  - docs/adr/012-wc3v-fork-integration.md
autonomous: false
requirements: [REPLAY-03]
must_haves:
  truths:
    - "Vendored wc3v lib/ analysis modules (GPL-3.0 headers preserved) produce supply/economy curves, precise expansion detection, and compare-to-pro output for a parsed replay"
    - "The advanced layer is fully additive and isolatable — the base mastery loop (08-01..12) ships complete whether or not this plan lands (D-07 descope valve)"
    - "A go/no-go decision on the w3gjs 3.0.0→4.1.0 internals patch is made and recorded before the vendoring effort is sunk"
  artifacts:
    - src/lib/replay-advanced.ts
    - src/server/replay-advanced.ts
    - docs/adr/012-wc3v-fork-integration.md
  key_links:
    - "vendor ONLY wc3v lib/ analysis modules (ResourceSeries, BuildingBackfill, BattleDetector, Player, World, ...) — NEVER client/, canvas, or stormjs (RESEARCH anti-pattern)"
    - "GPL-3.0 headers preserved on every vendored file (ADR 004 compatibility)"
    - "advanced analysis lives in its OWN module + server fn + report section — zero edits to the base 08-11 server path, so descope removes a self-contained set"
---

<objective>
Integrate the wc3v (GPL-3.0) fork as an additive advanced-analysis layer (REPLAY-03, D-06): supply/economy curves, precise expansion detection, compare-to-pro — on top of the w3gjs base signals. Structured as a fully isolatable slice so D-07's descope valve (split to Phase 8.x) removes a self-contained set of new files with zero edits to the base mastery loop. Gated by a human-verified vendor step and an early go/no-go on the w3gjs-version internals patch (Pitfall 2) — this is the third D-08 planning-gate spike (wc3v fork feasibility).

Purpose: Deliver criterion 4 in-phase if feasible within budget; keep it droppable so the phase stays shippable regardless (D-07).
Output: vendored `src/lib/wc3v/*`, `replay-advanced.ts` wrapper, `server/replay-advanced.ts` fn, a report section, and ADR 012.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-replay-parsing/08-RESEARCH.md
@docs/adr/004-gpl3-licensing.md
@src/lib/replay-parser.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify wc3v vendoring (GPL-3.0) + w3gjs-patch go/no-go</name>
  <action>
    Pause for human verification before vendoring third-party GPL-3.0 source. The human confirms wc3v's license + lib/-only import isolation on GitHub, reviews the executor's time-boxed finding on the w3gjs 3.0.0→4.1.0 internals patch (Pitfall 2), and decides GO (proceed in-phase) or NO-GO (descope to Phase 8.x per D-07). No source is vendored until GO.
  </action>
  <what-built>Nothing yet — this gate precedes vendoring third-party GPL-3.0 source and decides whether the wc3v integration proceeds in-phase or exercises the D-07 descope valve.</what-built>
  <how-to-verify>
    1. Open https://github.com/jblanchette/wc3v and confirm: GPL-3.0 LICENSE, active repo, and that the `lib/` analysis modules (ResourceSeries.js, BuildingBackfill.js, BattleDetector.js, Player.js, World.js) import only `rbush` + local helpers (NOT canvas / stormjs / d3).
    2. Confirm the intent to vendor ONLY `lib/` (never `client/`, `marketing/`, `tools/`).
    3. Review the executor's time-boxed finding (from a pre-check spike it will report) on whether w3gjs 4.1.0 already tolerates truncated/single-player blocks natively OR the wc3v `StatefulBufferParser` patch needs porting (Pitfall 2). Decide GO (proceed in-phase) or NO-GO (descope to Phase 8.x per D-07 — base signals already ship complete).
  </how-to-verify>
  <resume-signal>Type "GO" to vendor and proceed, or "NO-GO — descope to 8.x" (base loop already complete).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vendor wc3v lib/ + replay-advanced wrapper</name>
  <files>src/lib/wc3v/README.md, src/lib/replay-advanced.ts, src/lib/replay-advanced.test.ts, docs/adr/012-wc3v-fork-integration.md</files>
  <read_first>
    - .planning/phases/08-replay-parsing/08-RESEARCH.md (Spike 3 findings; wc3v lib/ module list + import-chain isolation; Pitfall 2; Open Questions 3/4 compare-to-pro dataset)
    - docs/adr/004-gpl3-licensing.md (GPL-3.0-or-later compatibility; header requirements)
    - src/lib/replay-parser.ts (ParserOutput the advanced layer consumes)
  </read_first>
  <action>
    Vendor the required wc3v `lib/` analysis modules into `src/lib/wc3v/` — ONLY the modules whose import chains were verified canvas/stormjs-free (ResourceSeries, BuildingBackfill, BattleDetector, Player, World, Unit and their local helpers). Preserve each file's upstream GPL-3.0 header verbatim; add `src/lib/wc3v/README.md` documenting provenance (jblanchette/wc3v commit), the lib/-only scope, and that these are vendored under GPL-3.0. If the go/no-go decided the w3gjs patch is needed, apply/port it in an isolated adapter within `src/lib/wc3v/` (never reaching into node_modules). Create `src/lib/replay-advanced.ts`: `export function deriveAdvancedAnalysis(parsed: ParserOutput): AdvancedAnalysis` wrapping the vendored modules to emit supply/economy curves, precise expansion detection, and compare-to-pro output — pure, server-side, imports nothing from db/auth. Record the integration approach + the go/no-go outcome + compare-to-pro dataset handling (Open Question 4) in `docs/adr/012-wc3v-fork-integration.md`. Test `deriveAdvancedAnalysis` against a fixture ParserOutput asserting the output shape.
  </action>
  <verify>
    <automated>npx vitest run src/lib/replay-advanced.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/wc3v/*` contains only lib/ analysis modules; every vendored file keeps its GPL-3.0 header; no canvas/stormjs/client code vendored.
    - `deriveAdvancedAnalysis(fixture)` returns supply/economy + expansion + compare-to-pro shaped output.
    - ADR 012 records the go/no-go outcome and integration approach.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Additive advanced server fn + report section (isolatable)</name>
  <files>src/server/replay-advanced.ts, src/routes/replays.tsx</files>
  <read_first>
    - src/server/replay.ts (authMiddleware + lexical createServerFn pattern to mirror — but do NOT edit this file; the advanced fn lives in its own module for descope isolation)
    - src/routes/replays.tsx (the report surface from 08-12 to extend with an additive advanced section)
    - src/lib/replay-advanced.ts (from Task 2)
  </read_first>
  <action>
    Create `src/server/replay-advanced.ts`: `analyzeReplayAdvancedHandler({ data, context })` (principal-keyed) that parses the replay (reusing `parseReplay`) and returns `deriveAdvancedAnalysis(parsed)` — exported as `analyzeReplayAdvanced = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((d: FormData) => d).handler(analyzeReplayAdvancedHandler)`, lexically visible, in its OWN file (zero edits to 08-11's `src/server/replay.ts`, preserving descope isolation). Add an additive "Advanced analysis" section to `src/routes/replays.tsx` that conditionally renders supply/economy curves + expansion detail + compare-to-pro when advanced output is present — a self-contained block that can be deleted wholesale if descoped. Keep exact visualization styling deferred to UI-SPEC (functional placeholders).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "build exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `analyzeReplayAdvanced` is principal-keyed, lexical createServerFn, in its own file — `src/server/replay.ts` is unmodified by this plan.
    - The advanced report section is an additive, self-contained block in `/replays`.
    - Build succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git source → build | Third-party GPL-3.0 wc3v source vendored into the tree |
| client → analyzeReplayAdvanced | Untrusted `.w3g` binary crosses into vendored analysis code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC2 | Tampering | wc3v vendoring | high | mitigate | Blocking-human vendor checkpoint (Task 1); lib/-only scope verified canvas/stormjs-free; GPL-3.0 headers preserved (ADR 004) |
| T-08-13a | Denial of Service | advanced parse of untrusted replay | medium | mitigate | Reuses `parseReplay` try/catch + upstream size cap; advanced analysis runs only after a successful base parse |
| T-08-13b | Elevation of Privilege | advanced server fn | high | mitigate | Principal-keyed via authMiddleware; no userId channel; read-only analysis (no writes) |
</threat_model>

<verification>
- `npx vitest run src/lib/replay-advanced.test.ts` green.
- `npm run build` succeeds with the advanced section present.
- If NO-GO: this plan is not executed; base loop (08-01..12) already satisfies REPLAY-01/02/04/05/06/07/08 and the phase ships with REPLAY-03 split to Phase 8.x.
</verification>

<success_criteria>
Advanced wc3v analysis (supply/economy, expansion, compare-to-pro) is delivered as an additive, isolatable layer — or cleanly descoped to Phase 8.x per D-07 with the base mastery loop intact.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Vendored:** `src/lib/wc3v/*` (GPL-3.0 lib/ analysis modules) + `src/lib/wc3v/README.md`
- **Function:** `deriveAdvancedAnalysis(parsed): AdvancedAnalysis` (`src/lib/replay-advanced.ts`)
- **Server fn:** `analyzeReplayAdvanced` / `analyzeReplayAdvancedHandler` (`src/server/replay-advanced.ts`)
- **File:** `docs/adr/012-wc3v-fork-integration.md`
- **UI:** additive advanced-analysis section in `/replays`

<output>
Create `.planning/phases/08-replay-parsing/08-13-SUMMARY.md` when done. If descoped (NO-GO), record the D-07 split-to-8.x decision explicitly.
</output>
