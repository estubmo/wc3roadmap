---
phase: 08-replay-parsing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/object-id-maps/index.ts
  - src/lib/object-id-maps/index.test.ts
autonomous: true
requirements: [REPLAY-08]
must_haves:
  truths:
    - "An object-ID string from a replay resolves to a human-readable node-relevant name/criteria via a map keyed by patches.ts objectIdMapVersion"
    - "The map resolves by objectIdMapVersion (not patch id directly) so patches sharing a version share one table"
  artifacts:
    - src/lib/object-id-maps/index.ts
    - src/lib/object-id-maps/index.test.ts
  key_links:
    - "object-ID map version is resolved via getPatch(patchId).objectIdMapVersion — the reserved patches.ts hook (D-12), never the raw buildNumber directly"
---

<objective>
Build the patch-aware object-ID map registry (REPLAY-08, D-12): a project-owned lookup that resolves w3gjs's raw object-ID strings to node-relevant meaning, keyed by `objectIdMapVersion` from the patch registry. This is the project's OWN patch-versioned map — NOT w3gjs's bundled `mappings.ts` (RESEARCH Pitfall 3).

Purpose: Threshold detection (08-09) needs stable, patch-correct identity for build-order/expansion units; a later patch can reuse an object ID for a different unit, so resolution must be version-keyed.
Output: `src/lib/object-id-maps/index.ts` registry + lookup + tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/patches.ts
@.planning/phases/08-replay-parsing/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: object-id-maps registry keyed by objectIdMapVersion</name>
  <files>src/lib/object-id-maps/index.ts, src/lib/object-id-maps/index.test.ts</files>
  <read_first>
    - src/lib/patches.ts (PatchEntry.objectIdMapVersion hook; `_PATCHES` private-array + `getPatch()` + readonly-view convention to mirror)
    - .planning/phases/08-replay-parsing/08-RESEARCH.md (Pitfall 3 — do NOT treat w3gjs bundled mappings as authoritative)
    - .planning/phases/08-replay-parsing/08-PATTERNS.md ("patches.ts extension for object-ID maps" section)
  </read_first>
  <behavior>
    - resolveObjectId(id, version) returns the mapped entry (e.g. { name, kind } or a node-criteria-relevant label) for a known id at a known version
    - resolveObjectId(unknownId, version) returns null (graceful, never throws)
    - objectIdMapVersionForPatch(patchId) === getPatch(patchId).objectIdMapVersion
    - Two patch ids sharing objectIdMapVersion resolve the same table (no duplication)
  </behavior>
  <action>
    Create `src/lib/object-id-maps/index.ts` (SPDX header). Mirror the `patches.ts` shape: a private map `_OBJECT_ID_MAPS: Record<number /*objectIdMapVersion*/, Record<string /*objectId*/, ObjectIdEntry>>`, a public readonly view, and pure lookups: `export function objectIdMapVersionForPatch(patchId: string): number` (delegates to `getPatch(patchId).objectIdMapVersion` — resolve via the patch entry, never the raw buildNumber), and `export function resolveObjectId(objectId: string, version: number): ObjectIdEntry | null`. Seed version `1` with the object IDs actually needed this phase for build-order + expansion detection (the four race town-hall/expansion buildings + key opening units referenced by the 08-10 content nodes). Add a doc-comment stating this is the project's patch-owned map, NOT w3gjs's bundled table (Pitfall 3), and that w3gjs `id` strings are INPUTS to this lookup. Cover all behaviors in `index.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/object-id-maps/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `objectIdMapVersionForPatch(CURRENT_PATCH.id)` equals the registry hook value.
    - `resolveObjectId` returns entries for seeded ids and `null` for unknown ids without throwing.
    - Registry keyed by `objectIdMapVersion` (version-sharing patches share one table).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure lookup module; replay-supplied object-id strings are treated as opaque keys (null on miss) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04a | Tampering | replay-supplied object ids | low | mitigate | Unknown/adversarial ids resolve to `null`, never used to index unsafely or throw |
</threat_model>

<verification>
- `npx vitest run src/lib/object-id-maps/index.test.ts` green.
</verification>

<success_criteria>
Patch-aware object-ID resolution exists, version-keyed off the reserved patches.ts hook (D-12), independent of w3gjs's bundled table.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `resolveObjectId(objectId, version): ObjectIdEntry | null`
- **Function:** `objectIdMapVersionForPatch(patchId): number`
- **Registry:** `_OBJECT_ID_MAPS` / public readonly view (`src/lib/object-id-maps/index.ts`)
- **Type:** `ObjectIdEntry`

<output>
Create `.planning/phases/08-replay-parsing/08-04-SUMMARY.md` when done.
</output>
