---
phase: 08-replay-parsing
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/w3champions-client.ts
  - src/lib/w3champions-client.test.ts
  - src/lib/replay-keys.ts
autonomous: true
requirements: [REPLAY-05]
must_haves:
  truths:
    - "fetchReplayBytes(gameId) downloads the replay from GET /api/replays/{gameId} and returns a Buffer, or an opaque status bucket on 429/404/unreachable"
    - "The outbound URL is built only from the hardcoded W3C_BASE_URL host + encodeURIComponent(gameId) (SSRF guard)"
    - "replay-keys.ts provides a client-safe TanStack Query key factory with no fetch/db imports"
  artifacts:
    - src/lib/replay-keys.ts
  key_links:
    - "fetchReplayBytes reuses the w3champions-client.ts SSRF guard (hardcoded host + encoded param) — never build URL from other fields"
    - "rate-limit (429) maps to an opaque bucket, never surfaces upstream status/error strings"
---

<objective>
Add the auto-pull download primitive (REPLAY-05): `fetchReplayBytes(gameId)` in the existing w3champions client, reusing its SSRF-guarded native-fetch discipline, plus a client-safe `replay-keys.ts` query-key factory. The endpoint is public (no token) per Spike 2; rate limits map to opaque buckets.

Purpose: The auto-pull server path (08-11) needs a single guarded download seam; the UI hooks (08-12) need a client-safe key factory that does not drag server-only code into the browser bundle.
Output: `fetchReplayBytes` in `src/lib/w3champions-client.ts` + tests; `src/lib/replay-keys.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/w3champions-client.ts
@src/lib/w3champions-keys.ts
@.planning/phases/08-replay-parsing/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchReplayBytes with SSRF guard + opaque rate-limit bucket</name>
  <files>src/lib/w3champions-client.ts, src/lib/w3champions-client.test.ts</files>
  <read_first>
    - src/lib/w3champions-client.ts (`W3C_BASE_URL` constant; `fetchW3championsSignals` fetch orchestration; `classifyW3championsResponse` opaque-bucket mapping; the SSRF module-header T-07-06a note)
    - .planning/phases/08-replay-parsing/08-RESEARCH.md ("w3champions replay download" code example; Pitfall 8 shared rate-limit partition; rate limits 30-50/hr, 70-100/day, HTTP 429)
  </read_first>
  <behavior>
    - fetchReplayBytes("valid-id") on a 200 octet-stream returns { status: "ok", bytes: Buffer }
    - a 429 response returns { status: "rate-limited" } with no bytes and no upstream string
    - a 404 returns { status: "no-data" }; a network throw returns { status: "unreachable" }
    - the fetch URL equals `${W3C_BASE_URL}/api/replays/${encodeURIComponent(gameId)}` and nothing else
  </behavior>
  <action>
    In `src/lib/w3champions-client.ts`, add `export async function fetchReplayBytes(gameId: string): Promise<ReplayDownloadResult>` returning a discriminated result reusing the existing opaque bucket vocabulary (`ok` | `rate-limited` | `no-data` | `unreachable`). Build the URL as `${W3C_BASE_URL}/api/replays/${encodeURIComponent(gameId)}` — reuse the hardcoded-host + encoded-param SSRF guard exactly (never interpolate any other client field). No auth header (public endpoint, Spike 2). Map `res.status === 429` → `rate-limited`, `!res.ok` → `no-data`, a thrown fetch → `unreachable` (try/catch), and success → `{ status: "ok", bytes: Buffer.from(await res.arrayBuffer()) }`. Never forward upstream status codes or bodies (T-07-06c). Add a module-JSDoc note about Pitfall 8 (shared egress rate-limit partition — a v1-scale watch item, not solved here). Test all buckets with a mocked `fetch` (mirror `w3champions-client.test.ts` strategy) and assert the exact URL shape.
  </action>
  <verify>
    <automated>npx vitest run src/lib/w3champions-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four buckets (ok/rate-limited/no-data/unreachable) covered; ok returns a Buffer.
    - A test asserts the outbound URL is exactly the hardcoded host + encoded gameId.
    - No upstream status/error strings appear in any returned value.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: replay-keys.ts client-safe query-key factory</name>
  <files>src/lib/replay-keys.ts</files>
  <read_first>
    - src/lib/w3champions-keys.ts (client-safe tuple-factory shape + "NO fetch, NO db" header rationale to copy)
  </read_first>
  <action>
    Create `src/lib/replay-keys.ts` (SPDX header + a header comment stating it is client-safe: no `fetch`/`db`/server-only imports, so client hooks can import it without pulling server code into the bundle). Export `replayKeys` with `all: () => ["replays"] as const`, `byGameId: (gameId: string) => [...replayKeys.all(), "byGameId", gameId] as const`, and `analysisStatus: () => [...replayKeys.all(), "analysisStatus"] as const` — mirror the `w3championsKeys` readonly-tuple convention.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "replay-keys" || echo "replay-keys typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `replayKeys.byGameId("g1")` returns a stable readonly tuple ending in `"g1"`.
    - The module imports nothing from `fetch`/`db`/server-only layers.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server → w3champions API | Outbound gameId path segment crosses to a third-party host |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08a | Tampering (SSRF) | fetchReplayBytes URL | high | mitigate | Hardcoded `W3C_BASE_URL` host + `encodeURIComponent(gameId)` only; no other field in the URL (T-07-06a precedent) |
| T-08-08b | Information Disclosure | rate-limit / error surface | low | mitigate | 429/404/throw map to opaque buckets; upstream status/body never forwarded (T-07-06c) |
| T-08-08c | Denial of Service | shared egress rate partition | low | accept | Pitfall 8 — documented watch item at v1 scale; API-token partition is a future mitigation if traffic grows |
</threat_model>

<verification>
- `npx vitest run src/lib/w3champions-client.test.ts` green.
- replay-keys.ts typechecks and is import-clean of server code.
</verification>

<success_criteria>
A single SSRF-guarded replay-download seam plus a client-safe key factory — the auto-pull plumbing for the server and UI.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `fetchReplayBytes(gameId): Promise<ReplayDownloadResult>` (`src/lib/w3champions-client.ts`)
- **Type:** `ReplayDownloadResult`
- **Key factory:** `replayKeys` (`src/lib/replay-keys.ts`)

<output>
Create `.planning/phases/08-replay-parsing/08-08-SUMMARY.md` when done.
</output>
