---
phase: 08-replay-parsing
plan: 05
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/lib/replay-signals.ts
  - src/lib/replay-signals.test.ts
autonomous: true
requirements: [REPLAY-02, REPLAY-07]
must_haves:
  truths:
    - "deriveReplaySignals(parsed, player) returns typed WC3 signals: buildOrder timing, eapm, controlGroupUsage, heroTiming, expansionTimingMs"
    - "The function is pure — same fixture input always yields same output; imports nothing from db/fetch/auth"
    - "isSoloMatch(parsed) identifies 1v1 replays (D-15 gate) structurally, before any threshold evaluation"
  artifacts:
    - src/lib/replay-signals.ts
    - src/lib/replay-signals.test.ts
  key_links:
    - "deriveReplaySignals mirrors detect-mastery-signals.ts discipline (pure, zero-I/O); it is NOT untouched-only (D-11) — that filter lives only in the write path"
    - "every signal it emits carries the value+timing needed for the 'you did X at Y' feedback (REPLAY-07)"
---

<objective>
Build the pure semantic signal layer (REPLAY-02, D-11): `deriveReplaySignals(parsed, player): ReplaySignals` turning raw w3gjs `ParserOutput` into typed, node-mappable WC3 events (build-order timing, eAPM approximation, control-group usage, hero timing, expansion timing). Also provide the structural `isSoloMatch(parsed)` 1v1 gate (D-15, Pitfall 7). This is a framework-free deep module mirroring `detect-mastery-signals.ts`.

Purpose: Keep "what happened in this replay" logic independently unit-testable with fixture objects (no real binary needed), and make every signal carry the value+timing that REPLAY-07's actionable feedback requires.
Output: `src/lib/replay-signals.ts` (pure) + tests over hand-authored `ParserOutput`/`Player` fixtures.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/lib/detect-mastery-signals.ts
@.planning/phases/08-replay-parsing/08-RESEARCH.md
@.planning/phases/08-replay-parsing/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveReplaySignals pure semantic layer + isSoloMatch gate</name>
  <files>src/lib/replay-signals.ts, src/lib/replay-signals.test.ts</files>
  <read_first>
    - src/lib/detect-mastery-signals.ts (module-header STRUCTURAL GUARANTEES + "NO I/O" discipline + minimal-structural-subset interface convention to copy exactly)
    - .planning/phases/08-replay-parsing/08-RESEARCH.md (Architecture Pattern 1 code sketch; Assumptions Log A3 eAPM heuristic caveat; Pitfall 7 1v1-only gate)
    - src/lib/replay-parser.ts (ParserOutput/Player re-exported types — the input contract)
  </read_first>
  <behavior>
    - deriveReplaySignals(fixture, player) returns buildOrder sorted ascending by ms, each entry {unitOrBuildingId, ms}
    - controlGroupUsage maps player.groupHotkeys → [{groupId, assigned, used}]
    - eapm is a number computed by a documented action-classification heuristic over player.actions (A3 — heuristic, not a scientific claim; documented + testable)
    - heroTiming and expansionTimingMs are derived from heroCollector / building order (expansionTimingMs null when no expansion detected)
    - Same input always yields identical output (purity); module imports nothing from db/fetch/auth
    - isSoloMatch(parsed) is true only when exactly two non-observer players are present (D-15)
  </behavior>
  <action>
    Create `src/lib/replay-signals.ts` (SPDX header + module JSDoc: STRUCTURAL GUARANTEES stating D-09 race-agnostic signals only, D-15 1v1 gate enforced by `isSoloMatch` upstream of threshold eval per Pitfall 7, and a "NO I/O" line naming allowed pure deps only). Define a minimal `ReplaySignals` interface (structural subset — `buildOrder`, `eapm`, `controlGroupUsage`, `heroTiming`, `expansionTimingMs`), NOT the full w3gjs types. Implement `export function deriveReplaySignals(parsed: ParserOutput, player: Player): ReplaySignals` following the filter/map shape of `detectMasterySignals` — but do NOT carry over any `existingProgressNodeIds` filter (D-11: this layer is NOT untouched-only). Factor the eAPM heuristic into a named helper `estimateEffectiveActions(actions)` with a doc-comment citing Assumptions Log A3. Add `export function isSoloMatch(parsed: ParserOutput): boolean` deriving 1v1 from non-observer player count / matchup. Test all behaviors with hand-authored fixtures (no real binary).
  </action>
  <verify>
    <automated>npx vitest run src/lib/replay-signals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - deriveReplaySignals emits a sorted buildOrder, numeric eapm, control-group usage, hero timing, expansion timing.
    - A purity test: identical fixture input → deep-equal output across two calls.
    - `import`s in the module include none from `#/lib/db`, `fetch`, or `#/lib/auth-middleware` (grep-asserted in test or verified by review).
    - isSoloMatch returns true for a 2-player fixture, false for a 3+/FFA fixture.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure transform over already-parsed output; no I/O |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05a | Tampering | signals from adversarial replay | low | mitigate | Pure function tolerates missing/empty arrays (returns nulls/empties, never throws); no signal is trusted for a write without the 1v1 gate + thresholds downstream |
</threat_model>

<verification>
- `npx vitest run src/lib/replay-signals.test.ts` green (fixture-driven, fast).
</verification>

<success_criteria>
Raw w3gjs output becomes typed, node-mappable signals through a pure, independently-testable module; the 1v1 gate is a structural function, not an ad-hoc check.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Function:** `deriveReplaySignals(parsed, player): ReplaySignals`
- **Function:** `isSoloMatch(parsed): boolean`
- **Function:** `estimateEffectiveActions(actions): number` (eAPM heuristic, A3)
- **Type:** `ReplaySignals`

<output>
Create `.planning/phases/08-replay-parsing/08-05-SUMMARY.md` when done.
</output>
