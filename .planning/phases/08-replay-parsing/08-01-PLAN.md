---
phase: 08-replay-parsing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/spike-replay-parse.ts
  - src/lib/__fixtures__/README.md
  - docs/adr/011-replay-parse-architecture.md
autonomous: false
requirements: [REPLAY-01, REPLAY-05]
user_setup:
  - service: w3gjs (npm) + sample replay
    why: "SUS-flagged package legitimacy gate + a real .w3g file is required to benchmark parse cost (top RESEARCH risk)"
must_haves:
  truths:
    - "w3gjs@4.1.0 is installed and parses a real .w3g buffer to a ParserOutput with buildNumber/version/players"
    - "The w3champions GET /api/replays/{gameId} endpoint returns octet-stream bytes parseable by w3gjs (Spike 2)"
    - "Measured parse time + peak memory for a real 1v1 replay is recorded and fits Vercel serverless limits (Spike 1), justifying the inline parse-location decision"
  artifacts:
    - scripts/spike-replay-parse.ts
    - docs/adr/011-replay-parse-architecture.md
  key_links:
    - "w3gjs npm legitimacy human-verified (SUS gate) before install"
    - "parse-location decision (inline serverless) recorded as ADR before any server code is written"
---

<objective>
Resolve two of the three planning-gate spikes (D-08) empirically and install the (SUS-flagged) w3gjs parser behind a human-verification gate. (The third D-08 spike — wc3v fork feasibility — is gated in plan 08-13's go/no-go checkpoint.) Benchmark real w3gjs parse time/memory on a real `.w3g` file (RESEARCH Open Question 1, the phase's top risk) and functionally confirm the public w3champions replay-download endpoint (Spike 2). Record the parse-location decision (inline principal-keyed server fn vs alternative) as ADR 011 so every downstream server plan builds on a decided architecture.

Purpose: De-risk the entire phase before writing feature code; the inline-parse architecture is assumed by every server plan and MUST be confirmed first.
Output: Installed `w3gjs@4.1.0`, a runnable spike script, and ADR 011 recording the parse-location decision.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-replay-parsing/08-RESEARCH.md
@.planning/phases/08-replay-parsing/08-CONTEXT.md
@src/lib/w3champions-client.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify w3gjs legitimacy (SUS gate) + provide sample replay</name>
  <action>
    Pause for human verification before any install. This is a NON-auto-approvable legitimacy gate (RESEARCH flagged `w3gjs` SUS on the low-downloads heuristic). The human verifies the package on npmjs.com and provides (or authorizes auto-fetch of) a real `.w3g` sample for the parse-cost benchmark, then approves or raises concerns. No files change until approval.
  </action>
  <what-built>Nothing yet — this gate precedes installation. RESEARCH's Package Legitimacy Audit flagged `w3gjs` as SUS on the low-downloads heuristic only (403/week), independently corroborated legitimate (MIT, no postinstall, 256-commit history, load-bearing dep of wc3v). Per the legitimacy gate this install is NOT auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/w3gjs and confirm: version 4.1.0, MIT license, repository github.com/PBug90/w3gjs, single dependency `protobufjs`, and no install/postinstall scripts.
    2. Confirm the GitHub repo is real and active (commit history, stars).
    3. Provide a real WC3 `.w3g` replay for the parse-cost benchmark: copy one (ideally a 1v1 and, if available, a long/team game) into `src/lib/__fixtures__/` and note the filename(s). If you have none, approve the executor to auto-fetch one from the public w3champions replay endpoint during Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" (and note the fixture filename or "auto-fetch") or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install w3gjs + run parse-cost + endpoint spike</name>
  <files>package.json, package-lock.json, scripts/spike-replay-parse.ts, src/lib/__fixtures__/README.md</files>
  <read_first>
    - src/lib/w3champions-client.ts (W3C_BASE_URL constant + encodeURIComponent SSRF-guard fetch pattern to reuse for the /api/replays/{gameId} fetch)
    - .planning/phases/08-replay-parsing/08-RESEARCH.md ("Code Examples → Parsing a .w3g buffer with w3gjs" and "w3champions replay download")
  </read_first>
  <action>
    Run `npm install w3gjs` (installs 4.1.0). Create `scripts/spike-replay-parse.ts`: import default `W3GReplay` from "w3gjs", read the fixture `.w3g` from `src/lib/__fixtures__/` into a Node `Buffer`, and call `await new W3GReplay().parse(buffer)`. Wrap the parse in `console.time`/`console.timeEnd` and log `process.memoryUsage().heapUsed` delta plus `result.buildNumber`, `result.version`, `result.matchup`, `result.players.length`. If no fixture was provided, first fetch a real replay: resolve a recent gameId from a public w3champions matches endpoint, then `fetch(\`${W3C_BASE_URL}/api/replays/${encodeURIComponent(gameId)}\`)` and `Buffer.from(await res.arrayBuffer())` — reusing the hardcoded-host + encoded-param SSRF discipline from w3champions-client.ts (never build the URL from any other field). Add `src/lib/__fixtures__/README.md` documenting that fixtures are real replays used for parse tests and are the vehicle for the Nyquist real-binary check. Do NOT commit large binaries — keep only one small sample.
  </action>
  <verify>
    <automated>npx tsx scripts/spike-replay-parse.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls w3gjs` shows `w3gjs@4.1.0`; `protobufjs` present as its only transitive dep.
    - Running the spike prints a non-zero `buildNumber`, a `version` string, and `players.length >= 2` for the sample replay (proves REPLAY-01 signal source and REPLAY-05 endpoint reachability).
    - Parse wall-time and heap delta are printed; both are recorded for the ADR (target: well under Vercel's 60s/2GB serverless limits).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record parse-location decision as ADR 011</name>
  <files>docs/adr/011-replay-parse-architecture.md</files>
  <read_first>
    - docs/adr/009-progress-persistence.md (ADR format/house style in this repo)
    - .planning/phases/08-replay-parsing/08-RESEARCH.md (Summary + Open Questions 1/2 + Pitfall 1 Vercel 4.5MB body limit)
  </read_first>
  <action>
    Author `docs/adr/011-replay-parse-architecture.md` following the existing ADR structure (Context / Decision / Consequences). Record: (a) the measured parse time/memory from Task 2; (b) the decision to parse INLINE inside the principal-keyed replay server function (not client-side, not a background job) — or, if the benchmark exceeded budget, the alternative and why; (c) the Vercel 4.5MB request-body limit + TanStack Start full-FormData buffering constraint (Pitfall 1) and the resulting client-side upload size cap (a generous ~4MB cap with a clear rejection message) that downstream upload plans MUST enforce. Cross-reference ADR 007 (principal-keyed server fns) and ADR 009 (server-stamped fields).
  </action>
  <verify>
    <automated>test -f docs/adr/011-replay-parse-architecture.md && grep -qi "parse" docs/adr/011-replay-parse-architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR 011 exists with Context/Decision/Consequences sections.
    - It states the chosen parse location with the measured numbers as justification.
    - It states the client-side upload size cap that plan 08-12 must enforce.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package (`w3gjs`) enters the dependency tree |
| w3champions API → server | Untrusted octet-stream binary crosses into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering | `npm install w3gjs` | high | mitigate | SUS-package legitimacy checkpoint (Task 1, blocking-human) before install; verify npmjs.com metadata + no postinstall |
| T-08-01a | Denial of Service | spike parse of untrusted `.w3g` | medium | mitigate | Spike runs offline on a single controlled fixture; production DoS guard (size cap + try/catch) is designed in ADR 011 and enforced in 08-11/08-12 |
| T-08-01b | SSRF | spike w3champions fetch | low | mitigate | Reuse hardcoded `W3C_BASE_URL` + `encodeURIComponent(gameId)` guard from w3champions-client.ts; never build URL from arbitrary input |
</threat_model>

<verification>
- `npm ls w3gjs` → 4.1.0 present.
- Spike script runs and prints parse timing + buildNumber for a real replay.
- ADR 011 committed with the parse-location decision.
</verification>

<success_criteria>
w3gjs installed behind the legitimacy gate; both remaining spikes (parse cost, endpoint) empirically resolved; parse-location architecture decided and recorded before any feature code.
</success_criteria>

## Artifacts this phase produces (this plan)

- **File:** `scripts/spike-replay-parse.ts` — one-off parse/memory + endpoint spike harness
- **File:** `docs/adr/011-replay-parse-architecture.md` — parse-location decision + upload size cap contract
- **File:** `src/lib/__fixtures__/README.md` — real-replay test-fixture convention
- **Dependency:** `w3gjs@4.1.0` added to package.json

<output>
Create `.planning/phases/08-replay-parsing/08-01-SUMMARY.md` when done. Record the measured parse time/memory and the parse-location decision so downstream plans can cite it.
</output>
