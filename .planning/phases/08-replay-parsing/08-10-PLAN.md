---
phase: 08-replay-parsing
plan: 10
type: execute
wave: 2
depends_on: [08-07]
files_modified:
  - content/nodes/build-order-human.mdx
  - content/nodes/build-order-orc.mdx
  - content/nodes/build-order-undead.mdx
  - content/nodes/build-order-nightelf.mdx
autonomous: true
requirements: [REPLAY-06]
must_haves:
  truths:
    - "Four canonical opening build-order MECHANIC nodes exist, one per race (human, orc, undead, nightelf), each with a replayCriteria threshold"
    - "Each node passes NodeFrontmatterSchema validation (required fields + non-empty citations + patchId) and the content build"
  artifacts:
    - content/nodes/build-order-human.mdx
    - content/nodes/build-order-orc.mdx
    - content/nodes/build-order-undead.mdx
    - content/nodes/build-order-nightelf.mdx
  key_links:
    - "these four nodes are the real mastery targets replay build-order detection advances (D-10) — without them 08-09 has nothing to advance"
    - "replayCriteria BO thresholds are content-authored from creator/pro wisdom with citations (Claude's Discretion — content-authored source of truth)"
---

<objective>
Author the four canonical opening build-order MECHANIC nodes (D-10) — one per race — so replay build-order mastery has real targets (REPLAY-06). This is the bounded, deliberate pull-forward of a thin RACE-* slice; remaining race/matchup builds stay v2. Build-order thresholds are content-authored from creator/pro wisdom with visible citations (Claude's Discretion: content-authored source of truth, fits the project's core value).

Purpose: 08-09's detector and 08-11's write path need concrete nodes carrying `replayCriteria` to advance; without them the mastery loop has no destination.
Output: `content/nodes/build-order-{human,orc,undead,nightelf}.mdx`, each a valid MECHANIC node with a `replayCriteria` build-order threshold.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/schemas/node.ts
@content/nodes/hotkey-discipline.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author human + orc build-order nodes</name>
  <files>content/nodes/build-order-human.mdx, content/nodes/build-order-orc.mdx</files>
  <read_first>
    - content/nodes/hotkey-discipline.mdx (a complete valid MECHANIC node — required frontmatter contract + citation shape)
    - src/schemas/node.ts (`NodeFrontmatterSchema` required fields; `ReplayCriteriaSchema` — the `replayCriteria` field to include)
    - .planning/phases/08-replay-parsing/08-PATTERNS.md ("content/nodes/build-order-{race}.mdx" — required-frontmatter contract, race NOT agnostic)
  </read_first>
  <action>
    Create `build-order-human.mdx` and `build-order-orc.mdx`. Each: `id` kebab-case (`build-order-human` / `build-order-orc`), `title`, `nodeType: MECHANIC`, `race: human` / `race: orc` (NOT agnostic — race-specific per D-10), `skillType: macro`, `difficulty: beginner`, `tags`, `patchId: <CURRENT_PATCH.id>`, non-empty `patch_context`, `last_reviewed` (today YYYY-MM-DD), `meta_volatile`, and a non-empty `citations` array (at least one creator/pro source backing the target timing — content-authored source of truth). Add a `replayCriteria` block: `{ signal: buildOrderTiming, beforeMs: <target opening-build completion time in ms> }` calibrated from cited creator/pro wisdom for that race's standard opening. Write real learning body content (Why It Matters / How to Apply) consistent with the project's science-backed voice. Keep the target object identifiable via the object-id-maps seeded in 08-04.
  </action>
  <verify>
    <automated>npm run build:content 2>/dev/null || npx content-collections build 2>/dev/null; echo "content build exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse under `NodeFrontmatterSchema` (content build succeeds).
    - Each has `nodeType: MECHANIC`, a race-specific `race`, non-empty citations, and a `replayCriteria` buildOrderTiming threshold.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author undead + nightelf build-order nodes</name>
  <files>content/nodes/build-order-undead.mdx, content/nodes/build-order-nightelf.mdx</files>
  <read_first>
    - content/nodes/build-order-human.mdx (the sibling authored in Task 1 — match structure/voice)
    - src/schemas/node.ts (`NodeFrontmatterSchema` + `ReplayCriteriaSchema`)
  </read_first>
  <action>
    Create `build-order-undead.mdx` (`race: undead`) and `build-order-nightelf.mdx` (`race: nightelf`) matching the structure of the Task 1 nodes: full required frontmatter, non-empty cited `citations`, a `replayCriteria` buildOrderTiming threshold calibrated per-race from cited wisdom, and real body content. Ensure ids are `build-order-undead` / `build-order-nightelf` and the four nodes together cover all four races exactly once.
  </action>
  <verify>
    <automated>npm run build:content 2>/dev/null || npx content-collections build 2>/dev/null; echo "content build exit=$?"; ls content/nodes/build-order-*.mdx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse; content build succeeds.
    - `ls content/nodes/build-order-*.mdx` lists exactly 4 files covering human/orc/undead/nightelf.
    - Each carries a `replayCriteria` threshold and non-empty citations.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| content authoring → build | Frontmatter validated by Zod at build time |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10a | Tampering | node frontmatter | low | mitigate | Build-time schema validation; malformed nodes fail the content build |
</threat_model>

<verification>
- Content build green with 4 new build-order nodes present.
</verification>

<success_criteria>
Replay build-order mastery has four real, cited, race-specific MECHANIC targets — one per race (D-10).
</success_criteria>

## Artifacts this phase produces (this plan)

- **Content nodes:** `build-order-human`, `build-order-orc`, `build-order-undead`, `build-order-nightelf` (`content/nodes/build-order-*.mdx`) — each a MECHANIC node with a `replayCriteria` build-order threshold

<output>
Create `.planning/phases/08-replay-parsing/08-10-SUMMARY.md` when done.
</output>
